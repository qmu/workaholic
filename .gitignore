.DS_Store
.claude/settings.local.json

# Codex CLI local scratch (file or dir, depending on Codex version)
.codex

# Developer-maintained leak denylist for release-scan (client names must never ship)
.workaholic/leak-denylist
