---
type: Feedback
title: The two routine-command entrances are deliberate
kind: instruction
source: discussion
created_at: 2026-08-04T21:55:32+09:00
author: a@qmu.jp
supersedes: 20260803221452-two-commands-now-survey-routines.md
---

# The two routine-command entrances are deliberate

The overlap between /workaholify and /setup-routines is decided, not drift. Ruled by the developer on 2026-08-04, resolving the low concern two-commands-now-survey-routines: the two entrances stay, deliberately. /workaholify keeps its routine survey and provisioning steps because a setup sweep must complete without a second command; /setup-routines stays as the single-purpose, developer-facing entrance for reading and changing routines one at a time. The knowledge and every script live once, in the workaholify skill (section 5) — the commands are both thin callers, so the logic cannot drift; only the prose describing the flow exists twice, and that duplication is accepted. The old concern's contingency remains the agreed fallback: if the duplicated prose is ever caught drifted, cut /workaholify's routine step down to a pointer at /setup-routines rather than repairing both texts.
