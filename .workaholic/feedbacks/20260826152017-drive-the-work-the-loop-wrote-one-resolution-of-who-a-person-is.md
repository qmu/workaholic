---
type: Feedback
title: Drive the work the loop wrote: one resolution of who a person is
kind: instruction
source: development
subject: observer_ai:[Propose] routine
created_at: 2026-08-26T15:20:17+00:00
author: a@qmu.jp
supersedes: 
---

# Drive the work the loop wrote: one resolution of who a person is

The [Propose] routine asks that the loop name a person once, so the work it writes is work it can drive.

Source: https://github.com/qmu/workaholic/issues/636

Measured on this repository at 2026-08-26T14:42Z, on a current, non-shallow survey of origin/main (plan-units.sh): backlog_size: 10 with backlog: [], and excluded: owned_by_other x 7 (2 missions, 5 tickets). Seven queued tickets and two active missions carry assignees: [] while the rest are stamped with an address the committed .claude/git-identities mapping does not name. /specificate stamps the triggering issue's assignee resolved by the run's own judgement rather than through the mapping, so it wrote an address that owns.sh compares by slug, answers other for, and plan-units.sh excludes. The loop originated the work and then locked itself out of it; the units have sat since 2026-08-21.

The sharpest fact: the mission that would repair the other half of this (refuse-ok-under-a-placeholder-identity) is itself one of the stranded units, so the defect is holding its own fix hostage.

What must become true: a person's addresses are named in exactly one place (the committed mapping) and resolved through exactly one reader, used by the writer that stamps an assignee and by the oracle that answers ownership; /specificate never stamps an address it cannot resolve, producing team-owned work and a named report instead; a survey that excluded its whole backlog says so rather than rendering like an empty queue; work no run can drive reaches a person once through /moderate; and the already-stranded units are recovered by a living migration rather than by hand.

What must not change: a colleague's queue stays untouchable. Only addresses the committed mapping names for the same login are one person; an unknown address answers other exactly as it does today. The 2026-08-14 incident of ~10 PR-units driven out of other people's queues is what the strictness exists for.

The ask names a mission grain: a title, the experience it demands, and an ordered set of eight tickets.
