# Post-merge follow-ups: fix check-deps, rewrite /release, and update the architecture narrative

## Summary

Three follow-up fixes after the single-plugin consolidation: a `check-deps` regression that blocked `/ticket` and `/drive` is fixed, the `/release` command is rewritten for the single-plugin layout, and the architecture documentation is brought in line with the unified `workaholic` plugin.

## Key Changes

- Fixed a `check-deps` regression that blocked `/ticket` and `/drive` (it probed for a `core` plugin removed by the merge)
- Rewrote `/release` for the single-plugin version files (dropped `plugins/core` / `plugins/tdd` / `sync-work.md`)
- Rewrote CLAUDE.md, README, and the command Notice headers to the single-plugin model, correcting the stale anti-spelunking Plugin Boundary Rule

## Changes

### Changed
- `check-deps` returns `{"ok": true}` for the single-plugin layout (unblocks `/ticket` and `/drive`)
- `/release` command rewritten for the consolidated version files
- Architecture narrative (CLAUDE.md / README / command Notices / marketplace description) updated to one plugin

## Metrics

- **Tickets Completed**: 3

## Links

- [Pull Request](https://github.com/qmu/workaholic/pull/44)
- [Branch Story](.workaholic/stories/work-20260617-082241.md)
