# Add always-triggered policy-lens hook for the workflow commands etc

## Summary

The engineering policies now apply automatically across the workflow: an always-triggered hook injects the planning/design/implementation/operation lens into `/ticket`, `/report`, and `/ship`. The root README is reconciled with the current policy model.

## Key Changes

- Added an always-triggered `policy-lens.sh` `UserPromptSubmit` hook that injects the engineering-policy lens into the workflow commands without blocking execution, replacing per-command prose that had drifted
- Fixed the README's lifecycle Mermaid diagram, which failed to render on GitHub because node labels contained unquoted forward slashes
- Added the `planning` (企画) pillar to the README policy index and documented how policies sync from qmu.co.jp via `standards-sync/*` PRs

## Changes

### Added
- Always-triggered policy-lens hook for `/ticket`, `/report`, and `/ship`, centralizing the engineering-policy lens and retiring the drifted per-command prose

### Changed
- README: fixed the broken lifecycle Mermaid diagram, added the planning pillar to the policy index, and documented the cross-agent policy-sync workflow

## Metrics

- **Tickets Completed**: 2

## Links

- [Pull Request](https://github.com/qmu/workaholic/pull/51)
- [Branch Story](.workaholic/stories/work-20260618-115347.md)
