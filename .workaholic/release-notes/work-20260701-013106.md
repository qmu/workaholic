# Release v1.0.70 — release PR #63's work under a free version

## Summary

v1.0.70 delivers the work merged in PR #63 under a distinct, free version. PR #63 had landed under v1.0.69, which was already published with different content, so its changes were unreleased. This version corrects that collision.

## Key Changes

- Fixed the branch guard over-blocking piped `git branch` list forms; diagnosed the "not firing" report as a stale pre-1.0.66 install.
- `check-deps` now surfaces the loaded plugin version and PreToolUse guard presence.
- `/catch` gained per-developer focus windows, struggles, branches, generation-style guess, and this-week deployments/releases.
- `/ticket` now interrogates for a quality gate that `/drive` surfaces at approval.

## Changes

### Changed
- Version bumped to v1.0.70 across the lockstep files; `outputs/` regenerated. No code changes beyond the bump and the relocated PR #63 carry-over concerns.

## Links

- [Pull Request](https://github.com/qmu/workaholic/pull/64)
