# Stories

Branch development narratives, one per branch, serving as the PR description and the historical record. See [README.md](README.md) for the story format.

<!-- okf:generated:begin -->
* [work-20260827-204109](work-20260827-204109.md) - The declared-handoff verdict now reaches the claim holder as one question naming the act it waits on, and stalled-units stops asking the wrong one.
* [work-20260827-164139](work-20260827-164139.md) - The loop reads whether the base survived what it merged — one checks reader, an attribution walk, a /moderate question keyed on the broken commit, and a reading that gates nothing
* [work-20260827-144111](work-20260827-144111.md) - The direction layer gains `arrived` — a direction whose work is all in is told to its owner once, instead of being reported as overdue.
* [work-20260827-114104](work-20260827-114104.md) - Give a live strategy a third writer so the operator revises a direction through the loop, bounded by refusals and by a publish seam that never auto-merges it.
* [work-20260827-084115](work-20260827-084115.md) - A claim whose remaining queued work declares verification_handoff: gets its own verdict, awaiting_verification, so the standing handoff claim is no longer offered for a takeover that could drive nothing.
* [work-20260827-054123](work-20260827-054123.md) - Act on the two claim verdicts that are proofs — retry an undelivered unit's merge and retire a claim already on the base — instead of reporting them and stopping.
* [work-20260827-021918](work-20260827-021918.md) - A unit the loop finishes now reaches main, or the run names the refusal that stopped it — in the report, in the survey, and once to a person by name.
* [work-20260827-014149](work-20260827-014149.md) - A unit held by a superseded claim and a live one now resolves to the live branch, derived once and read by every writer.
* [Let a fresh claim take a superseded claim's work](work-20260827-003503.md) - The survey re-offered work behind a superseded claim that both claim paths refused; a fresh claim may now take it.
* [work-20260826-195732](work-20260826-195732.md) - Retire the always-on mission-lens hook — its roster surfacing now lives in /moderate's closable-missions step and the on-demand /mission roadmap
* ["Drive the work the loop wrote: one resolution of who a person is"](work-20260826-154116.md) - A person's addresses are named once and read through one script, so work the loop writes is work the loop can drive.
* [work-20260826-122328](work-20260826-122328.md) - The claim oracle now tells a merged claim from a live one at both grains, stops asking about finished ones, and hands their work back to the survey.
* [work-20260826-114429](work-20260826-114429.md) - The loop says what it accepted and asks about what is still waiting — a reaction on every swept message, a tick step for unanswered channel asks, and the surface language rule.
* [work-20260826-084111](work-20260826-084111.md) - The direction layer gets a lifecycle reading and a voice — overdue, dormant and none are read once and asked about by name, and three refusals are pinned by a test and a drill.
* [work-20260826-043020](work-20260826-043020.md) - Every inbound mouth of the loop now names the direction its ask answers, from one writer, so work born on the channel or filed by hand stops citing its strategy at nothing.
* [work-20260826-041037](work-20260826-041037.md) - Report a claim whose content already reached the base as superseded, so the survey stops offering a branch that cannot land
* [work-20260826-034037](work-20260826-034037.md) - Split the drained-queue verdict on the story signal so a run that died before opening its pull request is recoverable instead of stranded
* [work-20260826-031417](work-20260826-031417.md) - The loop's unit of work moves from a change to a mission — `/propose` plans one, `/specificate` emits it, the brake bounds one per strategy, and the roadmap names the direction each mission serves.
* [work-20260826-023535](work-20260826-023535.md) - The loop's carry-forward link becomes mechanical, reported and floored, so `no_citing_artifacts` can only mean nothing has answered this direction yet.
* [Say what happened, not what the tick counted](work-20260823-161806.md)
* [Tell describing work from advancing work](work-20260823-155258.md)
* [Refuse a commit that splits a rename](work-20260823-151809.md)
* [Give the tick a route for the records it writes](work-20260823-144244.md)
* [Tell an unanswered question from an answered one](work-20260823-141725.md)
* [Close a mission the run can prove is finished](work-20260823-131214.md)
* [Make an Open Decision a question to answer, not a ruling](work-20260823-124835.md)
* [Route a stalled unit to a person who is asked by name](work-20260823-112419.md)
* [Expire the alert cool-down at the next working day](work-20260823-092805.md)
* [work-20260822-232014](work-20260822-232014.md) - No Slack post ends in a machine identifier any more — the constraint that had blocked this for a day was never tested, and one search disproved it
* [work-20260822-225608](work-20260822-225608.md) - The loop gains its first reading that is not a brake — whether a direction will arrive — and a person is told when one will not
* [work-20260822-224335](work-20260822-224335.md) - A tick proposes against every eligible strategy — the per-tick cap starved whichever direction was slowest, which is the one that most needed its next move
* [work-20260822-200945](work-20260822-200945.md) - The hourly Moderation root stops posting by construction — the diff runs on a stable form, and a question is now the root's precondition
* [work-20260822-195727](work-20260822-195727.md) - The loop stops answering a build strategy with documents — a describing move is refused by name, and a decomposable ask is planned instead of recorded
* [work-20260822-175716](work-20260822-175716.md) - A unit's finish is announced once per channel instead of once per feedback stem, and the thread it opens now has something a human can read
* [work-20260821-035855](work-20260821-035855.md) - A rename becomes one row in a shipped registry, and the last two old names — the housekeeping area and the report command — are its first users
* [work-20260819-115425](work-20260819-115425.md) - Made `stories/` a generated OKF area so the one ledger index with no repairer got one — the description moved into each story's own `description:` frontmatter, `/report` stopped hand-writing bullets, and the existing index opted in via markers. Regenerating recovered **22 stories** the hand-maintained index had silently lost. Two narrow per-area knobs (`order`, `skip`) carry what stories need; the README skip is deliberately **per-area**, because `deployments/`, `release-notes/` and `terms/` each index their own README as a real entry — the ticket's claim that a global exclusion was safe was false on this tree. 3162 passing / 0 failed - 1 ticket
* [work-20260819-113836](work-20260819-113836.md)
* [work-20260819-053729](work-20260819-053729.md) - The routine pair swaps names (Propose to Specificate, Housekeep to Propose) with the cutover ordering derived from the templates, and a third user scope adds the per-account Workaholic updater
* [work-20260818-222423](work-20260818-222423.md) - The release preparation post is bounded to one ask per Asia/Tokyo day by a second deploy-day token, with deploy:<digest> unchanged
* [work-20260818-213641](work-20260818-213641.md) - The housekeep reminder names the kind of finding in its first line, derived from blocked_by, with the stuck:<digest> key untouched
* [work-20260818-205051](work-20260818-205051.md)
* [work-20260818-193646](work-20260818-193646.md) - The web bootstrap guards user.name on the local scope, so a routine commit renders the developer name instead of Claude
* [work-20260818-134024](work-20260818-134024.md) - Closed the reading half PR #499 left open: the hourly `[Prepare Release]` tick derived its unreleased boundary from whatever refs its container arrived with and never fetched, so one unchanged repository reported **2721**, then **2950**, then the true **4** commits as refs came in — with the `deploy:<digest>` moving each time, which let the silence rule post repeatedly. `report-deploy-status.sh` now freshens the base branch and tags (bounded, best-effort, `WORKAHOLIC_DEPLOY_FETCH_TIMEOUT=0` opts out) and reports `refs: fresh|stale|skipped`; a boundary that stale refs collapsed to is `doubtful`, **redacted out of the digest** so differing stale containers key identically, and posted with the count withheld — the ticket's Open Decision, ruled toward its unlisted third option because silence is indistinguishable from a quiet repository and a bare count endorses a number the reader just flagged. `read-deploy-state.sh` gained no network I/O. Also moved the post heading to `📦 Release Preparation` and wrote the byte-identical shape pin `CLAUDE.md` already claimed existed. 3084 passing / 0 failed - 2 tickets
* [work-20260818-132434](work-20260818-132434.md) - A story sentence ends at a period followed by whitespace, so a backticked filename no longer cuts the line and leaves an unclosed backtick
* [work-20260818-130809](work-20260818-130809.md) - Key Changes falls back to the merge commit body's pull request title, so a story-less merge renders what landed instead of that nothing says what landed
* [work-20260818-123734](work-20260818-123734.md) - The housekeep persist unions by (tick, step) and runs again after the agent files, so the tick log's step-filed lines reach the base
* [work-20260818-113659](work-20260818-113659.md) - sync-main.sh §1b fast-forwards a detached, clean, strictly-behind checkout, so an /implement tick can survey again after it merges its first unit
* [work-20260818-083716](work-20260818-083716.md) - The version-bump predicate now measures against the resolved base and matches on the commit subject; its degraded read names its reason and bumps
* [work-20260818-073640](work-20260818-073640.md) - The thread lookup's case 3 now searches the originating Issue or pull-request URL and never one the run itself created; the two effort-ceiling reversals stay the operator's
* [work-20260818-063646](work-20260818-063646.md) - Renamed `/fullfill` to `/prepare-release` and the repository routine to `[Prepare Release]`, heading included (`📦 Prepare release`) — the ask's second half, which the previous day's rename had held back on a cost that proved false: nothing ever searched the heading, the dedup key is `deploy:<digest>`, and both canonical files now say so. The mechanical cost that *is* real — convergence matches routines by name, so a renamed record creates a duplicate no other account can delete — ships as a `renamed_from:` template field that carries the rename-don't-create instruction into the setup sheet and the convergence report, and is deleted once the fleet has cut over. 2986 passing / 0 failed - 1 ticket
* [work-20260817-180716](work-20260817-180716.md) - Rename /release-status to /fullfill for the one-word command convention; the routine record and the post shape deliberately do not move
* [work-20260817-173706](work-20260817-173706.md) - Unify /fb: every ask becomes an [FB]-marked GitHub issue, with an assignee and one documented fallback
* [work-20260817-163715](work-20260817-163715.md)
* [work-20260817-114453](work-20260817-114453.md) - /housekeep: the hourly maintenance tick, its log area, nine steps (two of them gated), and the repository-scoped routine
* [work-20260814-110344](work-20260814-110344.md) - Stopped the unattended runner taking colleagues' work out of the pre-P2 per-user queue (qmu/workaholic#454). Each ticket's claim was reproduced in a throwaway repository before anything was written: a legacy `todo/<slug>/X.md` really did answer `owners=[]` / `owns=unowned`, and archiving really did land a ticket unstamped while leaving its siblings in the per-user directory. The fix is a **third tier on the one oracle** rather than an exclusion rule in the survey — one tier fixes every consumer, and resolving instead of excluding keeps the runner's OWN legacy tickets answering `mine` instead of vanishing with everyone else's. Its path match is anchored at both ends (`parent = todo` **and** `grandparent = tickets`) or a flat `todo/X.md` would have resolved its owner to the literal string `todo`, turning every genuinely team-owned ticket into nobody's. `archive.sh` now composes `migrate-todo-owners.sh` **before** the move — the ordering is the design, since running it after would leave the ticket being archived as the one file the seam never stamps — plus a lexical re-resolution of a path the migration just flattened. That second half was forced by eight failing assertions in three unrelated tests, which were reporting a hazard rather than fixture staleness: a queue is listed once and driven ticket by ticket, so the first archive invalidated every remaining path and a drained unit read as resumable, the 2026-08-04 double-pick class. The third ticket's Open Decision was ruled **(A) with (B) recorded as the operator's**: taking (B) inside a ticket whose Considerations forbid narrowing the survey would have been the silent choice the decision exists to prevent. Held at its pull request by the size gate. v1.0.178; 2697 passing / 0 failed - 3 tickets.
* [work-20260814-104347](work-20260814-104347.md) - Turned `/workaholify`'s two wiring halves from audit-and-offer into applies, closing the ruling that it is the preparation command rather than an audit (qmu/workaholic#445). `apply-claude-md-reference.sh` **composes** `audit-claude-md.sh` instead of re-deriving conformance — so a block that satisfied the writer but not the checker is structurally impossible — and appends rather than rewrites, because an existing `CLAUDE.md` is the repository's own document. `apply-bootstrap.sh` maps one repair to each `check-bootstrap.sh` problem id and refuses **atomically**: an unparseable `.claude/settings.json` leaves the hook uninstalled too, since a hook present and unregistered would be a state the run itself created. It corrects the existing `SessionStart` group rather than appending a second one, which would have fired the bootstrap twice and left the check reading whichever it found last. The mission's second half made the converge seam a **declared registry**: a structural change ships its `gather/scripts/migrate-*.sh` and its `converge-layout.sh` registration in the same commit, walked by a new smoke check whose verdict function is shared with its own fixture proof, and whose read strips comments — the registry's header names both migrations in prose, so a whole-file grep would have passed a migration that was only documented as composed. The glob stays scoped to `gather/scripts/`, leaving `feedback/scripts/migrate-concerns.sh` (a read-time record migration) deliberately outside. Held at its pull request by the size gate: `archive.sh`'s `git add -A` swept the second ticket's already-written changes into the first ticket's archive commit. v1.0.178; 2695 passing / 0 failed - 2 tickets.
* [work-20260814-094401](work-20260814-094401.md) - Gave the unattended executor a second routing input so a unit nobody could verify here stops being announced as implemented (qmu/workaholic#452). The reproduction located the defect in two commands rather than assuming it: `grep -rl "Quality Gate"` over `drive/scripts/` returns nothing and `effective-policy.sh` calls `fm_field` exactly once, on `merge_policy` — the router was never wrong, it had no way to know — which is why the fix is a declared field and not a smarter reading of prose. `verification_handoff: <what cannot run here>` is optional ticket/mission frontmatter whose **value is the reason**, read by the new `verification-handoff.sh` before the merge-policy table; any member declaring it carries the whole unit, and `auto` does not outrank it. The ticket's Open Decision was resolved as **(a), widen `handoff`**: every consequence the case needs — open PR, non-droppable `## Handoff`, 🟡 finish line, `pending` token — is already what `handoff` produces, and an outcome is defined by its consequences, so a fourth route would have been two names for one state and the ask's daily aggregation would have had to query both. The soft-landing guard is strengthened rather than relaxed: "queue not drained" is replaced by a declaration that must exist on the artifact **before** the drive, which no run can write for itself. `/propose` may write it (`--verification-handoff`) although it still may not write `merge_policy` — one withholds a merge, the other grants one. The loop drill's `verify-implement` inverts its last two rows on that fixture, selected by reading the archived ticket rather than a flag. v1.0.178; 2622 passing / 0 failed - 1 ticket.
* [work-20260814-091758](work-20260814-091758.md) - `commit.sh` now stamps `Claude-Session: https://claude.ai/code/<id>` whenever the process environment carries a cloud session id, so a commit says **which run** produced it; every seam inherits it because the one commit writer reads the environment rather than a flag. Measured first in a live `[Implement]` container: the report's "commits are always Claude" is true of `user.name` (container global config) and false of `user.email` (set repo-locally by the web bootstrap), so the person was always attributable. The routine's **name** stays unrecoverable - nothing in the container's environment names it - and the author email is untouched because the claim protocol resolves ownership from it. 2599 passing / 0 failed - 1 ticket
* [work-20260814-084725](work-20260814-084725.md) - Gave routine templates a `scope:` (`developer` = one copy per person, `repository` = exactly one for the team) and split `/setup-routines` along it into `/setup-dev-routines` and `/setup-repo-routines`; the old name is removed, not aliased. Added the first repository-scoped routine, `[Release Status]` (`45 * * * *`, `/release-status`), which **reads and never writes**: the hourly-`/ship` ask was resolved by refusing every unit-less writer on a measurement — a merged note's plan is self-referential because the refresh's own commit changes the count it reports, an open PR's branch belongs to whoever holds its claim, and `/ship` merges. `CLAUDE.md`'s "do not reintroduce a third routine" is superseded with its reasoning answered. 2632 passing / 0 failed - 2 tickets
* [work-20260814-074927](work-20260814-074927.md) - Gave `/propose` a fourth artifact form: a dated, owned direction with no decomposable plan now comes out as a **strategy** in the proposal pull request rather than a mission nobody can decompose or record-only silence (qmu/workaholic#450). The mission's Open Decision — does `/propose` become a strategy writer at all, one day after three documents said no command, hook or routine writes one — was resolved as **(a), repaired**: option (a)'s own justification ("the proposal only reaches `main` when the operator merges the PR") had been falsified by the 2026-08-11 auto-merge change, so instead of picking a side the run restored the premise — a proposal that wrote under `.workaholic/strategies/` leaves `WORKAHOLIC_AUTO_MERGE` unset, and the operator's merge is the authorship. The bar is all three parts present in the ask (a stated `YYYY-MM-DD`, the triggering issue's assignee, an aim with no decomposable plan); an unassigned issue therefore cannot produce a strategy and reports `no_assignee`. Lifecycle announcements are recognised before the four forms and matched by **explicit slug only** — the same refusal `workaholic:notify` makes for reply threads — with *ended* reaching `close.sh`, *created* taking the strategy form, and *changed* record-only because the artifact has two writers and deliberately no third. v1.0.178; 2594 passing / 0 failed - 2 tickets.
* [work-20260814-033643](work-20260814-033643.md) - Gave `/propose`'s case-4 threads a readable opening: when the stateless lookup finds nothing, the connector now posts a description root — the feedback record's title and URL, the `fb:<stem>` key, no mention token of any kind — and threads the `🔵 Proposed` line under it, while a found thread still takes exactly one message and the tokened fallback still posts one keyed line and reports that it could not thread (qmu/workaholic#443). The reproduction ran on the live channel because `qfs` was absent and the drill only verifies after an hourly tick: `#dev-workaholic` separated three legitimate case-4 roots (asks that arrived as GitHub issues and were never pasted into Slack) from a case-3 query-form miss whose thread was findable and from the most recent tick, which threaded correctly — so the root fixes the not-found branch alone. Both Open Decisions were ruled with reasoning: no `[FB]` issue is auto-filed, because an assigned one is re-discovered by clock-fired discovery and structurally cannot be excluded while an unassigned one is a page nobody is asked to act on, and "no linked FB" means case 4, the only branch with no root to reply into. Scoped to `/propose` — `/implement` shares case 4 but its prompt names no such post, and the ceiling rule makes that a template edit plus a confirmation. The catalog's and the template's wording are pinned byte-identical by a new hermetic assertion. v1.0.178; 2594 passing / 0 failed - 1 ticket.
* [work-20260813-133929](work-20260813-133929.md) - Turned `/ship` from a deploy-verify-merge command into one that **drafts a deployment plan and merges**, the last per-unit rule still contradicting the release-tier doctrine now that `main` auto-merges continuously (qmu/workaholic#438). Three readers were built before anything was written — a target's identity, deploy model and optional ship paths on `read-deployments.sh`, the release-notes area's first frontmatter reader, and `read-deploy-state.sh` joining both to the merged-but-unreleased range. Both of the consolidation's "unrecommendable" forks were resolved by **reporting the answer's strength** rather than hiding a default: `attribution: whole_range` versus `declared_paths`, and a note/target join reported as `declared` or the weaker `recency`. The Release Note gained a second tense — a prospective `## Deployment Plan` carrying no clock, so a re-run against an unchanged base is byte-identical; the first attempt was *not*, because stamping `targets:` made the note match itself as its own latest note. The merge is no longer gated on an executed production confirmation and the relocation is stated in three places: the `release/*` window's confirmation is now **the** production evidence, not a second opinion. `record-evidence.sh` fills the story and the note from one call, keeping its `possible_secret` refusal load-bearing for both, and `not_run` joined the status set. The refresh carrier question was answered without a third routine — `[Implement]`'s existing tick, with the narrower reach that implies written down rather than dropped — and `loop-drill.sh verify-plan` proves it in seconds. v1.0.177; 2501 passing / 0 failed - 5 tickets.
* [work-20260813-113709](work-20260813-113709.md)
* [work-20260813-083658](work-20260813-083658.md) - Reconciled `okf/SKILL.md`'s claim that tickets carry a `type:` — a vocabulary (`enhancement|bugfix|refactoring|housekeeping`) retired on 2026-08-07 — against the three sources that state the opposite: `CLAUDE.md`, `.workaholic/README.md:10`, and every shipped ticket. The ticket's step 1 made the direction of the error provable rather than assumed: `refresh-index.sh` never reads ticket frontmatter, `okf.mjs` handles only `type: Engineering Policy`, and `validate-ticket.sh:244` records the fields as tolerated-never-validated, so the skill was the stale side and the fix stayed prose. The skill was also self-contradicting — line 15 listed tickets as `type`-bearing while line 34 said the script "deliberately does not touch anything inside `tickets/`". Two survivors of the retired vocabulary are named and kept: `create-ticket/reference/ticket-format.md:38` prohibits it, and the test fixtures exercise the grandfathered corpus. `outputs/` came back byte-identical — `okf` sits in the build closure without its prose reaching the bundle. Closes the documentation-refresh mission's fourth ticket, minted mid-run by its third. v1.0.176; 2447 passing / 0 failed - 1 ticket.
* [work-20260813-073630](work-20260813-073630.md) - Refreshed four documentation surfaces onto the shipped behavior: the root README, both operator runbooks, the artifact hub and the always-loaded rules. Three loop changes had landed without their documents — proposal/implement PRs auto-merging on opening with quality gated at the `release/*` window, the retired start post, and `/setup-routines` becoming a configurator — and each document had drifted in patches, so every ticket located the drift by reading against `CLAUDE.md` rather than grepping a phrase. That caught one proposal-time claim that was *not* drift (`/ticket` and `/mission` PRs keep their human merge) and six contradictions nobody had listed, including a runbook claim that the loop "never merges anything a policy did not authorize". Two dated decision passages were marked superseded in place rather than rewritten. Prose only; `docs/loop-engineering-workflow.md` untouched. v1.0.176; 2447 passing / 0 failed - 3 tickets.
* [work-20260812-233617](work-20260812-233617.md) - Made the argv-ceiling `scan-window` fixture derive both sides of its window comparison from one clock (qmu/workaholic#427). The suite's red line on the operator server was not a platform difference: `scan-window.sh` resolves its cutoff with `%cd`, which renders each commit in the offset that commit recorded, so the cutoff is the run's own local today — while the fixture's changelog event was frozen at `2026-08-12`. Measured before editing: `TZ=UTC` kept the event, `TZ=Asia/Tokyo` dropped it, and a one-day-ahead clock dropped it everywhere, which made the ticket's "time bomb from 2026-08-13" reading the diagnosis rather than a forecast. The fixture now pins its only commit to a `+00:00` instant derived from now and dates the event with that same string; a second event 30 days back keeps the unchanged length assertion proving that the cutoff still filters. Fixture-only — `scan-window.sh` untouched. v1.0.176; 2447 passing / 0 failed - 1 ticket.
* [work-20260812-225831](work-20260812-225831.md) - Narrowed `/drive`'s freshen refusal so a checkout parked on the base's exact tip with a clean tree surveys instead of ending the tick: the cloud harness checks out a session branch, which cost two independent runners their ticks inside one hour on 2026-08-12. The exception rests on a proof, never a `claude/*` name, and every failure of it still refuses `not_on_main`. Third case of the harness-artifact class after the plugin binding and the baked base branch. v1.0.175; 2446 passing / 0 failed - 1 ticket.
* [work-20260812-224603](work-20260812-224603.md) - Trimmed `CLAUDE.md` from 375 lines / 129,674 bytes to 195 lines / 27,962 bytes (-78% bytes, issue qmu/workaholic#355): current behavior only, decision narrative compressed to clauses behind a pointer block naming docs/, the runbooks, skill reference/ dirs, and git history; every command contract, script path, gate, and structural table preserved. v1.0.159; 2238 passing / 0 failed - 1 ticket.
* [work-20260812-223743](work-20260812-223743.md) - Pinned the plugin source across the freshen step: `plugin-src.sh` resolved the tree a run executes *before* `sync-main.sh` moved the working tree, and broke a version tie toward the checkout — the one candidate that step can change. Measured on the tick that minted the ticket, a container's stale baked `main` reverted a resolved checkout 200 commits mid-run, to a `sync-main.sh` answering `diverged` (which the caller reads as terminate `pending`), while the run still reported the version it no longer ran. The resolver now decides on two axes — newest version wins, an equal version goes to the immutable candidate — with immutability read off the path (version-addressed: the basename is the version it carries), so a bound checkout is correctly called mutable. New `src_immutable` and per-candidate `immutable` fields; the newest-wins rule is untouched. This unit ran under the defect it fixes. v1.0.174; 2439 passing / 0 failed - 1 ticket.
* [work-20260812-223641](work-20260812-223641.md) - Excluded HCL `var.`/`local.`/`data.`/`module.` references from the release scan's pass-2 secret rule (qmu/workaholic#378): the documented Terraform provider-credential wiring no longer hard-blocks on the non-overridable tier, while a quoted or literal HCL credential still flags; four must-not-flag rows and one must-flag row join the shared gate table. v1.0.158; 2238 passing / 0 failed - 1 ticket.
* [work-20260812-222443](work-20260812-222443.md) - Recorded loop-drill pass `20260812-221056` as the drill log's second row (qmu/workaholic#423), the first append to the table §8 opened one pass earlier. The row's outcome was checked rather than copied: issue #423 closed at 22:20:41Z via PR #424's `Closes #423`, so "exercised the propose–implement loop end to end" is evidence, not a formality — the ticket's own Considerations asked for exactly that. The run also hit and ticketed a defect in its own bootstrap: `plugin-src.sh` prefers the checkout on a version tie, but the freshen step can move that tree backwards, and here it silently swapped the running `sync-main.sh` for a copy predating its own §5 realignment. v1.0.173; 2434 passing / 0 failed - 1 ticket.
* [work-20260812-220214](work-20260812-220214.md) - Gave `docs/loop-drill-runbook.md` the drill log it never had (qmu/workaholic#419): §7 already kept every pass's artifacts on `main` as the loop's immutable history, but the index over them was missing, so "which passes ran, when, and how did each end" meant walking closed issues and merged pull requests by hand. `## 8. Drill log` carries the run id — the one fact unrecoverable from a listing of merged PRs — with its first row for run `20260812-215314`. The ticket's open decision resolved to *operator writes the row*, because `loop-drill.sh verify-implement` is read-only and a write side effect would let a failed append turn a passing pass red. v1.0.172; 2434 passing / 0 failed - 1 ticket.
* [work-20260812-213854](work-20260812-213854.md) - Mechanized the `[FB] ` marker on the `/fb` crossing's issue titles (qmu/workaholic#411), reversing the standing no-prefix rule. The fork was decided by measurement rather than argument: 17 of 17 crossing-opened issues already carried the marker by hand against 1 of 9 human-filed ones, so the written rule had never described the shipped behaviour and the ask was for a guarantee. `fb-title.sh` is the single idempotent implementation, called by the sender and by the confirmation so the developer still sees the wire string. v1.0.171; 2434 passing / 0 failed - 1 ticket.
* [work-20260812-212339](work-20260812-212339.md) - Stopped `loop-drill.sh verify-propose` reporting red on a green run: its ticket lookup described only the loose-ticket shape, so the mission-shaped PR #407 read as a missing ticket. The verifier now follows record → `mission.feedback` → `ticket.mission`, names the shape in an advisory `proposal_form` row, treats record-alone as a judgment rather than an absence, and accepts either spelling of the assignee through `.claude/git-identities` — a second defect the reproduction uncovered. `verify-propose 406` goes fail → pass against the real base. v1.0.170; 2424 passing / 0 failed - 1 ticket.
* [work-20260812-211712](work-20260812-211712.md) - Gave `/setup-routines` one job instead of two branches (qmu/workaholic#408): it attempts the configuration every time and reports `no_transport: RemoteTrigger-family tool` when the session can reach no account routine, with the copy-paste sheet demoted from equal branch to that refusal's recovery path. The absence of a second transport was measured rather than assumed — the `CronCreate` family is session-only and in-memory, and the CLI exposes no routine subcommand. v1.0.169; 2408 passing / 0 failed - 1 ticket.
* [work-20260812-211128](work-20260812-211128.md) - Reconciled `docs/drive-loop-runbook.md` with the same-day ruling that `loaded_version_behind_registry` is a warning, not a stop: the operator page still documented the retired termination as current, and `survey.md` back-referenced a stop its own text had already turned into a warning. Found by being the failure — the tick that minted the ticket ran on a 1.0.133 binding against a 1.0.166 registry. v1.0.168; 2408 passing / 0 failed - 1 ticket.
* [work-20260812-204905](work-20260812-204905.md) - Gave `workaholic:notify` the transport rule it never had (qmu/workaholic#406): the skills named the tokened `notify-slack.sh` as the way to post a finish line, but a routine session carries the Slack connector and no token, so the script recorded `{"notified": false, "reason": "no_token"}`, exited 0, and the post silently never existed — the FB thread lost its root. Connector primary, script the keyed-root-only fallback (measured: its payload carries no `thread_ts`), and the `/implement` run report now names each unit's notification outcome so an unposted line reads as unposted without moving the terminal token. v1.0.167; 2408 passing / 0 failed - 2 tickets.
* [work-20260812-193807](work-20260812-193807.md) - Made the propose–implement loop drillable on demand instead of testable only by its own hourly failures. `scripts/e2e/loop-drill.sh` (operator tooling outside the plugin, so it may assume the server's full `gh`/`qfs`) seeds a drill pair behind two refusals — an open assigned issue would be taken as the ask, an unmerged `work-*` branch is both a live claim and a dedup ref — reports the drill's residue, recovers an aborted run without touching anything it cannot prove it minted, and verifies each stage against artifacts alone (`origin/main`, REST issue/pull-request state, the unmerged-branch scan), never a session transcript. Three verdicts with distinct exit codes keep "not fired yet" (`pending`, 5) apart from "ran and failed" (`fail`, 1); Slack rows are advisory and can only be `null`. `docs/loop-drill-runbook.md` maps every named abort reason of both routines to the one file to read. 2394 passing / 0 failed - 3 tickets
* [work-20260812-193239](work-20260812-193239.md) - Stopped `archive.sh` leaving a ticket half-archived: it moved the file into `archive/<branch>/` and only then ran the commit-subject gate, so an off-policy subject left a staged rename with no commit and the obvious retry dying on `Ticket not found` — recoverable only by the manual `git mv` the drive workflow forbids. The canonical `check-subject.sh` now runs before anything moves, and an audit records why no other refusal in the seam is reachable after the mutation. v1.0.166; 2327 passing / 0 failed - 1 ticket.
* [work-20260812-190851](work-20260812-190851.md) - Completed the `make-the-workflow-scripts-survive-a-graphql-restricted-gh` mission (3/3): the last four GraphQL-backed `gh` call sites — `report/create-or-update.sh`, `mission/list-related-prs.sh`, `feedback/open-issue.sh` and `branching/list-worktrees.sh`, the last named by no ticket and found only by the sweep — now go through the one REST transport, and the hermetic suite fails on any non-comment `gh issue|pr|repo <verb>` with an empty allowlist. Measured finding: `search/issues` is refused too in a bound session, so slug matching moved to a repository-scoped endpoint with local filtering. v1.0.165; 2317 passing / 0 failed - 1 ticket.
* [work-20260812-185209](work-20260812-185209.md) - Unbroke `/catch` on a large ticket corpus (qmu/workaholic#387): `scan-window.sh` passed the whole serialized tickets array to `jq` as one `--argjson` value, and Linux caps a single argv entry at 128 KiB independently of the far larger total `ARG_MAX`, so the scan aborted with `Argument list too long` and emitted nothing at all. Both grow-with-corpus values now go through a trap-cleaned `mktemp -d` read with `--slurpfile`, leaving the filter body and the output envelope byte-identical. v1.0.164; 2290 passing / 0 failed - 1 ticket.
* [work-20260812-183726](work-20260812-183726.md) - Narrowed the cross-repository backstop a second time (qmu/workaholic#384): `check-outbound-body.sh` refuses this repository's bare basename only where it reads as a *reference* — backticked, or beside `repo`/`repository`/`checkout`/`worktree`/`project` in either order, possessives included — so a publish plan's own heading and a verbatim-quoted article title no longer trip a gate that fires after the developer's confirmation and names an unmaskable action. The four exact rules are byte-unchanged and no flag bypasses the check. v1.0.162; 2286 passing / 0 failed - 1 ticket.
* [work-20260812-182547](work-20260812-182547.md) - Routed issue discovery and the whole publish/merge path onto REST through a new shared transport `gather/scripts/gh-rest.sh` (FB 20260812172522): GraphQL-backed `gh issue list`/`gh pr create|merge|view|list` are 403'd in some Claude Code Web sessions, so a restricted tick ingested nothing and left published work pushed-but-unpublished. `pr_failed` now names its cause instead of hiding it behind `2>/dev/null`. Mission two-thirds done — the remaining readers and the anti-regression lint are handed off in the story. v1.0.160; 2273 passing / 0 failed - 2 tickets.
* [work-20260812-180600](work-20260812-180600.md) - Stopped stranding a ticket whose every named mission has closed (qmu/workaholic#382): `plan-units.sh` excluded on a merely non-empty `mission:` relation while only `missions/active/` yields units, so such a ticket was offered by neither path and the queue read as drained rather than broken — six tickets unreachable for weeks in one repository. Liveness moves into a new pure reader `mission/scripts/read-active-relation.sh` keying on the area (not `status`, which preserves /propose's safety property), the test is ANY not ALL across the many-valued relation, and the repair is annotated on the backlog row as `mission_closed` rather than reported as an exclusion. v1.0.160; 2266 passing / 0 failed - 1 ticket.
* [work-20260812-163500](work-20260812-163500.md) - Stopped making the harness's plugin binding a precondition of a run: `check-deps/scripts/plugin-src.sh` resolves the newest plugin tree on the machine (checkout, registry install path, clone, binding) and the run executes from it, so `loaded_version_behind_registry` demotes from a terminal gate to a reported fact and `no_plugin_source` is the only stop left. Root cause measured live: the container binds an image-baked project-scope install while the bootstrap updates only the user scope, costing twelve consecutive `[Implement]` ticks. v1.0.160; 2252 passing / 0 failed - 1 ticket.
* [work-20260811-001834](work-20260811-001834.md) - Closed the `give-propose-and-ticket-a-diagnosis-first-discovery-pass` mission: `/propose` now runs a history-mode discovery pass before scaffolding and records a genuinely unrecommendable fork as an explicit `## Open Decisions` item, and both `/propose` and `/ticket` apply a diagnosis-first rule (stated once in `workaholic:discover`) so a failure-report ask starts with reproducing and localizing the failure rather than adopting the reporter's fix directly. v1.0.157; 2474 passing / 0 failed - 2 tickets.
* [work-20260810-232944](work-20260810-232944.md) - Fixed `workaholic:notify`'s thread lookup at its measured root cause: the `fb:<stem>`/Issue-URL searches now run through `slack_search_public_and_private` (`include_bots: true`) instead of the public-only default, since `dev-<repo>` is a private channel the default search could never see. Reverts a first implementation attempt (a `thread_ref` field committed to the feedback record) that a concurrent developer ruling barred as an irretractable public-repository exposure before it merged; PR #373 was closed unmerged. v1.0.156; 2474 passing / 0 failed - 1 ticket.
* [work-20260810-210319](work-20260810-210319.md) - Fixed the `configure-routines-automatically-via-remotetrigger` mission's stuck `0/3` acceptance checklist (full-path markers instead of bare filenames); `link-acceptance.sh` now refuses a path-shaped marker at write time, repaired this mission's three markers to 3/3, and confirmed no other active mission carries the defect. v1.0.155; 2472 passing / 0 failed - 1 ticket.
* [work-20260810-203952](work-20260810-203952.md) - Scoped the `RemoteTrigger` tool-surface claim to the session class it was measured in and recorded the live drift an attended session found on both routine records; confirmed a start-notification retirement ticket had already been superseded by the auto-merge mission; and generalized `/drive`'s unbound-skill-surface fallback into a rule stated once in `rules/general.md` for every unattended entry point. 2468 passing / 0 failed - 3 tickets
* [work-20260810-202951](work-20260810-202951.md) - Closed the `configure-routines-automatically-via-remotetrigger` mission: `/setup-routines` now detects a `RemoteTrigger`-family tool per-session and converges routine wiring directly when exposed, falls back to the unchanged sheet otherwise, the two routine templates' schedule fix landed in every surrounding doc, and "manages nothing" is restated as session-class-dependent throughout. Minted a follow-up for a discovered acceptance-link marker mismatch. v1.0.154; 2469 passing / 0 failed - 3 tickets.
* [work-20260810-171152](work-20260810-171152.md) - Reconciled `/ticket`'s and `/mission`'s `merge_policy` interrogation wording with the auto-merge mission: both still asked whether a human must review the PR, no longer true for either option; now describe the real distinction (deploy-confirmation timing vs. immediate merge gated at the `release/*` QA window). Fourth and final unit of this run's notification-shape-and-review-model reconciliation chain. v1.0.153; 2468 passing / 0 failed - 1 ticket.
* [work-20260810-170054](work-20260810-170054.md) - Reconciled `drive/reference/routing.md`'s stale `review`-route description against `drive/SKILL.md` §6; minted a follow-up for `/ticket`'s still-stale "a human reviews the PR" framing. Records an urgent process incident: a shell cwd reset mid-run caused `archive.sh` to commit and push directly to `origin/main`, bypassing the PR/scan-gate protocol — corrected by merging, never by rewriting `main`'s history. v1.0.152; 2468 passing / 0 failed - 1 ticket.
* [work-20260810-164914](work-20260810-164914.md) - Softened `workaholic:notify`'s red-alert dedup per the developer's ruling (FB `20260810212924`): a first-occurrence precondition-stop signature (`unbound_in_claude_session`, `loaded_version_behind_registry`) posts a calm `⚪ Paused` root instead of the alarming red-alert shape, escalating to `🔴 Blocked` only if the same signature recurs. Named the shape in the `[Implement]` template as a clause on its existing first instruction rather than a new paragraph, after a first pass broke the pinned "two instructions" assertion. v1.0.151; 2468 passing / 0 failed - 1 ticket.
* [work-20260810-163619](work-20260810-163619.md) - Reconciled the follow-up 🟢/🟠 stale notification-shape references from `work-20260809-085355.md`'s minted ticket: the model had moved twice more since that ticket was written (start post retired 2026-08-11, finish settled on `🟢 Implemented`), so this branch reconciled `drive/SKILL.md`, `drive/reference/routing.md`, `workaholify/reference/routines.md`, and `CLAUDE.md` against `workaholic:notify`, the actual current source of truth, rather than the ticket's own superseded suggested target. Minted a further follow-up for an unrelated stale `review`-route description found in the same file. v1.0.150; 2468 passing / 0 failed - 1 ticket.
* [work-20260810-124512](work-20260810-124512.md) - Softened `/drive`/`/implement`'s survey-step gate on `unbound_in_claude_session` from a hard stop to a warning, generalizing FB `20260810070110`'s live developer correction (issue #338, PR #341). `loaded_version_behind_registry`/`registry_unreadable` stay a hard stop, with the reasoned exception recorded in `drive/SKILL.md`, `survey.md`, `check-deps/SKILL.md`, `workaholify/SKILL.md`, and `CLAUDE.md`. This session itself hit the condition while implementing the ticket and drove the run on checkout-relative script paths. v1.0.149; 2468 passing / 0 failed - 1 ticket
* [work-20260810-090432](work-20260810-090432.md) - Moved both `[Implement]` and `[Propose]` off their GitHub webhook triggers onto the same fixed 30-minute schedule (`0,30 * * * *`), per the developer's explicit ask covering both routines. `[Propose]`'s move leaves an open gap stated rather than hidden: a schedule fire alone gives it no ask in hand, so a pure clock tick still reports `nothing_in_hand`. Re-verified (via `ToolSearch`) that no `RemoteTrigger`-family tool exists in a session, so "revive `/set-routines`" is answered by `/setup-routines`'s existing `cron_expression` rendering rather than a new command. v1.0.148; 2468 passing / 0 failed - 2 tickets
* [work-20260810-083550](work-20260810-083550.md) - Aligned the `/propose` design-start post and every post's attribution line with the developer's own dictated wording in issue #333 (`📐 Designing` -> `📐 Proposing`; `by [Claude Code on the Web]` -> `by the [routine]`), the last delta of the color-code-notify-shapes mission, applied across both routine templates, `workaholic:notify`'s SKILL.md/reference/notifications.md, and two pinned test assertions; 2467/0 with a test-env-pollution concern recorded.
* [work-20260809-201846](work-20260809-201846.md) - Added the *prompt is the ceiling* rule to `workaholic:notify`: a session may emit only the notification events and shapes its own routine prompt or invoking command names — the skill's own documentation and prior in-session reasoning are never authorization, and an addition becomes standing behavior only after developer confirmation (issue #298 / FB `20260807082554`). `reference/notifications.md` reframed as an opt-in catalog. v1.0.144; 2455 passing / 0 failed - 1 ticket
* [work-20260809-085418](work-20260809-085418.md) - Threaded the triggering GitHub issue number through `/propose` so its pull request body carries a native `Closes #<N>` keyword, closing the gap where FB issues stayed open after their Proposal PR merged; live-verified against a disposable issue/PR squash-merged via the GitHub MCP tool. v1.0.142; 2460 passing / 0 failed - 1 ticket
* [work-20260809-085355](work-20260809-085355.md) - Erases the 🟣 purple-circle notification shape (qmu/workaholic#317): its only poster, the [Consent] routine, was retired 2026-08-06, so the shape documented a post nobody makes. Removed from notify/reference/notifications.md, notify/SKILL.md, drive/SKILL.md, drive/reference/routing.md, and CLAUDE.md; the auto/human merge distinction now rests on Auto Merge's presence plus GitHub's own merge record rather than a second emoji. Minted a follow-up ticket for two adjacent, pre-existing stale shape references (🟢/🟠) found out of scope.
* [work-20260809-035121](work-20260809-035121.md) - Replaced `publish-tree-pr.sh`'s enumerated file-path `## Artifacts` listing with a counts summary per `.workaholic/` area and status (e.g. "3 feedbacks added, 1 mission added, 2 tickets added"), with a generic fallback line for paths outside `.workaholic/` and renames folded into `modified`. v1.0.141; 2448 passing / 0 failed - 1 ticket
* [Legibly report the unbound-plugin no-reload gap](work-20260809-031402.md) - Investigated why a SessionStart-installed plugin is not invocable until a human types /reload-plugins, a command no unattended routine ever runs; confirmed against Claude Code's own documentation that no harness fix exists, and reported the gap legibly instead -- check-deps/scripts/check.sh gained claude_session_detected/registry_has_install/unbound_in_claude_session, and /drive terminates pending on the last of these before surveying, mirroring the earlier registry-drift stop. Reproduced live: the session driving this ticket itself hit Unknown skill after its own SessionStart install line, and the patched detector was verified firing against that exact state
* [Right-size /report to single-ticket-per-PR granularity](work-20260809-025458.md) - Right-sized `/report`'s Phase 2 to single-ticket-per-PR granularity: a scale gate on the branch's archived_tickets count routes a 2-or-fewer-ticket branch to one combined opus worker with no mermaid Journey, keeping the original 3-worker fan-out only for a genuinely Story-shaped batch of more than 2. The result record (per-ticket Changes, the story-file commit) and the cross-document relations (frontmatter tickets:/mission:, the stories index, the mission changelog/acceptance roll, the PR body's links) are produced by Phase 3/4/5, which the split never touches, so both survive by construction on either path -- this very report is the lite path's first live exercise, run against its own 2-ticket branch
* [work-20260807-161204](work-20260807-161204.md) - Gave cloud sessions the developer git identity: the first full routine-chain run ended ticket_owner_mismatch because the container pushes and merges as the developer GitHub account while its git identity is the anthropic default, so owns.sh ruled the developer own personally-assigned ticket owned-by-other. The web bootstrap now resolves the session GitHub login through the committed .claude/git-identities mapping (<login>=<email>, one per line) and sets the repo-local git identity -- only when the current email is unset or the anthropic default, never overwriting a real local identity, non-fatal in every branch with one legible log line each way, a malformed login refused rather than interpolated. Identity enters once per developer in a committed file whose emails are already public in git history; the routine prompts stay four lines. Eight new test assertions pin the three contract cases via the suite gh PATH-shim precedent, with GIT_CONFIG_GLOBAL=/dev/null masking the host git config so an unset-identity test actually asserts. Shipped behind v1.0.138 with the full suite green (2433/0)
* [work-20260807-135731](work-20260807-135731.md) - Moved the runtime notification model out of workaholify, the environment-setup gateway, into a new dedicated workaholic:notify skill -- a routine told to find its reply thread per the workaholic:workaholify lookup was being pointed at a setup skill for a runtime procedure, a category error (developer's ruling, 2026-08-07). The model itself survived verbatim in substance: the Q1 stateless thread lookup with its written bounds, per-unit posts, /implement-only scoping, post shapes, mention resolution, red-alert dedup, the bright line and the withdrawn-P9 history now live in notify (SKILL.md 50 lines + reference 59), workaholify shrank 121 -> 87 lines to a one-paragraph pointer, and drive, propose, both four-line routine prompts and the test pins all name the new home. Two relocation pins assert the model lives in notify and that workaholify no longer states the lookup, so it cannot silently grow back -- necessary because computeClosure follows script references only, so a prose-only skill rides the bundles as flattened references and nothing else would notice. Shipped as v1.0.137 with the full suite green (2425/0)
* [work-20260807-125614](work-20260807-125614.md) - Made the Slack reply target stateless (decision Q1), reversing P4 propagation one day after it shipped: instead of carrying a Notify-Thread line in public pull-request bodies, [Propose] and [Implement] each find their own thread through ordered exact-string searches (trigger message, fb:<stem>, Issue/PR URL, else a new keyed root) stated in workaholic:workaholify with the bound written as numbers -- at most two search queries per lookup, no full-channel read, resolved once per run -- and fuzzy/recency matching prohibited by name, since a fuzzy search is exactly the 2026-08-05 defect P4 existed to fix. A lookup that matches nothing declares itself by starting a new keyed root rather than picking the closest thing, which is what makes statelessness safe rather than a regression. WORKAHOLIC_NOTIFY_TARGET and read-notify-target.sh are retired with a plugins/-wide absence pin (WORKAHOLIC_PR_TITLE stays -- a separate fix), P9 accepted URL disclosure is withdrawn, and both routine prompts remain four lines. Two Quality Gate items need a live routine chain and are carried as open verification items: one real Issue-to-reply chain landing in one thread, and the not-found branch exercised against real Slack
* [work-20260807-102749](work-20260807-102749.md) - Encoded issue #300's literal `/propose`/`/implement` start/finish notification templates as the sole sanctioned shapes in `workaholic:notify`, retiring the earlier overlapping 🟢 Proposed/🟠 drive started/🟢 Merge Requested shapes while keeping the outcome-specific finishes (merge, handoff, blocked) that carry information the generic line would lose. 2441 passing / 0 failed - 1 ticket
* [work-20260807-102500](work-20260807-102500.md) - Slimmed the whole plugin surface for its primary reader, the AI agent that runs it: fourteen commands became thin skill-aliases (850 -> 203 lines) with every flow moved into its owning skill, sixteen oversized SKILL.md files went 6,040 -> 2,219 lines with genuinely needed detail relocated into reference/ companions and incident narration folded into per-skill Caveats, five unread ticket frontmatter fields were retired making the commit Category: trailer the single category source, tests and scripts were pared to the load-bearing set via a full caller map (one orphan script; 54 prose pins retargeted, 2,415 passed), README and CLAUDE.md were reframed agent-first with the developer as operator, the README gained a Documentation section over a previously orphaned docs/ tree, and a minted hook fix lets a drive run append its Final Report to a routine-authored ticket without rewriting provenance
* [work-20260807-101724](work-20260807-101724.md) - Made `archive.sh` push the claim branch itself immediately after the archive commit, reusing `heartbeat.sh`'s non-blocking push convention, so a finished claim never reads as `resumable` again just because the push was left to the driving session's discretion. 2445 passing / 0 failed - 1 ticket
* [work-20260807-081102](work-20260807-081102.md) - States that any legitimate /fb invocation is authorized regardless of how it reaches the session, so a bot-relayed ask via a Slack integration account is no longer treated with suspicion: skills/feedback/SKILL.md gains an Any legitimate invocation is authorized section and commands/fb.md points to it at trigger time, explicitly leaving the cross-repository crossing confirmation and release-scan gates unchanged
* [work-20260806-185233](work-20260806-185233.md) - Reduced the loop to two routines and one behaviour per command. The unattended executor became /implement and /drive went back to being interactive: O1 had carried both attendances behind a first word (auto, night), which made the contract every unattended caller rests on into an argument the caller could forget to pass, and forgetting it parks the tick on a prompt -- the exact failure O1 existed to prevent. A ticket's owner moved out of its PATH and into an assignees field, adopting the mission's model verbatim (plural, empty means team-owned and claimable), because todo/<user-slug>/ made "could not read" and "nothing queued" the same observation -- an hourly runner reported a healthy idle tick over a full backlog, and the proposed fix was a git config line in the routine prompt, the third patch in that area in a week. The survey now reads the queue whatever its identity and reports backlog_size plus owner_unresolved instead of terminating; the claim protocol's identity use is deliberately unchanged, since "is this my own run" must fail loudly. The routine set went from three templates to two four-line ones, retiring [Consent] at a stated cost: a human-merged pull request is now announced by nobody. /propose hands the notification target forward as a Notify-Thread line in the pull request body rather than letting the next routine re-derive it by search, the step that put a reply in the wrong place on 2026-08-05 -- and doing so surfaced that the documented [Proposal] title prefix could never actually be written, because one string went to both the PR title and the commit subject and check-subject.sh forbids a bracket prefix. Finally every literal-word dispatch went: ending a mission is /mission-close (moved rather than dropped, because close.sh is the only sanctioned writer of an end state), while /mission summary, /mission approve and /ticket summary were deprecation stubs for modes retired weeks earlier -- the stub itself was the surviving fork
* [work-20260806-153117](work-20260806-153117.md) - Turned /setup-routines from an API management surface into a copy-paste setup sheet renderer, and deleted the machinery behind the old one. A routine's GitHub trigger is configurable in the web UI only and the API record carries no event field, so the command managed the half the API exposes while blind to the half that decides whether the routine runs at all -- measured on one day, a paginated list (20 rows, has_more unread) surveyed as the whole account and six duplicate records carefully refreshed through the digest gate while the real wired [Propose] ran a stale prompt beyond page one, a drift-free fleet reported both times. Each template now declares its trigger as structured data (trigger_kind/trigger_event/trigger_filters) and render-setup-sheet.sh DERIVES the UI steps from it -- so a template whose trigger changes cannot leave a stale procedure behind in prose -- emitting name, model, repository, the prompt verbatim through the existing renderer, connectors and the dev-<repo> channel. Seven scripts and ~1370 lines went with their tests, and the retirement reached four documents plus two analogies that cited authorize-routine-change.sh as the canonical cannot-prove-a-human example, one of them inside land-unit.sh's own header in the drive critical path: a deletion has to sweep for script NAMES, not just callers, because an analogy breaks nothing and simply sends the next reader to a file that is gone. What the machinery got right is preserved as prose beside the retirement -- the digest gate closed substitution and batching, could-not-check is never does-not-exist, the account has no delete -- so nobody restores the code to recover the principle
* [work-20260806-152604](work-20260806-152604.md) - Settled how a Claude Code Web routine is actually triggered, after two sessions on two days drew opposite wrong conclusions from the same absent field and the repository carried both. The product documentation answers it: routines fire on a schedule, an API call, or a GitHub event (Pull request / Release, AND-combined filters including is-merged), and GitHub triggers are configured from the web UI only -- the record carries no event field, so the wiring is unreadable, unwritable and unverifiable from a session, and a template's trigger: states the DESIGNED trigger for the reader. The per-template design is recorded beside it: an assigned issue opening fires [Propose], a proposal's pull request merging fires [Drive], and [Consent] owns the merge announcement, one event one owner. The sharpest finding is that last_fired_at is trigger-kind-dependent rather than routine-dependent -- populated for a cron fire ([Drive]'s 02:56Z tick) and absent for a GitHub one ([Propose] turning issue #266 into PR #267 at 03:23Z) -- so it distinguishes nothing for exactly the routines that matter, no claim may rest on it, and whether a routine ran is read from what it produced rather than from the account. The ticket's observability half was declined deliberately: it aimed at the /setup-routines report, which a queued mission retires in favour of copy-paste setup sheets
* [work-20260806-150951](work-20260806-150951.md) - Made the SessionStart bootstrap provision gh, which the Claude Code web container does not ship and which fourteen plugin scripts shell out to: publish-tree-pr.sh pushes the artifact branch and then reports no_gh instead of opening the pull request, and merge-pr.sh cannot run at all, so every cloud auto unit was demoted to the PR path and every routine-published artifact waited for a human to open its PR by hand. Two earlier records had asked for the container IMAGE to ship gh -- which nobody in this repository can change -- so this moves the remedy to the one startup seam the repository owns, where the measured facts make it a package away (gh 2.45.0 in the distro archive, session running as root, GH_TOKEN already injected). The step is command -v gh-guarded so a provisioned container pays nothing and a resume/clear/compact refire is a no-op, root-only, and non-fatal in every branch: gh still absent afterwards is the status quo rather than a regression, and the hook says so once with the no_gh consequence named rather than failing silently at the first publish-tree-pr.sh call. All four paths were exercised against a hermetic PATH with shimmed claude/id/apt-get plus a second run proving idempotence; the surprise was that check-bootstrap.sh resolves the canonical copy relative to its own script location, so running the installed plugin's copy against a worktree reports a false hook_stale on byte-identical files
* [work-20260806-150055](work-20260806-150055.md) - Made the routines report honest about its own blindness: the per-routine trigger it prints is the template's declaration, not live wiring -- the API exposes no trigger field -- and on 2026-08-06 a merge-wired [Propose] read as github-issue-assigned in the report until a manual UI check surfaced the mismatch. The qualifier now travels in the data (trigger_source: template_declared per row, trigger_readable: false at top level) so no renderer can drop it, and step 4 states the limit in the report itself with the routines-UI pointer. En route, main's CLAUDE.md was repaired from a conflict-resolution defect that had survived seven merges: a hand-scripted resolution in PR #263 sliced by 0-based index over 1-based line numbers, consuming the entire /setup-routines table row and leaving a stray conflict marker -- invisible to CI because nothing parses CLAUDE.md content, for which a one-line conflict-marker grep is recorded as the fix
* [work-20260806-145355](work-20260806-145355.md) - Made /drive's superseded-binding stop reachable from a local session. check.sh computed the loaded-vs-registry axis only when CLAUDE_PLUGIN_ROOT was in the shell environment, and the harness expands that token in plugin markdown rather than exporting it -- so the sanctioned local step-0 invocation always landed in the silent branch and the one pre-flight stop never armed on the machine a developer actually types /drive on (the cloud container exports the variable and had fired the gate that same morning). The stand-in is the script's own resolved path, accepted only when it sits inside the harness's plugin cache with no env var present -- in that invocation the caller reached the script through the expanded token, so "where is this file" and "what is this session running" are the same answer -- and loaded_root_source (env / cache_path / none) reports which case decided. The deliberate silence for checkouts, bundles and non-Claude agents is preserved byte-for-byte and still pinned; CLAUDE_PLUGIN_CACHE exists purely as the hermetic test seam, without which the new branch would be testable only on a machine with a real install, which is how this class of gap survives
* [work-20260805-224001](work-20260805-224001.md) - Two answers to one measured outage: four consecutive hourly ticks that produced nothing while the channel read as a healthy idle fleet. The red-alert dedup rule was written against a failure that REPEATED and had no answer for one that PERSISTS through the whole 24-hour cool-down, so a suppressed tick now replies in the existing alert's thread instead of going silent, carrying elapsed time and a tick count -- information the previous reply did not have, which is what keeps it from being the repeat the rule exists to remove. Every suppressed tick replies and the reply is deliberately not rate-limited, because a throttled one reintroduces one level down the same ambiguity the top-level suppression created; the exponential backoff alternative is named and rejected. The failure underneath went looking for its repair and found none inside the plugin: the cache holds 25 version directories side by side while installed_plugins.json carries exactly one entry, so a session never picks the wrong directory -- it binds what the registry named AT STARTUP, strictly before the SessionStart hook can run. That kills the obvious sweep twice (it repairs nothing, and it would delete a directory a running session is bound to), and because every tick is a fresh container off the same image the condition reproduces hourly rather than self-healing, which is why the runbook's "start a fresh session" advice is corrected. A fresher image or a harness that rebinds after SessionStart is the actual fix -- an ask, not a workaround. En route the gate itself turned out to be unreachable: check.sh computes the registry axis only when CLAUDE_PLUGIN_ROOT is in the shell environment, and the harness expands that token in markdown rather than exporting it, so /drive's one pre-flight stop has never fired
* [work-20260805-221704](work-20260805-221704.md) - Answered the ask that a drive run start when a proposal's pull request merges, and found the trigger does not exist. Read back over the live account, a Claude Code Web routine record's whole trigger surface is cron_expression, run_once_at and an API token for an external caller POSTing /run -- there is no event-subscription field of any kind, so trigger: event never meant "watches an event", it meant "carries no schedule and waits to be invoked". A second claim from the same read -- that nothing invokes [Propose]/[Consent], since neither carries a last_fired_at -- was RETRACTED on 2026-08-06: a web session did the [Propose] routine's exact job on 2026-08-05 with the key still absent, so the key distinguishes nothing and the mechanism is an open question (see the story's Correction section). So [Consent] keeps ownership of the merged-pull-request event (one event, one owner) and [Drive] keeps its clock -- not as a floor beneath an event path but as the only path, and it survives an invoker anyway because handoff resumption, a lapsed claim and any /ticket-written ticket have no merge to key on. The invoker itself is queued rather than attempted, since standing up an outward-facing process is a human act. The reporting half shipped: a unit's start and finish are now per unit rather than per run -- "a run started" names no item and so had no thread to land in -- resolved to the fb:<stem> key by a new unit-feedback-stems.sh reading the relation through its one existing reader, posting into each of several stems' threads, keying on unit:<unit-id> when there are none because the start predates any pull request, and treating a handoff as the finish rather than a third post
* [work-20260805-182921](work-20260805-182921.md)
* [work-20260805-180653](work-20260805-180653.md)
* [work-20260805-105141](work-20260805-105141.md) - Gave /drive a second invocation form and its first prompt. Decision G1-G2's "no drive-time confirmation of any kind" was written against the per-ticket approval prompt -- may I do this -- and swept up a different question with it: which of these first. Measured on the 2026-08-05 morning run, an attended session spent its opening 40 minutes reopening a pull request the developer considered parked, because resumable-first ordering outranked their actual work in progress, and they interrupted twice to ask why; the parked_with_pr tier had narrowed that heuristic without moving the decision. Bare /drive is now the attended form: above one claimable or resumable target it asks exactly once, multiSelect over the partition it already reports, and drives the chosen units in the chosen order while reporting the rest as deferred_by_operator -- still claimable, so still forbidding the ok token. /drive auto (night as its synonym) is the unattended form, prompt-free at every step. Attendance follows the caller's invocation form and is never inferred from a TTY or the environment, since a wrong inference either parks a cron tick on an unanswerable prompt or silently strips the developer's choice; that made the callers the real blast radius, so the [Drive] routine template, the runbook cron line and five /goal /drive ok references all had to name auto in the same change. The drive scripts are untouched -- the gate is command and skill prose -- so fourteen sentinel pins hold both forms, one of which replaced a doc pin that had been vacuously passing since its heading was renumbered
* [work-20260805-101736](work-20260805-101736.md) - Made the repository boundary crossable as a GitHub issue on the target rather than a ticket file copied into its checkout, and retired /request in the same branch. The old route made this repository a writer into someone else's tree, delivering an ask as a file appearing in a git status its owners never expected; an issue arrives where they already read and their own [Propose] routine ingests it like any other inbound report. The retirement was sequenced as a relocation: the destination -- fb.md's routing branch, the feedback skill's Crossing a repository boundary section, and the scripts for target resolution, a body-scoped release scan and gh issue create -- was built and tested first, the shared identifier matcher was extracted while both callers still existed so no assertion had to be written twice, and only then were commands/request.md, skills/request/ and submit-request.sh deleted. Everything load-bearing survived with its measured history intact and dated: the one non-skippable verbatim confirmation, the judgement that no matcher can replace masking, 2026-08-02's identifier-not-substring narrowing, 2026-08-04's every-URL-form reading under a git insteadOf rewrite, and the assertion that the four real leaked sentences still pass unflagged. A live defect surfaced en route and was fixed: gh api prints its error body to stdout with a non-zero status, so the idiomatic fallback concatenated a 404 blob with the word unknown and made the visibility field -- the one the confirmation shows a human -- unparseable
* [work-20260805-002946](work-20260805-002946.md) - Made `release-claim.sh` report the composite state of a release instead of a bare pass/fail. Its two steps can disagree: the worktree comes down before the remote claim branch is deleted (so a refused teardown never advertises "this unit is free" over unpushed work), and on the hourly runner the delete can never succeed at all -- that container may push but not delete a branch, measured as 403 twice on a remote that had accepted several pushes minutes earlier, with the GitHub MCP server offering `create_branch` and no delete. Every such release ended worktree-gone / claim-live under `{"released": false}`, indistinguishable from a no-op. Now `state: released | half_released | untouched` names it, the refused-delete path reports which half ran and what a human must do, and two alternatives are recorded as rejected in the script header rather than left to be re-proposed: a tombstone commit (a second, weaker release channel over a protocol resting on one invariant) and inverting the order (it would publish a unit as free over possibly-unpushed work). 2259 passing / 0 failed - 1 ticket
* [work-20260805-002647](work-20260805-002647.md)
* [(cloud keys, GitHub/Slack tokens, bearer/basic auth, PEM keys, password=/token=](work-20260805-002631.md)
* [work-20260804-221713](work-20260804-221713.md) - Moved proposing into the session that receives the ask, so the feedback record and whatever the judgment warrants leave in one pull request whose merge approves both. The retired shape read only feedback already merged to main, which made the record a capture session had just written invisible to it by construction -- so a batch seat, a cron cadence and a shared pushed cursor existed purely to compensate, and record-only meant the same thing whether the proposer had judged or simply could not see. All of it is deleted with the reversal recorded and dated, the shipped template set is back to three, and proposal-cursor left the .workaholic root allowlist because the fold that justified it was in a script this branch removes
* [work-20260804-212445](work-20260804-212445.md) - Trued up the repository's front door, which still taught the mission lifecycle retired by K1/K2/J4: a draft status a human approves, a /mission approve subcommand deleted on 2026-07-31, a worktree spun up at mission creation, and artifacts publishing straight to main. The drift was already recorded as an open concern from PR #173 that deferred the fix to keep that branch narrow, and three sibling documents had been corrected since, leaving README.md as the last stale copy and the only one a new reader meets first. Every replacement was transcribed from CLAUDE.md and .workaholic/README.md rather than re-derived from the scripts, because re-deriving would have produced a fifth description of one model. The ticket named six spots and the whole-file re-read found about twice that -- the deepest being the two Mermaid diagrams, which drew draft to approved as topology, a form a reader absorbs faster than prose and no wording fix reaches
* [work-20260804-202110](work-20260804-202110.md) - Made the proposal loop able to actually propose, by closing the four independent reasons it produced nothing. The loudest was that the cursor was runner-local while the routine that would run the batch starts a fresh container every tick, so every firing bootstrapped to the tip, reported initialized: true and stopped -- permanently, while reading as healthy; it is now a pushed ref bootstrapped once per repository and advanced under --force-with-lease, with git's own behaviour measured first (pushes to refs/workaholic/* are fast-forward-checked without a force flag, and the lease is consulted only when the update changes the ref, which is why the race test advances to a different commit). Fixing that alone would have changed nothing observable, because the batch had no runner at all: the runbook prescribed a crontab nobody installed, and the only place /propose ran was inside the [FB] routine's session where the record it had just written was still on an unmerged branch and invisible to its own window by design. So a [Propose] routine template ships and [FB] stops attempting the step. Underneath sat two quieter ones: work requests were arriving as kind: concern, which the bar deliberately ignores, so the deciding question now lives once at the capture seam; and an atomic ask had no artifact to become, so it now becomes one loose backlog ticket carrying its own feedback: refs, with the dedup set widened to the union across missions AND tickets, archive included -- because a driven loose ticket is the strongest evidence its feedback was acted on. The version bump was deliberately skipped: two sibling branches from the same base had already claimed 1.0.127
* [work-20260804-202056](work-20260804-202056.md) - Measured why four edits meant to shorten the branch story made it 25% longer, then bounded the cause. Holding the workload at one ticket per story, all four existence edits delivered (-23.8 lines) and were swamped by +49.8 lines of prose in the sections that always exist; Concerns held its block count exactly while lines per concern rose 61%. Each section now carries a line budget set to its own measured pre-growth mean, with Handoff and Deployment Evidence exempt as evidence rather than prose. Verified by regenerating a shipped story: 135 to 87 body lines with every concern, severity and fact preserved
* [work-20260804-202044](work-20260804-202044.md) - Closed the last seam that could create a mission without tickets. The floor -- two or more tickets or it is not a mission -- was decided and written into three documents while close.sh still minted a ticketless successor from --successor-title, with a comment saying the refusal was sequenced behind an inheritance fix that had since merged; so the specification was the disagreement itself, and the mint branch was DELETED rather than gated, because sixty unreachable lines no test exercises still read as a description of what the script does. check-floor.sh splits enforcing from counting: queue-size.sh must stay a pure read (plan-units.sh and list.sh survey sub-floor missions routinely), so the verdict and its refusal text -- which names the alternative artifact, a feedback record or a plain ticket -- live in a separate script every creation seam calls. The cleanup half re-measured its own premise instead of trusting it and found one of the two named sub-floor missions already replanned; the archived one is annotated as pre-rule history with its file untouched, and six documents now state the floor
* [work-20260804-200217](work-20260804-200217.md) - Split the resumable offer into two tiers after an attended run spent its opening 40 minutes reopening a pull request the developer considered parked, while their actual WIP waited. A review unit stops at its PR by design, so its branch stays unmerged, its tip stops advancing and its heartbeat lapses exactly like an abandoned run's -- mechanically resumable, humanly wrong to force. The distinguishing fact turned out to be already recorded and simply never read: /report commits the branch story as it opens the PR, so that file at the tip is durable OFFLINE evidence the unit reached the PR seam, which kept the verdict free of the gh call that would have broken the scan's offline-capable property. heartbeat_lapsed stays the mandatory takeover that forbids ok; parked_with_pr is reportable and does not. Underneath sat a sharper defect: claim.sh resume failed with worktree_creation_failed in the exact case resumption exists for -- your own claim on your own machine, where the worktree is still on disk -- so runs hand-rolled the takeover by replicating the script's internals; it now adopts that worktree, gated on the same pinned tip the race check uses, and an abort never tears down a worktree it did not create. The CARRY-ranking alternative was deliberately not taken and the reason recorded, since it reorders offers across artifact kinds rather than adding a distinction
* [work-20260804-195932](work-20260804-195932.md)
* [work-20260804-195257](work-20260804-195257.md)
* [work-20260804-194806](work-20260804-194806.md) - Made `/request` treat a repository's origin URL as the several strings it actually is. Under a git `insteadOf` rewrite — which the cloud runner's own container injects through the `GIT_CONFIG_COUNT`/`KEY`/`VALUE` triple, invisible to `git config --global --list` — `git remote get-url` and `git config --get remote.origin.url` name one repository differently, so the clone-URL backstop compared bodies against a form no developer would paste: a body carrying this repository's literal clone URL fell through that rule entirely and was refused by the unrelated `owner/name` rule, naming the wrong thing to mask, past a human gate that had already confirmed the body verbatim. New `lib/remote-url.sh` is the single reader; `submit-request.sh` matches every form with all clone-URL forms tried before any slug (every clone URL contains its own slug, so the order is what decides which rule is reported); `resolve-target.sh` reports the configured URL a colleague could clone rather than one a local rewrite invented. The ten new cases configure the rewrite in their own fixture rather than sensing the environment, so they run on CI, which has no rewrite and could never have caught this. 2187 passing / 0 failed - 1 ticket
* [work-20260804-194735](work-20260804-194735.md) - Made the claim scan refuse to answer over history it cannot see. In-flight-ness was git rev-list --count base..ref, which cannot be reduced across a shallow graft, and the cloud container the hourly runner always starts in clones shallow -- so a branch merged as PR #109 counted 154 ahead instead of 0 and was offered for takeover, past BOTH safety gates (the identity matched because this runner had claimed it, and the heartbeat had lapsed precisely because the work finished). Roughly 180 merged-but-undeleted branches were candidates and which surfaced depended on clone depth, so the symptom was intermittent -- the worst shape for diagnosis. Two deliberately different answers: repair when possible, since claims_fetch can deepen once per container and restore the actual question, and degrade loudly when not, since an unreachable origin leaves shallow:true forbidding ok and every unprovable branch reporting resumable:false with reason shallow_history rather than heartbeat_lapsed. The claim is still listed, because over-reporting makes a runner wait while under-reporting double-picks work. Verified in the live shallow container before and after; the fixture gap was the real deliverable, since every existing fixture was a complete clone and so exercised the one condition the production runner is never out of
* [work-20260804-185735](work-20260804-185735.md) - Fixed two ship merge-path defects that shared one failure mode: a field named for one thing carrying another, and a classifier reporting "needs a human" for a case it could decide. merge-pr.sh returned the work branch's head as commit_hash, which Ship Flow step 7 feeds to the release step, so a release-on-tag project tagged a commit that is not on the base's first-parent line -- observed on two consecutive ships and recovered by hand each time; it now resolves GitHub's own mergeCommit, then the fetched base tip, and reports commit_hash_source, on_base and branch_head so a tag step can refuse a target the script could not place on the base. catchup-main.sh classed a mission ## Changelog or an OKF index.md as a content conflict because it classified by path, which unattended costs an auto unit its merge rather than a prompt and recurred on every concurrent pair; it now resolves those itself by PROVING the shape -- the merge base is an exact line-prefix of both sides -- rather than by a path list that would drift, bounded to .workaholic/ deliberately because appending is only evidence of independence in a log. A merge that never started stopped reporting as a mechanical conflict over an empty file list
* [work-20260804-113856](work-20260804-113856.md) - Audited seven gates for the failure mode that broke tick-acceptance.sh, each verdict cited from the code and carrying a count over live artifacts. None reproduces it, and the reason sharpens the mode: most of these gates DO depend on a shape, but the defect is narrower -- the shape must make a real condition UNREACHABLE, which none of these do. validate-ticket.sh would refuse 434 of 635 tickets for a missing ## Policies section and refuses 0, because it is scoped to todo/<user>/: the scoping is the correctness property, not an optimization. Nothing was fixed, per the ticket's own bar of changing only a measured misreport, and two gaps are recorded -- 56 of 102 stories carry no type:, so the documented OKF conformance of the live tree is false for 55% of that area with nothing measuring it; and size.sh's norm is satisfied by 0 of 10 missions ever written, four of which were closed as achieved while outside it. Closes the mission at 7/7
* [work-20260804-113101](work-20260804-113101.md) - Made the hourly [Drive] routine dedupe its red failure alerts. Measured 2026-08-02〜04: one near-identical blocked post per hour for two days from a single root cause, which trains the operator to ignore alerts. Each tick is a fresh container so no local state survives, but the Slack channel does and the routine already posts there, so the throttle is a read-before-post rule -- stable failure signature, 24h cool-down on the same signature, immediate post on a changed one -- rather than a counter needing somewhere to live. It fails OPEN because a throttle that cannot read its evidence and stays quiet converts a notification defect into a monitoring outage, and a suppressed tick names the suppression in its own terminal report so quiet-because-healthy and quiet-because-known-repeat are distinguishable. The live routines are deliberately NOT refreshed here: that is a verbatim one-at-a-time human act and /drive issues no confirmation at all
* [work-20260804-112542](work-20260804-112542.md)
* [work-20260804-112404](work-20260804-112404.md) - Made /request's last backstop match an identifier instead of a substring. A case-insensitive grep -F over the caller's basename refused any body where that word appeared anywhere -- including inside directory names belonging to the TARGET repo -- so a repository whose name is an ordinary English word could not submit a request whose body was seventy path pairs, and the refusal fired after the developer had already confirmed the body verbatim with an instruction to "mask" something that could not be removed. The exact owner/name and clone-URL checks went in FIRST, because the substring rule had been catching those only incidentally and narrowing alone would have lost true positives; the bare name then became adjacency-aware, the absolute-path check was left byte-identical, and every refusal now cites the matched text and its line, because an unexplained refusal past the human gate is what made the original defect unrecoverable
* [work-20260804-111346](work-20260804-111346.md) - Stopped two "changes nothing" operations from mutating the caller's git index. git commit --allow-empty only PERMITS a changeless commit, so a heartbeat fired mid-ticket over a staged git rm produced a Refresh heartbeat commit carrying three real deletions -- content right, message wrong, and the story then attributed real work to coordination noise; the beat is now built against a scratch index seeded from HEAD, so its tree equals HEAD's by construction and the caller's index is byte-identical. The concern migration, which runs as a side effect of a documented pure read, stops staging entirely and reports migrated: N instead, because a reader that commits is a bigger surprise than the 154-file commit being fixed. Neither defect is visible to any gate, so both tests assert on git show --name-only rather than on exit codes
* [work-20260804-105730](work-20260804-105730.md) - Made the claim reader's artifact resolution exact instead of statistical. Two independently-reported defects -- a survey offering already-driven tickets as fresh backlog, and a finished unit reported resumable -- were one cause: claims_scan followed archive.sh's rename with git's similarity-based detection, which does not fire when the appended Final Report drops the file under the 50% threshold, and the resulting empty artifact list is what both consumers misread (plan-units.sh subtracts a claim BY its artifacts, so it stopped subtracting and left no excluded[] row; claims_has_work took its "no artifacts means unknown" branch and invited a takeover of a branch under review). Reproduced against the real scripts before reading for a cause, which is what collapsed the two tickets into one and falsified the second's stated hypothesis. The fallback is an exact lookup on the ticket filename, scoped to tickets and to a single match, because every mission's artifact is named mission.md; the regression test asserts git really sees no rename before asserting the claim survives it
* [work-20260804-091426](work-20260804-091426.md)
* [work-20260804-085951](work-20260804-085951.md) - Closed the drift that made every hourly tick fire on time and do nothing for two days: an installed plugin older than the checkout ran the pre-K1 mission migration BACKWARDS on every prompt via the always-on lens, staged the rewrite, and left a dirty tree that sync-main.sh treats as terminal -- so each tick died before it learned what was queued. The fix is split deliberately between legibility and structure, because a bare "1.0.112" reads as healthy to anyone who does not know what the repository is on: check.sh now reports checkout_version and version_drift beside the loaded version, and drift is a WARNING by recorded decision, since the hard stop already exists where drift actually bites. The durable guard is that the status fold is a whitelist -- unknown means newer, so an older build cannot rewrite a vocabulary invented after it shipped -- and the test pins the SHAPE as well as the behavior, asserting an unrecognised word survives byte-identical AND unstaged, because staging is the separable half that turned a cosmetic rewrite into a loop-stopping one. The condition recurred while the ticket was being driven and was measured rather than narrated: the stale cache and the checkout disagreed about whether a unit was claimed, which is the double-pick the claim protocol exists to prevent
* [work-20260804-084744](work-20260804-084744.md)
* [work-20260804-035024](work-20260804-035024.md)
* [Make the branch story concise by default](work-20260803-212338.md) - Made the branch story concise by default: a section with nothing to report is now absent rather than padded with "None", Historical Analysis folds into Motivation, Release Preparation flattens to one labelled list, and section numbers run sequentially over whichever sections survive -- so both consumers that matched "## 6. Concerns" now match the heading by name, since a number-keyed miss is indistinguishable from a branch that raised no concerns. The instruction to list only concerns above low would have deleted knowledge, because extract-deferred-concerns.sh reads the committed story as its only source, so low concerns are filtered at RENDER and kept at EXTRACT: the PR body drops them and says how many, the story file keeps every severity. Measured on the previous branch story rather than asserted -- file 125 to 100 lines, body 120 to 85
* [Make a repository able to say what runs against it](work-20260803-212331.md) - Made a repository able to say what runs against it. The mission asked for configurable routines; the measured gap was inspectable, so the repository declares nothing and /setup-routines asks the Claude Code Web account and reports -- each routine with its trigger, schedule, target and per-field drift, and an unreachable account reported as checked:false with no routines key at all, so "I could not look" can never render as "nothing runs here". Adding, refreshing and removing a routine is gated by a digest over the confirmed body, closing substitution and batching without pretending to prove a human was present, and "remove" means disable because the API has no delete
* [Make acceptance ticking measure satisfaction](work-20260803-212324.md) - Made acceptance ticking measure satisfaction rather than marker shape. An acceptance item could only be checked if it carried a `(#filename)` link, and nothing ever wrote one: re-measured 2026-08-03, all 19 items on the four interrogation-authored missions were linked and all 37 on the six proposal-scaffolded ones were not, the one such mission that reached the archive closing `abandoned` at 0/9. The link stays the marker but is stamped by the seam that emits the ticket set (`link-acceptance.sh`, pairing supplied by the caller and never inferred), the match became item-scoped so a wrapped criterion is no longer unreachable, and the ticker's single negative reason split into `unlinked_items` (not addressable) versus `no_unchecked_match` (not satisfied) with an `unlinked` count on every `progress.sh` read. This mission's own board was repaired through that path -- 28 stranded items down to 21, no checkbox hand-edited. v1.0.119; 1878 passing - 3 tickets
* [Add a release staging tier with a durable ship record](work-20260803-212310.md) - Added the release staging tier the branching feedback asked for, deliberately smaller than "Git Flow": exactly one new branch form, release/YYYYMMDD-HHMMSS cut from the base as a QA window, and nothing else -- no develop, no hotfix/*, and the per-unit claim/branch/worktree/PR path untouched, which is verifiable rather than asserted since plugins/workaholic/skills/drive/ has zero changes on the branch. The two questions that blocked the original draft (what a claim means against a second base, what merge_policy means under two merge targets) are dissolved by never creating the second base. Promotion is batch-level and explicitly invoked so an auto unit is observably unaffected, and the durable record .workaholic/releases/<release-branch>.md derives everything from git at cut and confirm time with a literal commit range, so grep and git log answer "what did this deploy carry, and when" without opening GitHub; a failed confirmation is recorded rather than erased and its branch kept as the rollback boundary
* [Give non-ASCII concern titles stable hash ids](work-20260803-210404.md) - Gave non-ASCII concern titles stable hash ids in extract-deferred-concerns.sh. The ASCII word slug reduced a Japanese-only title to the literal fallback id concern, so the first such record occupied the id and every later Japanese concern was silently dropped as a duplicate -- a real run extracted zero records from a story carrying two live concerns. Titles the word slug cannot represent now hash their normalized form into c-<sha1[:8]>, ASCII ids stay byte-identical since ids are the stream's permanent keys, and fallback usage rides the output JSON as fallback_ids so the divergence is visible rather than silent
* [Count added implementation, not motion](work-20260801-210440.md)
* [Claim a unit by trailer, not by subject](work-20260801-205224.md)
* [Degrade the PR seams when `gh` is absent](work-20260801-134910.md)
* [Stop resuming a unit that already finished](work-20260801-110823.md)
* [Re-verify the containerization policy blocker](work-20260801-051756.md)
* [Make a claim survivable: announce it, resume it, hand it off](work-20260801-051742.md)
* [work-20260801-042633](work-20260801-042633.md) - Fixed the duplicate merge notification: two PRs merged four seconds apart produced four Slack messages because one routine started two sessions and each announced both recent merges, the prompt never having said which pull request it meant. The corrected template announces exactly one PR identified by its trigger and posts nothing when it cannot identify one, the Attention block is removed from both PR formats, and all fifteen live routines across seven repositories now carry the templates -- the first fleet-wide application of the one-template-set model
* [work-20260801-033154](work-20260801-033154.md) - Made /workaholify survey and refresh a repository Claude Code Web routines against three templates the plugin ships, after the first attempt targeted crontab and was withdrawn whole. One template set applied to many repositories, matched to a repo by source URL rather than name, with drift reported per field and every create/update confirmed verbatim. The withdrawn design own survey had reported crontab false on a machine whose loops run hourly, and that contradiction was filed as a concern instead of read as a refutation of the premise
* [work-20260801-023444](work-20260801-023444.md) - Made the workflow reclaim the storage it allocates, after a machine driving it across four repositories hit 100% disk with 53GB in .worktrees/ and 31GB of that fully merged and clean. Teardown was not missing but conditional -- three correct implementations all needing some run to reach its end -- so the answer is a sweep alongside them: survey reports size/ahead/dirty per worktree, reap removes only the merged AND clean ones with every skip named, and prune reclaims build output from a worktree worth keeping. The first smoke run against the real repo caught the survey nominating the MAIN checkout as reclaimable, because show-toplevel answers which tree you are standing in
* [work-20260801-022239](work-20260801-022239.md) - Gave missions the ceiling they never had: ## Scope deleted from the template and the /propose draft scaffold (nothing read it), ## Acceptance normatively three items or fewer, and a 60-line / 2KB document limit measured by a new size.sh. The norm reports for a human author and is enforced on the unattended batch, because the question is who holds the pen rather than how important the rule is; the approved floor is untouched and a test proves it, since a ceiling is not a loosened floor
* [work-20260801-012313](work-20260801-012313.md) - Widened /propose from a feedback-only reader into a state surveyor (missions, todo queue, commits since the cursor) whose bar keeps those new inputs as constraints that can only shrink a proposal, made a proposal carry its ordered ticket set rather than a title, and moved every artifact writer onto a work-* branch behind a pull request via a new publish-tree-pr.sh. The publish tree already solved the isolation half so only the push refspec changed, proposed tickets needed no new gate because plan-units.sh already drops any mission-related ticket as mission_member, and decision J1 is amended in writing with what the trade costs
* [work-20260731-201257](work-20260731-201257.md) - Fixed close-publish-tree.sh refusing on the happy path. Its success condition was that publish-main had reached origin/main, which stopped being true the day J4 made the PR publish path the default -- a PR-published commit sits on origin/work-* until a human merges it -- so the documented publish-then-close sequence always refused and every /ticket, /mission and /fb run left a .publish/ tree behind. The intent was right and the test was wrong: the predicate now asks whether any origin ref contains the tip, while the never-pushed refusal it exists for is directly asserted, because widening a safety predicate makes the proof it still bites the part that must not be skipped
* [work-20260731-195744](work-20260731-195744.md) - Stopped /drive's survey from answering questions it had no basis to answer. An unreadable ticket queue rendered identically to an empty one -- list-todo.sh exits non-zero without a git identity and a `|| true` swallowed it -- so a fresh cloud runner would report a healthy empty queue over a full one and terminate ok, the masked failure indistinguishable in a cron log from a correct idle tick. And approved missions carried no ownership filter at all, leaving the executor as the single consumer that could disagree with the roadmap about whose work it is. The queue read now discriminates by exit code into a named backlog_error that forbids ok, ownership resolves through mission-owners.sh with a new owned_by_other reason, and an equality assertion against list.sh's own partition is what keeps this a single source of the rule rather than a second copy of it
* [work-20260731-185904](work-20260731-185904.md)
* [work-20260731-163054](work-20260731-163054.md) - Recorded the standard that every workaholic artifact is committed on a work-* branch and reaches main through a merged pull request, and specced the /propose redesign it implies: widen the batch inputs from feedback alone to feedback plus missions, tickets and recent commit messages, emit the ordered ticket set alongside the draft mission, and land the proposal as a PR. Flags the three things that make it contentious rather than mechanical: it reverses decision J1 one day old, nothing yet watches for the merge event it relies on, and create.sh switches the caller checkout the publish tree was built to protect
* [work-20260730-202601](work-20260730-202601.md) - A drive tick claimed the last backlog ticket, the request to rule on whether long-running servers may run as bare host processes, and verified rather than inherited its blocker: the synced containerization hard copy is silent on where a server may run, qmu.co.jp/implementation carries none of the ruling vocabulary, and /operation 404s. The ticket stays queued and blocked on an article a human must publish, but its blocker is now a dated finding with two reproducible curl checks written into it instead of a four-day-old assertion
* [work-20260730-193046](work-20260730-193046.md) - Made the ship path work from a claim worktree, which is where /drive ships every auto unit and where it broke twice: merge-pr.sh's closing git checkout main cannot run there and exited non-zero AFTER the merge landed, and extract-deferred-concerns.sh then pushed a merged PR's concern records to the already-merged claim branch while reporting pushed: true. The exit status now reflects only the merge with the checkout as a reported field, and the extraction takes the base explicitly, reports its destination, and publishes through a publish tree when off-base — covered by a real linked-worktree fixture that needs no gh
* [work-20260730-191139](work-20260730-191139.md) - Fixed two live correctness holes in /drive's own coordination, both found by driving this repository with its own loop: claims_scan now reads a claim stamp at the artifact's CURRENT path so a batch unit keeps protecting its tickets after archive.sh renames them (the old reader reported 0 artifacts where the new one reports 2, over identical state), and mission drivability is measured against the ticket queue via a single queue-size.sh counter rather than acceptance-item count — so an approved merge_policy: auto mission whose acceptance was a /propose sketch with no tickets is excluded no_tickets instead of offered as claimable
* [work-20260730-180928](work-20260730-180928.md) - Shrank the mission skill from 562 to 380 lines with nothing deleted: the per-script contracts and the six schema subsections moved verbatim into a companion reference/ directory the SKILL.md indexes and links, the closing doctrine that had accreted inside ## Scripts was promoted to its own section, and the body-section list was lifted out of ### Duration. Carrying reference files into the generated cross-agent bundle required extending three build seams (copy with a ../ rewrite, publicize, closure), and the intra-bundle link check that became possible immediately found a ../feedback/SKILL.md link that had been 404-ing for every non-Claude consumer
* [work-20260730-171125](work-20260730-171125.md) - Completed decision I6 on the creation side: artifact creation now publishes to main through a publish tree — a checkout of origin/main at the git-ignored .publish/ that leaves the caller's branch and uncommitted work byte-identical — so /ticket and /mission cut no branch and build no worktree, and /drive fast-forwards before surveying while reporting whether its survey was current. claim.sh is the only branch-and-worktree creator left; its stranded-artifact tolerance became a named refusal, and the three tests that encoded the old worktree invariants were inverted rather than deleted
* [work-20260729-193859](work-20260729-193859.md) - Recorded the publish-to-main direction as four dependent ticket specs — a publish-tree primitive that writes an artifact to origin/main without touching the caller's checkout, /ticket and /mission migrated onto it, and a /drive survey that fast-forwards before reading the queue — completing decision I6's claim-born-worktree ruling on the creation side; plus renamed the /feedback command to /fb after confirming Claude Code 2.1.220 ships a built-in /feedback that silently sends project context to Anthropic (the only collision among all twelve commands), and switched the four specs from auto to review at report time once review established they rewrite the machinery /drive itself uses to merge
* [work-20260728-221717](work-20260728-221717.md) - Phase 3 of the loop-engineering reorganization: collapsed execution to one autonomous /drive — the mission lifecycle unified onto a single status axis with an executable approval flip and per-artifact merge_policy, the claim protocol making pushed branches the coordination medium so concurrent runners never double-pick work, /drive rewritten as the survey→partition→claim→drive→route Unified Run with gate-honoring merge routing and a 5-minute cron runbook, and /monitor, /trip, /carry plus the Agent Teams surface and reflection channel retired with every idea recorded where it landed
* [work-20260728-210259](work-20260728-210259.md) - Phase 2 of the loop-engineering reorganization: merged the concern corpus into the feedback stream (kind: concern records, superseding-record resolution, computed open set, lifecycle machinery retired, live corpus migrated) and built the proposal loop — the headless /propose batch (runner-local cursor, feedback windowing, dedup, unowned status: draft missions with feedback: traceability), the env-driven Slack bot notifier, and the 15-minute cron runbook
* [work-20260728-183130](work-20260728-183130.md) - Phase 1 of the loop-engineering reorganization: introduced the immutable feedbacks/ artifact stream (kind+source axes, supersedes-not-status, /feedback capture, write-time floor), returned mission ownership to the mission's own plural assignees through the unchanged single oracle, and retired the strategy layer with a living migration that preserves every strategy document as feedback and folds assignees down — plus the merged ownership-model sibling branch and the four-round decision record (docs/loop-engineering-workflow.md)
* [work-20260723-000846](work-20260723-000846.md) - Fetch origin and prefer origin/<base> when cutting a mission worktree so it starts from the merged tip, not a stale local ref (fail loud on an unreachable configured origin, surfaced local fallback for no-origin repos); added list-related-prs.sh to surface sibling PRs at /mission replan; regression tests
* [work-20260722-191036](work-20260722-191036.md) - Make the working-directory and `.workaholic/` layout guards **blocking by default**, enforced unconditionally in the plugin code (no `WORKAHOLIC_ENFORCE_CWD`/`WORKAHOLIC_STRICT_LAYOUT`/`.strict-layout` opt-out that fails open off the happy path) — the advisory design the prior opt-in-blocking work had already flagged as insufficient to hold an agent. Reconcile the allowlist against a real `layout-doctor` run (`strategies`, `guides`, `policies`, `scan-allow`, `leak-denylist`), then add a verifiable **closed-layout / lockstep-registration** policy to CLAUDE.md (a new artifact dir updates both the allowlist and the `rules/workaholic.md` table in the same change) backed by a `Validate Plugins` CI gate + Local Verification audit that fails the merge on drift — the `strategies/`-style drift becomes a caught error, not a latent correctness bug. v1.0.101; 1293 passing - 2 tickets
* [work-20260721-153431](work-20260721-153431.md) - Harden the mission and concern lifecycles. **Worktree cleanup** deletes only ephemeral `work-*` branches (fixes local-`main` deletion when a `/ship`-parked mission worktree is torn down). Bare **`/mission`** becomes a developer-centric **planning session**: two-tier roadmap from `list.sh`'s computed `relation`, per-mission replan loop driven by computed `ready`/`ready_reason`, honest `N/M drive-ready` reconciliation, a **strategy gap discussion** (`active_missions`), and a `/goal /monitor ok` hand-off — the `summary` mode retired into it. **Concern lifecycle** rebalanced from disposal to **prevention**: lane-aware triage (`owner`, `my_lane_count`), a **promotion floor** (story keeps everything, only `moderate`+/`Keep` enter the tracked corpus), and a reversible developer-confirmed **demotion pass** (`propose`/`demote`, `status: demoted`); four disposal mechanisms (incl. a `verify_command` code-execution surface) abandoned. Plus **story/trip OKF `type` validators** (git-tracked grandfathering). v1.0.100; 1289 passing - 8 tickets
* [work-20260721-025656](work-20260721-025656.md) - Reorganize missions under **strategies**: a new root artifact (`type: Strategy`, direction with **no completion conditions**, active/archive areas, create/list/read-relation/retire scripts, OKF-indexed) sits above missions, which become **overnight-executable execution plans** stamped `strategy:` at creation (infer / create-on-the-spot / ask only when ambiguous) — the four-layer granularity model (strategy → mission → ticket → commit) documented with its both-ways balance test. The overnight loop gets instrumented: per-mission **duration prediction vs accumulated actual hours** (idempotent per run-id), a `/monitor` **completion report** (fixed reason vocabulary) with a dated **`## Reflection`** the next planning reads back, **auto-PR for genuinely complete missions** (merge stays `/ship`), a per-commit changed-lines gate, and a **commit-count KPI** surfaced in `/catch`. The ask bar rises to **decide-and-record** (the Recommended-label test, stated once in `rules/interaction.md`). First strategy seeded: `agent-orchestrated-development`. v1.0.99; 1235 passing - 9 tickets
* [work-20260719-075112](work-20260719-075112.md) - Redesign `/monitor` for genuinely unattended parallel-mission runs: every foreseeable decision (claims, worktrees, authorizations, replan design rulings) front-loaded into **one pre-flight batch** before any leaf spawns — no mid-run `AskUserQuestion`, unforeseen items deferred and recorded for the morning — and an **honest terminal token** derived from `status.sh` (`ok` only on genuine completion, else `pending` with an N/M reconciliation), resolving the `/monitor` half of the goal-gate false-done ticket. Plus the mission **reorganize-and-carry doctrine** (replan away moot criteria, close `carried` into a successor — surfaced in `/monitor` reports and `/carry`), an always-loaded **interaction-necessity rule** (act-and-report by default; ask only genuine forks), `/request` collapsed to **one** verbatim-body confirmation with "file"-as-verb replaced by "submit", and an opt-in `WORKAHOLIC_ENFORCE_CWD` blocking mode for the working-directory guard. v1.0.98 - 6 tickets
* [work-20260717-141501](work-20260717-141501.md) - Hardened the workflow machinery against silent-success bugs: nine tickets closing ways a script reported success while doing the wrong thing - per-run `/report` artifacts, `archive.sh` reporting its mission mutators, `refresh-index.sh` preserving hand-written content, `commit.sh` never omitting a named file, base ref taken from `origin` not a stale local `main`, worktrees landing on the branch they report, mission resolution following the ticket not the cwd - plus `/monitor` fanning replan out to per-worktree leaves and `mission-lens` summarizing an unchanged roster. Merged up to date with main (28 behind) mid-drive. 9 tickets, 4 carried concerns from PR #88 confirmed still active
* [Story: work-20260716-152211](work-20260716-152211.md) - Triaged the deferred-concern corpus (41 items) to zero, executed the seven hardening tickets it produced (mission replan flow, commit.sh gates, catch scanner, tiered release-scan severity, worktree guards, concern machinery, unattended-drive floors, live validations), then added the /monitor parallel-missions executor on that floor — confirmed pre-flight, one autonomous drive per mission worktree, push-decisions contract, non-blocking dispatcher, per-worktree dev environments, validated live with two concurrent missions — plus the ask-decisions-only interrogation revision and the validate-ticket worktree mission-resolution fix
* [work-20260715-213222](work-20260715-213222.md) - Fixed four defects that each passed vacuously (a false push report, a cloned compound concern, trip-mode true since March, an unassigned mission invisible to everyone), then re-aimed the mission model on the developer's redirection: substance moved to `## Experience` with the kickoff gate demoted, `/mission` interrogates once and emits the whole drive-ready ticket set, an authorized queue drives without the per-ticket prompt, and an unqueued problem met mid-run becomes a ticket rather than a stop
* [work-20260715-112717](work-20260715-112717.md) - Confine every workflow's writes to the current repository (rule shipped with the plugin + a blocking `PreToolUse(Write|Edit)` guard) and add **`/request`** as the only sanctioned crossing, with a non-skippable verbatim-body confirmation — the incident behind it happened at four sites where the plugin was never installed, so distribution was the fix, not enforcement. Then the `secret` rule, hardened four times in sequence (suffixed keywords → TS annotations → scope resolution → repairing a false negative the second fix introduced) before **inverting pass 2 to judge the value, not the key name**: six subtractions deleted, five needing no replacement, measured 15/15 flag + 25/25 subtract. Also deleted the `leak` rule's internal-hostname pattern (0 of 5 real leaks detected, misfired on `metadata.internal`), made the artifact→mission relation many-valued, quieted the mission lens, and triaged the concern backlog 38→22. v1.0.94; 648 passing - 8 tickets
* [work-20260714-222630](work-20260714-222630.md) - Fix two self-defeating behaviors in the release path: the branch-safety scan flagged `process.env` key reads as credentials and hard-blocked `/ship` (5 false `hard` findings → 0; two-pass `secret_grep` where key shapes stay unconditional and only literal right-hand sides count), and archived tickets recorded a `commit_hash` that can never exist (a commit cannot carry its own hash) — now derived from git via `ticket-commits.sh`, taking reachable links from 3/37 to 37/37. v1.0.93; 538 passing - 2 tickets
* [work-20260714-000543](work-20260714-000543.md) - Three feature groups: read-only **`summary` mode** for /ticket, /trip, /mission (per-user assigned work, bare contracts preserved); the **mission-worktree model** — /mission creates a dedicated persistent `.worktrees/<slug>/` worktree with the statement + kickoff tickets, /ship preserves and re-branches it, /mission close tears it down, per-worktree ports, a Playwright-verified per-mission quality gate, and worktree-scoped mission lens; and a **release-safety scan** (release-scan skill) that warns at /report and blocks pre-merge at /ship on credentials/oversize/leakage, with a committed `.workaholic/scan-allow` allowlist. Plus two latent bug fixes (list-all-worktrees last-drop; `.worktrees`/`.env` git-embedding). v1.0.92; 524 passing - 12 tickets
* [work-20260713-144839](work-20260713-144839.md) - Make missions and deferred concerns self-maintaining: a mission `assignee` + always-on **mission lens** hook (re-anchors each developer's assigned mission on `UserPromptSubmit`/`Stop`, gated + non-forcing); and a concern **freshness engine** — stable `concern_id` with update-in-place extraction, a living migration that collapsed this repo's carried-from pile 65→32, and a `/report` **triage step** (judge-proposed A+B compounds, idempotent merge/close mutators). Plus the worktree `.env`-copy protocol. v1.0.91; 432 passing - 3 tickets
* [work-20260713-102453](work-20260713-102453.md) - Give missions a lifecycle: split `.workaholic/missions/` into `active/` and `archive/` areas mirroring the tickets convention, add a `close.sh` mutator + `/mission close` verb (the first sanctioned way to end a mission), and a living migration that relocates legacy flat mission dirs by status on any mission-script touch — plus the `missions` layout-allowlist gap fixed and all docs updated in lockstep. v1.0.90; 396 passing - 1 ticket
* [work-20260707-104047](work-20260707-104047.md) - Surface missions in `/catch`: `scan-window.sh` now emits a read-only `missions[]` block (derived progress + merged changelog events + unmerged in-flight tickets) and carries `mission`/`commit_hash` on tickets, and the report renders a top-level Missions section plus a per-developer attribution line — showing not just merged progress but the unmerged work heading toward each mission. Also fixed installed-plugin script helper resolution and added a hook-executable regression guard. v1.0.88; 374 passing - 4 tickets
* [work-20260706-182705](work-20260706-182705.md) - Add first-class missions (command, skill, OKF indexing, relations, and automated progress/changelog roll-up), fix ship deferred-concern pushes, and make workaholic hooks.json Codex-parseable. v1.0.85 - 5 tickets
* [work-20260706-113425](work-20260706-113425.md) - Turn three prose-only workflow conventions into machine-enforced guards: `/ship` now mandatorily catches up with `main` before deploy and `catchup-main.sh` classifies conflicts mechanical (version/lockstep + `outputs/`, agent auto-reconciles) vs content (halt); a blocking `PreToolUse(AskUserQuestion)` hook rejects any prompt whose question body lacks the `[project]` label (resolves deferred concerns #67/#69); and a thin `/workaholify` command wires a repo to the standards by referring to a new `workaholify` gateway skill (links all six policy pillars) instead of copying rules into `CLAUDE.md`, plus a non-blocking working-directory advisory. v1.0.80; 290 passing - 3 tickets
* [work-20260701-221800](work-20260701-221800.md) - Tighten three workflow constraints: re-aim night-mode `/drive` from over-protective skipping to attempt-first (skip only on a demonstrated failure or named hard blocker); make the generated knowledge OKF v0.1-compatible (committed `outputs/okf/` policy bundle + a self-maintaining `.workaholic/` bundle hierarchy kept fresh by a new internal `okf` skill across drive/report/ship); and extend the `/carry` resumption ticket with `## Findings` and `## Decisions` so a fresh `/drive` inherits the session's reasoning, not just its remaining tasks. v1.0.79 (re-bumped past main's standards-sync 1.0.78); 268 passing - 3 tickets
* [work-20260701-171611](work-20260701-171611.md) - Teach `/catch` to see teammates' pushed-but-unpulled work: `scan-window.sh` now runs a best-effort `git fetch --all --prune` before scanning and widens the developer scan from local `--branches` to `--branches --remotes`, so remote-only branches appear; a `strip_branch` jq helper normalizes the `origin/` prefix, a new `fetch_ok` flag drives a stale-view note and graceful degradation on fetch failure, and the SKILL's read-only claim was corrected. Extended the hermetic scan-window tests with reachable/unreachable-remote scenarios; 268 passing. v1.0.74 (synced past the already-released v1.0.73) - 1 ticket, 2 commits
* [work-20260701-093015](work-20260701-093015.md) - Surface the owning repo's identity as a `[workaholic]` prefix in every `AskUserQuestion` (new network-free project-label.sh); rename the "carry-over" concerns pipeline to "deferred concerns" across prose, four scripts, and test fixtures (freeing the "carry" verb); add `/carry` (capture-only session handoff — resumption ticket / trip checkpoint a fresh `/drive` resumes) and `/explain` (answer a repo question and browser-print a printer-ready PDF, Desktop→Home with a Home-consent gate). All Claude-only, no outputs footprint. v1.0.72; 260 passing - 4 tickets, 8 commits
* [work-20260630-050446](work-20260630-050446.md) - Diagnosed the branch-guard "not firing" report as a stale pre-1.0.66 install (not a defect), fixed the guard over-blocking piped `git branch` list forms, and surfaced the loaded plugin version + guard presence in `check-deps` so staleness is visible; enriched `/catch` with per-developer focus windows, struggles, branches, generation-style guess, and this-week deployments/releases; added a mandatory quality-gate interrogation to `/ticket` that `/drive` surfaces at approval. v1.0.69; 240 passing - 6 tickets, 10 commits
* [work-20260630-011820](work-20260630-011820.md) - Add `/catch`, a read-only by-developer catch-up report: scan-window.sh enumerates developers active in a window (default 2 weeks), groups commits by author email joined to ticket-author frontmatter, fans out one haiku general-purpose collector per developer, and synthesizes the overall direction before standing ready for follow-up Q&A; ships cross-agent via build.mjs DEFAULT_TARGETS, stays an Agent Skill (sequential fallback, no Agent Teams). 9 new scan-window assertions; 221 passing - 1 ticket, 2 commits
* [work-20260628-002047](work-20260628-002047.md) - Turn the prose-only commit-subject and branch-name conventions into a two-layer enforcement stack: always-on PreToolUse(Bash) gates (guard-git-commit.sh + guard-git-branch.sh) for the agent surface and an opt-in git commit-msg hook + installer for the human terminal, both sharing one check-subject.sh rule source; add a /commit escape-hatch command wrapping commit.sh; abandon the co-author-trailer-removal ticket (the trailer stays). 54 new tests; 212 passing - 3 tickets (1 abandoned)
* [work-20260627-153246](work-20260627-153246.md) - Finish the POSIX-sh re-enforcement (convert the last bash scripts incl. the validate-ticket hook, add a self-enforcing lint + dash/sh test gate), reconnect the commit body to /report (Why/Changes/Concerns/Insights/Verify keys, collect-commits body emission, a Category git trailer), and make a default design-first /trip flow through to the build with no developer pause - 6 tickets
* [work-20260626-124322](work-20260626-124322.md) - Enforce the canonical `.workaholic/` layout via a single-source allowlist + warn/strict Write/Edit hook gate, add a read-only layout doctor audit that reports pre-existing drift against the same allowlist, and add an explicit recorded "accepted-risk bypass" to the `/ship` gate for cannot-confirm contexts - 3 tickets
* [work-20260624-140219](work-20260624-140219.md) - Harden ticket-structure enforcement so /ticket and /drive cannot produce non-canonical layouts: tighten validate-ticket.sh (mandatory todo/<user>/, recognize abandoned/, reject done/ and nested todo/<user>/archive/) and add a new PreToolUse Bash guard guard-ticket-structure.sh that blocks mv/cp/mkdir/redirect moves into non-canonical tickets locations — the shell-move blind spot the Write|Edit hook never saw. 18 new hermetic tests; 113 passing - 1 ticket, 2 commits
* [work-20260623-235347](work-20260623-235347.md) - Unify /trip and /drive on the ticket abstraction: trip Planning-phase Decomposition gate (design → tickets), Coding phase reworked into a per-ticket drive with three-agent QA, /report convergence, and context-aware /trip (a populated queue runs queue-execute — the ticket → trip direction); plus the sources × executors docs - 5 tickets, 8 commits
* [work-20260623-181237](work-20260623-181237.md) - Triage the carry-over concerns corpus (canonicalize 21 → 7, archive 14 chained duplicates with accept/defer/resolve dispositions) and resolve the 4 actionable concerns: a verify.mjs cross-skill reference lint, a /ship pre-deploy confirmation-capability check, the build.mjs orphan-cleanup guarantee, and deploy-model docs - 4 tickets
* [work-20260621-192132](work-20260621-192132.md) - Tighten workflow boundaries and policy instrumentation: scope the /trip Agent Teams members in frontmatter so non-trip flows never invoke them, make /ship's ticket guard non-blocking, add Night Trip (autonomous unattended /trip night), keep the feature branch on merge (gh pr merge --delete-branch=false), add a real (non-mock) policy-lens injection test, and record a mandatory `## Policies` section in tickets that /drive and /trip read before implementing - 6 tickets, 12 commits
* [work-20260618-182253](work-20260618-182253.md) - Make the four-pillar policy lens actually load during workflows: generate hooks/policy-index.md from the pillar SKILL.md `## Policies` sections and inject it via policy-lens.sh (bring /drive into the lens), with a CI guard against a stale committed index; add a deterministic /report doc-drift check (doc-drift.sh + release-readiness judging); and catch the /trip surface up to recent architecture (policy lens, merge-last Trip Ship order, relabeled script homes) - 4 tickets
* [work-20260618-115347](work-20260618-115347.md) - Add an always-triggered policy-lens UserPromptSubmit hook that injects the planning/design/implementation/operation lens into /ticket, /report, and /ship (replacing drifted per-command prose that still gated on the merged-away standards plugin and omitted the planning pillar), and fix the README (broken Mermaid lifecycle diagram, missing planning pillar in the policy index, undocumented policy-sync workflow) - 2 tickets
* [work-20260618-003119](work-20260618-003119.md) - Fix the carry-over pipeline (apply-carryover-verdicts.sh now accepts the {verdicts} object; extract-carryover.sh dedups concerns by canonical identity across PR prefixes) and add a secret-scrubbing guard to record-evidence.sh so deployment evidence can't leak credentials into the public PR - 2 tickets, night-drive batch
* [work-20260617-231848](work-20260617-231848.md) - Fix /ship ordering: the PR merge is now the LAST step, gated on a passing pre-merge production confirmation (deploy + confirm from the branch, record evidence, then merge); adds catchup-main.sh + record-evidence.sh and this repo's own .workaholic/deployments/marketplace.md contract - 1 ticket
* [work-20260617-210627](work-20260617-210627.md) - Make /ship require an established deployment-confirmation method (new .workaholic/deployments/ convention + reader; invert silent-skip into a halt-and-ask hard gate that executes the confirmation after deploy) and make night /drive run the whole queue with no per-ticket checkbox (one group-inclusion question only on distinct topic groups) - 3 tickets, night-drive batch
* [work-20260617-082241](work-20260617-082241.md) - Post-merge follow-ups: fix the check-deps regression blocking /ticket+/drive, rewrite the stale /release command for the single-plugin layout, and rewrite CLAUDE.md/README/command-Notice architecture narrative (incl. the stale Plugin Boundary Rule) - 3 tickets
* [work-20260617-000311](work-20260617-000311.md) - Consolidate core+standards+work into one workaholic plugin; move release-note generation to /ship and publish GitHub Releases on ship (deferring to CI); add autonomous night-drive mode to /drive; rename dist/ → outputs/ - 5 tickets, implemented in one night-drive batch
* [work-20260528-122941](work-20260528-122941.md) - Cross-agent distribution hardening and multi-developer ergonomics: sync plugin versions through marketplace.json with Codex-metadata CI validation, split standards into design/implementation/operation policy-index skills, add hermetic workflow-script smoke tests, partition the todo queue per developer, and add a plugin-boundary guard against stale-install spelunking and obsolete-namespace guessing - 6 tickets, 8 commits
* [work-20260528-091259](work-20260528-091259.md) - Replace /ship cloud.md deploy convention with CLAUDE.md as a hard cutover (rename find-claude-md.sh, rewrite ship SKILL.md, regenerate dist/workflows), and document the actual Codex install commands in the README - 1 ticket, 3 commits
* [work-20260518-235327](work-20260518-235327.md) - Cross-agent distribution: package core/standards skills via the Agent Skills standard, flatten report/drive/ticket orchestration onto general-purpose subagents, build a CI-guarded committed dist/workflows for Codex and the skills CLI, add a carry-over concerns pipeline, decouple core:ship from trip - 17 tickets, 33 commits
* [work-20260417-092936](work-20260417-092936.md) - Eliminate manager tier, redraw core/work plugin boundary, thin six work-side umbrellas (drive, report, trip, ticket, ship, discover) into core-skill aliases, rewrite leading skills in viewpoint prose - 16 tickets, 27 commits
* [work-20260415-163724](work-20260415-163724.md) - Consolidate 7 lead domains to 4 (validity, availability, security, accessibility), rewrite policies in priority-based tone, soft dependency fix - 0 tickets, 4 commits
* [work-20260408-001129](work-20260408-001129.md) - Lead skill architecture maturation: four-tier structure, domain consolidation, drive abandon fix, dependency tracking - 3 tickets, 15 commits
* [work-20260406-193458](work-20260406-193458.md) - Housekeeping and structural improvements: directory rename, skill removal, release note fix, discoverer refactor, trip worktree fix - 5 tickets, 13 commits
* [work-20260404-101424-fix-trip-report-dir-path](work-20260404-101424-fix-trip-report-dir-path.md)
* [trip-trip-20260319-squashed](trip-trip-20260319-squashed.md)
* [feat-20260131-125844](feat-20260131-125844.md) - Architectural refinement with skill modularization and parallel optimization: skill-to-skill nesting, drive-workflow decomposition into four skills, driver agent for isolated context, performance-analyst moved to Phase 1 parallel execution, generate-changelog merged, intelligent ticket prioritization, structured commit messages, selective options enforcement, and dependency graph restructuring - 19 tickets, 36 commits
* [feat-20260129-023941](feat-20260129-023941.md) - Core infrastructure improvements and release automation: pragmatic release-readiness assessment, parallel source discovery in /ticket command, ticket validation hook, SDD terminology clarification in README, GitHub Actions release workflow, path reference fixes, and Mermaid diagram rendering improvements - 10 tickets, 21 commits
* [feat-20260128-220712](feat-20260128-220712.md) - TiDD philosophy framework, command simplification, and discovery tooling: README rewritten with TiDD philosophy, /report renamed to /story, directory structure flattened, branch creation integrated into /ticket, history-discoverer subagent added, command-flows specification created, approval loop simplified - 8 tickets, 15 commits
* [feat-20260128-012023](feat-20260128-012023.md) - Documentation clarity, workflow enhancement, and technical optimization: Motivation section added to README, Cultivating Semantics terminology, numbered headings formalized, Abandon workflow with failure analysis, Haiku subagent optimization - 9 tickets, 20 commits
* [feat-20260128-001720](feat-20260128-001720.md) - Skill consolidation: merged 8 utility skills into primary counterparts, extracted create-branch and create-ticket skills, documented architecture nesting policy, added story translation and markdown linking - 14 tickets, 27 commits
* [feat-20260126-214833](feat-20260126-214833.md) - Subagent architecture with concurrent execution, git -C prohibition via settings.json deny, skill extraction, bundled scripts for permission-free plugins, story format enhancements (11 sections with release readiness), and infrastructure improvements (POSIX sh, topic tree flowchart, ticket directory reorganization) - 42 tickets, 101 commits
* [feat-20260126-131531](feat-20260126-131531.md) - Workaholic directory standardization and conventions
* [feat-20260124-200439](feat-20260124-200439.md) - Ticket metadata and single source of truth consolidation
* [feat-20260124-105903](feat-20260124-105903.md) - Rule modularization and PR workflow improvements
* [feat-20260123-191707](feat-20260123-191707.md)
* [Story: Documentation Experience Improvements](feat-20260123-032323.md) - Documentation experience improvements and .workaholic/ directory restructuring
* [drive-20260403-230430](drive-20260403-230430.md) - Unified drivin and trippin into work plugin, unified branch naming, reset lead policies, added development patterns to story format - 10 tickets, 15 commits
* [drive-20260329-173608](drive-20260329-173608.md) - Plugin architecture reorganization: core consolidation, dependency declarations, i18n deprecation, skill consolidation, story template simplification - 10 tickets, 21 commits
* [drive-20260326-183949](drive-20260326-183949.md) - Agent discipline enforcement, standards plugin extraction, worktree lifecycle management, workspace safety guards, and developer experience improvements - 7 tickets, 14 commits
* [drive-20260312-102414](drive-20260312-102414.md) - Trip-drive workflow integration: quality differentiation, worktree detection guards, cross-command compatibility, trip plan state persistence, and strip-frontmatter extraction - 6 tickets, 11 commits
* [drive-20260311-125319](drive-20260311-125319.md) - System-wide configuration safety harness, agent personality spectrum rewrite, core plugin creation with unified /report and /ship commands, phase rollback mechanism, concurrent Planning Phase and Coding Phase execution - 10 tickets, 21 commits
* [drive-20260310-220224](drive-20260310-220224.md) - Trip workflow hardening, agent quality enhancements, multi-plugin README rewrite, complete delivery lifecycle commands (/report-drive, /report-trip, /ship-drive, /ship-trip) with cloud.md convention, shared ship script extraction, capitalization bugfix, and deployment confirmation - 16 tickets, 31 commits
* [drive-20260302-213941](drive-20260302-213941.md) - Multi-plugin marketplace architecture: renamed core to drivin, created trippin plugin skeleton, implemented /trip command with Agent Teams, enforced ticket context in drive approval prompts - 4 tickets, 8 commits
* [drive-20260213-131416](drive-20260213-131416.md) - Absolute path enforcement, schema restructuring, and archive reliability: codified path rule in CLAUDE.md, replaced relative paths across 39 files, nested Goal and Responsibility under Role in agent schemas, fixed unstaged ticket deletions by mandating archive.sh usage - 4 tickets, 10 commits
* [drive-20260212-122906](drive-20260212-122906.md) - Naming hygiene, workflow reliability, and developer experience: renamed manage-branch to branching and policy skills to principle, fixed duplicate Japanese specs and double version bumps, added constraint file convention, improved effort validation enforcement and drive approval UX - 9 tickets, 22 commits
* [drive-20260210-121635](drive-20260210-121635.md) - Manager tier introduction, commit message restructuring, cross-cutting policies, and workflow enforcement: created 3 managers (project, architecture, quality) with constraint-setting workflow, expanded commit format to 5 sections for lead consumption, added leaders-policy and managers-policy skills - 9 tickets, 20 commits
* [drive-20260208-131649](drive-20260208-131649.md) - Lead architecture migration: created define-lead schema, transformed all 10 analyst subagents to domain-specific leads, consolidated 4 viewpoint analysts into architecture-lead, migrated scanner into /scan command, added automatic version bumping, reformed badge system - 17 tickets, 29 commits
* [drive-20260205-195920](drive-20260205-195920.md) - Documentation traceability, drive workflow enforcement, scan architecture overhaul from 3-level to 2-level nesting with 17 parallel analysts, legacy spec migration, dual-mode scanning, effort validation, shell script hardening, and Mermaid rendering fixes - 17 tickets, 40 commits
* [drive-20260204-160722](drive-20260204-160722.md) - Documentation hygiene, workflow safety, and infrastructure: term document restructuring, translation removal from story-writer, git safeguards, centralized commit skill, release-note-writer subagent, GitHub URL transformation, script path fixes - 8 tickets, 24 commits
* [drive-20260203-122444](drive-20260203-122444.md) - Documentation workflow refinement: extracted /scan command from /report, fixed memory leaks in /ticket, added unified diff patches to tickets, renamed /story to /report, refactored ticket-organizer with cleaner naming - 9 tickets, 24 commits
* [drive-20260202-203938](drive-20260202-203938.md) - Reliability and consistency improvements: fixed release action trigger to use version comparison, standardized notice section format across commands, added code implementation prohibition to /ticket, configured Opus model for all Task tool invocations - 6 tickets, 12 commits
* [drive-20260202-134332](drive-20260202-134332.md) - Parallel subagent orchestration and workflow improvements: enabled subagent-to-subagent parallel invocation, ticket-organizer with 3 parallel discovery agents, story-writer orchestrating 7 documentation agents, continuous drive loop, revision tracking with Discussion section, approval skills consolidation - 14 tickets, 31 commits
* [drive-20260201-112920](drive-20260201-112920.md) - Architecture refinement and release workflow fixes: simplified /ticket command to thin orchestrator with ticket-organizer subagent, removed driver subagent from /drive to preserve implementation context, fixed release versioning to default to patch bumps - 3 tickets, 35 commits
* [drive-20260131-223656](drive-20260131-223656.md) - Systematic hardening for developer experience: nine focused improvements strengthening command execution and configuration management, defensive programming layers guide Claude toward correct behavior, terminology alignment (fail-to-abandoned, drive-/trip- standardization), validation guards prevent common errors - 9 tickets, 22 commits
* [claude-lucid-tesla-4yz3ob](claude-lucid-tesla-4yz3ob.md) - Recolored `workaholic:notify`'s Slack post shapes one color per exactly one state (🔵 Proposed, 🟠 Implementing, 🟢 Implemented, 🟡 Handoff, 🔴 Blocked, 🚀 Auto Merge kept outside the set) per qmu/workaholic#330, ending the double duty where 📐 covered Designing/Proposed and 🛠️ covered Implementing/Implemented; 📐 Designing stays unchanged since removing the Proposed collision already makes it unique, and 🟣 stays reserved rather than reinstated for a future human-merge shape.
* [Name the session type that cannot merge](20260823-name-the-session-type-that-cannot-merge.md)
<!-- okf:generated:end -->
