---
type: Story
branch: work-20260731-163054
tickets_completed: 0
mission: []
tickets: []
---

## 1. Overview

`/propose` reads only feedback records and pushes its draft mission straight to `main`. This branch records the direction to change that — as a feedback record — and specs the change as a ticket.

**Highlights:**

1. The batch's inputs widen from feedback alone to feedback + missions + tickets + recent commit messages.
2. The proposal carries its ordered ticket set, not just a draft mission with a provisional acceptance sketch.
3. The output lands on a `work-*` branch behind a pull request, so the merge event is what announces it to Slack.

## 2. Motivation

The loop only advances when a human supplies direction, and today's batch can convert only direction that was already written as feedback. Broadening the inputs and emitting the ticket set lets it propose the next step from the state of the work itself. Landing on a branch makes the proposal reviewable where reviewing is natural, and gives it a merge event to announce.

## 3. Changes

### 3-1. Register the direction as feedback ([4df9d387](https://github.com/qmu/workaholic/commit/4df9d387))

`kind: instruction`, `source: discussion` — the three changes and the reason behind them, in the immutable stream.

### 3-2. Spec the change as a ticket ([4df9d387](https://github.com/qmu/workaholic/commit/4df9d387))

Eight implementation steps, an acceptance list, and the considerations that make the change contentious rather than mechanical.

## 4. Outcome

No behavior changed. The direction is recorded and the work is specified; `/drive` can claim the ticket once this merges.

## 5. Historical Analysis

Decision J1 (2026-07-30) made artifact creation publish to `main` through a publish tree, creating no branch — one day before this. The standard recorded here supersedes it for artifact creation, and the ticket makes resolving that contradiction an explicit step rather than a silent overwrite.

## 6. Concerns

### The J1 reversal leaves two contradictory statements in the repository

- **Severity:** moderate
- **Description:** `CLAUDE.md` states that the claim is the only creator of a branch and that artifact creation publishes to `main` through the publish tree. The standard this branch records contradicts both for artifact writers (see [4df9d387](https://github.com/qmu/workaholic/commit/4df9d387) in `.workaholic/tickets/todo/a-qmu-jp/20260731163049-propose-surveys-repo-state-and-lands-on-a-branch.md`).
- **How to Fix:** The implementing change updates the J1 statement and the publish-tree section in the same commit, recording which motive wins for which artifact rather than leaving both claims standing.

### A merged PR notifies Slack only if something watches for the merge

- **Severity:** moderate
- **Description:** `notify-slack.sh` announces when it is called, and nothing calls it when a human merges in the GitHub UI. The standard's stated benefit — the merge event notifies Slack — has no implementation yet.
- **How to Fix:** Add a GitHub Action on `pull_request: closed` with `merged == true`, or route every merge through `/ship`, which can call the notifier itself.

### The publish tree exists to protect a dirty checkout

- **Severity:** moderate
- **Description:** `create.sh` switches the current checkout's branch, which is what the publish tree was built to avoid for a developer running `/ticket` mid-work.
- **How to Fix:** The artifact writers need a worktree or a publish-tree-like staging area on the `work-*` path, not a bare `checkout -b` in the caller's tree.

## 7. Successful Development Patterns

- Checking the safety property before proposing the design: `plan-units.sh` already excludes any ticket carrying a `mission:` relation as `mission_member`, so proposed tickets are unclaimable before approval without inventing a new gate.

## 8. Release Preparation

**Verdict**: Ready for release

### 8-1. Concerns

- None — knowledge artifacts only; no plugin code, manifests, or generated outputs changed.

### 8-2. Pre-release Instructions

- None — no version bump taken; nothing under `plugins/` or `outputs/` changed.

### 8-3. Post-release Instructions

- None.

## 9. Notes

The ticket records `merge_policy: review`.

