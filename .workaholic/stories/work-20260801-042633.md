---
type: Story
branch: work-20260801-042633
tickets_completed: 1
mission: []
tickets: [20260801042354-merged-pr-routine-announces-every-recent-merge.md]
---

## 1. Overview

Two PRs merged four seconds apart produced **four** Slack messages — each announced twice, in visibly different wording. Not duplicate configuration: one routine, two sessions, and a prompt that never said *which* pull request to report.

**Highlights:**

1. `merged-pr` now announces **exactly one** PR — the one whose merge started the session — and **posts nothing** when it cannot identify which.
2. The `⚠️ Attention` block is removed from **both** the PR-opened and PR-merged formats.
3. All **15** live routines across seven repositories carry the corrected templates — the first fleet-wide application of the one-template-set model.

## 2. Motivation

A cloud session starts with no state and no memory of what a sibling session already posted. Told to report "the pull request", it looks at what recently merged, finds two, and reports both. N merges landing before their sessions run gives N sessions × N merges — and a single merge in isolation looks perfectly correct, which is why this survived until a drive loop merged twice in four seconds.

## 3. Changes

### 3-1. Announce exactly one pull request per session ([abd188b1](https://github.com/qmu/workaholic/commit/abd188b1))

The `merged-pr` prompt gains an explicit subject and three ordered rules: one message per PR; post nothing when the trigger is unidentifiable; never re-announce a PR already in the channel. `fb` and `drive` were checked for the same ambiguity and scoped to their own output. The Attention block is gone from all three, and the templates were re-captured to match the live edits made at 19:12–19:14 UTC.

## 4. Outcome

Fifteen routines updated: 7 `[FB]`, 7 `Merged PR`, 1 `[Drive]`, across workaholic, research, qmu-co-jp, plgg, qfs, data-platform and coop-csnet. Two pre-existing drifts were corrected on the way — `Merged PR qmu-co-jp` and `[FB] coop-csnet` had no `model` while every sibling pinned `claude-opus-5`.

## 5. Historical Analysis

The templates were captured verbatim in PR #138 precisely so a fix could be made once and applied everywhere. This is the first use of that, and it worked as designed: one edit, fifteen routines.

## 6. Concerns

### The fix is unproven until two PRs merge close together

- **Severity:** moderate
- **Description:** The defect lives in a model's reading of a prompt. The suite asserts the instruction is present and unambiguous; it cannot assert obedience. The acceptance criterion — two PRs merged within a minute producing exactly two messages — has not yet been run.
- **How to Fix:** Merge two prepared PRs in quick succession and count the messages in `#dev-workaholic`. Re-run after any later edit to these prompts.

### A legitimate per-repository difference now reads as drift forever

- **Severity:** moderate
- **Description:** `[FB] data-platform` carries `- Speak/Write Japanese`. It was deliberately **kept** — a project's output language is a property of that project, not a deviation — so `compare-routines.sh` will report that routine as drifted on every future survey (`plugins/workaholic/skills/workaholify/scripts/compare-routines.sh`).
- **How to Fix:** The comparison cannot decide this by itself; it reports, and a human says which differences are drift and which are configuration. If such cases multiply, the template format needs a way to declare a per-repository addendum — but one instance is not yet evidence for that.

### Sibling sessions still cannot see each other

- **Severity:** low
- **Description:** Rule 3 asks the session to check recent channel history before posting, which is a mitigation rather than a guarantee: two sessions running concurrently can both check, both see nothing, and both post.
- **How to Fix:** Only the platform can serialise this. The primary defence is rule 1 (one message, one PR, identified by trigger), which makes the duplicate impossible without needing coordination; rule 3 is the backstop.

## 7. Successful Development Patterns

- Reading the *wording differences* between duplicate messages to identify the mechanism. Identical duplicates would mean one session retrying; different prose meant two sessions each composing independently, which pointed straight at "each session reported everything it could see" rather than at a delivery bug.
- Scoping template assertions to the prompt section. A template's header quotes the old defective wording verbatim to explain what was corrected, so a test reading the whole file flags the explanation as the bug — the same false positive that hit the bootstrap tests earlier today.
- Refusing to normalise a deliberate local difference. The Japanese-language line in one routine looked exactly like drift to the comparison, and dropping it silently would have changed that project's behaviour under cover of a template rollout.

## 8. Release Preparation

**Verdict**: Ready for release

### 8-1. Concerns

- The live two-PR test is outstanding. It does not block this branch — the change is strictly an improvement on a prompt that demonstrably misbehaved — but the fix is not proven until it runs.

### 8-2. Pre-release Instructions

- None.

### 8-3. Post-release Instructions

- Merge two PRs within a minute of each other and confirm exactly two messages arrive in `#dev-workaholic`.

## 9. Notes

Verification: `node scripts/test-workflow-scripts.mjs` 1676 passed / 0 failed. All fifteen routine updates confirmed by the API's response body.
