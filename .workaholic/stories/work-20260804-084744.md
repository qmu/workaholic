---
type: Story
branch: work-20260804-084744
tickets_completed: 1
mission: [make-a-mission-impossible-to-create-without-its-ticket-set]
tickets: [20260804173624-decide-the-mission-ticket-floor-and-what-a-carry-does.md]
---

## Handoff

**Done.** Ticket 1 of 3 (`20260804173624`, decide the floor) is implemented and archived — the boundary is decided and recorded. Ticket 2's first three seams are implemented and committed: the shared `meets_floor` verdict, the Creation Interrogation, the proposal batch, and documented exemptions on both scaffold writers.

**Not done.** Ticket 2 (`20260804173625`) is **`blocked` and stays in `todo`**, and ticket 3 (`20260804173626`) is untouched. The blocker is the fourth seam.

**The exact next step.** Drive [`20260804184949-a-carry-into-an-existing-mission-silently-drops-the-remainder.md`](../tickets/todo/a-qmu-jp/20260804184949-a-carry-into-an-existing-mission-silently-drops-the-remainder.md) first: relocate `close.sh`'s unmet-acceptance inheritance so it runs for **both** carry routes. Only then apply the `--successor-title` refusal that ticket 2's fourth seam calls for. The refusal is already written and reverted in this branch's history — `close.sh` carries a comment at the exact site explaining the ordering, so the next session does not have to rediscover it.

**That ticket did not exist until the second pass.** The first pass recorded the finding and the sequencing in `close.sh` and `reference/schema.md` citing ticket id `20260804085209` — but never wrote the ticket, so the id dangled in five shipped files and the obligation lived only as prose in a code comment. `drive/SKILL.md` names this exact failure: *"An observation is not an obligation. Only a ticket is."* The resuming pass verified the bug independently (read `close.sh` lines 152-230: the `--successor <slug>` branch resolves a path and falls through; the `unchecked()` inheritance begins at 177 inside the `else`), wrote the ticket, and repointed every reference. Worth keeping: the pass that found the bug was the pass that dropped the record of it, and a ticket id in a comment reads exactly like a ticket that exists.

**Why the refusal was reverted rather than shipped.** Implementing it revealed that the inheritance block lives *entirely inside* `close.sh`'s mint branch (lines 157-234); `--successor <slug>` resolves a path and falls through, inheriting nothing — despite `SKILL.md` and `CLAUDE.md` both claiming it "already carries the unmet items". Refusing `--successor-title` today would leave **no** carry route that transfers the remainder, trading a record defect (a ticketless mission on the roadmap) for a data-loss one (a carry that reports success and silently drops its payload). Shipping it would also have required deleting a ~50-line specification of the inheritance behavior to make the suite pass, which is the tell that the sequencing was wrong rather than the tests.

**No command failed.** This is a sequencing decision, not an external blocker.

## 1. Overview

The first ticket of the mission that gives a mission a lower bound: **created with two or more tickets, or it is not a mission**. This branch decides the boundary, records it, and lands the shared check plus three of the four enforcing seams.

## 2. Motivation

"Feedback", "ticket" and "mission" are only distinguishable if the middle one has a floor. Without it a ticketless mission is a feedback record on the roadmap and a one-ticket mission is a ticket with a progress bar — and two of eleven missions ever created were in exactly those states, both minted by seams that create without emitting tickets.

## 3. Changes

### 3-1. Decide the mission ticket floor and the carry ([b0717bd7](https://github.com/qmu/workaholic/commit/b0717bd7))

Three questions answered in `mission/SKILL.md` under **Granularity → The ticket floor**, where the "what makes a mission a mission" rule already lives, so the floor reads as the granularity table's bottom edge rather than a separate lint:

- **What counts, and when** — tickets naming the mission, present in the *same publication commit*, counted at the **publish seam**. Not at the write of `mission.md`: a `PostToolUse` hook fires before the interrogation has emitted anything, so a write-time floor refuses the normal authoring order. The 37-unlinked-acceptance-items measurement is cited as the already-paid cost of checking where the data does not yet exist.
- **Exactly two, refused not warned** — with the archived one-ticket instance named so the rule is not misread as a judgment on it.
- **The carry** — `--successor-title` refused; a carry names an existing mission. Rejected options and their costs are in `reference/schema.md`.

Two edits beyond the four steps, required by the ticket's own read-back criterion: the carry doctrine and the reorganize-and-carry recipe both described `--successor-title` as live, which would have left the document contradicting itself exactly where a reader checks.

### 3-2. Add the mission ticket floor verdict ([5b067dbe](https://github.com/qmu/workaholic/commit/5b067dbe))

**One counter, not four.** `queue-size.sh` already computed `total`, which *is* the quantity the floor is about, so the verdict rides there as `floor` / `meets_floor` rather than becoming a third count. Four seams read it; four inline counts would drift, and the drift would be invisible because each seam is exercised separately. It counts `total`, not `todo` — a fully-driven mission still satisfies the floor, because the floor is about how a mission was *created*.

**Computed, not enforced, in the reader.** `queue-size.sh` is a pure read called from drivability checks too; exiting non-zero on a sub-floor mission would break all of them.

**Three seams wired:** the Creation Interrogation states the floor *during* the rounds; `/propose` drops a candidate it cannot decompose and reports the drop rather than staying silently quiet; `create.sh` and `scaffold-draft.sh` carry documented exemptions explaining that a scaffold writer runs before any ticket exists — required so their absence does not read as an oversight.

## 4. Outcome

The boundary is decided and the check exists with one implementation. Three of four seams enforce it.

Verification: `node scripts/test-workflow-scripts.mjs` — **2095 passed, 3 failed**, against an identical **2095/3** environmental baseline (this sandbox's git remote is a local proxy, so three tests expecting a `github.com` URL fail; they fail on `main` too). Three new assertions cover zero / one / two tickets against the floor. `build.mjs`, `verify.mjs`, `validate-metadata.mjs` clean; `layout-doctor.sh .` conforming; branch-safety scan **pass**.

## 5. Concerns

### The fourth seam is deliberately unshipped, and the reason outranks the floor

See **Handoff**. `close.sh --successor-title` still violates the floor, knowingly, until `20260804085209` lands. A reviewer who wants the floor complete should merge that ticket's work first rather than adding the refusal here.

### `mission/SKILL.md` is within 3 lines of its size gate

The file is capped at 420 lines by a test asserting it stayed materially shorter than its 562-line original; this branch leaves it at 417. Adding the fourth seam's prose will breach it. The convention is already in place — move detail to `reference/`, as this branch did with the carry decision — but the next author should expect to do that rather than discover the gate.

### The docs claimed carry behavior the code never had

`CLAUDE.md` and `mission/SKILL.md` have both asserted that `--successor <slug>` "already carries the unmet items" for as long as both routes existed. That sentence was read as a description by the ticket that decided to refuse the other route. Corrected in `reference/schema.md`'s sequencing note; the full fix rides `20260804085209`.

## 6. Successful Development Patterns

**When a change forces you to delete a specification, suspect the change.** The refusal required removing ~50 lines of assertions describing inheritance behavior. That was the signal that the sequencing was wrong — not that the tests were stale.

**Put the verdict on the counter that already answers the question.** The floor needed a number `queue-size.sh` was already computing. Adding `meets_floor` beside it cost two lines and gave four seams one source; a second counter would have been the same information with a drift surface.

**A documented exemption is part of enforcement.** `create.sh` and `scaffold-draft.sh` are *not* enforcing the floor, and saying why in each file is what keeps that from reading as an oversight to the next person auditing the four seams.
