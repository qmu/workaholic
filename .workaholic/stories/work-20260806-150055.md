---
type: Story
branch: work-20260806-150055
tickets_completed: 1
mission: []
tickets: [20260806032128-say-in-the-routines-report-that-trigger-wiring-is-unreadable-from-the-api.md]
---

## 1. Overview

The routines report printed the template-declared trigger beside four live-account fields and marked none of them, so a reader took the whole block for live configuration. The qualifier now travels in the data, the report states the API limit where the reader is, and — found en route — `main`'s `CLAUDE.md` is repaired from a conflict-resolution defect that had survived seven merges.

**Highlights:**

1. `trigger_source: template_declared` on every row, `trigger_readable: false` at top level — no renderer can drop the qualifier.
2. The report itself now says: wiring is unreadable from the API; confirm it at claude.ai/code/routines.
3. `main`'s `CLAUDE.md` carried a stray `<<<<<<< HEAD` and had lost its `/setup-routines` row since PR #263 — restored.

## 2. Motivation

Measured 2026-08-06: this repository's `[Propose]` was firing on merged pull requests while the report said `github-issue-assigned` — the template's declaration, and the routine's designed trigger. The report was not wrong about anything it could see; it was silent about what it could not, and the silence cost a manual investigation. A limit that is reported is a known limit; one that is omitted reads as a measurement.

## 3. Changes

### 3-1. Mark the reported trigger as a declaration ([7b984751](https://github.com/qmu/workaholic/commit/7b984751))

`list_routines.py` emits the qualifier per row and at top level; `setup-routines.md` step 4 reports `trigger` as the template's declaration with the standing UI note and the reason it exists; SKILL §5 and the `CLAUDE.md` row agree. The `CLAUDE.md` repair restores the row the note had to be written into: a hand-scripted conflict resolution in PR #263 sliced by 0-based index over 1-based line numbers, consuming the `/setup-routines` row and leaving the marker.

## 4. Outcome

The interim report tells the truth about its own blindness. The queued setup-sheet mission retires this report entirely; the data-level qualifier carries into whatever renders next.

## 5. Concerns

### Nothing in CI reads CLAUDE.md content

- **Severity:** moderate
- **Description:** A conflict marker and a deleted table row lived on `main` through seven merges because no check parses `CLAUDE.md` — the file every session loads as ground truth.
- **How to Fix:** A one-line conflict-marker grep (`^<<<<<<<|^=======$|^>>>>>>>`) over tracked text files in the `Validate Plugins` workflow would have caught this the same hour.

## 6. Successful Development Patterns

- Putting a qualifier in the data rather than only in prose: two scripts already read the value and a third rendered it, and a prose note travels with none of them.
