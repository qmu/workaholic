---
type: Story
branch: work-20260806-152604
tickets_completed: 1
mission: [drive-on-a-merged-proposal-and-report-it-in-that-proposal-s-thread]
tickets: [20260805223514-stand-up-an-invoker-for-the-unscheduled-routines.md]
---

## 1. Overview

Two sessions on two days drew opposite wrong conclusions about how a routine is triggered, from the same absent field, and the repository carried both. The developer's answer and the product documentation settle it, so the record now states the mechanism with its evidence.

**Highlights:**

1. Routines fire on a schedule, an API call, or a **GitHub event** — and the GitHub wiring is configurable in the web UI only.
2. `last_fired_at` is populated for a cron fire and **absent for a GitHub fire** — so it answers nothing for the routines that matter.
3. The per-template design is recorded: assigned issue → `[Propose]`, merged proposal PR → `[Drive]`, merge announcement → `[Consent]`.

## 2. Motivation

This ticket was minted on a premise that was wrong ("nothing invokes them"), rewritten once when that was retracted, and its remaining question — what *does* invoke them — could not be answered from the account at all. The answer was never going to come from more reading: the record carries no event field, and the documentation says the wiring lives in the UI. What the repository needed was to state that plainly, with the citation, so the next reader stops inferring from absences.

## 3. Changes

### 3-1. Settle how a routine is actually triggered ([37ed359f](https://github.com/qmu/workaholic/commit/37ed359f))

The `workaholify` SKILL states the three trigger kinds, the web-UI-only constraint, the supported events and filters, and the per-template design; it also states that `last_fired_at` cannot distinguish "never ran" from "ran ten minutes ago" for a GitHub-triggered routine, and directs a reader to what the routine produced. `CLAUDE.md` and the proposal runbook match.

## 4. Outcome

Both remaining acceptance items are met — the second by its "cannot answer" branch, which the measurement forced. Nothing in the repository now rests a claim on the field's presence or absence.

## 5. Concerns

### The observability half was declined, not forgotten

- **Severity:** low
- **Description:** The ticket's original steps 3-4 asked for "has this run" to surface in `/setup-routines` output. That report is being retired by the mission PR #270 queued, so writing into it would be wasted work — and the underlying fact (the account cannot answer it) is recorded instead.
- **How to Fix:** Nothing; the setup-sheet mission carries whatever replaces the report.

## 6. Successful Development Patterns

- Reading the vendor documentation before designing around an API's apparent limits: one fetch replaced two days of inference, and contradicted both earlier conclusions.
