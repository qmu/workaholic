---
type: Story
branch: work-20260810-163619
tickets_completed: 1
mission: []
tickets: [20260809085953-reconcile-stale-notification-shape-references-post-p10.md]
---

# work-20260810-163619

## 1. Overview

Reconciled `drive/SKILL.md`, `drive/reference/routing.md`, `workaholify/reference/routines.md`, and `CLAUDE.md`'s claim-protocol prose against `workaholic:notify`'s actual current notification model, which had moved twice past what these files described: the `🟠` start post is retired entirely (2026-08-11) and the review-route finish reads `🟢 Implemented`, not the pre-P10 `🟢 Merge Requested` or the ticket's own now-superseded `🛠️` suggestion.

**Highlights:**

1. Removed every current-tense mention of the retired `🟠` start post from the drive skill, its routing reference, the workaholify routines reference, and `CLAUDE.md`
2. Fixed the review-route finish wording from `🟢 Merge Requested` to `🟢 Implemented` to match `workaholic:notify`, the actual source of truth
3. Minted a follow-up ticket for a second, unrelated stale-routing-model passage found in the same file, per the drive skill's scope boundary

## 2. Motivation

The ticket (created 2026-08-09) diagnosed several files as still describing two shapes P10 (2026-08-07) had retired, and prescribed replacing them with a `🛠️ Implemented`/`🛠️ Implementing` pair. By the time this branch drove it, `workaholic:notify` itself had reconciled further: mission `auto-merge-propose-and-implement-prs-under-a-dev-release-branch-split` (2026-08-11) retired the start post outright and settled the finish on `🟢 Implemented`, superseding the ticket's own suggested target. The acceptance criteria (no current-tense `🟢 Merge Requested` or `🟠 drive started`) held regardless, so this branch verified against `workaholic:notify` — the file every other doc defers to — rather than applying the ticket's stale literal steps.

## 3. Changes

### 3-1. Reconcile stale notification-shape references left behind by P10 ([4801dc16](https://github.com/qmu/workaholic/commit/4801dc16))

Re-grepped the repository for the retired shapes and found the drift had spread further than the ticket's own Key Files list: `drive/SKILL.md` and `drive/reference/routing.md` still described posting a `🟠` start line and a `🟢 merge requested` finish; `workaholify/reference/routines.md` cited the start line as a still-live softening factor for `[Consent]`'s retirement; `CLAUDE.md`'s claim-protocol paragraph described the same retired shapes. All four now match `workaholic:notify`'s SKILL.md and reference/notifications.md verbatim on shape names. Rebuilt `outputs/workflows` so its generated mirrors carry the fix. Minted `20260810164156-reconcile-drive-routing-md-s-stale-review-route-description.md` for a second, unrelated drift in `routing.md` (the `review` route's "stop at the PR" description predates the 2026-08-11 auto-merge mission) rather than fixing it inside this ticket's notification-shape scope.

## 4. Outcome

No file under `plugins/workaholic/`, `outputs/`, or `CLAUDE.md` describes the retired `🟢 Merge Requested` or `🟠 drive started`/`🟠 start` shapes as current; the two remaining mentions, in `notify/reference/notifications.md`, are historical ("retires the earlier ... shape") and match the acceptance criteria's carve-out. The full local verification suite passes against the rebuilt `outputs/workflows/`.

## 5. Concerns

### routing.md's review-route description predates the auto-merge mission

- **Severity:** low
- **Description:** `drive/reference/routing.md`'s *Routing mechanics (§6)* still describes `review` as "stop at the PR" pending a human merge, contradicting `drive/SKILL.md` §6's current "merge the PR immediately" model (see [4801dc16](https://github.com/qmu/workaholic/commit/4801dc16) in `plugins/workaholic/skills/drive/reference/routing.md`).
- **How to Fix:** Tracked as its own ticket, `20260810164156-reconcile-drive-routing-md-s-stale-review-route-description.md`, rather than fixed inside this notification-shape-scoped branch.

## 8. Notes

This session began `unbound_in_claude_session: true` and drove this unit — survey, claim, implementation, archive, and this report — by invoking the plugin's scripts on their checkout-relative paths rather than `${CLAUDE_PLUGIN_ROOT}`. `gh` is unavailable in this environment, so the PR is opened via the GitHub MCP server rather than `create-or-update.sh`.
