---
type: Story
branch: work-20260810-171152
tickets_completed: 1
mission: []
tickets: [20260810170140-reconcile-ticket-creation-s-review-wording-with-auto-merge.md]
---

# work-20260810-171152

## 1. Overview

Reconciled `/ticket`'s and `/mission`'s `merge_policy` interrogation wording with the 2026-08-11 auto-merge mission: both still framed `review` as "a human must review the PR", which is no longer true for either option.

**Highlights:**

1. `/ticket`'s §4d question and `/mission`'s equivalent ruling now describe the real distinction: `auto` proves the deploy before merging, `review` merges immediately and defers quality to the `release/*` QA window
2. Neither option asks a human to review the pull request any more; both merge unattended

## 2. Motivation

Found while implementing `20260810164156-reconcile-drive-routing-md-s-stale-review-route-description.md`, itself minted from `20260809085953-reconcile-stale-notification-shape-references-post-p10.md`: the developer-facing `merge_policy` interrogation is a decision prompt, not internal doc prose, so leaving it stale has a higher cost than the two prior doc-drift tickets — a developer answers it believing they are opting a human in or out of reviewing the PR, which the auto-merge mission made false for both choices.

## 3. Changes

### 3-1. Reconcile review wording with the auto-merge model ([7063790a](https://github.com/qmu/workaholic/commit/7063790a))

Rewrote `create-ticket/SKILL.md` §4d and `create-ticket/reference/interrogation.md`'s full question text, plus `mission/reference/command-flows.md`'s equivalent one-time ruling, to state the real distinction. A repository-wide grep for the retired "must a human review"/"stop at the PR for a human" framing came back clean.

## 4. Outcome

`/ticket` and `/mission`'s `merge_policy` interrogations now describe the current model accurately. `node scripts/test-workflow-scripts.mjs` (2468 passed, 0 failed), `verify.mjs`, and `validate-metadata.mjs` all pass against the rebuilt `outputs/workflows/`.

## 8. Notes

This session began `unbound_in_claude_session: true` and drove this unit by invoking the plugin's scripts on their checkout-relative paths rather than `${CLAUDE_PLUGIN_ROOT}`, using absolute paths for every script call after the previous unit's cwd-reset incident (`work-20260810-170054`'s story). `gh` is unavailable in this environment, so the PR is opened via the GitHub MCP server rather than `create-or-update.sh`. This is the fourth and final unit `/implement` drove this run — the third of three tickets minted successively from the same notification-shape-and-review-model reconciliation chain, each caught while implementing the previous one; the survey after this unit offers nothing further claimable.
