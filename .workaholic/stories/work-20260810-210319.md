---
type: Story
branch: work-20260810-210319
tickets_completed: 1
mission: [configure-routines-automatically-via-remotetrigger]
tickets: [20260810203351-fix-acceptance-link-marker-mismatch-full-path-vs-basename.md]
---

# work-20260810-210319

## 1. Overview

Fixed the `configure-routines-automatically-via-remotetrigger` mission's stuck `0/3` acceptance checklist: its items were linked with full-path markers instead of the bare filenames `tick-acceptance.sh` matches against, so none of its three already-archived tickets could ever tick their box. `link-acceptance.sh` now refuses a path-shaped marker at write time.

**Highlights:**

1. `link-acceptance.sh` refuses an `ARTIFACT` argument containing `/` (`path_not_filename`) before writing a marker, closing the class of mistake rather than only this one instance
2. Repaired this mission's three markers and ticked all three archived tickets — the checklist now reads 3/3
3. Grepped every other active mission for the same defect; none found

## 2. Motivation

Driving this mission's tickets in the prior branch (`work-20260810-202951`) surfaced that `archive.sh` reported `no_unchecked_match` for a ticket the mission's `## Acceptance` visibly named. Grepping every documented caller of `link-acceptance.sh` found each one already instructs a bare filename correctly — the mismatch was a one-off execution mistake, not a reproducible caller bug — so the fix that actually prevents recurrence is validating the input at the write boundary rather than auditing callers that were already correct.

## 3. Changes

### 3-1. Refuse a path-shaped acceptance link marker ([57561868](https://github.com/qmu/workaholic/commit/57561868))

`link-acceptance.sh` now rejects a `/`-containing `ARTIFACT` argument (`path_not_filename`, printed on stdout like its sibling refusals) before it can write an unmatchable marker. Repaired this mission's three existing markers by hand and re-ran `tick-acceptance.sh` for each of its three archived tickets, bringing the checklist to 3/3. Added a regression test asserting the refusal, its exit code, and that it leaves the mission file untouched.

## 4. Outcome

The `configure-routines-automatically-via-remotetrigger` mission is fully drained: 4/4 tickets archived, 3/3 acceptance items checked. `node scripts/test-workflow-scripts.mjs` (2472 passed, 0 failed), `verify.mjs`, `validate-metadata.mjs`, and `layout-doctor.sh` all pass against the rebuilt `outputs/workflows/`.

## 8. Notes

This session remained `unbound_in_claude_session: true` throughout and drove this unit the same way as the prior one in this run: checkout-relative script paths from a local `main` tracking `origin/main`, no `${CLAUDE_PLUGIN_ROOT}`. GitHub interactions go through the GitHub MCP server rather than `gh`.
