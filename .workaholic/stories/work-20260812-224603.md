---
type: Story
branch: work-20260812-224603
tickets_completed: 1
mission: []
tickets: [20260810161747-trim-claude-md-to-essential-load-bearing-content.md]
---

## 1. Overview

`CLAUDE.md` is trimmed from 375 lines / 129,674 bytes to **195 lines / 27,962 bytes**
(−48% lines, −78% bytes): it now states current behavior only, with a standing pointer
block naming where decision history lives (`docs/loop-engineering-workflow.md`, the
runbooks, each skill's `reference/`, the feedback stream, git history).

**Highlights:**

1. Every paragraph was tagged instruction-vs-narrative; rules survive, their argued justifications point elsewhere.
2. Preserved operationally: the nesting table, command contracts, script-path rules, branch-name literals, layout lockstep, Local Verification, Version Management.
3. The verbose Commands table rows (propose/workaholify at several KB each) compress to one factual contract per row.

## 2. Motivation

Issue qmu/workaholic#355: as project instructions loaded into every session's context,
130 KB of dated decision narrative (`decision I1`, `J4`, `K1`, …, each argued in full)
raised per-session overhead and buried the actionable rules inside what read as a
changelog. The trim criterion is the ticket's own: cut or compress narrative, never a
load-bearing rule.

### 3-1. Trim CLAUDE.md to load-bearing content ([3a9fc8be](https://github.com/qmu/workaholic/commit/3a9fc8be))

Rewrote `CLAUDE.md` section by section as terse current-behavior statements: the hooks
inventory became a compact list, the ticket-spine/ownership/lifecycle/claim-protocol
paragraphs became bulleted rules pointing at `skills/drive/SKILL.md` and `reference/`
for the full model, and each Commands row keeps its factual contract only. Cross-checked
`README.md`, `.workaholic/README.md`, `plugins/workaholic/rules/workaholic.md`, and the
test suite for references to renamed headings — none exist.

## 4. Release Preparation

- Version bumped to v1.0.159 (`74c8b772`); CI publishes the GitHub Release on merge.
- Branch-safety scan: `pass`.

## 5. Verification

- `wc`: 375 → 195 lines, 129,674 → 27,962 bytes, diffed here as the gate requires.
- `layout-doctor.sh .` conforming; `verify.mjs` clean; `test-workflow-scripts.mjs` 2238 passed / 0 failed.
- Left open deliberately (per the ticket's Considerations): whether `CLAUDE.md` should adopt a hard size ceiling — an operator decision for a follow-up.
