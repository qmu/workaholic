---
type: Story
branch: work-20260819-063001
description: Two housekeep-tick fixes driven on 2026-08-19 whose content was recovered by hand onto work-20260821-221006 and is already on main — this branch is superseded and should be closed, not merged
tickets_completed: 2
mission: []
tickets: [20260819052241-stop-the-housekeep-tick-posting-pr-status-notices.md, 20260819062058-fix-the-housekeep-check-in-s-already-asked-gate.md]
---

## 1. Overview

**This pull request should be closed without merging.** Both tickets were driven and
pushed here on 2026-08-19; the run then died before writing this story, so the branch sat
unreported for a week. Its content was recovered by hand onto `work-20260821-221006` and is
on `main` — merging this branch now would revert the `/housekeep` → `/moderate` rename.

**Highlights:**

1. The `🔧 Needs a decision` post shape is gone from the tick: the finding stays in the run report and the tick log, only the Slack post was removed.
2. The check-in's already-asked gate matches the derived step id instead of the log's free-text summary, and the id carries a digest so two long keys cannot collapse.
3. The branch is the first unit recovered by the new `report_incomplete` tier — and the first to show that a hand-recovered unit's original branch stays claimed forever.

## 2. Motivation

Issue #525 reported that the maintenance tick's responsibility is filing `[FB]` issues and
that it was also posting PR-status and merge-readiness notices. Issue #526's report was
narrower and sharper: the check-in's already-asked gate is documented as *mechanical*, and
it was not — it matched the raw key against an agent-composed summary, so the same
question was re-asked an hour later while it still sat unanswered in the channel.

Both were driven in one unit deliberately, so the scope question they share — whether
"only file issues" retires the `❓` check-in — could not be answered two ways.

## 3. Changes

### 3-1. Stop the housekeep tick posting PR-status notices ([1bd8dec8](https://github.com/qmu/workaholic/commit/1bd8dec8))

Diagnosis-first: the post was reproduced and localized to three documents before anything
was edited, and `step-stuck-prs.sh`'s `needs_agent[].action` was changed from `remind` to
`report` — leaving a row that tells an unattended agent to post would have been a latent
contradiction rather than a gate. The finding, the digest and the tick log are untouched.

### 3-2. Fix the housekeep check-in's already-asked gate ([1543e902](https://github.com/qmu/workaholic/commit/1543e902))

Two independent causes, each confirmed by varying one input: the needle was matched against
the summary, and `cut -c1-32` truncated the slug so two long keys shared one id. The gate
now matches the step id exactly, and the id gains a digest of the full key.

## 4. Outcome

The work is on `main`, delivered by `work-20260821-221006` (`04b5670c`, `92dc9b86`,
`1dbe25b3`) and refined since. This branch is a leftover: unmerged by the claim oracle's
definition because its *commits* never landed, even though its *content* did.

## 5. Concerns

### A hand-recovered unit's original claim branch stays claimed forever

- **Severity:** moderate
- **Description:** This unit was stranded on 2026-08-19 and recovered by re-applying its work onto a fresh claim branch. That leaves the original branch unmerged, so the claim oracle still holds it — and since the `report_incomplete` tier shipped it is offered as a **mandatory** takeover on every tick, forbidding `ok` indefinitely (see [1fdcddb2](https://github.com/qmu/workaholic/commit/1fdcddb2) in `plugins/workaholic/skills/drive/scripts/lib/claims.sh`).
- **How to Fix:** Ticket [`20260826041500-tell-a-superseded-claim-from-a-live-one`](https://github.com/qmu/workaholic/pull/611) — the survey should be able to say a claim's content already reached the base, rather than offering a branch that cannot merge. Closing this pull request releases this instance; the shape needs the reader.

### This branch cannot merge and must not be made to

- **Severity:** urgent
- **Description:** `git merge-tree origin/main HEAD` reports ten conflicts, including `modify/delete` on `plugins/workaholic/skills/housekeep/{SKILL.md,reference/workflow.md,scripts/ask-question.sh}` — deleted on `main` by the `/housekeep` → `/moderate` rename and modified here. Resolving them in this branch's favour would revert that rename.
- **How to Fix:** Close this pull request without merging. Nothing is lost: both tickets are archived on `main` under `.workaholic/tickets/archive/work-20260821-221006/`, with their Final Reports intact.

## 6. Notes

Phase 0's version bump was deliberately skipped: the branch is superseded and will not
merge, so a bump would only add another conflicting commit.
