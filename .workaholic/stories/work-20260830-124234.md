---
type: Story
branch: work-20260830-124234
description: Measures the last namespace a per-unit claim ref could use, records the grain that ref could never reach and why a ruling-blocked handoff keeps no verdict word, and hands off the three tickets whose repair the container's transport refuses.
tickets_completed: 1
mission: [stop-two-runs-from-claiming-and-driving-one-unit]
tickets: [20260831024448-stop-re-resuming-a-handoff-blocked-on-a-ruling]
---

## Handoff

**This branch is unfinished. Someone must continue it.**

- **Done:** The claim-race finding in `plugins/workaholic/skills/drive/reference/claims.md`
  (*What the claim contends for*) now rests on measurement rather than inference — every ref
  namespace a per-unit contended ref could use has been probed, on both transports, and the
  readings are recorded. Committed and pushed.
- **Not done:** The mission's first acceptance item and the three tickets behind it —
  `20260830082251-make-the-claim-contend-for-one-ref-per-unit.md`,
  `20260830082251-refuse-the-losing-claim-by-its-own-word.md` and
  `20260830082251-extend-the-drill-to-prove-the-repair.md`. All three are recorded `blocked` and
  left in `todo/`. The second and third rest on the first: without a contended ref there is no
  lost-race refusal to word and no repair for a drill to prove.
- **Next step:** Rule on **two** things, not one. First, whether an asynchronously released
  contended ref is acceptable, or the mechanism is re-scoped — ticket 1's own Considerations
  anticipate this: *"Measure whether creating the contended ref is permitted there before
  building on it; if it is not, that is a finding for the mission, not a workaround to invent
  here."* The measurement is in, and it says no namespace this container can create atomically
  can also be released. Second, and independently of the transport, that *a ref derived from the
  unit id* reaches the **mission** grain only: a batch unit's id is minted per run inside the
  claim act, so two racers over the same tickets name two different refs. Unblocking the
  namespace does not by itself deliver acceptance item 1 at the batch grain.
- **Re-measured 2026-08-31 by an independent run, not cited:** `git push origin <sha>:refs/claims/probe-<ts>`
  answers `RPC failed; HTTP 403` with `git ls-remote origin 'refs/claims/*'` empty, and
  `git ls-remote origin 'refs/heads/wk-transport-probe-*'` still returns
  `304652b2 refs/heads/wk-transport-probe-1788104778` — the branch the earlier probe created and
  neither transport could delete. Both halves of the finding hold on a second, separate reading:
  the create is refused where the ref would have to live, and the one namespace that accepts a
  create cannot be released. The three tickets stay `blocked` on the same ruling.
- **Minted mid-run, and DRIVEN on the eighth takeover:**
  `20260831024448-stop-re-resuming-a-handoff-blocked-on-a-ruling.md`, archived at
  [ee21f904](https://github.com/qmu/workaholic/commit/ee21f904). This branch has now taken
  **nine** consecutive takeovers in twenty-three hours (`36d94f95` → `16ee0ea2`), each but the
  eighth an empty `Resume a PR-unit` commit and zero lines of implementation, because a half-driven
  handoff blocked on an **operator ruling** reads `parked_with_pr` and is re-offered by every
  survey. The eighth's outcome is the ticket's own **step 3** — no existing tree signal carries
  both the pre-drive property and the meaning, so the verdict is left alone and the finding is
  recorded in `claims.md`. **That did not end the re-offer**, exactly as the eighth predicted: the
  ninth takeover (`16ee0ea2`, 2026-08-31 16:45 UTC) found the same three tickets, re-read the
  recorded measurements instead of re-probing for them, and drove nothing. Only a writer declaring
  `verification_handoff:` stops it.
- **Re-confirmed 2026-08-31 by this run, not cited:** `git push origin <sha>:refs/claims/probe-…`
  fails at the remote with `git ls-remote origin 'refs/claims/*'` **empty** (nothing created), and
  `git ls-remote origin 'refs/heads/wk-transport-probe-*'` still returns
  `304652b2 refs/heads/wk-transport-probe-1788104778` — the branch the first probe created and
  neither transport can delete. Both halves hold on a third, independent reading: the create is
  refused where the ref would have to live, and the one namespace that accepts a create cannot be
  released. The three tickets stay `blocked` on the same ruling.
- **Attempted:**

  ```
  $ git push origin <sha>:refs/claims/probe-<ts>
  error: RPC failed; HTTP 403 curl 22 The requested URL returned error: 403
  $ git ls-remote origin 'refs/claims/*'
  (empty — nothing was created)

  $ gh-rest.sh api repos/qmu/workaholic/git/refs --method POST -f ref=refs/claims/probe-rest ...
  {"message":"Write access to this GitHub API path is not permitted through this proxy."} (HTTP 403)

  $ git push origin <sha>:refs/tags/wk-transport-probe-<ts>
  error: RPC failed; HTTP 403 curl 22 The requested URL returned error: 403
  $ git push origin :refs/tags/wk-transport-probe-<ts>
  error: RPC failed; HTTP 403 curl 22 The requested URL returned error: 403
  (ls-remote empty both times — nothing created, nothing to delete)

  $ git push origin <sha>:refs/heads/wk-transport-probe-1788104778
   * [new branch]        304652b2 -> wk-transport-probe-1788104778     <- succeeds
  $ git push origin :refs/heads/wk-transport-probe-1788104778
  error: RPC failed; HTTP 403 curl 22 The requested URL returned error: 403
  $ gh-rest.sh api repos/.../git/refs/heads/wk-transport-probe-1788104778 --method DELETE
  {"message":"Write access to this GitHub API path is not permitted through this proxy."} (HTTP 403)
  $ git ls-remote origin refs/heads/wk-transport-probe-1788104778
  304652b2...	refs/heads/wk-transport-probe-1788104778               <- the ref survives
  ```

## 1. Overview

The claim-race finding said the proxy permits writes to `refs/heads/*` "and to nothing else", and
that a ref there could never be released either. Neither clause had been probed to its end: the
first was an inference over two namespaces, the second was carried over from `retire-claim.sh`'s
branch-delete row. Both are now readings.

**Highlights:**

1. `refs/tags/*` — the last candidate namespace — is refused for create and delete alike.
2. A `refs/heads/*` create succeeds while its delete is refused by *both* transports, with
   `ls-remote` confirming the ref survives.
3. The finding now names why an asynchronous CI-side release does not rescue the repair.
4. And it now names the **grain** the mechanism could never reach even in a permitted
   environment: a ref keyed on the unit id arbitrates the mission grain and not the batch
   grain, because `claim.sh` mints `batch-$(date +%Y%m%d%H%M%S)` inside the claim act itself.
5. Separately, the branch's own re-offer loop is now a recorded finding: a half-driven handoff
   blocked on an operator ruling keeps **no** verdict word, because no signal on the tree
   carries both the pre-drive property and the meaning.

## 2. Motivation

Three consecutive runs took this unit over and reported it blocked, each citing the recorded
measurement rather than making one. Ticket 1 asks the run to *choose* the contended ref, so the
choice is only closed when every candidate namespace has been read. `refs/tags/*` had never been
probed, and the decisive term — whether the one writable namespace can be released — rested on a
different script's precedent. A block whose decisive term is cited rather than measured is the
shape this repository calls a self-certifying claim.

## 3. Changes

### Measure the last namespace a claim ref could use ([8c68220d](https://github.com/qmu/workaholic/commit/8c68220d))

Records the re-probe beside the existing finding in `claims.md`: the three namespaces, both
transports, the `ls-remote` readings, and the reason an asynchronous release still leaves the
condition the repair must not create — a window in which a mission's own follow-up re-claim is
refused by a ref that no longer stands for anything.

### Bump version to v1.0.261 ([05e4b7a1](https://github.com/qmu/workaholic/commit/05e4b7a1))

Patch bump across the four version files, with `outputs/` regenerated.

### Record the grain the named ref cannot reach ([fb8ef4b6](https://github.com/qmu/workaholic/commit/fb8ef4b6))

The transport was measured to its end; the *mechanism* had not been. Records in `claims.md`
(*What the claim contends for*) that **a ref derived from the unit id arbitrates only where two
racers name the same unit**, which is the mission grain and not the batch grain: `claim.sh` step
2 mints `unit="batch-$(date +%Y%m%d%H%M%S)"` inside the claim act, so two runners surveying the
same unclaimed tickets mint two different unit ids and push two different refs — both creates
succeed and the loser is refused by nothing. Step 3's **artifact-overlap** check already
anticipated a cross-batch scoop, but it reads `claims_scan`, so it closes the *sequential* case
and is defeated by the same window as the unit-id check. Arbitrating the batch grain would need
a ref per **artifact** — N create-only pushes with no atomicity across them, in a container whose
ref deletes are refused. Recorded rather than designed, on ticket 1's own instruction to *say so
and re-scope rather than forcing the named mechanism*.

### Record why a ruling-blocked handoff keeps no word ([ee21f904](https://github.com/qmu/workaholic/commit/ee21f904))

Drives the ticket this branch minted about its own re-offer loop, to the ticket's own **step 3**:
*if, and only if, no existing signal carries the property, record that finding and stop*. Each of
the three candidates step 2 names was weighed against the two halves a sibling verdict word needs
— the **pre-drive property** (`awaiting_verification` is safe because the declaration is on the
artifact before the drive, so a run can never write it for its own unit) and the **meaning**
(*this work waits on a ruling*):

- the branch story's `## Handoff` is written by the run about its own unit — the self-certifying
  evidence the 2026-08-23 Open Decisions rule refuses by name — and is present on **every**
  half-driven handoff, so a verdict keyed on it would withhold takeovers from drivable units;
- a `blocked` stamp on the queued tickets does not exist, and minting one is the field on an
  artifact the ticket's own Gate forbids;
- an `## Open Decisions` item *does* carry the pre-drive property, and is not a blockage signal —
  the driving floor requires a run to **resolve** each item, so such a ticket is ordinarily
  drivable, and the measured unit carries none anyway.

So the verdict is left alone: no word added, no verdict widened, no field on any artifact, with
`lib/claims.sh`, `claim.sh`, `plan-units.sh` and `verification-handoff.sh` all byte-identical.
The finding also names what *would* carry both halves and already exists — `verification_handoff:`
declared at creation, which `/specificate` writes for an unresolved operator-only fork — so the
repair for this class is a **writer's**, at the writing seam rather than the oracle.

## 4. Outcome

The mission's remaining acceptance item is now blocked on **two** measured facts rather than one
inherited one: a transport that permits no releasable atomic ref, and a mechanism that reaches
one of the two grains it is specified over. The next run reads both answers instead of re-probing
for them, and the operator's ruling now has the whole question in front of it. The branch's own
re-offer loop is a closed question too — not repaired, but recorded, with the reasoning that
refuses each candidate signal written down so the next reader argues with it rather than
re-deriving it. Nothing about the claim protocol moved across any of it: no verdict word, no
script, no gate.

## 5. Concerns

### A probe branch was created on origin and cannot be deleted

- **Severity:** moderate
- **Description:** `wk-transport-probe-1788104778` was pushed to origin to measure whether
  `refs/heads/*` can be released, and both delete transports refused — the refusal *is* the
  finding, so the branch survives (see [8c68220d](https://github.com/qmu/workaholic/commit/8c68220d)
  in `plugins/workaholic/skills/drive/reference/claims.md`). It is not a `work-*` branch, so no
  claim reading sees it and `claim-retirement.yml` does not touch it.
- **How to Fix:** Delete it from the GitHub UI, or from any executor holding `contents: write`.

### The mission cannot reach its first acceptance item in this environment

- **Severity:** moderate
- **Description:** Acceptance item 1 requires a contended ref that wins at the remote; no
  namespace this container can create atomically can also be released, so the three tickets
  behind it are unreachable here rather than merely hard. Since
  [fb8ef4b6](https://github.com/qmu/workaholic/commit/fb8ef4b6) there is a **second, independent**
  half: the mechanism the item names reaches the mission grain only, so unblocking the transport
  would still leave the batch grain unarbitrated.
- **How to Fix:** Rule on an asynchronously released ref and its named cost, re-scope the
  mechanism to a per-artifact arbiter (and price its lack of atomicity), or declare the tickets
  `verification_handoff:` so no further tick claims them.

### Every tick takes this unit over and can drive none of it

- **Severity:** moderate
- **Description:** The claim reports `parked_with_pr`, so the survey offers a takeover on every
  tick; the takeover then finds three tickets it has already proved unreachable and publishes an
  empty `Resume a PR-unit` commit for nothing. Four ticks have now done this. The declared-handoff
  path (`awaiting_verification`) exists for exactly this and does not fire, because the tickets'
  `verification_handoff:` is empty — and a run may never declare it for its own unit, which is
  what keeps `handoff` from becoming a soft landing. **Nine** ticks have now done this, and
  [ee21f904](https://github.com/qmu/workaholic/commit/ee21f904) establishes that the oracle
  cannot close it on its own: no signal on the tree carries both the pre-drive property and the
  meaning, so the verdict was deliberately left alone.
- **How to Fix:** An operator or `/specificate` declares `verification_handoff:` on the three
  tickets, which moves the claim to `awaiting_verification` and stops the re-offer at the oracle.
  That is now the *only* remaining fix rather than one of several — the alternative of teaching
  the oracle to read the block was worked and refused, with its reasoning in `claims.md`.

## 6. Successful Development Patterns

- Probing the *last* candidate rather than stopping at the first refusal is what turned a
  three-tick citation loop into a closed question — a block is only finished when the alternative
  it did not try has been read too.
- Re-reading the *mechanism* after the *environment* had been closed found the second half of the
  block. A measurement that answers "can I write this ref" is not the same question as "would
  this ref decide the race", and only the first had been asked.
- A ticket that authorizes *record the finding and stop* as an outcome is what let this takeover
  produce something. The two halves a signal needed were stated in the ticket's own step 1, so the
  three candidates could be weighed against a written bar rather than against taste — and the
  refusal is now arguable, which a silent non-repair would not have been.

## 7. Notes

The three original tickets stay in `todo/` per the failure contract; the claim stays standing.
The ticket this branch minted about its own re-offer loop is archived, and its outcome does not
release the claim: only a writer declaring `verification_handoff:` does that.
