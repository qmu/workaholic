---
type: Story
branch: work-20260901-064758
tickets_completed: 7
tickets:
  - 20260828122109-reproduce-the-check-in-day-cap-jam-and-pin-it.md
  - 20260828122110-bound-the-check-in-day-count-to-one-derived-day.md
  - 20260828122110-drain-a-multi-day-question-backlog-oldest-held-first.md
  - 20260828122110-read-back-what-the-check-in-delivered-and-held.md
  - 20260828122110-make-a-tick-that-reached-nobody-a-visible-event.md
  - 20260828122110-drill-the-check-in-delivery-path-with-no-network.md
  - 20260828122110-state-the-check-in-cap-contract-in-the-skill.md
mission:
  - deliver-what-the-loop-already-knows-to-the-person-who-can-act
---

## 1. Overview

This mission reached `main` on 2026-09-01 when a four-day-stranded proposal merged, and **every one of its seven tickets asked for work already on the base** — the same ask had been planned a second time and driven on 2026-08-28. Each ticket was verified against the running behaviour and archived with that finding recorded; the mission closed `achieved` on the archive gate's own arithmetic.

**Highlights:**

1. No code changed. Seven Final Reports record what was checked and where the behaviour lives.
2. The slug now names **two** missions — the archived one did the work — which is minted as its own ticket, not fixed here.
3. Verification was behavioural, not a file test: implementations and the skill's written contract, and the drill was run rather than found.

## 2. Motivation

`/specificate` planned one ask twice, six hours apart, from two different feedback records. The later plan was driven the same day; the earlier one sat in publication #688, stranded from 2026-08-28 until the tick that repaired the stranded-publication path merged it. Landing it queued seven tickets for finished work.

The queue cannot be drained by ignoring them — a queued ticket nothing drives forbids `ok` on every later tick — so each was attempted under the failure contract's own terms. "Attempt every ticket" was satisfied by *checking the premise against the tree*, which is what the previous unit in this same run had already learned to do.

## 3. Changes

### 3-1. Reproduce the check-in day-cap jam and pin it ([6d79f6d8](https://github.com/qmu/workaholic/commit/6d79f6d8))

The hermetic reproduction already exists at `test-workflow-scripts.mjs:27093-27177`, pinning the exact discrimination asked for: a log whose asks are all on earlier days no longer refuses, while a cap spent on the tick's own day still does.

### 3-2. Bound the check-in day count to one derived day ([7e5b8e5f](https://github.com/qmu/workaholic/commit/7e5b8e5f))

`ask-question.sh` derives `TODAY` once from the tick id and passes it to `log-read.sh`'s existing `--since` — one derivation, no second reader, no stored cursor, which were this ticket's own constraints.

### 3-3. Drain a multi-day question backlog oldest-held first ([beb24203](https://github.com/qmu/workaholic/commit/beb24203))

`step-human-checkin.sh` orders `held` by the day each key was first held, tie-broken to a total order so a re-entered tick is byte-identical. The step orders; it neither caps nor asks.

### 3-4. Read back what the check-in delivered and held ([32065ccd](https://github.com/qmu/workaholic/commit/32065ccd))

`delivered` / `held_count` / `candidates` / `delivery` on every exit path, with `cap_spent` split from `cap_unbounded` so our own degradation can never render as a budget that worked.

### 3-5. Make a tick that reached nobody a visible event ([1b41aacf](https://github.com/qmu/workaholic/commit/1b41aacf))

The step supplies its own `event` when it had candidates and delivered none — the third gate on the tick root, fired only when the reading changed.

### 3-6. Drill the check-in delivery path with no network ([f3211592](https://github.com/qmu/workaholic/commit/f3211592))

`verify-checkin-delivery` was run, not merely located: it passes hermetically with a breaker row present, registered against this mission's own slug.

### 3-7. State the check-in cap contract in the skill ([acfb74fc](https://github.com/qmu/workaholic/commit/acfb74fc))

`workaholic:moderate` carries the contract, the measured jam with its numbers, and the four repairs it deliberately was not.

## 4. Outcome

Seven stale tickets left the queue and the mission closed `achieved` (3/3 acceptance, queue empty) on the archive gate's arithmetic. The base is unchanged in behaviour — the value is that the queue no longer offers finished work, and that the collision this incident left behind is now written down.

## 5. Concerns

### The slug `deliver-what-the-loop-already-knows-to-the-person-who-can-act` names two missions

- **Severity:** urgent
- **Description:** `missions/archive/<slug>/mission.md` (created 2026-08-28T18:19:13Z, driven on `work-20260828-184133`, `actual_hours: 1.05`) and `missions/active/<slug>/mission.md` (created 2026-08-28T12:20:17Z, closed by this branch) are both tracked and both `achieved`. The next attempt to move the active copy into the archive will collide. Both carry real history, so choosing which survives is a judgement about the durable record (see [acfb74fc](https://github.com/qmu/workaholic/commit/acfb74fc)).
- **How to Fix:** Minted as `20260901070000-collapse-two-missions-that-plan-the-same-ask.md` on this branch, with the three candidate seams and the rule that neither copy may be deleted silently. Not fixed here — `archive.sh` behaved correctly at every step, so this is a missing rule rather than a broken script.

### Landing a long-stranded proposal queues work the loop has already finished

- **Severity:** moderate
- **Description:** This mission and `say-when-the-loop-has-run-out-of-direction` were queued by proposals open for four and six days; between them roughly fifteen tickets for finished work entered `todo/`. Nothing checks whether a proposal is still worth landing, because until this week none stayed open long enough to matter.
- **How to Fix:** Already minted by an earlier tick as `20260901062000-check-a-stranded-proposal-is-still-worth-landing.md`, which weighs the three seams. That ticket and the one minted here are the two halves of the same incident.

### `say-when-the-loop-has-run-out-of-direction` is very likely the same shape

- **Severity:** moderate
- **Description:** Its nine queued tickets include *write the one reader of a direction's lifecycle state* and *add the moderate step direction-health*, and both `direction-state.sh` and `step-direction-health.sh` exist on the base with `CLAUDE.md` documenting the whole reader table.
- **How to Fix:** Drive it the way this one was driven — verify each ticket against the running behaviour before implementing, and archive with the finding recorded. It was left claimable by this run and is offered to the next tick.

## 6. Successful Development Patterns

- Checking each ticket's premise against the tree before implementing turned seven re-implementations into seven verifications — the second unit in this run to be saved by the same habit.
- Verifying behaviour rather than file existence mattered: the drill ticket was closed by *running* `verify-checkin-delivery` and reading its breaker, not by finding a function of the right name.
- Minting the collision as a ticket instead of resolving it kept a judgement about the durable record with the operator, where the failure contract puts it.

## 7. Release Preparation

**Verdict**: Ready for release

- **Concern:** The duplicate-slug pair above is in the tree either way; this branch documents it and adds no new instance.
- **Before release:** Nothing — the branch changes no behaviour and the scan is clean.
- **After release:** Drive `say-when-the-loop-has-run-out-of-direction` with the same premise check.

## 8. Notes

The branch-safety scan is `pass` with no findings. `record-run-hours.sh` recorded 0.4h against `implement-20260901-064758`.
