---
type: Story
branch: work-20260906-023953
description: The loop selects its mode from the session's own capabilities and states a native-parent branch that keeps the coordinator's turn, so each tick reports into the chat it was started in.
tickets_completed: 7
mission: [report-each-tick-in-the-originating-codex-chat]
tickets: [20260906022907-prove-the-behaviour-in-the-operator-s-own-codex-chat.md, 20260906022855-carry-the-loop-state-across-context-compaction.md, 20260906022855-delegate-each-due-role-as-a-bounded-native-child.md, 20260906022855-reserve-the-final-response-for-a-stop-or-a-refusal.md, 20260906022855-retire-only-the-supervisor-the-native-mode-replaces.md, 20260906022855-run-the-tick-as-a-native-parent-that-keeps-its-turn.md, 20260906022855-select-the-loop-mode-from-measured-capabilities.md]
---

## Operator acceptance — 2026-09-07

The operator stated that the remaining behavior is already known to work, that they will now use
it, and explicitly instructed merging without waiting for the additional question test. The
verification ticket is closed by this acceptance. The mid-wait question is **not claimed as
measured by this session**; the original declaration and the ruling remain in its Final Report.

The live run measured successive five-minute reports across a 24m47s child task, automatic
completion delivery, duplicate-dispatch refusal, and a separate one-minute reporting phase.
The dated environment and timestamps are in `plugins/workaholic/skills/work/reference/other-agents.md`.

## 1. Overview

The loop selected an external supervisor from a product label even when the session could keep
its coordinator and reports in the originating chat. It now selects a mode from the session's
actual wait/output/child/scheduler capabilities. An explicit interval such as `/work 1m` flows
through every mode; five minutes is the default, not a fixed override.

## 2. Motivation

Earlier timer repairs left the return path untouched: output in `.codex-loop/` did not reach the
person who started work. This branch keeps a capable parent present and reports through ordinary
commentary. The original one-minute request was never withdrawn; the immutable correction record
also distinguishes already-archived branch work from the stale queue still visible on main.

## 3. Changes

- Select native parent, same-chat schedule or external supervisor from capabilities, and name the
  destination and limits at startup. README and the command alias use that same selection.
- Keep startup-anchored deadlines and the resolved interval across early wakes and compaction.
  Use bounded children and live role identifiers; report each outcome once.
- Reserve a final response for stopping/refusal. A question is answered without ending the loop.
- Join dispatched fixture workers before deleting their files and locks. The CI failure was a
  real cleanup race, reproduced with a temporary deletion probe against the old test.

## 4. Outcome

**6,875 smoke assertions passed, zero failed**. The focused clock smoke passed 18 assertions,
including concurrent dispatch; the clock drill passed with 6 load-bearing rows and 2 breakers.
Build, self-contained-output and metadata validation passed. After merging the delivered
PR #1069 into this branch, the integrated clock drill and build/metadata checks passed again.
The full 50-drill run had four
editor-overlap snapshot failures, all passing when rerun with a stable tree; six existing drills
lack breakers and two require an external server. The original aggregate remains a failed run
with successful scoped rechecks, not a claim that the first run was green.

The live chat proves periodic reports across a >10-minute real delegated task and automatic
completion delivery. The remaining user-question criterion is operator-accepted, not marked as a passing measurement.

## 5. Concerns

- The actual mid-wait question was accepted by the operator for real use without another
  measurement. Shell tests and the earlier acknowledgement are not substitutes for that evidence.
- A separate requested-60-second trial produced visible reports at 14:07:03, 14:08:02 and
  14:09:01 JST while retaining the startup anchor. No adaptive interval policy was added.
- The Slack connector exposed no matching channel in this session. No Slack reply or duplicate
  reply prevention success is claimed by this measurement.
- The work skill exceeds the repository's preferred length. Its existing operational-detail
  extraction concern remains deferred until the acceptance work is settled.
- The release scan reports only the pre-existing oversized drill script (`override` tier),
  with no secret or leakage findings. App closure and hard harness limits remain untested.

## 6. Successful Development Patterns

- Read the actual PR head before interpreting a main-only queue as unimplemented work.
- Use real task runtime and visible conversation timestamps for acceptance, keeping missing
  evidence explicit rather than inferring it from tool availability or passing shell fixtures.
