---
type: Story
branch: work-20260907-045136
description: The tick's moderate cadence now reads its own recorded finish instead of the newest line in a log every loop writes to, so a busy loop can no longer silence the maintenance tick indefinitely.
tickets_completed: 1
mission: []
tickets: [20260907031134-gate-the-moderate-spawn-on-its-own-recorded-finish.md]
---

## 1. Overview

`/infinite-development` §2 read `moderate`'s 30-minute gate with `log-read.sh --latest-tick` and
no step filter, unlike its two siblings. All three read one file, into which the coordinator
writes every loop's finish line under its **own** tick id — so the bare read answered whichever
tick wrote last.

**Highlights:**

1. The gate now names `--step-prefix loop-finish-moderate`, keying each cadence on its own loop.
2. A suite row anchored on that paragraph fails when the filter is deleted — proved, not assumed.
3. `log-read.sh` is byte-identical: the reader answered both questions correctly all along.

## 2. Motivation

A `loop-finish-*` line is written on most ticks, so each write moved the bare reading forward and
the gate could read *moderate ran just now* indefinitely — silencing the one tick that notices
absence. Measured at coordinator tick `20260906-175547`: bare read `20260906-174615` (that
session's own `loop-finish-propose`), filtered `20260906-173626` — the real finish 19 minutes
older than the gate believed. Nothing reported it and nothing could: the wrong answer is a
well-formed tick id, so no `cadence_unreadable` fired. Two correct same-day changes composed into
it, #960 pointing the gate at `--latest-tick` and #961 then filling that file.

## 3. Changes

### 3-1. Gate the moderate spawn on its own finish ([9af8bacb](https://github.com/qmu/workaholic/commit/9af8bacb))

§2's gate now reads `log-read.sh --step-prefix loop-finish-moderate --latest-tick`, every
surrounding rule verbatim, beside a paragraph stating why the filter is what makes it moderate's
own. `skills/loops/SKILL.md` carried the same gate unfiltered and now matches. The suite gains
the row that would have caught it, anchored on that paragraph because the existing
`loop-finish-*` assertion passes on the `implement`/`propose` block alone. `log-read.sh` is
untouched; both sibling surfaces were re-read and already filtered.

## 4. Outcome

The divergence was reproduced first on a hermetic fixture, localizing the defect to the caller
rather than the reader. The new assertion bites in both directions — 6845/1 with the filter
deleted, 6846/0 restored — and build, verify and metadata are clean with no `outputs/` diff.
`verify-all` reports 41 proved, 0 failed.

## 5. Successful Development Patterns

- Reproducing the report before adopting the fix it named.
- Proving the new assertion **fails** on the un-fixed file, not only that it passes.
- Re-reading the sibling surfaces the ticket had cleared — the unnamed one carried the stale form.

## 6. Release Preparation

**Verdict**: Ready for release

The branch-safety scan reports `pass` with no findings. `doc-drift.sh` reports no candidates;
`area-freshness.sh` flags only `terms/retired-terms.md`, that record's own subject, untouched here.
