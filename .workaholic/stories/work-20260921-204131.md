---
type: Story
branch: work-20260921-204131
description: Refuse a routine turn whose named continuation has already lapsed, and let a turn declare what it intends to emit
tickets_completed: 1
mission: []
tickets: [20260921180208-prove-a-live-continuation-before-a-routine-turn-yields.md]
---

## 1. Overview

A native `/work` parent kept stopping after an ordinary progress report while accepted work and
observation remained. `final-response-contract.sh` proved only that a routine turn had **named** a
continuation; nothing compared what it named against the clock, so a continuation whose deadline
had already passed satisfied the contract and the turn yielded to nothing.
`runtime/scripts/lib/coordinator.jq` already made exactly that comparison and emitted
`continuation_lapsed` — the derivation existed and the gate that yields the turn did not consult
it. The reader now reads the same rule, and a routine turn declares what it intends to emit so the
contract can refuse the act the ask names.

**Highlights:**

1. A routine turn naming a continuation whose `next_due` has passed is refused `continuation_lapsed`.
2. `now` is required on any input naming a continuation — a caller cannot obtain a pass by omitting the clock.
3. `next_due < now` is stated as one rule with two call sites, and the suite fails on a third.
4. `intends_final_response` is a declared fact, refused `routine_emits_no_final_response` on a routine turn.
5. Nothing widened: three `interruption_kind` values, two `continuation.kind` values, three `blocked_on` values, three `path` values.

## 2. Motivation

The ask (issue #1260) named the cause precisely: persisting `control: running` is not sufficient
once the host turn has ended and no interruptible wait remains. The reader already refused
`continuation_unproved` for a turn naming nothing, and its `invalid_facts` assertion already
required a named continuation to carry a `kind` from the closed set, a non-empty `id` and a
numeric `next_due`. What it never did was compare that `next_due` against the moment of the turn.

**Measured before anything changed.** Driving the reader with `interruption_kind: "routine"` and a
continuation whose `next_due` was `1758400060`:

```
{"ok":true,"path":"resume","final_response":false,"question":null,"instance_id":"loop-1",
 "anchor":1758400000,"control":"running","hold_stands":false,"second_start":false,
 "continuation":{"kind":"same_chat_schedule","id":"sched-1","next_due":1758400060},…}   exit 0
```

Driving `coordinator.jq` with the same continuation at `now: 1758999999`:

```
{"resumed":false,"resumed_reason":"continuation_lapsed","control":"running"}
```

Two readers, one continuation, opposite answers — and the one that yields the turn was the
permissive one. The repair is therefore to read the derivation the repository already holds at the
one seam that yields the turn, not to invent a second liveness authority.

The second half is that the contract owned the turn's facts and carried **no fact describing what
the turn intends to emit**. `final_response` is an *answer*, so a run that emitted a routine final
response anyway was invisible to the reader and the contract could not fail closed on it.

## 3. Changes

**`plugins/workaholic/skills/work/scripts/final-response-contract.sh`**

- Takes **`now`** (epoch seconds, integer, `>= 0`). It is **required on any input naming a
  continuation** and refused `invalid_facts` when absent or malformed, rather than defaulted — a
  defaulted clock is exactly the pass the rung exists to deny. An input naming no continuation
  (every `review_handoff`) needs none and is byte-identical to before.
- Refuses **`continuation_lapsed`** for a `routine` turn whose named continuation's `next_due` has
  passed, on the rung immediately after `continuation_unproved`, so *named nothing* and *named
  something already dead* stay distinguishable. The comparison is `next_due < now`, byte-identical
  to the reducer's, so a continuation due this very second has not lapsed.
- Takes **`intends_final_response`** (boolean, **absent means false**) and refuses
  **`routine_emits_no_final_response`** for a `routine` turn that declares it.
- Both rungs are bounded to `routine` — the path that answers `final_response: false` and lets the
  turn end, which is the act the ask names. `review_required` still answers `final_response: true`
  with the one question and needs no clock; `task_review` still answers `false`.
- The header states the required-clock decision, the one-rule-two-call-sites fact, and plainly that
  fail-closed here is a **refusal and not a suppression**: the reader writes nothing, ever, and
  cannot stop a run from emitting text.

**`plugins/workaholic/skills/runtime/scripts/lib/coordinator.jq`** — behaviour byte-identical. It
gains the sibling half of the one-rule statement beside its `$not_resumed` ladder, naming the other
call site and why the reader cannot include this program (it is a reducer body, not a module).

**Surfaces** — one wording, appended to the existing continuation paragraph on
`skills/work/SKILL.md`, `commands/infinite-development.md` and
`skills/runtime/reference/native-loop.md`, pinned byte-identically by
`test-workflow-scripts.mjs`. `commands/work.md` carries the two new refusal words on its own
shorter line, and `CLAUDE.md`'s Architecture Policy paragraph is updated in the same commit.

**Tests** — `native-coordinator.test.mjs` gains the regression walk the ask names: a completed
worker recorded through `coordinator.sh finish`, a progress report classified `routine`, then
continued observation, asserting on the **recorded event sequence**
(`completed → reported → already_started → running`) and never on elapsed time, with no second
`start` and the anchor unmoved. It covers the lapsed continuation, the declared-intent refusal, the
live-continuation pass, the missing clock, and that `review_required` and `task_review` are
unchanged. Every existing call site that names a continuation now passes `now`.
`test-workflow-scripts.mjs` extends the pinned wording, adds five reader rows, and adds a walk that
fails when a **third** authored call site compares a continuation against a clock.

## 4. Outcome

`node --test scripts/tests/agentic-loop/native-coordinator.test.mjs` — 14 passed, 0 failed.
`node scripts/test-workflow-scripts.mjs` — 7918 passed, 0 failed (the contract row alone, 30
passed). `sh scripts/e2e/loop-drill.sh verify-observation-during-work` — `verdict: pass`, 8
load-bearing rows, 0 failed.

**Local proof set** (`branching/scripts/local-proof.sh`): `ok: true`, `complete: true`,
`not_run: []`, `failed: []` — `verify.mjs` (1s), `validate-metadata.mjs` (0s), `layout-doctor.sh`
(10s), `agentic-loop.test.mjs` (105s), `test-workflow-scripts.mjs` (767s), `loop-drill-hermetic`
(268s), all `ran: true, ok: true`; scratch directory used at
`/home/ec2-user/.cache/workaholic/proof-tmp.596177`.

## 5. Concerns

- **The liveness comparison exists twice.** `coordinator.jq` is a reducer body rather than a module
  of `def`s, so the reader cannot `include` it, and the two spellings are kept in step by an
  assertion rather than by a shared file. The assertion also fails on a third comparison anywhere
  under the authored plugin tree, which is the containment; it cannot make the two into one.
- **Requiring `now` is a tightened constraint on callers.** Every call site in the tree was updated
  in the same change and the suite now fails on a caller that names a continuation without a clock.
  No script in the plugin builds this reader's input — the executable callers are the two test
  files — so nothing else in the tree had to move; a caller outside this repository composing the
  facts by hand gets `invalid_facts` until it supplies the clock. That is the intended direction: a
  refusal is retried, a pass on an unread clock is not.
- **The rungs are bounded to `routine` and `task_review` was deliberately left alone.** A
  `task_review` turn may still name a continuation whose deadline has passed. The ticket scoped the
  repair to the path that yields the turn, and widening a refusal beyond the ask on a path that
  carries the paused-goal derivation is a change nobody measured. Worth a later look; not taken
  here.
- **The refusal cannot prevent the act.** A run that never asks the contract, or asks and emits
  anyway, is unaffected. What the refusal buys is a named *no* and a receipt.

## 6. Successful Development Patterns

- **Reproduce the divergence before proposing a mechanism.** Both readings went into the ticket
  before a line changed, and they are what established that the repair was *read the existing
  derivation*, not *add a liveness authority*. The reducer's answer was already correct; only the
  seam that yields the turn was not consulting it.
- **An apostrophe in a jq comment inside a shell script terminates the string.** These scripts hold
  jq programs inside single-quoted shell strings, so a comment reading ``coordinator.jq's ladder``
  closed the string and the shell reported a syntax error at the next `elif`. It is a **shell**
  error rather than a jq compile error, so the suite's *every embedded jq program compiles* row
  would not have caught it. Write such comments without apostrophes.
- **Pin the count, not only the text.** The two prose statements that the rule has two call sites
  are the readable half; the walk that fails on a third comparison is the half that survives
  somebody adding one without reading either file.
