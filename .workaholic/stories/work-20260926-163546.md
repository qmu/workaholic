---
type: Story
branch: work-20260926-163546
description: Emit live-proof evidence against a declared mount
tickets_completed: 0
mission: []
tickets: []
---

## Handoff

**This branch is complete but unverified here. Someone must verify it before it merges.**

- **Done:** the live-proof evidence emitter works against this repository's own declaration again. `describe-qfs.sh`'s declared-mount native path now carries its documented `described` / `mounts` envelope, and `emit-live-proof-evidence.sh` binds a route it cannot find to `null` instead of an empty stream. Both are pinned by tests, and the ticket's Progress Report records the re-measurement. All of it is pushed.
- **Not done:** "a verified Slack sender plus declared read_channel_delta, read_thread, list_thread_changes, post_root, post_reply, and add_reaction capabilities are not available in the unattended environment". The eleven delivery incidents remain open, and the ticket stays queued in `todo/`.
- **Verified here rather than assumed:** `describe-qfs.sh --workspace qmu --channel dev-workaholic` was run on 2026-09-26 with qfs unlocked. `/slack` (`team`) and `/slack-cc-for-qmu` (`cc-for-qmu`) both reach the channel, and both are `channel_verified: true`. Both now advertise `read_channel_delta`, `read_thread`, `search_exact` and `post_reply`. Both report `sender_id: null` / `sender_verified: false`, and both still carry `sender_unverified`, `thread_discovery_unavailable` (`threads_not_selectable`), `reaction_map_unverified` and `root_map_unverified_or_ambiguous`. No reachable route advertises `list_thread_changes`, `post_root` or `add_reaction`, and none proves the sender, so the declared limitation is **present**.
- **Next step:** declare the verified `sender_id` in `AGENTS.md` and run `sh plugins/workaholic/skills/transport/scripts/emit-live-proof-evidence.sh --root . --out proof.json`. Then perform one bounded root/reply/reaction through a route that carries those operations. Fill in each performed operation's `proved` and the `round_trip` block, run `sh plugins/workaholic/skills/transport/scripts/verify-live-proof.sh --root . --evidence proof.json`, and retire the incident set only on `closure_eligible: true`.
- **Attempted:** `sh plugins/workaholic/skills/transport/scripts/emit-live-proof-evidence.sh --root . --out proof.json` → `{"ok":true,"emitted":true,"declared_digest":"c14b7259f78d28aef8cfe74b58b4a4e9","route_readable":true,"sender_declared":false,"proved_operations":0,"unavailable_operations":[]}`. Passing that document through the gate returned `{"ok":true,"closure_eligible":false,"reason":"unverifiable_sender","sender_verified":false,"missing_operations":["read_channel_delta","read_thread","search_exact"],"round_trip_verified":false,"incidents_unresolved":11}`. Before the fix, the same emitter printed nothing and exited 0, and the gate answered `evidence_unreadable`. No Slack write was attempted.

## 1. Overview

After `AGENTS.md` began declaring `mount: /slack`, the live-proof evidence emitter stopped producing output against this repository, with no error and no output at all. This branch repairs the two readers behind that and re-measures the handoff.

## 2. Changes

### 2-1. Prove the declared Slack transport before retiring delivery incidents

- `describe-qfs.sh`: a declared mount is described through `describe-native-qfs.sh`, whose bare `{ok, observations}` was passed through without the `described` / `mounts` envelope the script's header promises. The envelope is now added. `.described` is what the emitter keys on.
- `emit-live-proof-evidence.sh`: `$route` was bound over `$obs[] | select(.mount == $mount)`. On an empty observation list this binds to an empty stream, so the whole document vanished. It now binds to `[...][0]`, which is `null` when absent, so every `available` reads `null` and never disappears.
- Tests: the explicit-mount describe in `scripts/tests/agentic-loop/repair-contracts.test.mjs` now asserts the envelope. `testSlackLiveProofEmitter` adds a declared-mount case with no `qfs` that must still emit.

## 3. Outcome

The gate's input is produced again from the live declaration. Live acceptance remains blocked on a verified sender and on three operations no route carries.

Local proof (`branching/scripts/local-proof.sh`): `ok: true`, `complete: false`. `verify.mjs`, `validate-metadata.mjs`, `layout-doctor.sh`, `test-workflow-scripts.mjs` and `loop-drill-hermetic` passed. `not_run`: `agentic-loop.test.mjs: timeout:300s`, because the host was also running the coordinator's own proof set. Run alone, `node --test scripts/tests/agentic-loop/repair-contracts.test.mjs` passed 30 of 30.

## Unposted Line

shape: handoff
reason: thread_unresolved: search_exact incomplete (complete:false, 0 matches)
text: 🟡 Handoff [#1283 宣言済みマウントに対してライブ証明の証拠を出力する](https://github.com/qmu/workaholic/pull/1283) — 次の実行が自動で引き継ぎます。`git fetch && git checkout work-20260926-163546` で先に取れます。残るのは検証済み sender_id の宣言と、root・reply・リアクションの一往復の読み戻しだけです。(メンション先未解決: 誰にも通知していません)

