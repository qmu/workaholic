---
created_at: 2026-08-18T06:23:40+00:00
status: done
author: a@qmu.jp
assignees: [a@qmu.jp]
depends_on:
feedback: [20260818062117-rename-fullfill-to-prepare-release-and-update-the-routine-notification-heading.md]
merge_policy:
verification_handoff: 
claim: work-20260818-063646
---

# Rename /fullfill to /prepare-release

## Overview

<!-- PROPOSED. Merging the pull request this was published on is what turns it from a
     proposal into queued work. -->

Rename the `/fullfill` command to `/prepare-release`, and rename the routine record
`[Release Status]` to `[Prepare Release]` so the routine's heading matches the command it
runs. Issue #485 asks for both halves in one breath.

**This deliberately reverses two decisions made a day earlier** (2026-08-17, PR #484,
ticket `20260817113323-rename-release-status-to-fullfill`), and the ticket is written that
way rather than as a fresh idea:

1. `/release-status` became `/fullfill` **for a one-word command naming convention**. The
   new ask states its own reason for reversing that: the name should describe what the
   command does (preparing a release) rather than optimize for a single word. Behaviour is
   unchanged by either rename — the command is still a pure reader.
2. The routine record was left named `[Release Status]` as that ticket's recorded **Open
   Decision 2**, with a stated cost: `/setup-repo-routines` converges an account's routines
   **by name**, so renaming the template's `name:` does not rename an operator's existing
   routine — the next convergence creates a *second* one, and a routine is an account-level
   record no other account can list or delete, so the old `[Release Status]` keeps firing
   hourly beside it until its owner removes it by hand.

That cost has not gone away; the ask overrides the decision, not the mechanics. The rename
therefore has to ship **with** the one-time cutover instruction, not instead of it.

## Policies

- `workaholic:implementation` / `policies/directory-structure.md` — conventional project layout
- `workaholic:implementation` / `policies/coding-standards.md` — style and structure conventions
- `workaholic:operation` / `policies/observability.md` — the routine's post shape is an operator-facing surface

## Key Files

Every current `fullfill` hit outside `.workaholic/` (the history area is never rewritten):

- `plugins/workaholic/commands/fullfill.md` — the command file itself; renames to
  `prepare-release.md`, and its `name:` frontmatter and *On the name* paragraph both change
  (that paragraph currently justifies the one-word spelling and records the routine-kept-its-name
  decision — it must be rewritten to state the new reason, not deleted).
- `plugins/workaholic/skills/workaholify/routines/release-status.md` — the routine template:
  `id:`, `name: "[Release Status] {repo_name}"`, the filename, and the opening paragraph that
  explains why the record kept its name.
- `plugins/workaholic/skills/ship/SKILL.md` — §7 names `/fullfill` and `[Release Status]`.
- `plugins/workaholic/skills/ship/scripts/run-note-cadence.sh` — header comment names the routine.
- `plugins/workaholic/skills/notify/SKILL.md` and `notify/reference/notifications.md` — the
  `📦 Release status` post shape and the sentence that attributes it to the routine.
- `plugins/workaholic/skills/workaholify/reference/routines.md` — the scope-split record.
- `scripts/test-workflow-scripts.mjs` — the routine-template drift pin and the setup-sheet
  assertions assert the literal strings `"[Release Status] workaholic"` and the sheet heading
  `## [Release Status] workaholic` (around lines 10291-10305, 15055).
- `scripts/e2e/loop-drill.sh` — `verify-status` help text and the §904 comment.
- `docs/loop-drill-runbook.md`, `docs/drive-loop-runbook.md`, `docs/proposal-loop-runbook.md`
  — the runbooks name both command and routine.
- `CLAUDE.md` (command table, routine table, the three routine paragraphs) and `README.md`
  (command table, `/setup-repo-routines` row).
- `outputs/workflows/skills/ship/SKILL.md` — **generated**; regenerated by `build.mjs`, never
  hand-edited.

## Implementation Steps

1. Enumerate the current surface before touching anything:
   `grep -ril "fullfill" --exclude-dir=.git --exclude-dir=.publish .` and
   `grep -rn "Release Status\|Release status" plugins/ scripts/ docs/ CLAUDE.md README.md`.
   Record the hit list; every hit must end up either changed or explicitly accounted for, the
   way the previous rename did.
2. `git mv plugins/workaholic/commands/fullfill.md plugins/workaholic/commands/prepare-release.md`
   and set `name: prepare-release`. Rewrite the *On the name* paragraph: the new name says
   what the command does; note that it supersedes the one-word convention that produced
   `/fullfill` on 2026-08-17, and why (issue #485).
3. Rename the routine record: `git mv .../routines/release-status.md .../routines/prepare-release.md`,
   set `id: prepare-release` and `name: "[Prepare Release] {repo_name}"`, and **replace** the
   opening "the routine record keeps its name" paragraph with the cutover it now needs (step 4).
   Confirm `list-routine-templates.sh` and `render-setup-sheet.sh` read the id from the file
   rather than a hardcoded list — if any list is hardcoded, it is part of this step.
4. Ship the cutover instruction with the rename: `/setup-repo-routines`' report and the
   rendered setup sheet must say, once, that an account already running
   `[Release Status] <repo>` should **rename that routine** rather than create a second, and
   that nothing in the plugin can detect or remove a duplicate on another account (the
   account-level-record limit `/setup-repo-routines` already states).
5. Update every prose reference from step 1's hit list: skills, references, runbooks,
   `CLAUDE.md`, `README.md`. `CLAUDE.md`'s routine table row becomes
   `prepare-release.md` / `[Prepare Release]` / `repository` / `45 * * * *` /
   `/setup-repo-routines`.
6. Update the literal-string assertions in `scripts/test-workflow-scripts.mjs` and the
   `verify-status` text in `scripts/e2e/loop-drill.sh`.
7. Resolve Open Decision 1 below **explicitly in the Final Report** before touching (or
   deliberately not touching) the `📦 Release status` post shape.
8. Regenerate and verify: `node scripts/build-plugins/build.mjs`, `verify.mjs`,
   `validate-metadata.mjs`, `node scripts/test-workflow-scripts.mjs`,
   `bash plugins/workaholic/hooks/layout-doctor.sh .`
9. Re-run step 1's greps. Every surviving `fullfill` hit must be inside `.workaholic/`
   (immutable history: the feedback record, the archived ticket, the story, the indexes) —
   those are correct to leave, exactly as the previous rename left its `release-status`
   survivors.

## Open Decisions

<!-- Recorded, not resolved: this session cannot ask the reporter. The driving session
     resolves each explicitly and records the resolution in its Final Report. -->

1. **Is the Slack post root `📦 Release status` in scope?** The ask says "update the
   routine's notification heading from `[Release Status]` to `[Prepare Release]`". It names
   the **routine record's name** literally, but "notification heading" also describes the
   `📦 Release status` post root (`notify/reference/notifications.md`, byte-pinned against
   the routine template by `test-workflow-scripts.mjs`). The two are different surfaces, and
   the 2026-08-17 ticket left the post shape alone on a stated reason: it names the **event**,
   not the command, and its prefix is the notify lookup's own exact-string dedup key, so
   changing it posts one duplicate line at the cutover. Renaming the record only is the
   literal reading of the ask; renaming both is the consistent one. Neither is recommendable
   over the other without the reporter, and the run must state which it took.

## Quality Gate

**Acceptance criteria** — the checkable conditions that must hold:

- `/prepare-release` is the command's only name: `plugins/workaholic/commands/prepare-release.md`
  exists, `fullfill.md` does not, and no `fullfill` hit survives outside `.workaholic/`.
- The routine record is `[Prepare Release] {repo_name}` with `id: prepare-release`, and the
  one-time cutover instruction (rename the existing routine, do not create a second) is
  present in `/setup-repo-routines`' report path and its rendered setup sheet.
- Behaviour is unchanged: the command is still a pure reader that writes nothing into the
  repository, still `scope: repository`, still `45 * * * *`, still carries no `Write`/`Edit`
  in `allowed_tools`.
- Open Decision 1 is resolved explicitly in the Final Report, with the surface(s) changed
  named.

**Verification method** — the commands/tests/probes that prove them:

- `node scripts/test-workflow-scripts.mjs` (the routine-template drift pin and the setup-sheet
  assertions must pass on the new strings, not be deleted)
- `node scripts/build-plugins/build.mjs && node scripts/build-plugins/verify.mjs && node scripts/build-plugins/validate-metadata.mjs`
- `bash plugins/workaholic/hooks/layout-doctor.sh .` → `conforming: true`
- `sh scripts/e2e/loop-drill.sh verify-status --json`
- The step-1 greps re-run, every survivor accounted for in the Final Report

**Gate** — what must pass before approval:

- All of the above, plus the Final Report naming Open Decision 1's resolution and the
  duplicate-routine cutover as a stated operator action.

## Considerations

- **This is the second rename of the same command in two days.** The Final Report should say
  so plainly; the cost of a rename is paid by every operator who has the routine configured,
  and paying it twice is worth recording as a fact rather than smoothing over.
- **The generated bundle** (`outputs/workflows/skills/ship/SKILL.md`) is rebuilt, never
  hand-edited; the `Outputs Freshness` CI fails on any drift.
- **`.workaholic/` history is not rewritten.** The feedback record, archived ticket and story
  from the 2026-08-17 rename keep saying `/fullfill`, which is what they recorded.
- The 2026-08-17 ticket also observed that `/setup-dev-routines`, `/setup-repo-routines` and
  `/mission-close` remain multi-word; with this rename the one-word convention is no longer
  the project's stated naming rule at all, which the command file's *On the name* paragraph
  should reflect rather than leave dangling.

## Final Report

Development completed as planned.

### Open Decision 1, resolved: **both** surfaces were renamed

The ask names one string (`[Release Status]`, the routine record's name) and describes the
other ("the routine's **notification heading**", which is the `📦 Release status` post root).
This run renamed **both**: the record to `[Prepare Release] {repo_name}`, and the post root to
`📦 Prepare release`.

The tie was broken by a fact, not a preference. The 2026-08-17 ticket left the post root alone
on one stated cost — "its prefix is the notify lookup's own exact-string dedup key, so changing
it posts one duplicate line at the cutover". **That cost does not exist.** The lookup searches
`` `deploy:<digest>` `` and nothing anywhere queries the `📦` prefix
(`notify/SKILL.md`, *The repository tick's status line*; `notifications.md`'s dedup paragraph).
With its only measured cost gone, the remaining argument for leaving it — "it names the event,
not the command" — did not outweigh the ask's own stated goal, which is a heading consistent
with the command name. The correction is recorded in both places that carried the wrong claim
(`notify/SKILL.md`, `notifications.md`) rather than silently dropped.

Unchanged by this: the `📦` emoji, the `deploy:<digest>` key, both silence gates, and the
`Draft note:` line.

**One surface deliberately not renamed**: `ship/SKILL.md`'s section title `## 7. Release status`.
It names the *read* the section documents (and is the cross-reference the command file cites by
name), not the command and not a notification; the ask reached neither. Left as written.

### The duplicate-routine cutover is a stated operator action

Renaming the record does **not** rename an operator's live routine. `/setup-repo-routines`
converges by rendered `name`, so the next run **creates a second routine** beside the old one,
and a routine is an account-level record no other account can list or delete — nothing in the
plugin can detect or remove the duplicate. The instruction ships with the rename rather than
instead of it, and is carried **mechanically** so it cannot rot:

- the template declares `renamed_from: "[Release Status] {repo_name}"`;
- `render-setup-sheet.sh` renders it as the sheet's first note ("Already running
  `[Release Status] workaholic`? Rename that routine — do not create a second.");
- `/setup-repo-routines`' report path states it for the convergence path, which renders no sheet;
- `workaholify/SKILL.md` §5 and `CLAUDE.md` record the field's lifecycle: **delete it from the
  template once the fleet has cut over**, because it describes a migration, not a routine.

Two `test-workflow-scripts.mjs` assertions pin both surfaces, and they are written to assert
nothing once the field is deleted — the intended end state.

### This is the same command's second rename in two days

`/release-status` → `/fullfill` (2026-08-17) → `/prepare-release` (2026-08-18). Behaviour did
not move under either; the cost is paid by every operator with the routine configured, and this
time the record moved too, so the manual cutover above is owed to all of them. `CLAUDE.md` and
the command's *On the name* paragraph both state it plainly. The one-word command convention
that produced `/fullfill` is now retired outright — `/setup-dev-routines`,
`/setup-repo-routines` and `/mission-close` were already multi-word and nothing was left holding
the line.

### Surviving `fullfill` / `[Release Status]` hits, accounted for

Every survivor outside `.workaholic/` is a deliberate historical reference naming the old name:
`commands/prepare-release.md` (the *On the name* paragraph), `routines/prepare-release.md`
(`renamed_from:` plus the cutover paragraph), `workaholify/SKILL.md` §5, its
`reference/routines.md`, `notify/SKILL.md`, `notifications.md`, and `CLAUDE.md`'s rename
paragraph. Everything inside `.workaholic/` — the feedback records, the archived ticket, the
stories, the indexes — is immutable history and is correct to leave, exactly as the previous
rename left its `release-status` survivors. No hit was left unchanged by omission.

### Discovered Insights

- **Insight**: The `📦 Release status` heading was never the lookup's dedup key; the key is
  `` `deploy:<digest>` `` and always was.
  **Context**: A decision was made on 2026-08-17 to *not* rename that heading, and its sole
  stated justification was that the prefix carried dedup weight. Neither `notify/SKILL.md` nor
  `notifications.md` ever said so — both name the token — but the claim was written into the
  routine template and read as authoritative the next day. A cost recorded in the wrong file is
  worse than one not recorded at all: it survives review by looking like a measurement. Both
  canonical files now state that the heading is free wording, so the next rename does not have
  to rediscover it.
- **Insight**: `renamed_from:` is the first template field that describes a *migration* rather
  than a routine, and it is designed to be deleted.
  **Context**: `list-routine-templates.sh` and `render-routine.sh` both read fields by name and
  ignore the rest, so the field costs nothing to add and nothing to remove. Because
  `render-setup-sheet.sh` derives the note from the field and the tests iterate over whichever
  templates carry it, deleting the field after the cutover removes the note, the report line and
  the assertions in one act — no prose is left behind naming a migration that finished.
