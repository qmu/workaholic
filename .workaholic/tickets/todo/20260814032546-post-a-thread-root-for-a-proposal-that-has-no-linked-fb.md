---
created_at: 2026-08-14T03:25:46+00:00
author: a@qmu.jp
assignees: [a@qmu.jp]
depends_on:
feedback: [20260814032508-auto-file-a-short-fb-issue-for-propose-notifications-with-no-linked-fb-so-proposed-can-reply-to-it.md]
merge_policy:
claim: work-20260814-033643
---

# Post a thread root for a proposal that has no linked FB

## Overview

PROPOSED. When a `[Propose]` tick finishes and `workaholic:notify`'s stateless lookup
finds no existing thread (case 4), the `🔵 Proposed` finish line is itself posted as the
new top-level root. The reporter asks for a different arrangement in that case: Claude
first posts a short description together with the FB's URL — **carrying no `@Claude`
mention** — and then posts `🔵 Proposed` as a threaded reply to that message, so the
item's Slack thread opens with something a human can read and reply to rather than with a
status line. The report's title asks additionally that a short FB issue be auto-filed for
a proposal that has no linked FB, so the root has a URL to point at; whether that half is
adopted, and what the root links when it is not, is left as an Open Decision below rather
than resolved unattended.

The change is a notification-model change, so it is not finished when the propose run
posts differently: `workaholic:notify` states the model once, its `reference/notifications.md`
is the shape catalog, and *The prompt is the ceiling — no self-authorized shapes* means the
`[Propose]` routine template must name the new post before any session may emit it. All of
that lands in one change; splitting it would ship a documented-but-unauthorized shape.

## Policies

- `workaholic:implementation` / `policies/directory-structure.md` — conventional project layout (all code work)
- `workaholic:implementation` / `policies/coding-standards.md` — style and structure conventions (all code work)
- `workaholic:implementation` / `policies/observability.md` — the run's signal must distinguish a healthy loop from a broken one; a notification path that silently changes shape is the failure mode this policy names
- `workaholic:implementation` / `policies/objective-documentation.md` — the post shape is a written contract mirrored in three places (skill, catalog, routine template); each copy must be verifiable against the others

## Key Files

- `plugins/workaholic/skills/notify/SKILL.md` — *One thread per feedback item* states the ordered lookup and case 4's "post a new root carrying `fb:<stem>`"; *Post shapes…* names `🔵 Proposed` as the `[Propose]` routine's thread root. Both sentences move if the root becomes a separate post.
- `plugins/workaholic/skills/notify/reference/notifications.md` — the shape catalog (`### /propose — finish only`), where a new root shape would be written verbatim.
- `plugins/workaholic/skills/propose/SKILL.md` (*Notifier contract*) and `plugins/workaholic/skills/propose/reference/workflow.md` (step 12) — the propose run's own notify step and what step 13 reports about it.
- `plugins/workaholic/skills/workaholify/routines/fb.md` — the `[Propose]` routine prompt; the ceiling rule means the new post must be named here, and the live account routine must then be re-synced (`/setup-routines`).
- `plugins/workaholic/skills/propose/scripts/notify-slack.sh` — the tokened fallback: it builds `{channel, text}` only, so it can post a keyed root and can never post a threaded reply.
- `plugins/workaholic/skills/feedback/scripts/open-issue.sh`, `plugins/workaholic/skills/feedback/scripts/fb-title.sh` — the only sanctioned issue opener and the `[FB] ` title stamper, if the auto-filed-issue half is adopted.
- `outputs/workflows/` — regenerated by `node scripts/build-plugins/build.mjs` once any of the above skills change.

## Related History

The lookup this ticket sits on top of has been corrected three times, each time by pinning
one more detail rather than by replacing the mechanism; the same restraint applies here.

- [.workaholic/feedbacks/20260813121647-propose-s-thread-lookup-searched-an-issue-reference-form-that-exists-nowhere.md](.workaholic/feedbacks/20260813121647-propose-s-thread-lookup-searched-an-issue-reference-form-that-exists-nowhere.md) — case 3 composed an `owner/repo#N` query that exists in no message, so a findable thread was missed and a root was posted instead. Still unproposed; a run that falls to case 4 by mistake would now post *two* messages instead of one.
- [.workaholic/feedbacks/20260810202108-notify-s-thread-lookup-posts-new-roots-instead-of-replying.md](.workaholic/feedbacks/20260810202108-notify-s-thread-lookup-posts-new-roots-instead-of-replying.md) — the same symptom reported earlier; its measured root cause was the search surface (`slack_search_public` cannot see the private `dev-<repo>` channel), fixed by naming `slack_search_public_and_private`.
- [.workaholic/feedbacks/20260805021414-propose-roots-a-new-slack-thread-even-when-the-trigger-message-already-is-the-item-s-thread.md](.workaholic/feedbacks/20260805021414-propose-roots-a-new-slack-thread-even-when-the-trigger-message-already-is-the-item-s-thread.md) — the origin of case 1; one item with two roots is not repairable by a routine, which is why the not-found branch is written so carefully.
- A persisted thread key (`thread_ref` committed beside the record) was drafted and ruled out by the developer on 2026-08-11: a Slack thread coordinate in this public repository is the exposure the P9 withdrawal already refused. Do not reintroduce it as a way of avoiding the root.

## Implementation Steps

1. **Reproduce and localize first.** Run a `/propose` tick (or `sh scripts/e2e/loop-drill.sh verify-propose`) whose ask has no thread in `#dev-workaholic`, and record what actually lands: which lookup case the run took, what the `🔵 Proposed` message contained, and whether the miss was case 4 legitimately or the case-3 query defect recorded in Related History. Confirm the same on the tokened path, where `notify-slack.sh` cannot thread at all. Do not change a shape before this run is on the record.
2. **Decide the Open Decisions below** explicitly and record the ruling in the Final Report — they are the ticket's real content, and a silent choice here writes a new class of unattended GitHub write.
3. **State the model change once, in `workaholic:notify`**: in case 4, the run posts a description root first and the finish line as a reply into it; say what the root carries (a short description, the FB's URL, and the `` `fb:<stem>` `` key, which must stay on the root or case 2 stops finding anything) and state the prohibition explicitly — **the root carries no `@Claude` mention**, because a mention would re-trigger the Slack app on the routine's own post.
4. **Add the root's exact wording to `reference/notifications.md`** beside the `/propose` finish shape, and mark plainly that the `🔵 Proposed` line is now a reply in this case rather than the root.
5. **Name the new post in the `[Propose]` routine template** (`routines/fb.md`) so a session is authorized to emit it, keeping the template's copy and the catalog's copy verbatim identical.
6. **State the fallback's behavior rather than leaving it implied**: with no connector, `notify-slack.sh` posts the keyed finish line alone, exactly as today; the run reports that it could not thread instead of pretending it did.
7. **Update `workaholic:propose`** (*Notifier contract* and `reference/workflow.md` steps 12–13) so the run's report names both posts and their outcomes.
8. Regenerate the bundle (`node scripts/build-plugins/build.mjs`) and update `CLAUDE.md` / `README.md` where they describe the notification model, per the same-change documentation rule.

## Quality Gate

**Acceptance criteria** — the checkable conditions that must hold:

- A `/propose` run whose lookup finds no thread posts exactly two messages: a description root carrying the FB's URL and `` `fb:<stem>` ``, and `🔵 Proposed` as a reply whose `thread_ts` is that root's timestamp.
- Neither message contains a Claude mention token (no `<@U…>` resolving to the Claude app, no literal `@Claude`).
- A run whose lookup **does** find a thread posts exactly one message, the finish line, as a reply — unchanged from today.
- The root's wording is byte-identical in `notify/reference/notifications.md` and `workaholify/routines/fb.md`.
- The tokened fallback path posts one keyed message and the run reports that it could not thread.
- Each Open Decision below is answered in the Final Report with its reasoning.

**Verification method** — the commands/tests/probes that prove them:

- `sh scripts/e2e/loop-drill.sh verify-propose` on a seeded ask with no existing thread; read the resulting messages in `#dev-workaholic` and confirm the two-message shape and the `thread_ts` linkage.
- `node scripts/build-plugins/build.mjs && node scripts/build-plugins/verify.mjs` — the bundle regenerates and is self-contained.
- `node scripts/test-workflow-scripts.mjs` — green.
- `bash plugins/workaholic/hooks/layout-doctor.sh .` — `conforming: true`.
- A diff of the two wording copies (catalog vs template) showing no drift.

**Gate** — what must pass before approval:

- The reproduction from step 1 is recorded in the Final Report, with the lookup case it actually took.
- The suite above is green and `outputs/` carries no uncommitted diff.
- The live `[Propose]` routine's prompt is re-synced, or the report states plainly that it was not and that the new shape is therefore unauthorized until it is.

## Open Decisions

- **Whether a short FB issue is auto-filed at all, and what the root links when it is not.** Options: (A) the run opens a short `[FB]` issue on this repository through `feedback/scripts/open-issue.sh` and the root links it, as the report's title asks; (B) no issue is filed and the root links the feedback record's file URL on `main`, as the report's body ("the FB's URL") also reads. Neither is clearly recommendable: (A) creates a class of unattended write the crossing rules currently reserve for *other* repositories, and it interacts with clock-fired discovery — an issue filed **assigned** would be re-discovered as an inbound ask on the next tick and cannot be excluded, because the record naming it was already written and records are immutable; an **unassigned** issue is never discovered, which makes it safe and also makes it a page nobody is asked to act on. (B) writes nothing outside the repository and gives the human no GitHub thread to reply in, which is the affordance the report is asking for.
- **What "a proposal that has no linked FB" selects.** Options: the lookup's case 4 (no thread was found, whatever the ask carried) versus "the ask carried no originating `[FB]` issue" (which can be false while a thread still exists, and true while one does). Case 4 is the only branch where no root exists to reply into, so it is the reading this ticket's steps assume — but it is not what the report's words literally say, and picking it silently would answer a question the reporter may have meant differently.

## Considerations

- The ceiling rule means the template edit is load-bearing, not documentation: a session may emit only the shapes its own routine prompt names (`plugins/workaholic/skills/notify/SKILL.md`, *The prompt is the ceiling*). Shipping the model change without the template edit ships a shape nobody may post; shipping the template edit without re-syncing the account routine leaves the live prompt behind (`/setup-routines` commonly reports `no_transport`, so this is an operator step, not a code step).
- Two messages per proposal raises the channel's volume against the bright line "an event earns its post" (`plugins/workaholic/skills/notify/SKILL.md`) — the root is not a new event, it is the same event split into a readable header and a status line. Say so where the shape is written, so a later reader does not mistake it for a second announcement.
- The root must carry `` `fb:<stem>` `` or the whole stateless lookup regresses: case 2 searches for that key, and moving it onto a message the finish line replies to is fine only as long as the root itself carries it (`plugins/workaholic/skills/notify/SKILL.md`, case 2).
- The case-3 query defect in Related History is still open and unproposed; if it lands first, fewer runs reach case 4 at all. Neither change blocks the other, but a run measured under the defect will over-report how often the root is needed (`plugins/workaholic/skills/notify/SKILL.md`, case 3).
- Whatever the root's link target, it must not be a Slack thread coordinate written into a public artifact — that reintroduces the exposure the P9 withdrawal refused (`plugins/workaholic/skills/notify/reference/notifications.md`, *The carried-target disclosure*).
