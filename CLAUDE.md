# Workaholic

Private, cross-agent distribution of structured development workflows and engineering standards. **AI agents are the primary users** — routines invoking `/implement` and `/specificate`, and sessions running the workflow skills — while the **developer is the operator** who approves pull requests, configures routines, and rules on deferred decisions. Richest on **Claude Code** (plugin marketplace: commands, hooks, the always-on policy lens); portable skills also ship to **Codex** (`.agents/plugins/marketplace.json` → `outputs/workflows`) and to 40+ agents via the Agent Skills standard / `skills` CLI. Authored source lives under `plugins/`; cross-agent artifacts are generated into `outputs/`.

This file states **current behavior only**, as briefly as it can. Decision history, rationale, measurements and rejected alternatives live in `docs/loop-engineering-workflow.md`, `docs/proposal-loop-runbook.md`, `docs/drive-loop-runbook.md`, `docs/loop-drill-runbook.md`, each skill's `SKILL.md` and `reference/` directory, the feedback stream (`.workaholic/feedbacks/`), and this file's own git history. **Do not re-import them here** — a rule belongs here, its story does not.

## Important

- Edit `plugins/`, not `.claude/` — this repo develops plugins; changes go to `plugins/` unless explicitly requested otherwise.
- **Update the docs in the same change.** When a change alters behavior, structure, commands, skills, or conventions, update every affected document (`README.md`, `.workaholic/README.md`, `CLAUDE.md`, `plugins/workaholic/rules/*.md`) in the same commit. Outdated documentation is a defect; `/story` runs `doc-drift.sh` and `area-freshness.sh` only as backstops.
- **Write each surface in the language its audience reads.** Reasoning on the `#dev-workaholic` channel and in Claude Code Web routines is **Japanese**. For Slack posts the rule also ships **in the plugin**, beside the shapes (`rules/interaction.md`, *The language of a post is the language its readers use*): measured 2026-09-01, a consuming repository whose own `CLAUDE.md` carried the Japanese rule still got an English `🔎 Moderation` root, because the ceiling names the only shapes a session may emit and every one of them is written out in English. **And that Japanese must be read on first sight, not decoded** (2026-09-02): the bar is an outcome — *a channel reader understands what is being asked without opening the English record behind the link* — so an established technical term keeps its katakana or English form (ビルド, CI, デプロイ, PR, `terms/` entries), a title's **meaning** is translated rather than its words, and a title that resists translation is paraphrased rather than transliterated. It ships in **one wording** across `rules/interaction.md`, the notify catalog and the four routine-fired command ceilings, pinned byte-identical by `test-workflow-scripts.mjs`; what a run actually emits is checkable by nothing, and the rule says so. GitHub artifacts (issues, pull requests, commit messages) and `.workaholic/` artifacts are **English** — the durable, shared record. Code, code comments and this repository's `docs/` tree are untouched by the rule.

## Project Structure

```
.claude/                 # Local Claude Code configuration (rules/, git-identities, settings.json)
.claude-plugin/          # Marketplace configuration (marketplace.json)
plugins/
  workaholic/            # The single plugin (no dependencies)
    .claude-plugin/      # Plugin configuration
    .codex-plugin/       # Hand-maintained Codex-facing manifest
    skills/              # Workflow skills (branching, catch, check-deps, commit, create-ticket,
                         # discover, drive, explain, feedback, gather, mission, notify, moderate,
                         # okf, propose, release-scan, review-sections, ship, specificate, standup,
                         # story, strategy, system-safety, validate-writer-output, workaholify,
                         # write-release-note) + policy skills (planning, design, implementation,
                         # operation, safety, development; English hard copies under policies/)
    commands/            # Claude-only thin aliases (ticket, drive, implement, commit, specificate,
                         # propose, fb, story (report = deprecated alias), ship, prepare-release,
                         # standup, mission, moderate, mission-close, catch, explain, workaholify,
                         # infinite-development, setup-dev-routines, setup-repo-routines)
    hooks/               # Validation + guard hooks (see "Hooks") and generated policy-index.md
    rules/               # diagrams, general, interaction, shell, typescript, workaholic
scripts/
  claude.sh              # Launcher
  build-plugins/         # Generates outputs/ (argument-less run = full build)
  test-workflow-scripts.mjs  # Hermetic smoke tests
  e2e/loop-drill.sh      # Drill the loop's own mechanisms; `verify-all` runs the classified
                         # set (docs/loop-drill-runbook.md §9); CI runs its hermetic part on push
outputs/                 # GENERATED, committed cross-agent artifacts — never hand-edit (CI-guarded)
  workflows/             # Self-contained portable workflows plugin (+ .codex-plugin/plugin.json)
  okf/                   # OKF v0.1 bundle of the four pillars' policies
.agents/plugins/marketplace.json  # Codex plugin list (workflows -> ./outputs/workflows)
.github/workflows/       # release.yml, release-note-draft.yml, claim-retirement.yml, loop-drills.yml, …
docs/                    # Documentation; VitePress site (.vitepress/ + articles; dependencies/ = dependency-decision logs)
                         # wrangler.jsonc = the assets-only Cloudflare Worker serving the built site
```

**The documentation site deploys on merge** (2026-08-26): `docs/` is a VitePress site served at <https://workaholic.qmu.co.jp> by the `workaholic-docs` Cloudflare Worker, an **assets-only** Worker whose `assets.directory` is the `docs/.vitepress/dist` that `npm run docs:build` writes (`docs/wrangler.jsonc`; `not_found_handling: "404-page"`, because VitePress emits real `.html` files and the SPA form would answer 200 for every dead deep link — a soft 404 no HTTP check catches). The `Docs Deploy` workflow (`.github/workflows/docs-deploy.yml`) builds and deploys on push to `main` filtered to `docs/**`, with `workflow_dispatch` for the first deploy and for a change that alters the rendered site without touching `docs/**` — the stated cost of the `paths:` filter. **Without the Cloudflare secrets it builds and skips the deploy, naming the skip in the job summary** rather than failing: the same shape `release-note-draft.yml` uses for its own optional credential, so a fork never sees a permanently red workflow and an absent credential is never read as a successful deploy. The site is a registered deployment target (`.workaholic/deployments/docs-site.md`, `deploy-on-merge`, `api-probe`), so `/prepare-release` reports it and `/ship` reads its confirmation; that record states it is **inactive until `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` exist and `workaholic.qmu.co.jp` is bound to the Worker as a custom domain**, so a target reported as waiting is reporting that precondition rather than an unexplained arrears. Neither target declares `paths:`, so both read `attribution: whole_range`: declaring it on one alone makes every component the other has not claimed report as an `unmatched_component` gap.

### Hooks (all shipped active in hooks.json)

- `validate-ticket.sh` (PostToolUse Write|Edit) — ticket floor on the `todo/` queue only: frontmatter, location, mandatory `## Policies`/`## Quality Gate`, resolvable `mission:` relation.
- `validate-mission.sh` (PostToolUse) — Experience/≥1-acceptance floor on any mission under `missions/active/`; archive never retro-blocked.
- `validate-story.sh` / `validate-trip.sh` / `validate-feedback.sh` / `validate-strategy.sh` (PostToolUse) — OKF `type:` and schema floors on new writes; git-tracked history is grandfathered. `validate-strategy.sh` requires a `YYYY-MM-DD` `target_date`, a **non-empty** `assignees` (the one artifact where empty is a refusal, not team-owned), non-empty `## Aim` / `## Schedule`, and — when present — a `stage:` from the closed set.
- `guard-ticket-structure.sh` (PreToolUse Bash) — blocks non-canonical ticket moves.
- `guard-git-commit.sh` / `guard-git-branch.sh` (PreToolUse Bash) — commit-subject and branch-name gates.
- `guard-askuserquestion-label.sh` (PreToolUse AskUserQuestion) — every question body must open with a `[<project label>]` prefix (`gather/scripts/project-label.sh`).
- `guard-working-directory.sh` (PreToolUse Bash) — denies a top-level cwd-moving `cd`. A `( cd … )` subshell, an absolute path, a `--prefix` form, and a **leading `cd` onto a repository root** (main checkout or worktree, resolved via `git rev-parse --show-toplevel`) all pass. A chained mid-command `cd`, a target carrying unresolvable shell syntax, and bare `cd` are denied. No env-var toggle.
- `guard-repo-confinement.sh` (PreToolUse Write|Edit) — blocks writes outside this repository and its worktrees (the agent's per-project memory store is exempt). Syntactic backstop only; the primary rule lives in `rules/general.md`.
- `policy-lens.sh` (UserPromptSubmit) — injects the engineering-policy index for commands carrying the `workaholic:policy-lens` sentinel; bodies are read on demand from `skills/<pillar>/policies/`. Regenerate `hooks/policy-index.md` with `build.mjs` after editing a pillar's `## Policies` list (CI fails on drift).
- `layout-doctor.sh [path]` — read-only audit of `.workaholic/` against the layout allowlist.

## Architecture Policy

### Component Nesting Rules

| Caller                     | Can invoke                                                                                     | Cannot invoke              |
| -------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------- |
| Command                    | Skill, `general-purpose` subagent                                                              | —                          |
| Skill                      | Skill; (when loaded by a command/main agent) may direct it to spawn `general-purpose` subagents | Command                    |
| `general-purpose` subagent | Skill (via preload)                                                                            | Command, Task (no nesting) |

- **No per-workflow agent files.** The repo ships no agent `.md` files at all. A command spawns `subagent_type: "general-purpose"` subagents whose prompts name the skill to preload, the section, the inputs, and the return schema.
- **One-level fan-out.** A subagent cannot nest `Task` and cannot call `AskUserQuestion`; all fan-out and all user interaction happen at the command/main-agent level. Leaves do non-interactive work and return JSON.
- **One plugin.** `workaholic` has `dependencies: []`. All skill references are same-plugin (`${CLAUDE_PLUGIN_ROOT}/skills/<name>/...`, `workaholic:<name>`). The `workflows` marketplace entry is the generated `outputs/workflows` bundle, not an authored plugin.

### The ticket spine

Everything converges on the **ticket** as the unit of work: *sources* fill `tickets/todo/`, one executor drains it to `tickets/archive/`.

- **Sources**, each named with **whose input it carries**: `/ticket` (a **human's** direction, with discovery), `/mission`'s Creation Interrogation (a **human's** direction, decomposed into a whole ordered ticket set), and `/specificate` (the feedback stream — **a human's ask, or a strategy a human authored**, never a record the loop wrote about itself). All publish through a **publish tree** onto a `work-*` branch behind a pull request; the artifact reaches `main` — and the executor's survey — when that PR merges. **What may originate a mission is stated once and cited, never restated** — `plugins/workaholic/rules/workaholic.md`, *What May Originate a Mission* (2026-09-02): a human's ask or a human-authored strategy may; a record a routine wrote about the loop's own apparatus (`self_authored`), a proposal refining a prior self-proposal (`self_refining`) and a tick into a window where only the loop has spoken (`only_the_loop_spoke`) may not.
- **Mission floor and ceiling**: two or more tickets or it is not a mission (`mission/scripts/check-floor.sh` at the publish seam; a ticketless direction is a feedback record, a one-ticket one is a plain ticket). Ceiling: `## Acceptance` normatively ≤3 items, whole `mission.md` ~60 lines / 2 KB (`mission/scripts/size.sh`, enforced on `/specificate`'s unattended drafts). Write-time floor: `## Experience` + ≥1 acceptance item.
- **One executor, two commands**: `/drive` (attended — asks exactly one thing: which units to take when several are offered) and `/implement` (unattended — no prompt at any step). Same Unified Run: freshen (`sync-main.sh`) → survey (`plan-units.sh`) → partition into PR-units → claim → drive → report → route by effective merge policy → account. `plan-units.sh` emits `backlog_all_excluded` (a derived reading over `backlog_size`/`backlog[]`/`excluded[]`, with a count per exclusion reason) so a queue emptied by claims and one emptied by `owned_by_other` never read alike, and **`placeholder_identity`** (2026-09-02) so a queue emptied by comparing every artifact against an address that stands for nobody never reads as an empty one — the survey's five `ok`-forbidding facts are `current: false`, `shallow: true`, `backlog_error`, `owner_unresolved` and that one. **And the offer has a derived, stated order** (2026-09-01): `missions[]` came back in walk order with nothing anywhere saying so, which with 30 tickets queued against three directions dated the same day was the difference between converging one direction and touching all three. The terms are **a mission unit before loose backlog** (the shape of the offer, plus `/drive` §3's own preference), then the **nearest `target_date` of the direction the mission serves** — resolved through `mission-strategy.sh`, so no relation, no field and no second walker — then the mission's own ticket order, which is untouched. Ordering changes **order, never eligibility**: which units are offered and every exclusion reason are byte-identical. Each row carries `direction`, `direction_target_date`, `days_to_target` and **`order_reason`**, and the four groups run `direction_date` → `direction_undated` → `unattributed` → **`direction_unreadable`** last, the walk order breaking every tie. A derivation that failed annotates **every** row `direction_unreadable` rather than falling back to an unannotated walk order — a silent non-reorder is the one outcome the reading forbids. Cost, stated: `mission-strategy.sh` measured 21s here, once per survey, and is a **local** read, so the survey stays offline by construction. Full contract: `skills/drive/SKILL.md` and its `reference/`.
- **Merge policy** (`merge_policy: auto | review`, recorded at creation on missions and tickets; **absent means review**): `auto` ships through `/ship` — which drafts the plan and merges, never deploys — then tears the claim down; `review` merges its PR immediately after `/story` opens it. **No gate is ever overridden**: a `secret` finding hard-stops; a `size`/`leak` block or a missing confirmation method demotes an `auto` unit to the PR path.
- **The `review` route reads the severity tier, never the binary verdict.** `gate-decision.sh` computes `override_only` beside `overridable` — the one derivation of the tier policy — and the route merges on `pass` **or** `override_only`, reporting every finding in the run report and the PR body. `secret` hard-stops; `leak` holds the PR open.
- **An Open Decision is a question to answer, not a ruling that the question is unanswerable.** Writing floor (`create-ticket/reference/ticket-format.md`): three required parts — the question, **the sources consulted and what they said**, and the fork's sides; it may name whose ruling would settle it and may **not** declare the question unanswerable or tell a later session not to decide. Driving floor (`drive/reference/ticket-workflow.md` §1, `reference/failure-contract.md`): before honouring one as a blocker a run **reads the sources the item names**, plus the whole of any page it cites, and a `blocked` outcome **names those sources and what they said**. A block that cannot name a source it read is not a block. Prose contract, not a script gate. `/ticket` never writes one — it asks a human instead.
- **The verification axis is read before merge policy**: a mission or ticket may declare `verification_handoff: <what cannot run here>` **at creation** — a credential, device or third-party account an unattended run does not have. `drive/scripts/verification-handoff.sh` is the one reader, and since 2026-09-01 (issue #793) it also **derives** one the ticket never had to declare: a `## Key Files` entry naming `.claude/` is a handoff because Claude Code classifies that path as sensitive and an unattended run has no one to answer the prompt. **Measured**: seven `[Implement]` runs over five hours all read `requires_action`, each ending at the same permission prompt, while nothing anywhere said so — the resume beats the heartbeat before the edit, so `stalled-units` counted the unit healthy. A **declared** value always wins, the test is syntactic and narrow (prose elsewhere in the ticket is not read), and it is derived rather than declared because the ticket that froze the loop was already in `todo/`. Any member declaring it — or deriving one — carries the whole unit, which takes the **`handoff`** route whatever `merge_policy` says: the PR opens and stays open, the non-droppable `## Handoff` quotes the reason, the claim stays standing, the finish line is `🟡 Handoff` and the token is `pending`. Two writers only — `/ticket`, and `/specificate` (`scaffold-proposed-ticket.sh --verification-handoff`) when the ask states it or an operator-only fork survives the operator-record check; the reader's derived case is not a writer and stamps nothing. A run never declares it for its own unit. Such a fork must never be written as a `## Quality Gate` item, which `/implement` can neither ask about nor override. The claim behind such a unit reads `awaiting_verification` and is not re-offered.
- Retired predecessors (`/monitor`, `/trip`, `/carry`) and their landed ideas: `docs/loop-engineering-workflow.md` I1/I3/I5. `.workaholic/trips/` is read-only legacy history.

### `.workaholic/` runtime conventions

`README.md` and `index.md` are the only files allowed at the `.workaholic/` root.

- **OKF floor**: every knowledge artifact carries a non-empty `type:` (`Story`, `Mission`, `Feedback`, `Strategy`, `Deployment`, `Term`, `Release Note`, `Release`); `okf/scripts/refresh-index.sh` regenerates the bundle indexes before each knowledge commit. **Two exceptions**: `tickets/` (internals, never index-managed) and `moderations/` (an operational log — and since 2026-09-01 not on this branch at all).
- **`moderations/`** — the `/moderate` tick log: one file per UTC day, one `## <tick-id>` section per tick, one line per step, no frontmatter, no `index.md` (the root index links the directory bare). An operational log, not knowledge. `moderate/scripts/log-append.sh` is the only writer (append-only, idempotent per `(tick, step)`, **never prunes** — deleting a day file is the operator's act); `log-read.sh` is the reader a step consults before re-filing. **The log is committed nowhere at all** (2026-09-03, the developer's instruction, in those words: *this strategy is never to be taken again*). `.workaholic/moderations/` is **git-ignored** and stays in the checkout that wrote it, which is the checkout the next tick reads. **The orphan `workaholic-log` branch is retired and must not be reintroduced**, and neither may a notes ref, a remote store or any other place a commit could land: `log-ref.sh`, `ensure-log-ref.sh`, `hydrate-log.sh` and `migrate-moderations-off-main.sh` are **deleted**, and `run.sh` no longer makes an opening persist. It existed from 2026-09-01 to 2026-09-03 as a repair for the log's ~50 commits a day on `main`, and it **moved the mess rather than removing it**: 126 commits nobody read, three per tick, a fetch at the head of every tick, and a seed commit pushed by a script that would push to whatever remote it was pointed at — which is how the *hermetic* test suite pushed to the real origin on 2026-09-03. Its premise — *the tick's container is discarded* — stopped being true on 2026-09-02, when the loop moved onto the developer's own server as a session whose working directory persists. **The cost is stated**: a tick running somewhere its state genuinely cannot survive loses its log and its dedups re-fire; the repair is to stop running it there, never to give the log a branch again. **The records take the other road, and that is now `persist-log.sh`'s whole job**: `--record <path>` carries the tick's feedback records to **`main`** in their own commit — a feedback record is knowledge — named one by one, never a sweep of the index; each reported `carried` / `already_on_base` / `missing` / `unreadable` / `unlanded`. A `<step>-filed` line does not prove a filing: `filed-records.sh` asks the tree, an `unlanded` record counts as not filed, and `readable: false` is named rather than rendered as an empty set. **And since 2026-09-03 this repository keeps no log branch at all** (the developer's instruction): every word above rests on the container being discarded, and since 2026-09-02 the loop turns **locally** in a session whose checkout persists between ticks, where `.workaholic/moderations/` is git-ignored and simply stays on disk — so the branch was carrying a copy of a file that never went anywhere, and `workaholic-log` had grown 126 commits no local tick ever read. `WORKAHOLIC_LOG_PERSIST=0` — declared in `.claude/settings.json`'s `env` block, the home `WORKAHOLIC_WIP_LIMIT` and `WORKAHOLIC_CADENCES` already use — turns `persist-log.sh`'s branch half and the whole of `hydrate-log.sh` off **together**, so the writer and the reader cannot disagree about whether a branch exists; each reports `skipped` / `log_persist_disabled`, which is the honest word (the read did not fail, it was not owed). **Absent means persist**, so a repository on the Web-routine fallback is byte-identical to one before this existed, and the stated cost of declaring it there is that the tick loses its memory across containers and re-fires its questions. **`--record` is untouched**: a feedback record is knowledge and still reaches `main` whatever this says. `log-ref.sh` and the branch name do not move — the branch is still where the log goes for anyone who needs one.
- **Mission rolling**: commit seams (archive, ship, report) append `## Changelog` lines and tick `## Acceptance` items via the mission skill's idempotent mutators (`append-changelog.sh` / `tick-acceptance.sh`). Progress (`checked ÷ total`) is computed, never stored. Acceptance items become tickable through their `(#<filename>)` link, stamped only by the emitting seam via `link-acceptance.sh`. The `mission:` relation is many-valued; read it only through `read-relation.sh`.
- **One slug can name two missions, and the audit reports it** (2026-09-01, ticket `20260901070000`): `/specificate` dedups on the ask's **feedback refs**, so two records written for one ask produce two missions whose slugs — derived from the title — collide. `create.sh` already refuses a slug that resolves in either area, and that check alone **could not** catch this: it resolves against the checkout it runs in, so a mission living only on an unmerged publication was invisible, and the collision then arrived by **merge**, where no create runs at all. **Since 2026-09-01 (ticket `20260901072600`) it reads the unmerged branches too** — `/specificate`'s own `lib/unmerged-branches.sh` over `.workaholic/missions/*/<slug>/mission.md`, git-native and no API call — refusing **`exists_on_branch`** and naming the ref, with nothing written. **A walk that could not complete reports on stderr and creates anyway** (shallow, no base ref, not a repository): that walk over-reads on every ambiguity by design, which is right for a dedup and wrong for a gate, and stdout stays exactly the line every caller parses. `close.sh`'s `archive_slug_conflict` answer is untouched — it was never the defect. Measured: both pairs in this repository (`deliver-what-the-loop-already-knows-…`, `say-when-the-loop-has-run-out-of-direction`), in each case the later record's mission driven and archived while the earlier sat in a refused publication and landed days later. `layout-doctor.sh` reports each pair as an **advisory** — never a finding, which would fail the merge gate over history that harms nothing until something tries to move the active copy into an occupied archive. It **deletes nothing and chooses nothing**: both copies are history (one carries the implementation changelog and run hours, the other the verification) and which survives is the operator's ruling. Refusing at the close or the move was rejected — `archive.sh` runs unattended with no person attached.
- **Mission lifecycle**: `status: active | achieved | abandoned | carried` — one in-flight state; `active/` vs `archive/` areas; `mission/scripts/close.sh` is the **only** writer of an end state. Merging the mission's PR is the approval; drivability is derived (active area + plan + queued tickets), never a status word. `archive.sh` closes the one outcome that is arithmetic — `close.sh <slug> achieved` when archiving a ticket leaves acceptance fully checked (`progress.sh`: `checked == total`, `unlinked == 0`) and the queue empty (`queue-size.sh`) — **never `abandoned` or `carried`**, which assert intent. An unreadable reader is not a proof and leaves the mission alone; the close and any refusal are named in the archive output and the run report. A mission finished by a path that archives no ticket is caught by `/moderate`'s `closable-missions` step, which re-proves each candidate in a publish tree and lands the closes (only `achieved`; a rejected re-proof is reported, not closed). That step never reaches `plan-units.sh`, which stages what its living migrations converge.

#### The strategy layer

`.workaholic/strategies/<slug>.md` is the operator's **outbound, resolved direction**: an **Aim**, a **Schedule** (`target_date`), an **Assignee** (non-empty `assignees`) and a **Stage** (`stage:` from the closed set `進行中 | 改良中 | 観察中`, the operator's vocabulary kept verbatim; **absent means 進行中**, resolved in `read.sh` alone). Flat, one file, operator-authored through `workaholic:strategy`; `/drive` never surveys one. The stage is **declared, never derived** — every other reading here is evidence the loop computed, and no reading may compute, move or infer the stage; a reading may suggest a transition and the operator's announcement performs it. Stuckness (a handoff, a block, a stale claim, a queue that will not drain) is orthogonal to the stage and never read as one.

**Three writers and no fourth** (pinned by the suite): `create.sh` creates (`--stage`, refusing `bad_stage` with nothing written), `amend.sh` revises a live direction's `## Aim`, Schedule, Assignee and Stage, `close.sh` ends. `amend.sh` is bounded to those four parts, asserts `slug`/`type`/`status`/`created_at`/`author`/`feedback:` immutable, refuses a closed direction `not_active`, carries the write-time floor itself, writes **nothing** on any refusal, and appends one dated `## Schedule` line naming what moved (a stage move names both ends, e.g. `進行中 → 改良中`, reading the previous value through `read.sh`). `close.sh` is the only writer of an end state; re-opening is offered nowhere. `carry-attribution.sh` is **not** a fourth writer — it writes one `feedback:` line on a **mission**. A run never amends or moves a stage on its own reading; `/specificate` carries the operator's words verbatim.

**Readers** (each composes the ones below it; no second walker, no relation of its own, no field on any artifact):

| Script | Answers |
| ------ | ------- |
| `read.sh` / `list.sh` | the artifact, with the absent-stage default resolved once (`stage_declared` says which answer a caller has) |
| `attributed-work.sh` | which work belongs to a strategy — `strategy.feedback[] ∩ artifact.feedback[]` (`direct`) plus the hop through a mission attributed that way (`via_mission:<slug>`); reports ticket **and** mission grains (`waiting_missions*`, `waiting_kind`/`waiting_describing`/`waiting_advancing` via `work-kind.sh`) |
| `mission-strategy.sh` | the inverse — which strategy a mission belongs to |
| `unattributed-work.sh` | what no direction claims (active missions by slug/path with queued-ticket counts, loose queued tickets) |
| `survey-strategies.sh` | one row per strategy: `refusal`, `pace`, `overdue`, `expiring`, `dormant`, `quiescent`, `landed`, `stage`, `residue`, `target_date`, `days_to_target` |
| `direction-state.sh` | the lifecycle: `live \| arrived \| overdue \| expiring \| dormant \| unreadable`, plus the repository-level `none`; `--with-leaving` attaches the leaving per row by handing that row back through `--state-row` |
| `closing-residue.sh` | what a direction is leaving — the waiting grains, the residue, and its last lifecycle reading, each block carrying its own `readable` and reason |
| `landing-arithmetic.sh` | what remains against how long is left — `clears` / `does_not_clear` / `no_target_date` / `unreadable`, over `standup/scripts/digest.sh`'s own board reading |
| `default-target-date.sh` | the operator's one-week default (the constant **7** lives here and nowhere else) |

The lifecycle precedence is fixed: **`unreadable` > `arrived` > `overdue` > `expiring` > `dormant` > `live`**. A sixth value is refused — `stage` rides **beside** the answer and never enters the precedence. Definitions: `overdue` = `days_to_target < 0` (a `null` date is never overdue); `expiring` = `0 <= days_to_target <= window_days`; `dormant` = legible, `active`, `mine`, not overdue, cited, nothing landed in the window, nothing waiting at either grain, no open proposal; `quiescent` = `dormant`'s complement on one term (`landed` non-empty) with **no date term at all**, and **`false` when the residue read was degraded**; `pace` (`late | on_course | unknown`) changes order, never eligibility.

**Degradation is stated, never derived into a verdict.** A walk that could not complete answers `readable: false` with a named reason (`corpus_unreadable`, `patterns_unreadable`, `attribution_unreadable`, `strategy_unreadable`, `waiting_unreadable:<reason>`) and **null** counts — never `no_citing_artifacts`, which means the opposite. **`readable` is absent on a completed walk** (the `merge_policy`/`status:` convention: absent means it completed), so the test is `readable == false`, never `readable // true`. Named empties stay honest: `no_feedback_refs`, `no_citing_artifacts` (*nothing has answered this direction yet* — guaranteed by `/specificate`'s carry floor), `no_activity_in_window`. Attribution is **transitive, lossy and `exhaustive: false`**: work a run never emitted, an ask naming no refs, an unresolved ref and any hand-written artifact stay uncited for ordinary reasons.

**Succession**: a *created* announcement may name a **predecessor slug**; that predecessor's own `feedback:` refs are read through `read.sh`, composed through `feedback/scripts/ask-feedback-line.sh --refs-only` and handed to `create.sh` as the argument it already takes — `create.sh` learns nothing about succession. Refusals: `strategy_not_found`, `predecessor_active`, `no_predecessor`.

**A strategy-touching publication never auto-merges** — the seam's rule, not the caller's: `publish-tree-pr.sh` derives it from the tree and reports `merge_reason: strategy_touching`, whatever `WORKAHOLIC_AUTO_MERGE` says. The operator's merge is the authorship; closing is the refusal.

The retired `strategy:` mission relation and its ownership hop stay retired; `mission/scripts/migrate-strategies.sh` and the `missions_migrate_strategies` seam are retired — **do not re-add them**. Full record: `skills/mission/SKILL.md`, *The strategy layer: retired, then redefined*. **Which direction a mission serves is a render, not a field** (2026-09-01): `mission-strategy.sh` answers it and three mission-facing surfaces state it — the bare `/mission` roadmap, the **Mission Position Report** (`/mission-close` and `/drive`), and `/standup`'s per-strategy digest — each rendering an explicit *no strategy* where nothing could be attributed and naming an unreadable walk as unreadable. Why the field is not the answer is recorded in `mission/reference/schema.md`, beside the frontmatter block where the question recurs.

#### The planning job

**Planning is a job, and it is not a command or a routine** (2026-09-01, mission
`adjust-the-plan-hourly-not-only-report-it`). The ask that produced it was accurate about the
documents: the loop had three clerks — intake adds, execution drives, bookkeeping reports — and
nothing named planning, so no reader could tell whether *nothing re-plans* was a defect or the
design. It is now five acts, each belonging to a tick that already runs; **no new command, no new
routine and no new artifact**.

| Act | Where it lives | What it does |
| --- | -------------- | ------------ |
| **Hold new divergence** | `/propose`'s `wip_limit` rung (`survey-strategies.sh`) | refuses origination while the repository already carries `WORKAHOLIC_WIP_LIMIT` active missions with queued work; **absent means no limit** |
| **Order the offer** | `plan-units.sh` (`order_reason`) | offers missions by the nearest `target_date` of the direction each serves; changes **order, never eligibility** |
| **Do the arithmetic** | `strategy/scripts/landing-arithmetic.sh` | per direction, what remains against how long is left, at the direction's own measured rate |
| **Escalate a date** | `/moderate`'s `date-will-not-hold` step | asks the assignee, **before** the date, when the board will not clear; `overdue`/`expiring` keep their own cases |
| **Say what moved** | `/moderate`'s `📋` clause (`strategy-pace`'s `plan` block) | one line on the hourly root: how many directions advance, how many are held, and whether the repository's own limit is holding new work |

**Its authority is the repository's existing rule and is cited, never restated**
(`drive/reference/claims.md`, *Proofs and judgements* and *When a bounded act may read a
judgement*): planning may **act** on a proof it re-derives at the moment of the act, idempotently,
refusing every bound by its own word; everything else it may only **report** or **ask about**. Of
the five acts, exactly one refuses work (`wip_limit`, on a declared number) and one reorders an
offer; the other three read and say.

**What planning may not do, each with the rule that forbids it**: it may not **re-date a
direction** (`amend.sh` carries only a revision the operator announced by explicit slug, and a
loop that moves its own deadlines when it misses them is a loop whose dates mean nothing); may not
**merge two missions** (no writer exists and it asserts intent); may not **retire a ticket it
judges mooted** (that is a reading about behaviour, not a file test — a shape refused here
before); and may not **close a mission that is not arithmetic** (`close.sh` is the only writer of
an end state, and `archive.sh` closes only `achieved`). A mission at full acceptance with tickets
still queued is therefore **named and asked about** (`mission-leftovers:<slug>`), never closed.

**So the honest answer to the ask is that the loop plans less than it wanted, and the gaps are
named rather than implied.** What is now the loop's: holding divergence against a number the
operator declared, ordering its own offer, doing the arithmetic, and saying what moved. What
remains the operator's: every date, every end state, and every judgement about whether queued work
still matters. **The ask's fourth item — that a commit is a change to the development target — was
already largely answered** by two changes that shipped for exactly that reason: the tick log came
off `main` (2026-09-01, and off git entirely on 2026-09-03 — the orphan branch it moved to was
itself the mess, and is retired) and every pull request the loop merges is squash-merged
(2026-09-01). What remains there is not re-proposed here.

#### Tickets, ownership, identity

- **Ticket state is a field, the archive is a place**: `tickets/` is **two-state** — `todo/` and `archive/<branch>/`. State is `status:` frontmatter: **absent means queued**, `done` (stamped by `archive.sh`), `abandoned`, `icebox` (deferred and promotable; `promote-icebox.sh` **clears** the field). A ticket never driven lands in the synthetic `archive/unbranched/`. The retired `icebox/` and `abandoned/` directories are still read by every reader while `gather/scripts/migrate-ticket-states.sh` converges them at the write seams; `layout-doctor.sh` reports a survivor as `retired-ticket-state`.
- **Ownership**: plural `assignees` on every artifact; **empty means team-owned/claimable**. One oracle — `gather/scripts/owners.sh`, with `owns.sh` returning `mine`/`unowned`/`other`/`unresolved` (compared by slug, both sides canonicalised).
- **Identity**: `.claude/git-identities` maps `<login>=<canonical>[,<alias>...]` (first field canonical, the rest the same person's other addresses) and `gather/scripts/identity.sh` is its **one reader**, resolving a login *or* an address. It **never guesses**: an absent file, entry or unparseable line answers `resolved: false` and echoes the input back (the identity function), so a caller that resolves unconditionally behaves exactly as before it existed. A line with no comma is what the format always was. The only sanctioned second parser is the web bootstrap's step 0b (`cut -d, -f1`), which runs before the plugin is installed. Already-written artifacts are recovered by `gather/scripts/migrate-assignee-aliases.sh` (a living migration at `/workaholify`'s converge seam: idempotent, **stages and never commits**, leaves canonical entries byte-identical, and touches nothing it cannot resolve, naming every such address). Claim authorship and resumption key on `git config user.email` separately; `WORKAHOLIC_CLAIM_IDENTITY` overrides the scan identity for an executor with none configured (see the claim protocol).
- **Ticket-only tolerance tier**: a queued ticket still at `tickets/todo/<user-slug>/<file>.md` with neither field resolves to `<user-slug>` — the directory that *was* the ownership record. Only that path shape, only when both field tiers are silent, only tickets; a ticket directly in `todo/` answers `unowned`, and `author:` stays out of ownership. Written to be deleted with the layout.
- **Feedback subject**: every new record names **whose opinion it is** — `subject: <kind>[:<identity>]`, kind from `person | meeting | observer_ai | customer | team | other`. Three axes: **subject** = who formed the opinion, **source** = the channel it arrived through, **author** = the git identity that ran the capture. `create.sh` takes `--subject` and **refuses** (`no_subject`) rather than default it. `/specificate` supplies the triggering issue's author; `ship`'s extractor supplies `observer_ai:<author>`; `validate-feedback.sh` floors new writes and grandfathers older ones.
- **Deferred concerns** live in the feedback stream: the story's Concerns section is the durable record (PR body drops `low` blocks at render); `ship`'s `extract-deferred-concerns.sh` persists each as a `kind: concern` record keyed on `concern_id`; the open set is computed — a concern is open until a later record names it in `supersedes`.
- **The red-alert cool-down** expires at the **earlier** of 24 hours after the first report and the start of the next working day (the first hour inside `WORKAHOLIC_WORK_DAYS` at the end of `WORKAHOLIC_QUIET_HOURS`, in `WORKAHOLIC_QUIET_TZ` — the check-in gate's own terms, composed rather than re-derived). A root re-posted at expiry names **how long** it has been failing. The `↳ still failing` reply, the first-report rule and the unreadable-history rule are unchanged (`workaholic:notify`).

#### Areas

- **The two hand-maintained areas** (`deployments/`, `terms/`) each carry a definition (`rules/workaholic.md`) and one upkeep seam: `story/scripts/area-freshness.sh`, read by `/story` beside `doc-drift.sh`. It **reports, it never writes** — two mechanical facts per record: `retired_terms` (it still names a de-listed area or retired namespace) and `stale_days` (thresholded by nobody). `/ship` is the only live *reader* of a deployment record and gates on its `## Confirmation`. Records carry `type: Deployment` / `type: Term`; `terms/inconsistencies.md` is an ordinary term entry.
- **Retired areas**: `.workaholic/policies/`, `guides/` and `specs/` are gone — content deleted, not relocated; reference documentation lives in the repository's own `docs/` tree, and the `"docs" → specs/` request mapping points there. `layout-doctor.sh` classifies them `retired-area`; the content decision stays the owner's.
- **A rename is a row, not a sweep**: `skills/gather/scripts/renames.tsv` declares every rename, read only through `list-renames.sh`. Two kinds. An **`area`** row (a `.workaholic/` top-level directory) is **applied** — `migrate-renamed-areas.sh` `git mv`s it and fixes the generated root index, and `layout-doctor.sh` classifies a survivor `renamed-area` **before** consulting the allowlist. A **`name`** row (a command name, skill namespace, path fragment) is **proposed, never applied** — `rename-conversions.sh` counts survivors outside `.workaholic/` and prints the bulk conversion for the operator to run or decline. A retirement is not a rename and stays out of the table. A row is deleted once the fleet has cut over; an empty `rename_conversions` is the signal that it has. The registry never uses a live token as its own example. `WORKAHOLIC_RENAMES_TABLE` points the chain at another table — a data source, never a gate opt-out.
- **Closed layout**: the permitted top-level `.workaholic/` directories are fixed in **two lockstep sources** — `hooks/workaholic-layout-allowlist.txt` and the table in `plugins/workaholic/rules/workaholic.md`. A new artifact directory must be registered in both **in the same commit** that first writes to it; `layout-doctor.sh` audits and the `Validate Plugins` CI workflow fails the merge on `conforming: false`.

### Claim protocol

The repository is the coordination medium. The model is stated once in `skills/drive/SKILL.md` (**Claims**) with full detail in `skills/drive/reference/claims.md`.

- A runner claims a PR-unit (mission slug or `batch-<ts>`) as a `Claim <unit-id>` commit on a pushed `work-*` branch; **unmerged remote branches are the only claim oracle** (`list-claims.sh`). One unit ↔ one branch ↔ one worktree (`.worktrees/<unit-id>/`) ↔ one PR.
- A merge releases a claim by definition. `release-claim.sh` explicitly discards an unfinished unit (never a recovery path) and sets `CLAIMS_FETCH_OK` after its own fetch, as `claim.sh` does. `claim.sh resume` takes over **your own** claim whose heartbeat lapsed; a colleague's is untouchable at any age. Staleness is reported, never acted on.
- The heartbeat is the branch tip (`heartbeat.sh`, an empty commit built against a scratch index). `archive.sh` pushes the claim branch after each archive commit. **The beat is step 0 of every ticket, not a cadence** (2026-08-31, ticket `20260831150500`): `archive.sh` refreshes the tip for free but only at the **end**, so a unit that is one long ticket runs its whole implementation on the claim commit's own timestamp and loses its own claim to the 30-minute `WORKAHOLIC_CLAIM_HEARTBEAT_STALE_MINUTES` resume window by construction — not the 24-hour `WORKAHOLIC_CLAIM_STALE_HOURS`, which is the *reported* staleness and governs no takeover. Measured on `batch-20260831141002`: resumed by a second tick at 33 minutes, and a complete, validated, locally-committed implementation was discarded rather than force-pushed over a branch another run was actively driving. A **beat inside a per-ticket seam** was refused (the only one that runs early is a pure reader shared with other callers) and so was **making the loss visible instead** (a reading after the fact returns none of the work); a background timer is refused outright, because it would be a second liveness authority beside the branch tip. No store, no cursor, no field, and the suite pins both the behaviour and that the workflow still names the step.
- Worktrees are **claim-born and ship-torn** (`cleanup-mission-worktree.sh` is the sanctioned cleaner; `survey-worktrees.sh` / `reap-worktrees.sh` back the teardowns up). `.worktrees/` and `.publish/` sit inside the repo root — add them to `.dockerignore` and any archiver's ignore list.
- `branching/scripts/ensure-worktree.sh` **refuses** a branch name that already exists on origin rather than minting a local branch at `HEAD` that shadows it; attaching to a published branch is `create-mission-worktree.sh --branch`'s job.

#### Verdicts

| Verdict | Meaning | Resumable | Survey exclusion |
| ------- | ------- | --------- | ---------------- |
| `claim_active` | live claim, heartbeat fresh | no | `claimed_*` |
| `stale` | tip older than `WORKAHOLIC_CLAIM_STALE_HOURS` (default 24) — *look at this*, not *take it* | own claim only | — |
| `superseded` | the unit's tickets reached the base **and the branch is empty against it** | no | `claimed_superseded` (work **resurveyed**) |
| `stranded` | the tickets reached the base and the branch **still holds work found on no other ref** | no | `claimed_stranded` |
| `queue_drained` | drained, story at the tip, PR open — waiting on a person | no | `claimed_reported` |
| `report_incomplete` | drained, **no story** — a dead run's remains | **yes** (re-enters the Unified Run at §5) | `claimed_resumable` |
| `report_undelivered` | the run recorded the transport's refusal into its own branch story | no — the next act is a **merge retry** | `claimed_undelivered` |
| `awaiting_verification` | remaining queued work declares `verification_handoff:` | no | `claimed_awaiting_verification` |
| `parked_with_pr` | reported with follow-up work on the branch | yes | — |
| `heartbeat_lapsed` / `foreign_identity` / `identity_unresolved` / `shallow_history` / `ambiguous_claim` | as named | no | — |

**Proofs and judgements** (`skills/drive/reference/claims.md` — the one home, keyed on the word `lib/claims.sh` emits, with no field on any artifact and no classifier function): a consumer may **act** on a proof and may only **report** or **ask about** a judgement. **`superseded` now proves the branch is empty, not merely that the tickets landed** (2026-09-01, issue #788). `retire-claim.sh` deletes a branch on that strength alone — *what makes a destructive act safe here is the proof, and nothing else* — and offers as recovery that *its content is on the base, that is what `superseded` means*. It did not mean that: the step from *the tickets are archived* to *the branch holds no work* holds only when a branch carries nothing but its own unit's tickets. **Measured**: two branches whose tickets had landed through *different* branches still held ~300 lines of code and a documentation section present on no other ref, and the tick was asking for both to be deleted; only a 403 on `push --delete` had prevented the loss, for five days, while the tick reported that refusal as the problem. `claims_branch_empty_against_base` (one `merge-base`, one `diff --quiet`, no network) now gates the verdict, an unanswerable emptiness answers **`stranded`** rather than `superseded`, and a `stranded` unit is asked about by `/moderate`'s `retire-claims` step and reaches no retirement, no `resurveyed[]` and no delete.

**`superseded` and `report_undelivered` are the only proofs** — readings the tree or a merged pull request established, which cannot become false by looking again. Everything else is a judgement, the absence-of-a-reading words (`shallow_history`, `ambiguous_claim`, `unanswerable`) most of all. Seven further vocabularies live there as keyed sub-tables, **one of them holding three proofs and the rest none**: what made a branch a retirement candidate (`candidate_reason` — `superseded_only`, `pull_request_merged`, `pull_request_closed_unmerged`, each a proof and each re-derived at the moment of the act), the base's own checks, whether an operator-facing pull request was acted on, how long a condition has been standing, a branch's mergeability, whether an act the loop took had its effect (`drive/scripts/act-effect.sh`, the one reader for both the retirement and the delivery act, owning the assembly and no act's vocabulary), and **what became of a person's own answer** (`moderate/scripts/answer-outcome.sh` → `settled:nothing_filed` / `settled:issue_closed` / `pending` / `unreadable:<reason>`, composing `question-state.sh`, `log-read.sh` and the one transport at one bounded read per **filed** candidate and none for the rest; an issue can be closed and reopened between two reads, so nothing may merge, close, gate, hold work or re-ask on it, and a question with **no recorded answer** is refused `not_answered:<state>` rather than called `unreadable`). An eighth, differently-keyed table records **which question reads which age** (nothing derives an age twice). Each names its consumers, and `scripts/test-workflow-scripts.mjs` fails when a script emits an unclassified word, a table classifies a word nothing emits, a row is called a proof, or an enumerated consumer reaches an acting call site.

**When a bounded act may read a judgement** (`claims.md`): only when it **re-derives that judgement at the moment of the act**, is **idempotent**, is **reversible**, and **refuses every bound by its own word**. Consumers are enumerated (`catch-up-claim.sh` is the only one). The exception reaches no judgement that is the absence of a reading and none whose next step is a person's judgement. Two gating shapes, one rule: a **licensing** act refuses on an absence, a **gating** act proceeds on one.

**Resolution**: a unit held by two branches resolves to the **live** row, derived once in `lib/claims.sh` (`claims_unit_resolution` / `claims_unit_row` / `claims_unit_live_branches`) and read by every writer; `superseded_only` returns the superseded row for callers that must keep refusing under it. **Two live claims are reported, never picked between** (`ambiguous_claim`, naming both). A `superseded` row beside a live one governs nothing while staying reported; `claim.sh` skips it so a fresh claim over proved-empty work goes through, and `plan-units.sh` names the freed work in `resurveyed: [{kind, id, claim}]`, never in `excluded[]`.

**`superseded` at both grains**: a **batch** claim is answered from the tree, network-free (every one of the unit's tickets archived on the base, matched by filename under any branch directory); a **mission** claim from the claim's own tip (`claims_mission_landed` over `claims_tickets_for_mission`, `every` and never `any`), with `drive/scripts/claim-merged.sh` — the protocol's **one network read** — as the fallback. That lookup is **three-valued**: `unanswerable` leaves the row the verdict it would have had without it and is named in `merged_lookup_unanswered[]`; it is skipped by name when it cannot succeed (`offline`, `disabled` under `WORKAHOLIC_CLAIM_MERGED_LOOKUP=0`).

**A claim race is settled at the remote, and where it cannot be, the damage is bounded later.** `claim.sh resume` always contended on one ref (the loser `resume_race_lost`); a **fresh** claim minted its own `work-*` name, so two runners that surveyed before either pushed both succeeded — measured 2026-08-30, two branches drove the same four tickets for over an hour. **Since 2026-09-02** `claim.sh` **§3b** wins one ref per **claimed artifact** (`drive/scripts/claim-arbitrate.sh`, `refs/claims/artifact/<path>`, create-only lease, all-or-nothing) **before §4 creates anything**, so the loser refuses **`claim_race_lost`** holding no branch, worktree or commit. The ref is keyed on the **artifacts, not the unit id** — `batch-<timestamp>` is minted inside the claim act, so a unit-keyed ref would reach one grain only — and its value is **unique per claimant**, because git answers `Everything up-to-date` to a push of the value a ref already holds. **The lock's lifetime is the claim act, not the claim**: `claim.sh` releases it the moment the push lands (the oracle sees the claim from then on) and `abort_claim` releases it on every earlier failure, so a merged, released, retired or `superseded` claim needs no lock handling at all — measured, holding it for the life of the claim broke *a merged stamp is history, not a claim*, because a merge runs nothing in the container. The only other release is the arbiter's **reap**, for an act killed inside its own window — a lock **no live claim stands behind** *and* older than `WORKAHOLIC_CLAIM_ARBITER_STALE_MINUTES` (default 10), run lazily by `claim.sh` only when a take is lost, then the take retried once. `claims_scan` still reads `work-*` refs alone: the contended ref is the **arbiter, not a second oracle**, no verdict word was added and the proofs-and-judgements tables did not move. **Where the transport refuses the ref write it answers `unavailable` and the claim proceeds exactly as before** — the earlier 403 measurements were the **cloud routine's proxy**, re-measured 2026-09-02 over SSH where create, create-only lease, compare-and-swap and delete all succeed — so the bounded-later repair still carries every routine-fired container: `archive.sh` **re-derives the claim immediately before the ticket moves** through `drive/scripts/claim-holder.sh`, refusing `claim_taken_over` and `ambiguous_claim` with the tree byte-identical and **proceeding** on any absence. A live race reaches a person through `drive/scripts/list-raced-units.sh` and `/moderate`'s `raced-units` step (`raced-unit:<unit>`); three sibling steps filter and count it through `moderate/scripts/lib/raced-units.sh`. No consumer releases a claim, picks between branches, deletes a branch, closes a PR, reverts, merges, gates or holds work on it.

#### The three acts on a claim

Each acts on a proof, re-derives it at the moment of the act, is idempotent, and refuses every bound by its own word with nothing written and exit 0. Full contracts: `skills/drive/reference/claims.md`.

- **Retirement** — `drive/scripts/retire-claim.sh <unit>` acts on `superseded`: close the pull request, delete the remote branch, reap the worktree, in that order (the local reap last, so a refusal there leaves both remote facts correct). Success words (`already_closed` / `already_gone` / `absent`) and blocked words (`gh_unavailable`, `slug_unresolved`, `pull_request_close_failed`, **`branch_delete_failed`**, `worktree_reap_refused`) are each their own; a refusal reports `not_attempted` for all three; judgements are refused by their own verdict word. It merges nothing, pushes into no branch and touches no `.workaholic/` artifact. **Act 2 is refused in this container** (a session-type refusal both transports agree on), so it runs in **`.github/workflows/claim-retirement.yml`** — `permissions: contents: write` and nothing wider, `push: [main]` + `workflow_dispatch`, a full-history checkout with every remote head — which owns no proof logic: `list-retirable-claims.sh` supplies candidates and `delete-retired-claim-branch.sh` acts. **Four candidate classes, each carrying its own `candidate_reason` and each a proof** (2026-09-01; the fourth 2026-09-02): `superseded_only` (the oracle's own emptiness), `pull_request_merged` (the tree established it — a squash merge leaves no ancestry, so `--no-merged` lists the branch forever), `pull_request_closed_unmerged` (**a person's recorded decision**, never folded into the second: *the loop delivered this* and *a person discarded this* are different questions), and **`mission_not_active`** (the unit's mission has ended — `close.sh` moved it into `missions/archive/` — read through `drive/scripts/claim-mission-state.sh`, with `mission_status` on the row). The fourth is enumerated from the **oracle's rows** rather than the refs, because the per-ref arm skips every unit the oracle has a row for and a claim branch always has one; it is bounded by resolution `single`, never `claim_active`, and **a pull request that is still open is not offered at all** — the act's `pull_request_open` bound is not widened, because deleting an open pull request's head branch leaves it unmergeable forever (the headless shape measured on #813, #799, #688, #635, #625). A degraded scan yields no candidates **and its reason**, and an unreadable pull request yields no candidate and lands in `pull_request_unreadable[]` — never a bare omission, which reads like an open one. The act re-derives its class at the moment of the act and refuses by its own word: the shape and transport bounds (`not_a_work_branch` / `release_branch` / `not_on_base` / `pull_request_open` / `gh_unavailable` / `slug_unresolved` / `branch_delete_failed`) on all three, plus `not_superseded:<verdict>` (a live row always beats a merged pull request), `pull_request_unreadable:<reason>`, `not_merged:<state>`, `not_closed_unmerged:<state>`, `no_unit_for_mission_class`, `mission_unreadable:<reason>`, `mission_still_active:<state>`, and — on the third and fourth classes, where the emptiness is a **gate** rather than the evidence it is on the row — `branch_holds_work` and `emptiness_unanswerable`. `record-ci-retirement-turn.sh` records the candidate reading and each act's `state`/`reason` as `::notice::` check-run annotations (no permission needed; bounded by `WORKAHOLIC_CI_RECORD_MAX`, default 20, a truncated record saying so), and `read-ci-retirement-record.sh` answers per unit `taken` / `refused:<word>` / `pending` / `unavailable` / `unreadable`, matched on `head_sha`. CI-side scripts run under `lib/runner-identity.sh`, which may set `WORKAHOLIC_CLAIM_IDENTITY` — an **override, not a default** — only with no identity configured, only as an author the committed mapping names, taking that author's own row; the identity-first precedence in `lib/claims.sh` does not move.
- **Delivery retry** — `plan-units.sh` emits `undelivered: [{unit, branch, merge_outcome}]` (a **field**, not a loosened exclusion). `drive/scripts/retry-undelivered.sh <unit>` acts on `report_undelivered`: **one** `PUT .../merge` through the same REST seam that refused it, creating no branch, worktree or claim. Every other verdict is `not_undelivered:<verdict>`; a scan-held PR is refused `scan_held:<tier>` regardless. A still-refused unit has the **new** word recorded back onto its branch with no worktree, through `record-merge-outcome.sh` (still the one writer of that section). `--own-tip` relaxes exactly one term (`WORKAHOLIC_CLAIM_HEARTBEAT_STALE_MINUTES=0`) so a just-caught-up branch is not refused `claim_active`.
- **Catch-up** — `drive/scripts/claim-mergeability.sh` answers `clean | mechanical | content | unanswerable` per branch from `git merge-tree --write-tree` (no worktree, index, ref or extra network call), rendered on every claim row as `mergeability` / `mergeability_reason` / `mergeability_content_files`. **The merge is computed with the repository's own `.gitattributes` out of reach** (2026-09-01, ticket `20260901041500`) — run from an empty directory with `GIT_DIR` set, because git reads merge attributes from the **working tree** and **GitHub applies none of them when it computes `mergeable`**. Measured, same git and the same two commits: from the checkout `merge-tree` exits 0 while a clone with no `.gitattributes` exits 1 on `.workaholic/feedbacks/index.md` and the API answers `mergeable_state: "dirty"`; five open publications read `clean` here and `dirty` there, and the loop attempted the merge and was refused `merge_not_allowed` every hour. The union driver is **not** the defect and is not removed — the **writer** (`catchup-main.sh`, in a real checkout) still resolves such a path with no judgement, which is why `mechanical` is the honest class and why the existing catch-up settles these branches. `drive/scripts/catch-up-claim.sh <unit>` composes `ship/scripts/catchup-main.sh` (never a second merge engine), attaches the worktree through `create-mission-worktree.sh --branch`, regenerates with the repository's own tooling and runs its fast checks before pushing; `already_current` touches no ref. **And since 2026-09-02 it delivers what it made mergeable** (mission `resolve-a-conflicted-pull-request-in-the-tick-not-report-it`): resolving and stopping leaves the pull request open for a claim holder who never comes, so a **`queue_drained`** claim's pull request is merged after the push, through the same REST seam and the same `merge-method.sh` derivation every other caller reads. The delivery rides its own field, **`delivery`** (`merged` / `merge_refused: <word>` / `not_attempted[: <reason>]`), never collapsed into `outcome`: a branch can be genuinely caught up while the merge is held or refused. **The scan runs before the merge** and is read through `gate-decision.sh`'s severity tier — `secret` stops it, `leak` holds the pull request open, only `override_only` proceeds — so no gate is overridden. It delivers **only** `queue_drained` because a `report_undelivered` unit's delivery already belongs to `retry-undelivered.sh`, which `/implement` runs straight after a `caught_up`; two attempts at one pull request in one turn would report the second as a refusal over a merge that had already landed. Refusals: `content_conflict`, `not_my_claim`, `foreign_identity`, `identity_unresolved`, `claim_active`, `dirty_worktree`, `scan_held:<tier>`, `not_a_work_branch`, `ambiguous_claim`, `mergeability_unanswerable:<reason>`, `catchup_<class>`, `validation_failed:<check>`, `push_failed`, `pull_request_reviewed` (three-valued — `reviews_unreadable:<reason>` never falls through as *nobody has reviewed*; a bot's review is not a person's). The classification rule lives in **one** place, `ship/scripts/lib/conflict-class.sh`, read by the reader and by `catchup-main.sh`; it covers the generated OKF indexes by path and a flat area's marked region by proof (mechanical only when neither side changed anything outside the `okf:generated` markers). `catchup-main.sh --resolve-mechanical` binds the caller to regenerate (a version manifest is raised to the higher semver and merged, never taken wholesale); without the flag it is byte-for-byte what `land-unit.sh` gets. `list-catchable-claims.sh` answers this identity's **reported** claims whose `mergeability` is `mechanical` **or `content`** (2026-09-02, mission `resolve-a-conflicted-pull-request-in-the-tick-not-report-it`). **A `content` reading is a prediction, and the tick now tests it instead of trusting it**: the reader computes with the repository's `.gitattributes` deliberately out of reach — its job is to predict GitHub, which applies no merge driver — while the writer merges in a real checkout where those drivers are in force, so the reader is pessimistic by construction and every branch in that gap was refused without anybody attempting it. The refusal **moved rather than went**: a hunk the merge itself cannot settle still refuses `content_conflict` with the branch byte-identical and nothing pushed, and still reaches its holder through `/moderate`. `unanswerable` keeps its refusal on both paths — it is the absence of a reading. `conflict-class.sh` is untouched, no path was reclassified, no second merge engine exists, and the fast checks still gate every push. The strategy per class — which side wins for a generated file, what a regenerable one does, and what happens to a divergent hand-written hunk — is stated once in `drive/reference/claims.md`, *The resolution strategy, per class*.
- **A publication is not a claim, and has its own reader and act** (2026-08-31): a publish-tree publication carries no `Claim …` commit, so the oracle gives it no row and no catch-up path ever saw one. `branching/scripts/list-stranded-publications.sh` names each open pull request on a `work-*` branch the claim oracle does **not** own and whose publication-refusal word is empty (an operator-facing one is `list-operator-facing-pulls.sh`'s), carrying `claim-mergeability.sh`'s class verbatim; `branching/scripts/settle-stranded-publication.sh <number>` acts on **`mechanical`, `clean` and — since 2026-09-02 — `content`** — a `mechanical` or `content` one catches up through `catchup-main.sh --resolve-mechanical`, regenerates, runs the fast checks and pushes before its one REST merge; a **`clean`** one takes none of that (2026-09-01, mission `deliver-a-stranded-publication-that-needs-nothing-but-a-merge`: nothing collides, so `merged`/`regenerated`/`validated`/`pushed` all report `false` and no ref is written before the merge) — re-deriving its verdict at the moment of the act and refusing `not_mechanical:<class>` (`unanswerable` is the absence of a reading and is never actable) / `content_conflict` (**the writer's residue**, the one place a content collision refuses now: the reader's prediction is attempted, and a hunk the merge cannot settle waits on a person) / `has_claim_commit` / `not_a_work_branch` / `scan_held:<tier>` / `validation_failed:<check>` / `push_failed` by name, with nothing pushed and no worktree left behind. The gate runs before the merge on **both** paths, and the worktree is attached on both because the scan needs a checkout. Measured before the widening: five of six open publications read `clean`, the oldest six days old, every one green and delivered by nothing. It emits **no claim verdict word** and `reference/claims.md` gains no row. **A pull request whose head branch is gone is not a publication either** (2026-09-01, ticket `20260901112558`): GitHub leaves it open and nobody can ever merge it, so `list-stranded-publications.sh` drops it by its own term and counts it (`headless`), and `branching/scripts/list-headless-pulls.sh` — one repository-scoped REST listing of the branches, never the local remote-tracking refs, a fork's head **counted** rather than called gone — is its one reader, asking the operator to close it under `/moderate`'s `headless-pull:<number>`. The loop closes nothing itself. **A publication carries its own age** (2026-09-01, ticket `20260901062000`): `lib/publication-age.sh` is the one derivation — read by the reader, by the act, and by `publication-effect.sh` — rendering `age_hours`, and **null rather than 0** when the timestamp cannot be read, because zero reads as *opened this second*. It is **evidence and never a gate**: the act settles a stale publication exactly as it settles a fresh one, and what the age earns is that a person is told. Measured 2026-09-01: five of six open publications were `clean`, the oldest six days old, and landing them queued ~15 tickets for work the loop had already finished. An age threshold on the **act** was refused by name — it would strand precisely the publications the `clean` widening exists to deliver — and so was a survey-side *already implemented* test, which is a judgement about behaviour rather than a file test.

#### The publish tree

The claim is the only creator of a branch a runner may drive. Artifact writers use the **publish tree**: a checkout of `origin/main` at git-ignored `.publish/` on local branch `publish-main` (`open-publish-tree.sh` → write → `publish-tree-pr.sh` (default, PR path) or `publish-tree-commit.sh` (direct, post-merge seams only) → `close-publish-tree.sh`). The caller's checkout is left byte-identical.

**Two publications never auto-merge, and both refusals are the seam's**: a tree touching `.workaholic/strategies/` reports `merge_reason: strategy_touching`; a tree carrying a **ruling** — a mission that already existed on the base whose `feedback:` line the diff moves, or any touch of `.claude/git-identities` — reports `merge_reason: ruling_touching`. The test is on the **shape of the change**, never a title or label, so a brand-new mission (what every `/specificate` proposal writes) is not caught. `branching/scripts/lib/publication-refusal.sh` holds that test as **one rule**; the seam holds the adapter and the act, and `branching/scripts/publication-effect.sh` reads such publications back for `/moderate`'s `operator-pull:<number>` question.

The claim oracle degrades offline; the writer fails loudly. Under `/implement`, each unit posts **one finish line per unit** into the thread the stateless lookup found (`unit-feedback-stems.sh` + `workaholic:notify`); when several stems match, the first-sorting stem's thread wins; when none does, the run posts the **description root** and replies the finish line into it. Transport: **connector primary** — it is the only transport that can **search**, so it resolves every thread — with the tokened `notify-slack.sh` as the machine fallback. A **directed** post (`/moderate`'s `🙋` question, the `🟡 Handoff` ask) takes the **bot identity** when its mention target resolves to the posting identity, because Slack notifies nobody of their own message; the change is *which account speaks*, never *which transport finds the thread*. The directed set is **enumerated in one place** (`workaholic:notify`, *Which transport carries which shape, and why*) and extending it is a deliberate edit there, never a judgement made at post time. `notify-slack.sh --thread-ts` posts into a resolved thread under the same `chat:write` (refusing `bad_thread_ts` rather than dropping it, byte-identical payload without the flag) and still **cannot search**. `notify-slack.sh` resolves its channel through `workaholic:notify`'s own stated order — an explicit `WORKAHOLIC_SLACK_CHANNEL`, then `WORKAHOLIC_INBOUND_SLACK_CHANNEL`, then the repository's own name (2026-09-01, issue #806: it used to require a second variable for one channel, so the transport designated to survive a connector outage answered `no_channel` during one). With **no bot token** a directed post falls back to the connector and the run **reports the surface that carried it** — never dropped, never silently downgraded. An attended `/drive` run posts nothing to Slack. **A refused call and an absent surface are different outcomes** (2026-09-03, mission `deliver-a-post-the-transport-refused-or-say-it-reached-nobody`): `post_refused` is one call a transport that exists declined — the line is still sendable, so the run **carries it** on the unit's own story (`story/scripts/record-unposted-line.sh`, idempotent, one `## Unposted Line` section replaced rather than stacked; `read-unposted-line.sh` its one reader) and a later tick sends it **once** off `drive/scripts/list-unposted-lines.sh`, clearing the record on a landed send (`clear-unposted-line.sh`) and leaving it standing on a refused one. `no_slack_transport` is this session holding no surface at all, which nothing inside the run can change; a refusal is per call, an absence is per session, and reporting the first as the second is what made a run whose every call was denied say the post did not exist. The retry rides a tick that already runs — no timer, no queue, no cursor, no second liveness authority — and **a unit that merged has no branch left to carry the record**, so its refused line is reported by the run that lost it and carried by nobody. **A directed post carrying no mention token says so in its own line** (`(メンション先未解決: 誰にも通知していません)`): with no `SLACK_BOT_TOKEN` the two-transport model is one transport, so a directed shape whose addressee is the posting identity provably reaches nobody, and an unanswered thread must never be read as silence from the person. One wording across `notify/SKILL.md`, the catalog and the `/implement` and `/moderate` ceilings, pinned by `test-workflow-scripts.mjs`; changing a question's body **never re-asks**, because `already_asked` keys on the step id.

### The release tier (`release/*`)

`main` is the continuously auto-merged development branch; **quality is gated at the `release/*` QA window**, not at merge time. A release branch is cut from `main` by `branching/scripts/cut-release-branch.sh` (`release/YYYYMMDD-HHMMSS`, batch-level, explicitly invoked — never a step of the per-unit ship), carries no commits of its own, and is invisible to the claim protocol. `/ship` §5 does not deploy or confirm pre-merge: what a unit branch used to prove before landing is proved here, over the batch, or not at all. The durable ship record is `.workaholic/releases/<release-branch>.md`, written by `ship/scripts/record-release-cut.sh` and `confirm-release.sh`, both derived from git. A promotion's confirmation is never skipped; a failed confirmation deletes nothing — the next attempt cuts a fresh release branch. No `develop`, no `hotfix/*`.

### Design principles

- **Thin commands, comprehensive skills.** Commands are a few lines naming the skill, section, and entry-point contract; knowledge (templates, rules, scripts) lives in skills (~50-150 lines; overflow goes to `reference/`).
- **Common operations go through skills**: git context via `gather/scripts/git-context.sh`, ticket metadata via `gather/scripts/ticket-metadata.sh` — never inline `git branch --show-current` etc. in command/agent markdown.
- **No complex inline shell** in command/subagent markdown (no conditionals, pipes, loops, text processing, defaulting expansions) — extract to a skill script.
- **`${CLAUDE_PLUGIN_ROOT}` for every skill script reference** — relative paths do not resolve at runtime. The variable is the plugin boundary's notation **in markdown** and is not set in the Bash tool's environment: a session **expands** it when it composes the call it actually runs (`plugin-src.sh`'s `src`), writing the path out in full with the reader as the first token and no assignment prefix — `rules/general.md`'s ceiling bullet, stated in full in `rules/shell.md`.
- **Plugin boundary**: invoke skills by their loaded `workaholic:` namespace; never read global/marketplace installs on disk to *find* skill content, never guess a namespace. The one sanctioned crossing is `check-deps/scripts/plugin-src.sh`, which resolves the newest plugin tree on the machine so an unattended run whose binding is missing or superseded still executes the workflow — the harness binding is an input, never a precondition (`rules/general.md`). It resolves on **two axes: newest version wins, and an equal version goes to the immutable candidate**; callers read `src_immutable` and either require it or re-resolve after a tree-moving step. If a skill is missing from context in an *attended* session, ask which plugins are loaded.

### Cross-agent exposure and generated outputs

- Script-bearing skills carry `metadata.internal: true` (hides them from the `skills` CLI; Claude Code ignores the field) and keep `${CLAUDE_PLUGIN_ROOT}`, reaching non-Claude agents only through the generated bundle. Pure-prose skills (`design`/`implementation`/`operation`, `write-release-note`, `review-sections`) are intentionally exposed.
- `outputs/workflows/` is the self-contained public bundle: `${CLAUDE_PLUGIN_ROOT}` rewritten relative, internal/preload metadata stripped (`publicizeSkillMd`), `reference/` directories carried with paths rewritten. **Regenerate with argument-less `node scripts/build-plugins/build.mjs` whenever a workflow skill or its script closure changes.** `outputs/` is committed; the `Outputs Freshness` CI workflow rebuilds and fails on any diff.
- The `workflows` marketplace entry keeps **`"strict": false`** (the bundle deliberately has no `.claude-plugin/plugin.json`; do not add one). Codex reads `.agents/plugins/marketplace.json`; `hooks/hooks.json` must keep `hooks` as its only top-level key.
- `outputs/okf/` is the OKF v0.1 policy bundle generated by `okf.mjs`; consumers read it from the repo path. Adoption record: `docs/dependencies/okf.md`.

## Commands

| Command | Contract |
| ------- | -------- |
| `/ticket <description>` | Write an implementation spec for this repository, recording `merge_policy` at creation, and publish it via publish tree onto a `work-*` branch behind a PR — from any branch, caller's checkout untouched, no branch of its own created. Discovery applies the **diagnosis-first rule** (`workaholic:discover`): a failure-report ask gets reproduce-and-localize steps first, the reporter's fix demoted to Considerations. Bare `/ticket` reports the queue (yours + unowned). |
| `/drive [<unit>]` | The executor, attended. Survey → partition → claim → drive → report → route → account; asks exactly once (multiSelect) which units to take when several are offered, else nothing. `<unit>` is a scope, not a mode. Never overrides a gate. |
| `/implement [<unit>]` | The executor, unattended: same run, no `AskUserQuestion` anywhere. See *The run report* below. |
| `/commit` | Commit working changes with a policy-conformant message (small non-ticketed changes; prefer `/drive` for ticketed work). |
| `/specificate` | Judge the ask in hand and emit, in one publish-tree PR, the feedback record plus what the work's shape selects. See *`/specificate`* below. Never prompts. |
| `/propose` | Read the running identity's own active strategies, plan the one mission whose evolutionary move brings the nearest one closer to its aim, and open it as a GitHub issue assigned to that identity. Reads and writes no Slack — the channel is the tick's (*Loops*). See *`/propose`* below. |
| `/fb [<content>]` | File one ask as an `[FB] `-marked **GitHub issue** — only for genuine feedback or something that must not be overlooked. See *`/fb`* below. |
| `/story` | Context-aware story + PR creation (warn-tier scan; `doc-drift.sh` and `area-freshness.sh` as documentation backstops). Right-sized: ≤2 archived tickets → one combined worker, no Journey; >2 → 3-worker fan-out. Identical result record and cross-document relations either way. **`/report` is a deprecated alias** that states the new name once and runs the identical workflow. The skill is `workaholic:story`, at `skills/story/`. |
| `/ship` | Context-aware: **draft the deployment plan, then merge** — it deploys nothing. §5 step 3 writes the Release Note's `## Deployment Plan` (per target: what is waiting, the procedure, the verification required) via `draft-deploy-plan.sh` over `read-deploy-state.sh`; blocks pre-merge on the scan (secrets non-overridable) and halts when a target declares no confirmation method (§1-4). Deploying is §5-D, on the developer's instruction only: run the procedure, confirm, record the attempt (`pass`/`fail`/`not_run`/`bypassed`) into the story **and** the note's append-only `## Deployment Verification`. |
| `/mission ["<title>" \| "<instruction>"]` | Create a mission (asks the one `merge_policy` ruling, interrogates to drive-ready, publishes mission + whole ticket set in one PR; under two tickets publishes nothing and names the alternative), **replan** an in-flight one (free-form instruction; also how a `carried` successor gets fleshed out), or show the roadmap (bare — each mission named with its strategy through `mission-strategy.sh`, an explicit **no strategy** where nothing could be attributed). No word of the argument is a subcommand. A direction change is answered by reorganize-and-carry: replan, then `carried` with `--successor <slug>`. |
| `/mission-close <slug> [achieved\|abandoned\|carried]` | End a mission into `missions/archive/` (archive move only). States the Mission Position Report, asks the outcome only when the argument omits it, runs `close.sh`. The route for `abandoned`, `carried`, and an `achieved` mission the archive gate could not prove. |
| `/catch [window]` | Read-only catch-up: commits/tickets/stories by developer, Missions view, Orchestration Throughput (`commit-kpi.sh`), then Q&A. |
| `/explain <question> [dir]` | Answer a repo question and export a printer-ready PDF (HTML staged in-repo at git-ignored `.explain/`, printed by a real browser; exports to `dir`, else Desktop→Home). |
| `/workaholify` | **The preparation command, not an audit** — running it leaves the repository prepared. See *`/workaholify`* below. |
| `/standup` | Report the day's development activity **per strategy** (`standup/scripts/digest.sh` over `attributed-work.sh`). A **pure read** — no file, commit, branch, PR, merge, deployment, prompt, or GitHub read. See *`/standup`* below. |
| `/prepare-release` | Report what is waiting to deploy on the base, per target, and what about it needs a human (`ship/scripts/report-deploy-status.sh` over `read-deploy-state.sh`), plus each target's draft-note state (`run-note-cadence.sh` without `--write`). **It writes nothing, anywhere.** One network **read**: `report-deploy-status.sh` freshens the base branch and tags before deriving the boundary (`WORKAHOLIC_DEPLOY_FETCH_TIMEOUT=0` opts out) and reports `refs: fresh\|stale\|skipped`. A command a human runs; it posts nothing. |
| `/moderate` | The hourly maintenance tick. See *`/moderate`* below. |
| `/setup-dev-routines [repo]` | Configure the `developer`-scoped routines (`[Propose]`, `[Implement]`). See *Routine convergence* below. Never touches a `repository`-scoped routine. |
| `/setup-repo-routines [repo]` | Configure the `repository`-scoped routine (`[Moderate]`). Same flow, scoped to `repository` templates. **Run it from one account** — a designated person or a project/service account — because N members converging it leaves N copies firing every hour; a routine is an account-level record no other account can list, so this is a **stated convention the plugin cannot enforce**. Never touches a `developer`-scoped routine. |
| `/work` | Start the development loop in this session: invoke the `loop` skill with `5m /infinite-development` and let it run. No stop argument — a command's behaviour never depends on the first word of its argument. |
| `/infinite-development` | **One tick of the loop** (2026-09-03): read the Slack channel and answer on it, spawn `propose` and `implement` (and `moderate` on a 30-minute gate) as background subagents, and end — it never waits for them. Run under `/loop 5m /infinite-development` in one session. See *Loops* below. |

### `/implement` — the run report

Ends with the `N units: X shipped, Y PR'd, Z blocked` reconciliation and the honest terminal token (`ok` only when nothing claimable remains and the survey was current and readable) — the `/goal /implement ok` contract. A mid-run problem becomes a ticket; a unit ends in `handoff` (the PR body's non-droppable `## Handoff`) when it is half-driven **or** was declared `verification_handoff:` at creation. The report is the **only** surface for everything below — none of it is appended to the finish post.

Per unit:

- **Notification outcome** — the surface the finish line used and whether it landed; an unposted line is reported as unposted and never moves the token.
- **Whose work it was** when the tickets were not the runner's (`drive/scripts/unit-authors.sh` → `foreign`/`unresolved`/`mine`) — disclosure of an existing behaviour (empty `assignees:` is claimable by anyone), never a claim decision.
- **A `review` unit's merge outcome**: `merged`, `merge_refused: <merge-reason.sh word>`, or `merge_not_attempted: <hard|confirm>` when a scan finding held the PR — never collapsed. `merge_refused` **forbids `ok`**, and the withheld token names the unit and the refusal; `merge_not_attempted` still reports `ok` (that PR waits on a person by design).
- **The connector retry** is a numbered step of that route with its own reported outcome: precondition `merge_reason == session_type_cannot_merge` and no other word, one attempt through `mcp__github__merge_pull_request` and no other tool, outcome `merged` or the PR left open with **both** refusals named. A run reporting `session_type_cannot_merge` and no retry outcome is non-conformant on its face.
- **Each `undelivered[]` entry** gets one catch-up and one retry, reported in their own vocabularies: `caught_up` / `already_current` / `catch_up_refused: <word>`, then the **recorded refusal it was retrying** and the new outcome in the merge vocabulary (`merged` / `merge_refused: <word>`), a still-refused unit naming **both** words. Naming an entry and reporting no outcome for it is non-conformant. A delivered retry stops withholding `ok`; a still-refused one keeps withholding it. `catch_up_refused: content_conflict` moves no token (it waits on a person, who is reached by `/moderate`).
- **Each *reported* claim's `mergeability`**, so a decay from `mechanical` to `content` is visible the hour it happens. Evidence, never a verdict.
- **Each stranded publication** (`branching/scripts/list-stranded-publications.sh`, read once per run): its pull request, `mergeability` and `age_hours` (a `null` age named **as unreadable**, never as fresh) whether or not the run acted, and — for each `mechanical` **or `clean`** one it acted on — `settle-stranded-publication.sh`'s own word (`settled` / `already_current` / `settle_refused: <word>`) beside the delivery in the merge vocabulary (`merged` / `merge_refused: <word>` / `not_attempted`). One line shape for both classes; the class is already on it. Naming one and reporting no outcome is non-conformant. A settled-but-refused delivery **forbids `ok`**; a delivered one stops withholding it; a refusal that waits on a person (`content_conflict`, an operator-facing publication) moves no token — that person is reached by `/moderate`'s `stranded-publication:<number>` question.

**One per-run reading DOES move the token**, and is listed here rather than below for that reason:

- **`placeholder_identity`** — the survey judged ownership against an identity that stands for nobody (an **empty** `git config user.email`, or a **known placeholder domain**: `noreply@anthropic.com`, what a container keeps when no `.claude/git-identities` mapping resolves it). It **forbids `ok`** with `owner_unresolved`'s standing, and the report **names the fact** rather than emitting a bare `pending` — a run that resolved every artifact to `owned_by_other` against a non-person has established nothing about what is assigned to it, so an empty offer is not evidence of an empty queue.

Once per run (each **moves no token and gates nothing**; the person who must act is reached by a `/moderate` question):

- **`backlog_all_excluded`** — a queue that holds tickets and offers none of them, with a count per exclusion reason, derived from whatever `excluded[]` carries (no hand-maintained list).
- **The base's own health** at the top of the report: `green`, `red` (with the attributed merge and the failing checks, or `unattributable` and its reason), or `unanswerable`. A degraded read is reported as degraded and **never as green**; the attribution walk runs only on `red`. **And where that colour was read**: a `no_checks` tip is walked past, so on a base this loop writes to the reading is normally an **ancestor's** — the report names that commit and how far behind the tip it is (`checked_at` / `checked_behind`, both absent when the colour was read at the tip itself). Stated, never thresholded: a green ancestor fifteen commits back is a weaker sentence than one directly behind, and which is too far is the operator's judgement.
- **What is waiting on the operator** — each un-acted operator-facing pull request with its number, age and the **refusal word** that made it the operator's. A settled reading is not named; an `unreadable` one is named as unreadable and never as *nothing waiting*.
- **How long each standing blocker has been standing** — for each subject already named (an undelivered unit, a stalled claim, a blocked retirement, an undrivable queued artifact), the age of the **question** about it through `moderate/scripts/condition-age.sh` (`age.ticks` ticks since `age.first_seen`, *at least* that when `age.first_seen_is_floor`; the walk is bounded by `WORKAHOLIC_CONDITION_AGE_MAX_DAYS`, default 30, expressed as the newest N day files, and a cut walk says `truncated` rather than reporting a date it could not establish). An absent age is not mentioned; an unreadable one is named as unreadable.

### `/specificate`

Judges the ask **in hand** — the argument, a record just written, or, on a clock-fired tick with nothing handed in, the **discovered inbound issues** (`list-inbound-issues.sh`: open GitHub issues assigned to the session's own identity through `gather/scripts/gh-rest.sh`, oldest-first, pull requests filtered out, minus those a feedback record already names **on the base or on an unmerged branch**; never unassigned issues, never a title filter). The two capture words are distinct and neither gates anything: `already_captured` (the record is on the base) and `captured_on_branch` (it is on an open proposal's branch, so the ask waits on that pull request). **A third exclusion is a gate** (2026-09-02, issue #864): `self_originated` — an issue whose body header reads `source: moderate` is the tick's own finding about the loop's own artifact, and it is never taken as an ask. Measured: five consecutive self-filed issues in one day, each proposed, ticketed, implemented and merged by the next ticks, every link refining the one before it, while the operator's development stopped. Only a human's ask, or a strategy a human authored, originates a mission; the finding stays open as knowledge. Both halves of the run now read unmerged branches through **one** walk — `specificate/scripts/lib/unmerged-branches.sh`, shared with `list-proposed-refs.sh` — over-reading on every ambiguity, naming a degraded walk on stderr, and never turning a readable inbox into a refusal. `nothing_in_hand` only when that inbox is empty too; an unreadable inbox reports its reason; an issue assigned to someone else is `not_mine`.

The run writes the feedback record into a publish tree, reads base state as constraints (`survey-state.sh`), runs a history-mode discovery pass (diagnosis-first for failure reports), dedups via `list-proposed-refs.sh` (including unmerged branches), and emits in one PR the record plus what the shape selects.

- **Only a human's ask originates a mission** (2026-09-02, mission `refuse-an-ask-the-loop-wrote-to-itself`): at the **judgment** — not at discovery, because there the record would never be written — the run reads the ask through `feedback/scripts/ask-origin.sh` (`human` / `machine` / `unreadable:<reason>`, off the `subject:` axis, no new field) and, on `machine`, applies the second half itself — *is its subject the loop's own apparatus?* — as a judgement written into the pull-request body. Both holding is **record-only, reported `self_authored`**; `unreadable` never refuses (a grandfathered record is an ordinary ask); the **inbound sweep is never caught**, because a person's channel message filed by a routine carries `subject: person:<author>` and reads `human`. Measured: five self-authored `[FB]` roots in one day, each proposed, ticketed and merged by the next ticks, until the operator abandoned the direction mid-drive. Discovery's `self_originated` is a different test on a different signal and is untouched.
- **The four forms are an ordered rule consulted first**, and *when unsure, record-only* applies only to an ask they did not resolve; the run reports `precedence:<form>` or `unsure:<what>`. Forms: a mission with ≥2 tickets, one loose ticket (mandatory `feedback:` refs), one **strategy**, or the record alone.
- **A strategy is not a mission factory**: intended scale is one mission of ~7–8 tickets plus at most one follow-up mission of 3–4 repair tickets. An ask advancing a strategy whose active mission already exists lands as tickets **into** that mission; a new mission only when the old is closed or its `## Experience` cannot cover the work, reported either way.
- **The strategy form** needs an **owner** (the triggering issue's assignee; unassigned or unmapped is record-only, never the running identity) and an **aim with no decomposable plan**. The date is the one the ask states, else the **one-week default** from `default-target-date.sh`, counted from the **triggering issue's own date**; a stated date this run cannot resolve to a single `YYYY-MM-DD` is `no_target_date` with no date emitted. **Every surface says the date is a default** — the `## Schedule` prose (naming what it was counted from and how to change it), the PR body, and the run report as `target_date:default` / `target_date:stated`. No frontmatter key records it.
- **The operator-record check**: an `active` strategy's Aim and the `subject: person:` records it cites are the operator's *resolved* direction and are **binding** — cited and proceeded on, never reopened. A fork that survives the check is declared `verification_handoff:`, never written as a Quality Gate item. Unrecommendable forks become `## Open Decisions` items under the writing floor above.
- **Lifecycle announcements** are recognised before the four forms and land only on the strategy named by **explicit slug** (read through `list.sh`; a title or paraphrase never matches, an absent slug is record-only naming `strategy_not_found`): *created* → the strategy form (optionally naming a **predecessor**, reported `successor_of:<predecessor>:<n>`); *changed* → `amend.sh` (step 9d) carrying exactly the revision the ask states, record-only under `not_active` or `no_revision`; *ended* → `close.sh` (step 9c), after which the route reads `closing-residue.sh` and names the leaving in the PR body and the run report, a degraded read named as degraded; *answers* (a strategy slug **and** a mission slug) → `carry-attribution.sh` (step 9e), appending that strategy's own `feedback:` refs to that mission and nothing else, refusing `strategy_not_found` / `mission_not_found` / `not_active` / `no_revision` / `immutable_field` and writing nothing on any refusal (`already` on a re-run).
- **An ask that already names a mission is emitted as that mission** — that plan, in that order, never re-derived. Every gate stays over the top of it, and a named plan that breaches one is **demoted and reported by name**, never trimmed to fit.
- **The carry-forward is mechanical, reported and floored**: `read-ask-feedback-refs.sh` reads the ask's own `feedback:` line (the mirror of `read-feedback-relation.sh` on the artifact side) (`{line_found, carried, dropped:[{ref, reason}]}`, exit 0 always); both surfaces name per emitted artifact what was carried and every dropped ref with its reason; `check-carry-floor.sh` refuses at the publish seam, beside the two-ticket floor, when a **resolved** carried ref is missing from the emitted mission (or the loose ticket when there is none) — a run failure with the repair named, never a demotion. A mission's tickets need not repeat its refs (`via_mission:<slug>` reaches them).
- **The direction is judged when the ask names none**: explicit slug, else a judgment against the `active` Aims (step 5b already reads them), else `unattributed`. A line beats a judgment; both surfaces report `direction:<slug>:<line|slug|aim>` or `direction:unattributed`; `check-carry-floor.sh` stays keyed on the ask's own resolved refs; an unreadable strategy set is named rather than collapsed.
- **Assignee**: the triggering issue's assignee rides every artifact (`--assignee`), never the running identity, and **only after it resolves** through `identity.sh` from login to canonical address. An unmapped login gets **no `--assignee`** and is reported `assignee_unmapped: <login>` in the run report and the PR body — a different fact from an unassigned issue, because only the first is repaired by a mapping line.
- Otherwise the PR auto-merges on opening (`WORKAHOLIC_AUTO_MERGE=1`; a scan finding or the seam's own `strategy_touching`/`ruling_touching` leaves it open), titled `[Proposal] …` via `WORKAHOLIC_PR_TITLE`, with `Closes #<N>` when the ask carried an issue number.

### `/propose`

**The routine that supplies the loop's own ask.** A pure reader of this repository: no file, commit, branch, PR, merge, deployment or prompt — its only writes are GitHub issues, outside the tree, the inbound sweep's Slack receipt, and — since 2026-09-02 (ticket `20260902043117`) — **two lines on the tick log**, `propose-open` at the start and `propose-close` as the run's last act. That is the one widening of the pure-reader contract and it is bounded to what the log already is: git-ignored, on the `workaholic-log` orphan branch, so nothing reaches `main`, no `.workaholic/` artifact lands and no pull request opens. It buys the reading `/moderate`'s `blocked-tick` step makes — **an opening with no closing** — for the routine that *originates* the loop's work, which was measured parked hourly while reading as scheduled and healthy because it left no trace anywhere. Reading the routine's own record instead was refused: a routine record is account-level and no repository-side reader can list one.

- **The unit is a mission**: the issue names a mission **title**, the **experience** it demands and an **ordered ticket set** at the ruled 7–8 scale. `open-proposal.sh`'s body floor requires `## Experience` and `## Tickets` with a **two-or-more** count, refused `under_planned`. The ceiling stays a judgement. `/propose` plans; `/specificate` writes.
- Every proposal declares exactly one **move** (`depth | breadth | contraction`) and names what it is chosen against (`## What this is chosen against`). A tick that cannot name a move reports `no_evolutionary_move` and opens nothing. A move producing documentation *about* an Aim whose subject is to build something is refused `describing_move`; a strategy whose Aim is itself documentation is unaffected.
- **Gates, each reported by name**: `not_active`, `not_mine`, `observing` (the operator declared 観察中 — the one **declared** gate, placed after `not_active`/`not_mine` and before `past_target_date`; it stops origination and nothing else, and inbound work still reaches such a direction), `past_target_date`, `no_feedback_refs`, **`arrived`** (2026-09-02, issue #860: a direction whose work is all in — `quiescent` — receives no machine-originated proposal; measured, an arrived direction was getting a new self-invented `depth` mission every hour while the directions with real need sat refused `work_waiting`. The close-or-extend question reaches the assignee through `/moderate`'s `direction-arrived` ask; inbound work still lands. **The gate reads the direction's age**: a direction younger than the survey window cannot have arrived — a successor inherits its predecessor's landed work through the carry and reads `quiescent` on its first hour, and the operator created it precisely so origination would resume), `work_waiting`, `open_proposal`, **`wip_limit`** (2026-09-01: `work_waiting` + `open_proposal` bound a **strategy**, and nothing bounded the **repository** — measured, six missions in parallel with every merge re-conflicting five open pull requests on the loop's own generated index. Declared as `WORKAHOLIC_WIP_LIMIT` in `.claude/settings.json`'s `env` block, where `WORKAHOLIC_CADENCES` lives and for the same reason; **absent means no limit** and such a repository is byte-identical to one before it existed. Placed **last** in the ladder so a direction refused earlier is not also counted; it counts **active missions with queued work**, never open pull requests and never a sum over the per-row `waiting_missions`; a `bad_limit` or an unreadable count holds **nothing** and says so; it holds **origination only**, and the survey carries `wip: {declared, limit, count, readable, reason}` so a held tick says why rather than looking idle), `attribution_unreadable`, `inbox_unreadable` (a gate that cannot be read is not a gate). Three judgement refusals ride beside them: `describing_move`; **`self_refining`** (2026-09-02, mission `refuse-an-ask-the-loop-wrote-to-itself`: a `depth` move whose subject traces back not to a human's ask or a human-authored strategy but only to a previous proposal this loop wrote — measured, a five-link chain in one day, every link a defensible `depth`, the direction abandoned mid-drive; it catches neither a second mission answering a **human's** ask on the same subject nor the scale's own follow-up repair mission, and it reaches no mechanical gate, sort or `selected`); and **`invented_obligation`** (2026-09-02, issue #862: a proposal whose deliverable is a new per-page verdict/stamp/score/gate over a repository's pages is scope the operator never granted — proposable only through a human ask naming it).
- **`work_waiting` + `open_proposal` are one gate in two halves** giving *one mission per strategy in flight at a time*, with no cursor and no stored state: `work_waiting` reads an **active attributed mission** OR'd with a queued attributable ticket (neither counts, so seven queued tickets are one mission in flight); `open_proposal` reads the `strategy: <slug> / move: <move>` marker through `list-open-proposals.sh`. Describing work does not gate a building aim — `work-kind.sh` derives the kind from each queued ticket's `## Key Files` and **`unknown` counts toward advancing**; the Aim exemption is passed in with `survey-strategies.sh --aim-kind`, and absent the flag the gate is byte-for-byte what it was.
- **A tick proposes against every eligible strategy** (`over_cap` is retired; `WORKAHOLIC_PROPOSE_MAX` survives as an opt-in bound whose default is unbounded), ordered by **stage first (改良中 first), then late-first, then nearest date** — stated in `survey-strategies.sh`'s header so no consumer re-derives it. No weight, score, tunable constant or cross-direction arithmetic.
- **The run report names the evidence and gates on none of it**: `pace`, `overdue`, `expiring`, `arrived`/`quiescent` (with its residue, a degraded read named as degraded), each un-acted operator-facing pull request, and how long each standing blocker has been standing. `survey-strategies.sh` never reaches the age reader.
- **When only the loop has spoken, the tick originates nothing** (2026-09-02, mission `refuse-an-ask-the-loop-wrote-to-itself`, the operator's instruction). The reading comes **off the sweep the run already makes** — no second query, no second window, no cursor — over the sweep's own `WORKAHOLIC_INBOUND_SLACK_WINDOW_HOURS` (default 26), answering `human_spoke` / `only_the_loop_spoke` / `unreadable:<reason>`. It is the one **run-level** brake, refusing every direction at once where every other gate is per-direction; **`unreadable` never brakes** (no transport is not silence), the **reactive** half — issues, sweeps, `/specificate` — is untouched, and the reading is reported whether or not it fires. The cost is stated rather than tuned against: a quiet weekend reads as abandonment and costs one tick of proposals, against the measured day of merged work the operator tore out.
- **The Slack sweep left this command on 2026-09-03** — reading the channel, answering on it and capturing an ask are `/infinite-development`'s (*Loops*). What still reaches this run is one word, **handed in**: `human_spoke` / `only_the_loop_spoke` / `unreadable:<reason>`. On `only_the_loop_spoke` it originates nothing and reports that word — the one **run-level** brake, refusing every direction at once where every other gate is per-direction; `unreadable` never brakes, and a run handed no reading treats it as `unreadable`. The sweep's scripts (`list-swept-slack-refs.sh`, `file-inbound-ask.sh`) stay under `skills/propose/scripts/` and are called by the tick.
- **Every capture is acknowledged where it was written**: the `📥 受理` reply into the swept message's **own thread** (carrying the issue link and the session URL) plus the catalog's `:inbox_tray:` reaction on the message itself — both riding the `slack-ref` just written, so no lookup and no second query. **Only a message this run filed**; an already-swept one gets neither. Never load-bearing: the issue is open before either is attempted, and failures are reported per message as `ack_failed: <reason>`. This is the routine's whole Slack vocabulary.

### `/fb`

File one ask as an `[FB] `-marked GitHub issue. **One artifact, two addresses.**

- **In-repo** (destination-less): the issue opens on this repository **assigned to the invoking identity** (`open-issue.sh --assignee`, the login from `gh api user`), carrying the `kind`/`source`/`subject` judgment in its body so the receiving `/specificate` inherits it, plus **the direction it answers** on the next line — judged as the inbound sweep judges it, carrying that strategy's refs through `ask-feedback-line.sh` (the one writer). It **writes no record**; the one-line report names `direction:<slug>`/`direction:unattributed` beside `assigned`. Its one degradation is `fb-fallback.sh`: when the issue cannot be opened, the record is written, the report names the fallback and its reason, and the stated cost is that a fallback record is captured but **not** discovered by `[Propose]`'s `/specificate` half.
- **Cross-repository** (`/fb <ask> to <owner/name>`): compose in the target's vocabulary, apply the masking judgment, run the secret/leak scan and the self-name backstop, and open a GitHub issue on the target — the only sanctioned crossing (`/request` is retired). **Neither path confirms** (2026-09-03, the developer's instruction, restated after a session read the crossing as an exception): the verbatim `AskUserQuestion` over destination, visibility, title and body is retired on both, the two mechanical layers are now the crossing's only gates, and what it gives up is stated at `feedback/SKILL.md`, *No confirmation, on either path* — an unmasked ask reaches the target as written. It passes `--assignee <the invoking login>`, self-limiting because the API *drops* a login without access; the report names `assigned` either way. **The crossing never carries the `feedback:` line** (it would name records the target cannot resolve).
- The `[FB] ` marker is stamped idempotently in one place (`feedback/scripts/fb-title.sh`), called by `open-issue.sh` and by the confirmation so the developer sees the wire string. `open-issue.sh` composes no header opinion either way — the `/specificate` discovery excludes an open issue a record already names.
- `.workaholic/feedbacks/` is written by `/specificate`, `/ship`, `/story` and `/fb`'s fallback — never by `/fb`'s primary path.

### `/standup`

- Reports per strategy what moved in the window, what is queued and waiting, and each strategy's `days_to_target`. Headline number is `commit_count` (repository commits in the window); strategies render numbered with bold titles on their own line; the honesty line names tickets and the window.
- **Each strategy names the missions serving it, and the digest names the whole queue** (2026-09-01): `missions[]` rides each strategy record — per mission its title, acceptance `checked`/`total` and queued count — and `queued_total` rides beside the other counts, so the ordinary question *where does the work stand* is answered by a reading rather than by hand. It is a **composition of readers that already existed** (`attributed-work.sh` → `progress.sh` / `queue-size.sh`, with `list-todo.sh` for the total): no second walker, no relation, **no field on any artifact**. The `STANDUP_MAX_ITEMS` cap applies with `missions_omitted` counted, and a grain that could not be read is named by its reason with **null** counts, never `0/0`. The shape reaches the operator daily — in `/moderate`'s JST-morning root and in `/standup` — and **not hourly**: an unchanged answer restated every hour is what `📦 Release Preparation` was retired for, and that refusal is written where the step is read.
- **Every strategy names its declared stage** — one word beside the title, off the row the digest already reads, an unresolved stage rendered as unreadable rather than as 進行中. The stage is **never a reason to post**.
- A quiet strategy renders an explicit "no activity" line rather than vanishing; work belonging to no strategy is a **count** (`unattributed`), never a list; the render is capped (`STANDUP_MAX_STRATEGIES`/`STANDUP_MAX_ITEMS`) with every cut counted; a degraded strategy is rendered by name with `degraded_count` beside the other counts and a null honesty line.
- Posts one `📣 Standup` root (no mention token, no printed key) only when the digest is **not** a no-op: `no_strategies`, `no_activity` (nothing moved and no `target_date` inside `STANDUP_TARGET_HORIZON`, default 14 days), `strategy_list_unreadable` and `all_attribution_unreadable` each post nothing.
- The `[Standup]` routine is retired — `/moderate`'s `strategy-digest` step renders this digest at the top of the JST-morning `🔎 Moderation` root, once per day, and it is a second gate on that root beside the question gate. `/standup` survives as a command a human runs on demand.

### `/moderate`

The hourly, unattended maintenance tick: **thirty-three steps**, one run (`catchup-blocked` was retired 2026-09-02: the tick resolves and merges a conflict rather than handing it to a claim holder who never comes). Its purpose is the repository's development progress — seeing it, and unblocking it. It **files through the existing seams** (a finding is a feedback record, work is a ticket or a mission, a question is a Slack post) and writes nothing else anywhere but its own log (`.workaholic/moderations/<UTC-day>.md`). **No `AskUserQuestion` at any step**; it never merges a pull request, never pushes into a branch the claim protocol owns, and never edits a live strategy — **bounds on which acts are licensed, never a claim that the tick repairs nothing**. **Four steps act, and the licence is a proof** (`workaholic:moderate`, *What the tick repairs, on what licenses it, and who does the rest*, citing `drive/reference/claims.md` rather than restating it): `retire-claims` on `superseded`, `closable-missions` on arithmetic it re-proves, `standing-rulings` drafting one pull request, `file-findings` opening one issue. Everything else is reported or asked, and that section names **who repairs each class** it does not — a `mechanical` **or `content`** conflict and a `mechanical`/`clean`/`content` publication to `[Implement]`'s next tick (2026-09-02 — the class is a *prediction*, so the next tick attempts it; only a hunk the merge itself could not settle comes back to a person), that residue reported by the act that met it — **no step asks anybody about a conflict any more** — and an operator-facing pull request to a person, a repairable finding the long way round through `[FB]` → `/specificate` → `/implement`, and `cadence-lapse` / `blocked-tick` / `raced-units` to nobody yet, by design. `run.sh` invokes every step and every step contributes a report line; a degraded read is reported **by name**, never as a step that ran and found nothing, and a step not reached inside `--deadline-seconds` is reported as unreached (the asking step is exempt from the deadline).

**Posting**: one thread a **day** — a `🔎 Moderation` root carrying what changed **since the reader last heard from us**, addressed to nobody, with every question as a **mentioned reply inside it** (`🙋 <@U…>`, asked once). Two speech acts, one place to look, told apart by position in the thread. **No printed dedup key reaches a reader.** **The root is day-keyed since 2026-09-01** (`tick-day:<YYYYMMDD>`, derived once in `moderate/scripts/lib/tick-thread-key.sh`): under `tick:<tick-id>` the string an hour searched for was one no earlier message could contain, so the lookup could never match the previous hour and each tick opened a fresh top-level post — measured, **14 roots in one window, 12 carrying no question**. The day's **first speaking tick** posts the root; every later speaking tick posts `render-tick-post.sh`'s `reply_text` — the same body with **no head** — as a reply into it, and the command names which (`commands/moderate.md`, the ceiling). The delta reply carries **no mention token**, exactly as the root does not. **The exact-string rule is untouched**: one string the tick derives, never a similarity match and never recency (`workaholic:notify`). Each root line names a **repository event**, supplied by the step beside its log-facing `summary`; a step with no `event` renders no line, and **an event names no identifier** — *how many* is news and *which* is a task, so a slug belongs in the question that addresses the person who can act on it, never in a line addressed to nobody (2026-09-01). A change is a **diff against the previous tick's summary for the same step**, read from the log (no cursor, no field), normalising out timestamps, bare hex object names and clock times before comparing. **A root the tick could not deliver is filed, not lost** (2026-09-01, issue #806): when the rendered post says *post* and no transport reaches the channel, the tick files the rendered root as **one** `[FB]` issue through `file-inbound-ask.sh`, carrying it verbatim and naming why each transport was out — measured as 7 change lines, 2 impairment lines and **18 held questions** existing only in a discarded container's log. The **ledger is untouched**: a question that reached nobody is never recorded as asked, so every held question stays held. The repair is **provisioning, not code**, and the issue asks for it.

**Nobody is woken to be told nothing.** Before any gate, the root is held by the **same speaking window that holds the questions** — `WORKAHOLIC_QUIET_HOURS` / `WORKAHOLIC_WORK_DAYS` / `WORKAHOLIC_QUIET_TZ`, derived once in `moderate/scripts/lib/speaking-window.sh` and read by `ask-question.sh` and `render-tick-post.sh` alike (2026-09-01). Measured before it: a root posted at **04:01 JST** reading `質問 0 件` while that same tick held **seventeen** questions, every one refused `quiet_hours` — the loop woke the channel to say something addressed to nobody and held back the only things addressed to someone. **Held is not dropped**: the change diff's baseline is the last tick that **spoke** (read from its own `human-checkin-post` line, no cursor added), so the first working-hour tick says what changed across the whole silence and asks what the window held. The `🔴 Blocked` red alert is untouched — it has its own cool-down and its own reason to break a quiet hour.

**And the root carries the plan's own delta** (2026-09-01): the daily digest says where the work stands once a day, while the hourly root carried only per-step change lines — so an hour in which the board moved read as a list of anomalies rather than as a plan that had moved. `strategy-pace` now carries a **`plan`** block off the one survey it already makes (`advancing` / `held` / `held_reasons` / `wip`) and puts the same numbers in its summary; `render-tick-post.sh` renders one `📋` line **beside** the change lines. It is **gated on that summary's own diff**, so an unmoved plan adds no line — the difference between this and the retired `📦 Release Preparation` — and it **earns no post**, exactly as the impairment clause does. It carries **no identifier** (*how many* is news, *which* is a task) and **no mention token**, a degraded read says *the plan could not be read this tick* rather than rendering an empty delta, and a tick the repository's own `wip_limit` is holding says so with the count and the limit — a quieter loop must not be indistinguishable from a stopped one. **What it deliberately does not say** is *which unit is next*: the executor's order is `plan-units.sh`'s, which no step here may reach, and naming a unit would put an identifier on a line addressed to nobody.

**One gate: a question.** `idle`, `no_question`, `no_previous_tick` and `no_log` all post nothing (`no_log` named rather than rendered as quiet); the JST-morning digest, a delivery-failure event and a **changed impairment** are the three further gates on the root.

**Every root names the steps that could not read.** `render-tick-post.sh` derives `impaired[]` (the `degraded` and `blocked` rows in `STEPS` order, each with its own status and reason) and `impaired_count` **on every exit path including the silent ones**: **the head counts no steps** — it read `N step(s) could not read` until 2026-09-01, and a reader of the channel has no model of *steps*, which are this loop's own machinery — and the body carries **each step's own `summary`**, the one field of the four that is a sentence (`the base's checks could not be read (tip_no_checks); a red base is indistinguishable from a green one this tick`), bounded by `WORKAHOLIC_IMPAIRED_MAX` (default 5) with the rest **counted** rather than cut. A step with no summary falls back to `<step> — <status>: <reason>` rather than rendering nothing. `skipped` is deliberately **not** impairment — a step declining to run for a stated, healthy reason did not fail to see. **The statement is outside the change diff and the gate is inside it**: a step degraded the same way for twenty-four ticks has an unchanged summary, so a diff-gated clause would say it once and let it vanish, while an unchanged answer restated hourly is what `📦 Release Preparation` was retired for. A **changed** impairment is therefore the fourth gate — appearing and clearing break silence, persisting does not — and a root earned by it alone reports `ready_impairment`. The set compared is `(step, status, stabilized summary)`, strictly finer than `(step, status)` so it errs toward opening a root; `reason` never reaches the tick log, so it cannot enter the comparison without the store this must not add. It reads **no age** and asks nobody anything. It works **business days as well as business hours** (`WORKAHOLIC_WORK_DAYS`, default `1-5`); held is not dropped.

**Questions** go through `ask-question.sh` — one derivation of the question id (`lib/question-id.sh`) shared by the gate, the writer and the reader, keyed per subject so one subject costs one question however many ticks see it. A question has **three states**: `never_asked` / `asked` / `answered` (`question-state.sh`); `record-answer.sh` is the one writer of an answer. Log lines are `human-checkin-ask-<slug>` / `-answered-` / `-confirmed-` / `-reasked-`. `day_cap` counts the `human-checkin-ask` lines on the current `WORKAHOLIC_QUIET_TZ` day (derived once, from the tick id, passed to `log-read.sh --since`), `max_per_tick` stays with the gate, and `step-human-checkin.sh` hands `held` back ordered by the day each key was **first** held (a proposal by age, not urgency), reporting `delivered` / `held_count` / `candidates` with a named reason when it delivered nothing (`cap_spent` — the budget worked — distinguished from `cap_unbounded`, our own degradation, beside `all_held`, `all_asked_before`, `no_candidates`). **Each held entry carries the gate's own refusal word**, from a read-only probe per candidate — `all_held` is one token over four refusals that call for four different acts — and the arrears report `held_oldest_day` / `held_days` (**null**, never `0`, on a degraded read), off the first-held day the drain ordering already derives. **An `all_held` tick whose arrears outlived the designed hold earns a root line**, naming the depth and the age once: the bound is the **working-day boundary** (the first hour inside `WORKAHOLIC_WORK_DAYS` at the end of `WORKAHOLIC_QUIET_HOURS`, in `WORKAHOLIC_QUIET_TZ`, composed as the red-alert cool-down's expiry is, introducing **no constant**), a hold inside it stays silent, and the keys, caps, holds, `ask-question.sh`, the diff rule and which questions are asked are all untouched. A still-`live` question first asked on an earlier day is re-asked **at the next working day and never again** (`question-liveness.sh` re-derives `live`/`settled`/`unknown` from the tick's own run report; `settled` and `unknown` are never re-asked). A settled question gets one `✅ 解消を確認` reply into its own thread.

**What a question must CARRY is stated once**, in `workaholic:notify`'s catalog beside the `🙋` shape (2026-08-31). The shape fixed the *form* and said nothing about the *content*, so each step composed from whatever identifier it held and a question opened with a unit id, an artifact path, a claim verdict or a slug — unreadable to an operator looking at Slack with nothing else at hand. The contract: **lead with what happened**, in words a reader outside the repository understands; **the identifier after it, never before**; **a verdict word, key or step id never alone**, always beside the plain fact it stands for; and **the body names the one act** asked of the addressee. The ≤25-word body is a **ceiling, not a target** — a question that will not fit is aimed at the wrong reader, and the repair is self-containment rather than more text. Every question-asking step's own spec in `moderate/reference/workflow.md` was rewritten to it in one change (thirteen steps, twenty keys) so the operator meets one voice; the named details keep riding the **heading** (a direction's stage, residue and leaving, the days left, the branch a retirement could not delete, the files a conflict collided on, how long the subject has been asked about), and the one case that could not be made self-contained inside the bound — `direction-arrived`'s residue — is **named rather than stretched**. It is prose, so nothing mechanical checks it; what it buys is that a question leading with an identifier is visibly non-conformant. **No key, cap, hold, addressee or gate moved with it**, and changing a body **never re-asks**, because `already_asked` keys on the step id `lib/question-id.sh` derives from the key, never on the text.

**Steps with rules of their own** (each **asks and nothing else** unless stated — no merge, close, revert, re-run, gate, hold, claim touched or gate lifted; none reaches `plan-units.sh`, which stages what its living migrations converge; none consults the running identity where the finding is about the repository):

| Step | Question key | Reads | Addressed to |
| ---- | ------------ | ----- | ------------ |
| `strategy-digest` | — | `standup/scripts/digest.sh` | the JST-morning root |
| `strategy-pace` | `strategy-pace:<slug>` | `survey-strategies.sh` | the assignee |
| `direction-health` | `direction-arrived` / `-cutover` / `-settled` / `-overdue` / `-expiring` / `-dormant` / `-last` / `-none`, each `:<slug>` | `direction-state.sh --with-leaving` | the direction's assignee (`-none` nobody) |
| `date-will-not-hold` | `date-will-not-hold:<slug>` | `landing-arithmetic.sh` (`does_not_clear`) filtered to `direction-state.sh`'s own `live` rows | the direction's assignee |
| `undrivable-units` | `undrivable-unit:<path>` | `owners.sh` over `identity.sh`, walking the two areas | the direction's assignee |
| `stalled-units` | `stalled-unit:<unit>` | `list-claims.sh` (`stale`) | the claim holder |
| `undelivered-units` | `undelivered-unit:<unit>` | `list-claims.sh` (`report_undelivered`) | the claim holder |
| `handoff-units` | `handoff-unit:<unit>` | `list-claims.sh` (`awaiting_verification`) + `declared-handoff-detail.sh` | the claim holder |
| `stranded-publications` | `stranded-publication:<number>`, `stranded-publication-stale:<number>` | `list-stranded-publications.sh` (`mergeability == content`; and `mechanical`/`clean` past `WORKAHOLIC_PUBLICATION_STALE_HOURS`, default 48) | the publication's author |
| `raced-units` | `raced-unit:<unit>` | `list-raced-units.sh` | the claim holders |
| `retire-claims` | `retire-blocked:<unit>:<refusal word>`, `stranded-unit:<unit>` | `list-claims.sh` (`superseded`) → `retire-claim.sh`; `ci-retirement-turn.sh` for what CI took; `list-claims.sh` (`stranded`) for the second key, which is asked and never acted on | the claim holder |
| `base-health` | `base-red:<commit>` | `attribute-base-red.sh` over `read-base-checks.sh` — **a `no_checks` tip is walked past** to the newest checked ancestor (2026-09-01, issue #785), and the verdict names which commit it rests on and how far behind; every other `unanswerable` stays terminal | the attributed merge's author |
| `drill-health` | `drill-failing:<drill>` | `read-drill-verdicts.sh` over `read-base-checks.sh` + the drill register | the shipping mission's assignee |
| `blocked-tick` | `blocked-tick:<tick-id>` | `log-read.sh` over the tick before last | nobody (a stopped tick is a fact about the repository) |
| `cadence-lapse` | `cadence-lapsed:<name>` | `cadence-state.sh` over the repository's declared cadences | nobody (the declaration names no person) |
| `operator-pulls` | `operator-pull:<number>`, `headless-pull:<number>` | `list-operator-facing-pulls.sh` + `publication-effect.sh`; `list-headless-pulls.sh` | the operator |
| `unanswered-asks` | `unanswered-ask:<channel>:<ts>` | the inbound sweep's own channel and window | the writer |
| `question-answers` | — | one `slack_read_thread` per `asked` question, at its recorded coordinate | records, files, stamps |
| `thread-reconcile` | — | `reconcile-candidates.sh` | the item's own thread |
| `standing-rulings` | — | `list-standing-rulings.sh` → `draft-standing-rulings.sh` | one pull request |
| `file-findings` | — | the classification table in `moderate/reference/workflow.md` | one `[FB]` issue |
| `closable-missions` | `mission-leftovers:<slug>` | `summary.sh` + `progress.sh` + `queue-size.sh`, then `close.sh` in a publish tree; the same scan also names a mission at **full acceptance with tickets still queued**, which is a question and never a close | the mission's owner |
| `unrecorded-missions` | `unrecorded-mission:<slug>` | `summary.sh` + `progress.sh` + `queue-size.sh` + the mission's own `## Changelog`, then one `branch-pull-request-state.sh` read | the mission's assignee |

Notes that carry rules:

- **A tick id read out of the log is an input, not a guarantee.** The log is append-only and carries whatever any tick ever wrote, so a sentinel section (`## 20260819-999999`) sorted last and was handed to `inbound-sweep` and `doc-drift`, which each converted it by the same copied, unvalidated substitution into `2026-08-19T99:99:99Z`. **Measured: seven days of blind ticks** — GitHub answered `422 The since parameter needs to be in ISO 8601 format` to every sweep while the step reported `ok` (the transport's exit status was swallowed by `|| true` and the error document was parsed as a row), and `git rev-list --before` matched nothing, so `doc-drift` answered `no_baseline` and never ran once. `moderate/scripts/lib/tick-iso.sh` is now the **one** derivation for both: a tick id that is not a timestamp widens the window to the tick's own day start and **the widening is named in the summary**, and a failed read is `degraded`/`gh_read_failed` rather than a finding. Nothing prunes the poisoned entry — the log is append-only, and a machine deleting a line it disliked is a worse failure than the one it would cure. `run.sh` carries each step's **`needs_agent` array** rather than its length, with `needs_agent_count` beside it, so `question-liveness.sh` can match a key inside the payload instead of answering `settled` for every key by construction.

- **`blocked-tick` is what notices the tick itself stopping** (2026-08-31, mission `stop-an-unattended-tick-from-waiting-on-a-person`). Measured: three consecutive ticks sat at `requires_action` waiting on a permission prompt raised by two **reads** of a plugin script, and the base carried no trace of any of them — `persist-log.sh` was the tick's *closing* act, so the record that would show a stop is the record the stop prevents. Two halves, and neither is worth anything alone: `run.sh` now **persists once immediately after `open-log`** (reported under its own `opening_persist` key and logged under `persist-log-opening`; the writer's contract is untouched, since it already unions by `(tick, step)`), and this step reads for an opening with no closing. **Closed means a `human-checkin` line**, not the persist's — `run.sh` writes the persist's own line *after* the push, so it never reaches the base on the tick that wrote it and a healthy tick would read as stopped. **It reads the tick before last, not the previous one**: a tick still running when the next starts also has an opening and no closing, and rather than tune a threshold the bound is structural — the subject has had a full extra hour to finish. The stated cost is that a stopped tick is named an hour later than the earliest possible moment; the measured failure lasted hours. It **says what is known and no more** — the reason is not on the base by construction, so the question names the tick, the steps it recorded and the last one it reached, and never guesses a cause. `log-read.sh` and `ask-question.sh` are byte-identical; a missing log area is `skipped` by name and an unreadable one `degraded`. Drilled offline by `verify-blocked-tick`.
- **`cadence-lapse` is the one step that watches ABSENCE** (2026-08-31, mission `notice-a-periodic-artifact-that-stopped-being-produced`). Every other step here is driven by an object that **exists** — an open pull request, a commit, a claim, a ticket, a record — so a producer that dies produces nothing and no step has anything to find; measured, a daily record stopped for four days while hourly ticks ran throughout and none of them reported it. What it may notice is only what the repository **declared**: `WORKAHOLIC_CADENCES` in `.claude/settings.json`'s `env` block, `<name>|<pattern>|<period>` (`;`-separated) and nothing else — no credential, no command, no schedule expression that would copy a routine's `cron_expression` — stated once in `workaholic:moderate`, *Where a cadence is declared, and what it says*, with the two rejected homes (an area `README.md`, a new `.workaholic/` area) and their costs. **Absent means nothing is declared**: the step reports `skipped` by name, asks nobody, supplies no event, and such a repository is byte-identical to one before the step existed. `cadence-state.sh` is the one reader and answers `current` / `lapsed` (with the age and the period) / **`unreadable`** with a named reason and a **null** age — never a zero, which reads as *produced this second*, and never `lapsed`, because a wrong `lapsed` sends a person after a producer that is working. The age is the newest **commit** that touched the pattern rather than a file's mtime, and that is a **measurement**: a routine's container is a fresh clone and git stamps every checked-out file with the checkout's own time (measured 2026-08-31 in a routine container here — 1843 files at the image's bake date, 481 at the freshen's, and a five-day-old moderation day-file showing that morning), so an mtime reading answers `current` for every cadence, always, in exactly the place it has to work; the caveat inverts and is stated rather than assumed, so a producer that writes **outside** git cannot be measured here and must not be declared. An unreadable cadence is `degraded` by name and asked about by nobody while any cadence that **did** read `lapsed` is still handed over — losing a question because a *different* cadence was unreadable trades one silence for another. It carries **no `condition-age.sh` reading**: its own answer is the condition's age off the commit, which is stronger than the age of the question, and the table pinned at four consumers does not move. Its finding is classified **`needs_ruling`**, because the reading says an artifact stopped and cannot say **why** — a routine switched off, a credential that expired, a producer that moved, or a declaration that is now wrong — and `note-cadence` loses the analogy because it names one workflow this repository owns and can fix. Drilled offline by `verify-cadence-lapse`.

- **`retire-claims` acts** and asks only about what neither executor could take: it hands every `superseded` row to `retire-claim.sh` (its **only** caller), and the question fires only once `ci-retirement-turn.sh` says the act did not take. Only `taken` and `pending` hold the question — `refused:<word>`, `unavailable` and `unreadable` all leave it standing, because an over-eager question beats a silently dropped one. `stalled-units` filters `superseded` and `awaiting_verification` out of its own candidates and counts them instead: **one step asks and the other filters**, and either half alone is a defect. **A `stranded` row is filtered there too** (2026-09-02): its tickets are archived and its branch still holds work, so it has been orphaned rather than stalled, and `retire-claims` asks about it naming the **files** the row carries (`stranded_files` / `stranded_file_count`, rendered by `list-claims.sh` for that verdict alone because the listing costs a second diff) with `condition-age.sh`'s reading beside them. **Since 2026-09-02 the filter reads the retirement path's own candidate set** — `stalled-units` subtracts every unit `list-retirable-claims.sh` names (composed, never re-derived from its four classes) and count it in their summaries, because asking a person about a branch the tick is about to delete is asking them to do the tick's own job; an **unreadable** retirement read filters nothing and is named, since a gate that cannot be read is not a gate.
- **`question-answers`** names every `asked` question with the coordinate `ask-question.sh --record-ask` wrote at post time (`lib/question-coordinate.sh`; a coordinate-less question reads a **named absence** rather than being searched for), reads at most `WORKAHOLIC_ANSWER_READ_MAX` (default 10) threads newest-first, records through `record-answer.sh`, files an answer that asks for something as one `[FB]` issue through `file-inbound-ask.sh`, and stamps the answer message with the catalog's `:ballot_box_with_check:` reaction. **No reply is posted for that event.** Nothing parses the answer; the filing and the stamp are never load-bearing; the dedup is structural (an `answered` question is not a candidate).

  **And once the loop has ACTED on the answer, one reply says what came of it** — `🧾 対応結果`, the catalog's sixth shape, no mention token, carrying the answer **as recorded** and its outcome. The reaction says *received*, which is not *acted on*, and nothing said the second thing: from the thread, an answer that became a merged mission and one that was read and dropped looked identical. It **narrows** the no-reply rule rather than dropping it — that rule was written against a *restatement*, right at recording time and wrong once something has happened — and the bounds are the narrowing: **once ever per question**, **after the act**, and carrying **facts the thread does not already have**. Only a `settled:` reading from `answer-outcome.sh` posts; `pending` and `unreadable:<reason>` post nothing and are counted, because an unread outcome rendered as a settled one would tell somebody their answer was acted on when nobody knows. The candidates come from the **same pass** the step already makes — the answered slugs it was already subtracting, minus those with a `human-checkin-outcome-<slug>` line — so there is no second walk, no second reader and no cursor; the pool shares the step's own `WORKAHOLIC_ANSWER_READ_MAX`, the holds are `✅ 解消を確認`'s and are **stated rather than recomputed** (a third copy of the clock gate is how three copies drift), held is not dropped, and a failed post is `outcome_post_failed: <reason>` that changes nothing about the recording, the filing, the stamp, the question's state or the reading. The two events keep **different emoji** on purpose.
- **`standing-rulings`** settles what the loop cannot decide itself (which direction an unattributed mission answers, which account an unmapped address belongs to). The judgement is the **run's**, passed as one `--judgement <subject>=<answer>` per candidate; an unjudged candidate reads `undecided` and reaches no writer. The draft lands as one pull request the seam refuses to auto-merge (`ruling_touching`), with the marker lines in the **body** (they ride `why`, the one argument the body template renders). **At most one open ruling at a time**, read off the open pull requests by `list-open-rulings.sh` — a **brake** keyed on the title on purpose, unlike `list-operator-facing-pulls.sh`, which derives membership from the seam's own refusal word — with no stored state; an unreadable brake drafts nothing. Once a ruling names a subject, that subject's hourly question is **held** through `ruling-suppression.sh` — keyed on the subject, never on the existence of a ruling (a partially covered residue still asks and says so; `overdue`/`dormant` are never held; an unreadable read holds nothing).
- **`file-findings`** turns a **`repairable`** finding into one `[FB]` issue through `file-inbound-ask.sh` (still the one filer), assigned to the running identity, carrying the direction through `ask-feedback-line.sh`. Which findings may take it is a **closed table** keyed on the step id, with **an unclassified step id reading `needs_ruling`**. A step reports a finding when it supplied an `event` or reported `degraded`/`blocked`; everything else is `left`, a count and never a list.
- **`thread-reconcile`** replies once into an item's own thread when that thread's last word is false — a `🟡 Handoff` thread whose unit merged, or a `🔵 Proposed` thread whose proposal was **closed unmerged**. **A merged `🔵 Proposed` posts nothing** (2026-09-01, issue #787): merging a proposal lands a record and a ticket set, so it is the moment the work becomes *queued* — the start of the item — and `🟢 Implemented` there asserts the opposite of what happened, with a second line that explains away its own lateness. Reported `proposal_merged_is_not_a_finish`; the real finish arrives when the work is driven. The merged form **reuses `🟢 Implemented`** (marked by its sentence, never a fifth colour); `⚫ Closed` is the genuinely new shape. Both name by whom and when, an unresolved one stated as unresolved. `reconcile-candidates.sh` is a pure **repository-derived** read (never a channel scan): the branch's merge commit diff, the branch-keyed ticket archive, and the story's `mission:` relation, resolved to stems through `unit-feedback-stems.sh`. Only a candidate the tree could not resolve costs one bounded read of that pull request's changed files. A candidate resolving to no stem is named `stems_unresolvable`.
- **`unrecorded-missions` is `closable-missions`' photographic negative** (2026-09-02, ticket `20260902065500`): acceptance `0/N`, nothing ticked, the queue still full, and the mission's own `## Changelog` recording **no archived ticket** — because the branch that would have recorded the work was **closed unmerged**. Measured 2026-09-01/02: three missions in that state with **17 queued tickets** between them, all three offered by the `/implement` survey, and the behaviour each asked for already on `main`, put there by a person's own single commit (#790/#789, #801/#800, #802/#800). Four tree terms gate one bounded pull-request read; only `closed_unmerged` is a candidate, while `merged` and `open` are **counted** and named by nobody. **The branch is resolved from two sources**, and the second is why the step works at all: `claim.sh` writes the mission's `claim:` field **on the claim branch**, so a branch closed unmerged never carries it to the base — all three measured missions read `active` with no `claim:` line — and the claim oracle's own row for the unit is read second. Neither resolving is **`claim_branch_unresolved`**, counted and asked about by nobody, never a candidate. It **closes nothing, excludes nothing and touches no claim**: what it establishes is that nothing *recorded* the work, never that the work is undone, and whether to close or re-drive is the assignee's. An unreadable pull request is `degraded` by name and never rendered as *nothing to close*, while every candidate the step did prove is still handed over.
- **Ages**: `undrivable-unit`, `retire-blocked`, `undelivered-unit` and `stalled-unit` attach `condition-age.sh`'s reading to their candidates through `lib/read-age.sh`, keyed on the key the step already composes, and the composer names it in the question — the reader's words **verbatim**, an unreadable age named as unreadable, an absent one not mentioned. `retire-claims` attaches it **after** its act; `operator-pulls` reads the pull request's `created_at` and the tick log not at all. **No step summary carries an age or a timestamp** — that would mark the step changed every tick by construction.
- `step-strategy-proposals.sh` was deleted. The two retired status roots (`🔧 Needs a decision`, `📦 Release Preparation`) stay retired: a status line addressed to nobody is noise whatever its dedup key.

### `/workaholify`

Running it leaves the repository prepared. Each apply takes exactly one confirmation, and **report-only is a named refusal's recovery path, never the ordinary outcome**.

1. **Web bootstrap** — `check-bootstrap.sh` (cloud sessions need `.claude/hooks/session-start.sh`, version-gated, or every routine stops at its plugin precondition), then `apply-bootstrap.sh`: one repair per named problem, one confirmation; `settings_unparseable` / `hook_source_missing` / `unwritable` refuse with **nothing written, the hook included**. **`index_merge_union`** joins them (2026-09-01, issue #780): the OKF bundle indexes are generated, so a branch that adds an artifact rewrites the same sorted region and every open branch then conflicts on it — measured as three open proposal pull requests, every open proposal there was, each carrying exactly one conflict and the same generated file in all three. The repair appends `merge=union` (a **built-in** driver, so no per-clone `git config` — the sessions that hit this run in fresh containers) and rewrites no existing line. Cost, stated: until the next `refresh-index.sh` an index can carry a duplicated or mis-sorted line, which the generator rewrites from the tree. **And a second, measured 2026-09-01 (ticket `20260901041500`): GitHub applies no `.gitattributes` merge driver when it computes a pull request's `mergeable`, so the union resolves the conflict for every *local* merge and for none of the remote's — the branch still has to be caught up and pushed before GitHub will merge it.** That is what `claim-mergeability.sh` now predicts (above); the driver keeps doing what it was added for.
2. **The remote's branch hygiene** — `check-repo-settings.sh` then `apply-repo-settings.sh`: `delete_branch_on_merge`, the **one** GitHub setting a workaholic mechanism reads. The claim oracle is *unmerged remote branches*, and in the shallow clone a cloud routine gets, a merged-but-undeleted branch reads as ahead and is offered as a live claim (measured 2026-08-04; 268 of 302 branches here on 2026-09-01). The check composes into the apply, one field is PATCHed, `not_admin` is its own refusal checked before the request, and an `unanswerable` reading never renders as conforming. The setting is **forward-only** for the *remote*, and that cost now has a backward-looking repair (2026-09-01): `list-retirable-claims.sh` names a `work-*` branch whose own pull request **merged** (`candidate_reason: pull_request_merged`) or was **closed unmerged by a person** (`pull_request_closed_unmerged`) as its own retirement candidate, beside the original `superseded_only`, read through `drive/scripts/branch-pull-request-state.sh` and acted on by `claim-retirement.yml`. **So the printed deletion command is the recovery path for the branches CI cannot take — a refusal's remedy — and no longer the only route.** Measured 2026-09-01: 30 unmerged branches, 22 dead, 17 with a merged pull request, and `superseded` reaching almost none of them because it is keyed on a **unit** and needs a claim commit, which a publish-tree publication never has; the printed command was 17 lines long and nobody had run it. Re-measured 2026-09-02 after CI had taken its turns: **7**, of which one is a fresh `closed_unmerged` candidate, two are open pull requests, two are live claims and two are the tick log's own refs. A **live** claim row beats a merged pull request always, and an unreadable pull-request read yields no candidate and its reason.
3. **`CLAUDE.md`** — audit, then `apply-claude-md-reference.sh` appends a reference to the gateway skill, never a copy of the rules, never rewriting the repository's own document.
4. **Layout** — `converge-layout.sh` runs `layout-doctor.sh`, applies the mechanical migrations (`migrate-todo-owners.sh`, `migrate-ticket-states.sh`, `migrate-assignee-aliases.sh`), re-runs the doctor and reports the delta. It **stages and never commits**; everything needing a judgment (a `retired-area`, a legacy nested `strategies/` tree) is **reported with the decision it needs, never guessed**. A converged repo produces an empty delta. It is also **the living-migration registry**: a structural change ships its `gather/scripts/migrate-*.sh` and its registration there in the same commit, checked by `test-workflow-scripts.mjs` (exclusions carry a reason; retired migrations are deleted, never excluded). It carries the **rename registry**'s two halves — applying every `area` row, reporting every `rename_conversions` entry with its ready-to-run command.
5. **Routines** — §5's list/diff/apply flow over **every** scope (`list-routine-templates.sh` unfiltered), reporting per routine what was created and what fields moved. What is genuinely unanswerable is said rather than implied. **The environment is resolved before any create** (2026-09-02, mission `make-the-routine-create-body-documented-and-buildable`): enumerated at run time in four branches — exactly one is used **without asking**, more than one is asked about by an interactive caller and refused by an unattended one, and none reachable or none at all refuses. That refusal is **`no_environment`**, the section's second named one, and it **renders no setup sheet** — the sheet is `no_transport`'s recovery path because that refusal means the account is out of reach, while a session that reached it is not helped by paste-by-hand instructions. Measured: the account has exactly one environment (`Default`), so the question `render-routine.sh` called "a question for the developer" had one answer, was asked of nobody, and the run simply stopped — with `no_transport`, the wrong word, as its only vocabulary. **One place builds the request body**: `build-routine-body.sh <template-id> <repo-url> <environment-id>` composes `render-routine.sh` (still the one frontmatter reader) and takes the id as an argument, so no script holds a fallback. Templates declare `sources:` as data, the renderer emits `*_json` twins beside the display spellings the setup sheet prints, and the sheet's output is byte-identical. The record's own shape is written down field by field, per field **observed or unverified**, in `workaholify/reference/routines.md` — including the create-vs-update `mcp_connections` asymmetry (a create cannot empty the set; `clear_mcp_connections: true` on a following update is what does) and the body **nesting**, which the builder reports unproven (`body_shape_verified: false`) because only a write followed by a read-back settles it.
6. **Identity mapping** — `audit-identity-coverage.sh` reports **three** named problems about one file: `identity_map_missing`, `identity_map_uncovered` (every `assignees:` value under `tickets/`, `missions/` and `strategies/` resolved through `identity.sh`, each uncovered address named **with the mapping line that would cover it**; `assignees: []` is never one), and — since 2026-09-02 — **`identity_map_no_entry_for_account`**, the lookup the web bootstrap's step 0b actually performs: no entry for the **GitHub login this session runs as**. The third is not implied by the second, and that is the measured state the loop failed in — a mapping naming every address the tree uses can still leave a routine's own account unmapped, and the container then keeps `noreply@anthropic.com`. The login is **passed in** (`--account`, resolved by `check-bootstrap.sh` through `gh api user`, bounded and non-fatal) so the audit stays a pure local read, and the check is **three-valued**: no login reads `checked: false` with a named reason and raises nothing. `apply-bootstrap.sh` scaffolds the file's header when absent and appends each proposed line as a **comment** — it proposes and never decides, with the account line's placeholder on the address side because there the login is the known half. **None of the three gates `ok`** (the `identity_map_missing` ruling deferred in 2026-08-26 was made 2026-09-02: its scaffold carries no entries, so a gate would go green on a file that leaves step 0b exactly as dead); all three ride an `advisories` field.
7. Confirm the working-directory guard.

### Routine convergence (`/setup-dev-routines`, `/setup-repo-routines`)

One job with one named failure mode, never two branches. Attempt it every time through a `RemoteTrigger`-family tool: list the account's routines, diff each against its template (name / prompt / model / `cron_expression` / `autofix_on_pr_create` / connectors / **`notifications`**, where **absent is a value** — the template's own rule is *absent means off*, so an omitted field corrects a live routine that has a notification on, and a type refusing the parameter is reported by name rather than dropped), apply create/update to converge, report per-routine changes; no questions, and never framed as luck. Every report line also names that routine's **enabled state** — read and reported, never converged. **Two named refusals, and only one renders a sheet**: `no_environment` (the environment could not be resolved — see `/workaholify` §5 above) reports and stops. **No transport reachable** (the routine-fired class; the session-only `CronCreate` family cannot touch an account routine): report `no_transport: RemoteTrigger-family tool`, then render the copy-paste setup sheets for that scope (`render-setup-sheet.sh --all <repo-url> <scope>`) **as that refusal's recovery path**, with the preconditions (Slack channel probe — `checked: false` is never "does not exist"; web bootstrap) and what cannot be verified. The account-management surface (digest gate, drift/fleet reports) stays retired.

### Loops

**One session, one loop, and the tick never waits** (2026-09-03, the developer's instruction). `/loop 5m /infinite-development` in a single session: each tick reads the inbound Slack channel and answers on it, spawns `propose` and `implement` as **background `general-purpose` subagents**, and ends. The main agent owns **Slack and nothing else** — a person's steering is answered in seconds while the work runs beneath it, which is exactly what an inline run could not do. **The tick reads its own checkout first, because nothing else in the loop will** (2026-09-03): `/implement` drives in a claim worktree and `/specificate` writes through a publish tree, both leaving the caller's checkout byte-identical by design — so no part of the loop ever looks at the tree it runs in, while a subagent **reads the plugin out of that tree**. Uncommitted lines there are not pending work but behaviour the loop is already executing, on no base and reviewed by no pull request. One `git status --porcelain` before anything else; a dirty tree is named in the report as `checkout_dirty` with the file count and what it means, a clean one is not mentioned, and it **blocks nothing and commits nothing** — the tree belongs to a person, half a change is the normal state of one, and a loop that commits what it finds lying around is a worse failure than the one it would cure. Measured: this command's own first version, the retirement it performed and the environment declaration beside it sat uncommitted through every tick of the loop's first hour, driving it the whole time.

**The `ListAgents` listing is the whole record and there is no store**: a loop whose subagent is still `running` is not spawned again, and an `idle` one — a finished run — is **reaped with `TaskStop` immediately before the spawn**, never at the finish. Reaping at the spawn is what makes the idle agent double as each loop's **clock**: `started N ago` is when that run began, so an agent younger than its loop's cadence is left standing and reported `not_due`, an older one is reaped and respawned, and an empty listing means every loop is due. Two facts were measured on 2026-09-03, the loop's first hour: three ticks left three idle `propose` agents standing (the third could not take its own name and was spawned as `propose-3` — roughly a hundred corpses a working day, inside the very listing the concurrency rule reads), and those same three ticks were three full agent runs every one of which answered `work_waiting` / `nothing_in_hand` and wrote nothing. **`implement` therefore runs every tick** (there is always more of its work, and the claim protocol refuses what is taken) while **`propose` runs on a cadence** — `WORKAHOLIC_PROPOSE_CADENCE_MINUTES`, **absent means 15**, `0` means every tick — because its answer is a function of a queue that moves only when `/implement` lands something or a person writes an ask. A change-detector was refused by name: *has the queue moved* is a second derivation of the ladder `/propose` already owns. `moderate` is a third subagent on a **30-minute** gate read from its own tick log (`log-read.sh`) rather than from the listing, because its acts are hourly by nature; an unreadable log spawns it.

**The Slack turn and the inbound sweep are the tick's, not `/propose`'s** (they moved 2026-09-03): read the channel over `WORKAHOLIC_SLACK_TURN_WINDOW_MINUTES` (default 10), answer a question in its own thread with `💬` (read the thread first; post nothing if a reply of ours already follows), file every **ask** through `file-inbound-ask.sh` at once and post the `📥 受理` receipt with its `:inbox_tray:` reaction, react `:eyes:` to the rest. The scripts stay in `skills/propose/scripts/` — moving them would be churn — and `commands/infinite-development.md` is the one place their use is specified. The `only_the_loop_spoke` brake still holds, but the reading is now **handed in** to the propose subagent rather than taken by it.

**And the tick announces what finished** (2026-09-03, mission `announce-an-ask-that-landed-outside-a-unit-route-in-its-own-thread`): `🟢 Implemented` is a **per-unit** post of `/implement`'s route step, so an ask whose work landed through a session working it directly reaches no route step and its thread ends at the `📥 受理` receipt — measured 2026-09-02, three merged pull requests and a closed issue that the operator learned of by asking a session. `propose/scripts/list-unannounced-closed-asks.sh` names the candidates from the **repository and the issues alone** — never a channel scan, one listing call plus, per candidate, one timeline read and one single-pull GET per merged cross-reference (capped by `WORKAHOLIC_ANNOUNCE_LANDED_MAX`, default 5) — and carries `landed[]` beside `closed_unmerged`, because *a person closed this* and *this merged* are different sentences. The tick resolves each item's thread by the `fb:<stem>` exact string, **reads it first**, and replies once with the catalog's shape (`🟢 Implemented`, marked by its sentence and **not** a fifth colour). **The dedup is the thread itself** — no ledger, no cursor, no field on any artifact — and the tie goes to silence: an idle tick reports `no_candidates`, an unreadable read reports the reader's own reason and is never rendered as `no_candidates`, an unresolved or ambiguous thread is left alone, and a candidate whose `landed[]` could not be read is `held` rather than announced with an invented name. The shape and its bounds are byte-identical in `notify/reference/notifications.md` and `commands/infinite-development.md`, pinned by the suite, and drilled offline by `verify-announced-asks`.

**The three-tmux-session premise is retired and must not be reintroduced**: `/spawn-loops`, `spawn-loops.sh`, `loop-status.sh`, `stop-loops.sh`, the loop table, one clone per loop, the `~/.claude.json` trust-dialog write and the `no_tmux` refusal are **deleted**. Measured, the split was the defect: the propose loop reported `work_waiting` every five minutes for hours while the implement loop reported nothing claimable — each correct in isolation, neither able to see that five pull requests had sat conflicted since the previous day. Three sessions meant three places to look and no place that held the whole loop. **Sessions still run with permission prompts off** (`--dangerously-skip-permissions`): an unattended run never waits for a person, and an allowlist would have to enumerate every read the loop will ever make (issue #865). Full record: `workaholic:loops`.


### Routines (the fallback)

Claude Code Web routines, from the templates in `skills/workaholify/routines/`, each declaring its own **`scope:`** — **the fallback premise** for a machine nobody runs a session on (see *Loops* above). The two premises must not run against one repository at once:

| Template | Routine | Scope | Cron | Configured by |
| -------- | ------- | ----- | ---- | ------------- |
| `propose.md` | `[Propose]` (runs `/propose` then `/specificate`) | `developer` | `15 * * * *` | `/setup-dev-routines` |
| `implement.md` | `[Implement]` | `developer` | `30 * * * *` | `/setup-dev-routines` |
| `moderate.md` | `[Moderate]` | `repository` | `50 * * * *` | `/setup-repo-routines` |

**Scope semantics**: `developer` means one copy per developer, per repository; `repository` means exactly one for the whole repository, created by a designated person or a project/service account. The scope lives on the template so both commands and both setup sheets read one source (`list-routine-templates.sh [<scope>]`, `render-setup-sheet.sh <id|--all> <repo-url> [<scope>]`). The repository scope is a **stated convention the plugin cannot enforce** — a routine is an account-level record no other account can list. `/specificate` and `/propose` act only on artifacts assigned to the running identity, which is what keeps them `developer`-scoped.

**Two turning routines, one loop** (2026-09-02: `[Specificate]` was absorbed into `[Propose]` — the FB ingestion happens inside Propose): `[Propose]` `:15` runs `/propose` then `/specificate` in one session — the ask it supplies is in the inbox its own discovery reads seconds later — and `[Implement]` `:30` drives it, so the loop is Propose → Implement and the ask-to-drive distance is fifteen minutes where the three-routine loop took fifty. At `:15` the propose half judges against everything up to the previous hour's driving, the same freshness the retired `:40` slot bought. What a person supplies is a direction, not a ticket.

**Template rules**:

- A template is a thin pointer: **the prompt names the command and the load fallback, and nothing else** (2026-09-01) — and **the command carries what the run must read, rather than naming where to find it** (2026-09-02, ticket `20260902043747`). Naming the shapes was half the principle: an instruction to perform *the `workaholic:notify` lookup* hands a routine session a name to resolve, and resolving one at run time is what parked the routines — a shell read under the plugin cache is a permission prompt an unattended run cannot answer. A rule the run must **read to act** is therefore inlined into the command **byte-identical** to its source and pinned in the suite beside the post formats; a **provenance citation** stays a citation, since nothing is opened to act on it. `/implement` and `/specificate` carry the stateless thread lookup verbatim; `/propose` and `/moderate` do not, because neither performs the search — a later contributor adding one is making a decision, and the suite asserts the current answer in both directions. Text too large to inline is **reported as unscoped**, never inlined as a summary: a paraphrased ceiling is a third version of the rule. Every runtime rule — the post shapes, their transports, what may be read — lives in that command's own notification section (`plugins/workaholic/commands/{implement,specificate,propose,moderate}.md`), because a routine record is account-level and a rule written into a prompt reaches a fleet only by being re-pasted into every copy; **the command is the ceiling** (`workaholic:notify`). Every other rule stays in the skill that owns it (`workaholic:drive`, `workaholic:ship`, `workaholic:notify`, `rules/`). Post formats are byte-identical to `notify/reference/notifications.md`'s copies and pinned against drift by `test-workflow-scripts.mjs`; a prompt carries **no substitution at all**, and **no two templates may render the same `name:`** (also pinned).
- **A routine declares no environment variables at all** — it *selects* a cloud environment (`job_config.ccr.environment_id`) and the variables live on that record, which is **account-level** and shared by every session and routine selecting it. A per-repository value therefore lives in the repository's own `.claude/settings.json` `env` block, which is why routine prompts stay byte-identical across repositories. Stated once, with its evidence and limit, in `skills/workaholify/SKILL.md`, *Where a routine's environment variables live*.
- The API's minimum interval is one hour and a bare `:00` minute is rewritten to server jitter, so explicit non-zero minutes are used. The API stores a bare cron with **no timezone field**, so a JST-morning cadence is expressed in UTC.
- `autofix_on_pr_create` (at `job_config.ccr.session_context.autofix_on_pr_create`) is `true` on the developer routines. `[Propose]` declares `mcp: [Slack]` — the connector reads the channel for the inbound sweep and posts that sweep's receipt — and is the only template declaring `notifications: push`.
- **`renamed_from:`** is convergence's **input** for a rename, not an operator obligation. Matching is by rendered **name**, so a template whose `name:` moved cannot recognise the routine that should become it; `renamed_from:` supplies that link, and the run writes the new name through the API's `update`, which **renames in place** — same routine id, same `created_at`, no duplicate (measured 2026-09-01 against a live record: 15 routines before and after, no routine left under the old name). A session with **no transport** reports `no_transport` and the manual cutover rides the setup sheet as that refusal's recovery path — `render-setup-sheet.sh` still renders the field as the sheet's first note. The field is **deleted once the fleet has cut over**. **A swap is still ordered**: an in-place rename is one `update` per routine, so two templates trading names hold one rendered name twice between the calls. A **reclaimed** name owes the operator the opposite instruction: delete any routine still rendering it before converging.
- **Nothing is deduped by a routine's name** — the Slack keys are searched, never a heading or a routine name.

**Retired, and not to be reintroduced**: `[Consent]` (a human-merged pull request is announced by nobody), `[Workaholic]` and the whole `user` scope with `/setup-user-routines` (no `RemoteTrigger`-family tool is exposed to a clock-fired container, so it converged zero routines on every tick of its life; convergence runs from the two setup commands in an interactive session, where the transport measurably exists), `[Prepare Release]` (merged into `/moderate` as two steps; its `📦 Release Preparation` root retired rather than carried over), `[Standup]` (integrated into `/moderate`'s `strategy-digest`), and `[Specificate]` as a standing routine (absorbed into `[Propose]` on 2026-09-02 — the merged routine runs `/propose` then `/specificate` at `:15`; the `/specificate` command itself survives, run by that routine and by any human on demand).

**A routine cannot subscribe to a repository event** (the API's trigger surface is `cron_expression` / `run_once_at` / API token only), so `[Propose]`'s `/specificate` half discovers its own asks. **No trigger narrows to a person**: the data decides ownership (`owned_by_other` at `/implement`'s survey, `not_mine` at `/specificate`'s input).

#### Release notes and deployment plans

- **The per-target release notes are kept current by CI, not by any tick.** A routine's container cannot write a release by any transport (`gh release` is GraphQL-refused; REST answers *"Creating, editing, or deleting releases is not permitted for this session type"*), so the writer is `.github/workflows/release-note-draft.yml`, which holds `contents: write` and defines its own checkout (`fetch-depth: 0` + tags) rather than inheriting a container's refs. `run-note-cadence.sh` requires `--write`, passed by that workflow and by nothing else. The home is a per-target **GitHub draft release** — outside git, so the draft cannot count itself. Neither store is the source of truth: **the derivation is** (`draft-release-note.sh` over the base state), and `sync-release-note.sh` writes the non-authoritative side as a projection, never a merge, reporting every divergence and never overwriting a *published* release. It refreshes at most once per `Asia/Tokyo` day (a floor derived from the draft release's own `updatedAt`, never a stored cursor), plus immediately whenever the release stage advances (`draft` / `staging` / `confirmed`).
- **The reading half freshens itself**: `report-deploy-status.sh` — the one entry the tick reaches — freshens the base branch and tags before reading them, bounded and never fatal, reporting `refs` / `refs_reason` / `doubtful`. `read-deploy-state.sh` stays a pure local read.
- **The renderer derives the facts; an agent's plan arranges them.** `draft-release-note.sh --plan <path|->` renders a plan's grouping, order, risk notes and held-back set **over** the facts it derives; with no plan the output is byte-identical to the pre-seam render. A plan (`skills/ship/reference/release-plan.md`) may never invent a change, drop a merge or move a derived fact: an unnamed merge renders under *Not arranged by the plan*, a reference outside the range is named rather than fabricated, a plan whose `base_sha` is not the rendered base is shown as **stale**. Its home is the caller's — no well-known path, never inside git. A plan that cannot be applied is not applied.
- **The judgment runs in CI, beside the writer**: `plan-release.sh` authors each due target's plan in the same workflow run and hands it to that run's cadence (`--plan-dir`). The planner command is pluggable (`WORKAHOLIC_PLANNER_CMD`, default `claude -p`), the planner steps are skipped when `ANTHROPIC_API_KEY` is unset, every non-arrival is named (`no_planner` / `planner_failed` / `invalid_plan` / `empty_range` / `no_renderer`), and **the note says on its face** when a plan was expected and did not apply.
- **A target's note carries the whole release life**: `## Deployment Plan` → `## Releases` → `## Deployment Verification`, the last two derived by `read-release-history.sh` (a derivation, not a copy — no third store; `record-evidence.sh` / `confirm-release.sh` remain the single writers of what they own). A release window is **repository-wide**, so a batch confirmation never reads as "this target was checked", and `fail` / `not_run` / `bypassed` render exactly as loudly as `pass`. An empty stage says so in words.
- **A story-less merge keeps its substance**: `## Key Changes` prefers a branch story, falls back to the merge body's pull request title, and adds what that merge **published** as sub-bullets (`resolve-merge-substance.sh`): the feedback record (`Asked for: …`), the mission it planned, the tickets it queued. Local git plus base-tree reads only (`--enrich` stays off by default); neither the `feedback:` nor the `mission:` relation gains a second parser.
- **The deployment-plan refresh rides `[Implement]`'s tick**: every `auto` unit reaches `/ship`'s drafting phase, which refreshes that unit's plan inside the unit's own pull request. Nothing commits to `main` outside a merge, the refresh is idempotent (`draft-deploy-plan.sh` → `changed: false`), and a degraded read is reported and skipped, never half-written. Stated limit: a repository whose units are all `review` sees that plan refreshed only when an `auto` unit ships.

#### Drills

`scripts/e2e/loop-drill.sh` drills the loop's own mechanisms (`seed` / `status` / `reset` / `verify-*`). **`verify-all`** is the aggregate verb, deriving what it runs from the dispatcher's own `case` arms plus the **drill register** (`docs/loop-drill-runbook.md` §9 — one table, one reader, `drive/scripts/drill-register.sh`) and emitting one verdict per drill in the drill's own vocabulary (`pass` / `fail` / `skipped:<reason>`), exiting non-zero **only** on a real failure. `.github/workflows/loop-drills.yml` runs the **hermetic** part on every push and pull request with no credential and no permission beyond the default read, **one matrix leg per drill** so the check run that goes red is named after the drill — which is what lets `/moderate`'s `drill-health` step name it. A drill with no `bearing: "breaker"` row is **`unproved`** and counted outside the passing total; a drill the register does not classify is `skipped:unclassified`, which `test-workflow-scripts.mjs` fails on. Every drill carries a **breaker row written against the behaviour**, not a return shape. Operator procedure, cron race windows and failure-reason→file blame tables: `docs/loop-drill-runbook.md`.

## Development Workflow

1. `/ticket` — write specs (published behind a PR; merging queues them) — or let `/propose` supply the ask from a strategy and `/specificate` turn it into specs
2. `/drive` (attended) or `/implement` (unattended) — implement each spec
3. `/story` — story + PR
4. `/ship` — merge, deploy, verify

## Type Checking

No build step required — this is a configuration/documentation project.

## Local Verification

Before pushing changes to workflow scripts or plugin manifests:

```bash
node scripts/build-plugins/build.mjs              # regenerate outputs/ AND hooks/policy-index.md
node scripts/build-plugins/verify.mjs             # assert generated skills are self-contained AND the policy index is in sync
node scripts/build-plugins/validate-metadata.mjs  # assert Codex manifests are well-formed and version-aligned
node scripts/test-workflow-scripts.mjs            # hermetic smoke tests for branching + drive scripts
bash plugins/workaholic/hooks/layout-doctor.sh .  # audit .workaholic/ for an unregistered artifact directory
sh scripts/e2e/loop-drill.sh verify-all           # run the classified drill set; CI runs the hermetic part on every push
```

The smoke tests create throwaway repositories under the OS temp dir, never touch the working tree, and never call `gh`/network.

## Enforcement gates

- **Commit trailers** (`skills/commit/SKILL.md`, *The trailer block*): `commit.sh` is the one writer and callers stay trailer-agnostic — `Category:` when `--category` was passed, `Claude-Session: https://claude.ai/code/<id>` whenever the environment carries `CLAUDE_CODE_REMOTE_SESSION_ID`, and `Co-Authored-By:` always. The trailer says which **run**, never which routine — nothing in the container names the standing routine record. Outside a cloud session the session id is omitted, never faked. The **author email is deliberately untouched** (`lib/claims.sh` resolves claim ownership and resumption from it); the web bootstrap sets a repo-**local** `user.name` (guarded on `git config --local user.name`, preferring `gh api user --jq .name`) since GitHub renders the name. A developer's own repo-local name is never overwritten; the repair is forward-only, and the name stays cosmetic to every mechanism.
- **A commit that would be half a rename is refused**: with default staging, a **staged deletion co-existing with an untracked file** is what half a convergent migration looks like. `commit.sh` refuses, names both halves and the concrete repair, commits nothing, and leaves the working tree untouched (no `git reset`). Untracked files alone never trigger it, and it never fires when the caller already named `files...`. **A caller that has just written a NEW file passes it in `files...`** — stated in `workaholic:commit`, referenced by `/story` Phase 4 and `/drive` §5.
- **Commit subject** (present-tense, ≤50 chars, no `feat:`/`[bracket]` prefix — `skills/commit/SKILL.md`): canonical validator `skills/commit/scripts/check-subject.sh`, run by `commit.sh` itself, by `archive.sh` **before it moves the ticket**, by the `PreToolUse` hook `guard-git-commit.sh`, and by the opt-in git-native `hooks/git/commit-msg` (install with `sh ${CLAUDE_PLUGIN_ROOT}/hooks/install-git-hooks.sh`; bypass one commit with `--no-verify`). One rule source; the layers cannot drift.
- **An embedded jq program that does not compile is our defect, never an empty answer** (`rules/shell.md`). These scripts carry readings as jq programs inside single-quoted shell strings under `… | jq -c '…' 2>/dev/null || echo '[]'`, which is right for a data problem and catastrophic for our own; the two are told apart by **jq's own exit status** (3 = compile error, 5 = runtime/input), so every existing fallback keeps its data behaviour. Two halves: at **run** time `skills/moderate/scripts/lib/jq-guard.sh` — a shadowing `jq` function sourced by every script in that skill that embeds a program, calling `command jq`, capturing nothing, redirecting nothing, inert when nothing is listening — *records* the fact, and `run.sh` is the **one** place that reads the record and reclassifies the step `degraded` with reason `jq_compile_error` (`needs_agent` is deliberately not zeroed). At **build** time `test-workflow-scripts.mjs`'s `every embedded jq program compiles` row extracts every `jq` in command position across `plugins/workaholic/`, `scripts/` and `hooks/`, redeclares each `--arg`/`--argjson` name, and compiles with `jq -n` against closed stdin, failing on exit 3 alone; programs built by string interpolation cannot be extracted and are **counted in the assertion's own name**. The `2>/dev/null` is not removed — the fix is to classify, not to shout.
- **Branch names**: exactly two literal patterns — `work-YYYYMMDD-HHMMSS` (named only by `branching/scripts/create.sh`) and `release/YYYYMMDD-HHMMSS` (named only by `cut-release-branch.sh`); `guard-git-branch.sh` blocks everything else.
- **One behaviour per command**: no command's behaviour depends on the first word of its argument (bare-versus-argument is a scope and is kept; `/fb`'s cross-repository mode routes on destination, not a first word). Retired forks: `/mission close` → `/mission-close`; `/mission summary|approve`, `/ticket summary` dropped; `/drive auto|night` → `/implement`.
- **AskUserQuestion**: the label hook enforces the `[<label>]` prefix; *whether* to ask is governed by `rules/interaction.md` — the **Recommended-label test**: if an option could honestly be marked "(Recommended)", don't ask; decide, record, let the developer veto. An unattended run goes one mechanism wider (*An unattended run never waits for a person*): it never blocks on a prompt of any kind, and **composes only commands an allowlist can name** — a command that cannot be allowlisted is a prompt the run has chosen to raise. Two instances of that axis: what the run reaches for (a read tool, never a Bash text pipeline) and how it spells it (the path in full, reader first, no assignment prefix — `rules/general.md`'s ceiling over `rules/shell.md`). **The first names a shape, not only a principle** (2026-09-02, ticket `20260902043747`): a skill section, a reference file or a command body **under the plugin tree** is read with the **Read tool**, and `sed`, `grep`, `cat`, `head`, `tail` or `awk` over that path is the shape that parks the run — the container classifies a path under `~/.claude` as Claude's own configuration, so a shell read of it prompts and an unattended run has nobody to answer. Measured as `sed -n '/One thread per feedback item/,/^## /p' $S/skills/notify/SKILL.md | head -50`, which no command body contains: the session composed it to resolve a by-reference instruction. The rule's home is `rules/shell.md`, *A skill or reference file is read with the Read tool, never with a shell*, and it is carried into the four routine-fired command ceilings because **a ceiling outranks a general document read earlier**; the suite pins that each of them carries it. **No hook blocks the shape** — a `PreToolUse` deny would turn a prompt into a mid-run refusal, which is a different failure. Prose, not a check, for the composition itself: it happens at run time and never appears in this tree's markdown.
- **Repository confinement**: every write lands inside this repository or its worktrees; the only crossing is `/fb`'s issue-opening mode with its verbatim human confirmation. Do not grow the confinement hook toward content matching — vocabulary is a human judgment. The rule only reaches repos that install the plugin; enable it user-level (`~/.claude/settings.json`).
- **Every pull request this loop merges is squash-merged, and the method is one derivation** (2026-09-01): `gather/scripts/merge-method.sh` answers it and the four call sites (`ship/scripts/merge-pr.sh`, `branching/scripts/publish-tree-pr.sh`, `drive/scripts/retry-undelivered.sh`, the `review` route in `workaholic:drive`) **read it, never spell it** — `test-workflow-scripts.mjs` fails on a literal `merge_method=` at a call site. **A commit is a change to the development target**: measured on a consuming repository's `main`, one day, 275 commits — 138 touching only `.workaholic/`, 25 empty, 44 merges, **5 touching only the product**. The claim commit, the heartbeat and a unit's story are correct where they live and are not facts about `main`; a merge commit carried every one of them over verbatim. **The cost is stated, not hidden**: a unit's per-ticket commits collapse into one, and that granularity lives on the pull request, in the branch story and in `archive/<branch>/`. It is **safe** only because `superseded` is derived from the **tree** rather than from ancestry (a squash-merged branch is never an ancestor of the base), and **bounded** only because `delete_branch_on_merge` removes the branch at the merge — which is why the two shipped together.
- **GitHub over REST only** (`rules/shell.md`): every workflow script reaches GitHub through the one transport `gather/scripts/gh-rest.sh` (`slug` / `api` / `available` — the last probes `GET /rate_limit`, which every token type can call including a workflow's installation token; a caller needing a **person** calls `gh api user` itself and answers `identity_unresolved`, and `available`'s `login` is vestigial). Never `gh issue …`, `gh pr …` or `gh repo …`, which are GraphQL-backed and which a Claude Code Web session may 403 mid-run. A bound session refuses `search/*` too, so slug matching uses repository-scoped endpoints and filters locally. `gh release …` is REST-backed and stays. **One qualification**: a REST merge answered `403 "Merging pull requests is not permitted for this session type"` gets `merge_reason: session_type_cannot_merge` (its own rung beside 405 and 409, keyed on the message with a generic `merge_forbidden` behind it), and **that one refusal may be retried through `mcp__github__merge_pull_request`** — a second attempt behind REST, never a replacement, for the one act a script cannot perform at all. Reads, writes and pull-request creation stay REST; the step moves into the calling agent, which reports both outcomes by name. `node scripts/test-workflow-scripts.mjs` fails on any non-comment `gh issue|pr|repo <verb>` under `skills/` or `hooks/`; its allowlist is empty on purpose.

## Release-safety scan

`skills/release-scan/scan-branch-safety.sh` is a deterministic, script-only gate over `git diff <base>..HEAD`, run by `/story` (warn) and `/ship` (block). Three rule families, each finding citing `file:line` + rule:

- **`secret`** (hard, never overridable) — `lib/secret-patterns.sh`: pass 1 unmistakable key shapes; pass 2 matches on the **value** (quoted-alphanumeric or line-ending bare run = literal; identifiers, calls, env reads, HCL `var.`/`local.`/`data.`/`module.` references, templates, annotations are subtracted). Generated paths are exempt from pass 2 (the `Outputs Freshness` CI carries the residual risk).
- **`size`** (overridable) — per-file/branch ceilings plus a per-commit changed-lines rule (`lib/commit-size.sh` — read its header before changing it; it exempts binary rows, lockfiles, generated paths, `.workaholic/` prose, files already over the per-file ceiling, and merge commits, and counts additions only).
- **`leak`** (confirm) — re-introduction of terms from the git-ignored, developer-maintained `.workaholic/leak-denylist`. Read its scope literally: it matches listed terms in the branch diff, nothing more; a `pass` never means "no client context here". The "never name other repos/clients" convention is enforced by confinement + the crossing flow's human confirmation, not by this scan.

The **severity tier** tells the consumer how hard to block; both consumers key on it, never on the binary verdict alone. A branch whose only findings are `override`-tier stays releasable (`gate-decision.sh`'s `override_only`).

## Version Management

Version bumps are manual (no `/release` command). After bumping and pushing to `main`, CI (`.github/workflows/release.yml`) publishes the GitHub Release.

Version files (all at the same semver; `.claude-plugin/marketplace.json` is the source of truth):

1. `.claude-plugin/marketplace.json` — root `version` AND every `plugins[].version` (workaholic, workflows)
2. `plugins/workaholic/.claude-plugin/plugin.json`
3. `plugins/workaholic/.codex-plugin/plugin.json`
4. `outputs/workflows/.codex-plugin/plugin.json` — generated; rebuild with `build.mjs`, never hand-edit

Bump PATCH by default, update 1-3, regenerate 4, commit as `Bump version to v{new_version}`.

**A bump is checked against the base twice, because the base moves** (2026-09-02, ticket `20260902135525-bump-the-version-against-the-base-not-the-branch`). `check-version-bump.sh` answers **two** questions — `already_bumped` (did this branch make a bump commit) and **`version_ahead`** (is the number it landed on still greater than the base's, compared field-wise so `1.0.9 < 1.0.10`, three-valued with `null` for an unreadable manifest). `/story` Phase 0 bumps unless **both** hold, and `/drive` §6 re-reads immediately before the merge and **re-bumps** when the base has overtaken the branch. **Measured**: five consecutive merges carried two versions between them (`1.0.285` three times, `1.0.284` twice) — every branch had bumped correctly against *its own* base, and each later squash then carried a bump commit whose content the base already held, so it produced no version diff and nothing reported it. Since the loop's subagents run beside the developer's own sessions, concurrent branches are the normal state. **It is a repair, never a gate**: the merge happens either way and a failed re-bump is reported by name, because a version collision is a bookkeeping fact rather than a defect in the work. **The already-collided numbers are recorded and left**: `1.0.284` and `1.0.285` each name more than one tree, and re-cutting published releases to tidy that is a worse act than the one it would cure — the next bump names the current `main`.
