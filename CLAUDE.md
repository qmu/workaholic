# Workaholic

Private, cross-agent distribution of structured development workflows and engineering standards. It is richest on **Claude Code**, where it installs as a plugin marketplace (slash commands, hooks, the always-on policy and mission lenses); the portable skills also ship to **Codex** (via `.agents/plugins/marketplace.json` → `outputs/workflows`) and to **OpenCode** plus 40+ other agents via the Agent Skills standard / `skills` CLI. Authored source lives under `plugins/`; cross-agent artifacts are generated into `outputs/`.

## Important

Edit `plugins/` not `.claude/`. This repo develops plugins - changes go to `plugins/`, never `.claude/` unless explicitly requested.

**Update the docs in the same change.** When a change alters behavior, structure, commands, skills, or conventions, update every affected document — `README.md`, `.workaholic/README.md`, `CLAUDE.md`, `plugins/workaholic/rules/*.md` — in the same commit as the change. Outdated documentation is a defect: before requesting approval for any change, check whether the docs that describe the touched area still tell the truth (`/report` runs `doc-drift.sh` as a backstop, but the fix belongs at change time, not report time).

## Project Structure

```
.claude/                 # Local Claude Code configuration
  rules/                 # Repository-scoped rules
.claude-plugin/          # Marketplace configuration
  marketplace.json       # Marketplace metadata and plugin list
plugins/                 # Plugin source directory
  workaholic/            # The single plugin (no dependencies; skills exposed cross-agent)
    .claude-plugin/      # Plugin configuration
    .codex-plugin/       # Hand-maintained Codex-facing manifest
    skills/              # workflow skills (branching, catch, check-deps, commit, create-ticket, discover, drive, explain, feedback, gather, mission, okf, propose, release-scan, report, review-sections, ship, system-safety, validate-writer-output, workaholify, write-release-note) + policy skills (planning, design, implementation, operation, safety, development, each linking English hard copies under its policies/ dir)
    commands/            # ticket, drive, implement, commit, propose, fb, report, ship, mission, mission-close, catch, explain, workaholify, setup-routines (Claude-only; ignored by other agents). `fb.md` is the feedback command — abbreviated because Claude Code ships a built-in `/feedback`; only the trigger is short, the artifact/skill/corpus stay "feedback"
    hooks/               # ticket validation (validate-ticket.sh, PostToolUse Write|Edit — frontmatter, location, the mandatory `## Policies` / `## Quality Gate` body sections, a resolvable `mission:` relation, and the remaining-only lint on `resume-*` tickets, all on the `todo/` queue only) + mission validation (validate-mission.sh, PostToolUse Write|Edit — Experience/Acceptance floor on **any** mission under `missions/active/`, keyed on the area rather than a status word since `draft` was retired; ownership is not required; an end state written into active/ is exempt; archive/ never retro-blocked) + story/trip OKF-`type` floor (validate-story.sh / validate-trip.sh, PostToolUse Write|Edit — a NEW write must carry `type: Story` / an allowed trip `type`; `trips/` has no writer since 2026-07-28, so validate-trip.sh now only guards a hand-authored edit to that legacy tree; history is grandfathered by git-tracked-ness so predating artifacts are never retro-blocked; the release-note writer already emits its `type`, so it needs no hook) + feedback validation (validate-feedback.sh, PostToolUse Write|Edit — schema floor on NEW `.workaholic/feedbacks/` writes: filename pattern, `type: Feedback`, `kind`/`source` enums; tracked history grandfathered) + structural move guard (guard-ticket-structure.sh, PreToolUse Bash — blocks non-canonical ticket moves like done/ or an archive nested under todo/) + always-on policy lens (policy-lens.sh) + always-on mission lens (mission-lens.sh, UserPromptSubmit + Stop) + generated policy-index.md
    rules/               # diagrams, general, interaction, shell, typescript, workaholic
scripts/                 # Repo tooling
  claude.sh              # Launcher
  build-plugins/         # Generates outputs/ from plugins/workaholic/skills (run argument-less for a full build)
outputs/                    # GENERATED, committed cross-agent artifacts — do NOT hand-edit (CI-guarded)
  workflows/             # Portable workflows plugin: .codex-plugin/plugin.json + self-contained skills/
  okf/                   # OKF v0.1 knowledge bundle of the four pillars' policies (Open Knowledge Format)
.agents/                 # Codex marketplace
  plugins/marketplace.json  # Codex plugin list (workflows -> ./outputs/workflows)
docs/                    # Documentation
  dependencies/          # Dependency-decision logs (vendor-neutrality policy)
```

## Architecture Policy

### Component Nesting Rules

| Caller                         | Can invoke                                   | Cannot invoke              |
| ------------------------------ | -------------------------------------------- | -------------------------- |
| Command                        | Skill, `general-purpose` subagent            | —                          |
| Skill                          | Skill; (when loaded by a command/main agent) may direct it to spawn `general-purpose` subagents | Command                    |
| `general-purpose` subagent     | Skill (via preload)                          | Command, Task (no nesting) |

There is exactly **one** kind of subagent in this repo, and the table above governs it. **`general-purpose` subagents** are the built-in Claude Code subagent type. They have **no `.md` agent file** — a command (or a skill running in the command's main-agent context) spawns them with a `Task` call whose prompt says "preload `workaholic:<skill>`, run `<section>` with these inputs, return `<schema>`." This is how `/report`, `/drive`, and `/ticket` fan out. Skills cannot issue `Task` calls themselves; the prohibition on Skill→Subagent that previously appeared here is lifted **only** in the sense that a skill's prose may instruct its loading agent (a command/main agent) to spawn `general-purpose` leaves.

The plugin ships **no named agent files at all**. It once carried three Agent Teams members (`planner`, `architect`, `constructor`) that only the retired `/trip` command launched, exempt from the nesting table; the whole `plugins/workaholic/agents/` directory went with that command (decision I1, 2026-07-28). Nothing in the plugin is Agent-Teams-based any more.

Everything converges on the **ticket** as the unit of work. The model is **sources × one executor**: the ticket is the spine, *sources* fill the `tickets/todo/` queue and the executor drains it to `tickets/archive/`.

- **Sources** (write to `todo/`): `/ticket` (human-directed, with discovery) and `/mission`'s Creation Interrogation, which emits a mission's whole ordered ticket set in one pass. A mission has a **floor as well as a ceiling**. The floor is the ticket count: **a mission is created with two or more tickets, or it is not a mission** — a ticketless one is a feedback record on the roadmap and a one-ticket one is a ticket with a progress bar, so the three artifact kinds are only distinguishable if the middle one has a lower bound. It is checked at the **publish seam** (`mission/scripts/check-floor.sh`, over `queue-size.sh`'s `meets_floor`), never at the write of `mission.md` — the tickets do not exist yet then — and every refusal names the alternative to write instead. The ceiling is size: a mission is deliberately **small**: `## Scope` was dropped from the template (2026-08-01 — nothing read it), `## Acceptance` is normatively **three items or fewer** (the minimum conditions for calling the work done, not a coverage list), and the whole `mission.md` is held to ~60 lines / 2 KB — a **norm** measured by `mission/scripts/size.sh` for a human author, **enforced** on `/propose`'s unattended drafts. That is a ceiling; the write-time floor (`## Experience`, ≥1 acceptance item — ownership dropped, 2026-07-31) is unchanged in substance. Both write through a publish tree and **publish onto a `work-*` branch behind a pull request** (decision J4, superseding J1's direct-to-`main` half); the artifact reaches `main`, and therefore `/drive`'s survey, when that pull request merges. `/propose` feeds them upstream by proposing missions **with their ticket sets** from the feedback stream and the repository's own state.
- **There is one executor, reached through two commands** (decision G1, 2026-07-28, amended by O1 and superseded in its command shape by P1, 2026-08-06). One run drains `todo/ → archive/`, identically whether a developer typed it or a routine invoked it (`docs/drive-loop-runbook.md`), and it lives in **one skill with two entry points**: **`/drive [<unit>]`** is the **attended** command, which asks — once, `multiSelect`, over the reported partition — *which* units to take when more than one is claimable or resumable, and asks nothing else at any step; **`/implement [<unit>]`** is the **unattended** command, prompt-free at every step, and it is what the routine template and any `/goal /implement ok` loop invoke by name. O1 carried these as two *invocation forms* of `/drive` (`auto`, with `night` as a synonym); P1 made them two commands, because a behaviour selected by a first word is a second command wearing one name — and a contract a loop rests on must not be an argument the caller can forget to pass. The optional argument is a **scope, not a mode**: the behaviour is identical with and without it. **Attendance is chosen by which command was invoked, never inferred** from a TTY or the environment — a wrong inference parks a routine tick on an unanswerable prompt or silently strips the developer's choice. What the attended command recovers is the *choice among peer units*, never approval (that stayed at artifact creation and at the PR merge) and never the partition's composition, which is still reported and never asked; a unit the developer defers is reported `deferred_by_operator` and, being still claimable, forbids the `ok` token. O1 closed a measured failure: on 2026-08-05 an attended run spent its first ~40 minutes on a parked pull request its `resumable[]` ordering ranked first while the developer's actual work in progress waited. The shape both share is the **Unified Run** (`skills/drive/SKILL.md`): *freshen* the checkout (`sync-main.sh` — artifacts live on `main`, so a runner behind `origin/main` would survey a stale queue and report it confidently; decision J3) → *survey* what is claimable (`plan-units.sh` — the active missions on `main` this runner may take, **filtered by ownership through `gather/scripts/owns.sh`** so a colleague's artifact is dropped as `owned_by_other` and an unowned one stays claimable, plus the unclaimed backlog under the same rule, with in-flight claims subtracted through the shared claim reader — and any claim of this runner's **own** whose heartbeat lapsed offered back as `resumable[]` rather than stranded — every drop reported with its reason, and the survey's own trustworthiness reported as `current` plus `backlog_error`/`backlog_size`/`owner_unresolved` — an *unreadable* queue never renders as an *empty* one, and since P2 an unidentified runner reads the queue and says so rather than finding no directory) → *partition* the remainder into **PR-units**, one unit being "what deserves one merge" (a mission is one unit; related backlog tickets batch into one, conservatively — the partition is **reported, never asked**) → *claim* each unit on a pushed branch (`claim.sh`, born with its `.worktrees/<unit-id>/`) → *drive* its tickets there under the closed four-outcome failure contract (implemented / failed / blocked / `deferred`-as-a-minted-ticket) → *report* via `/report`'s own story + PR seam → *route* by the unit's **effective merge policy** (`effective-policy.sh` per decision G5: the mission's recorded policy for a mission unit, unanimity across members for a batch, **absent means review**) — `auto` ships through the full evidence-gated `/ship` doctrine and then tears the claim down (I6), `review` stops at the PR (under `/implement` its URL is posted to Slack; an attended `/drive` reports it in the session and posts nothing) — → *account* (`record-run-hours.sh` per mission unit, I7) and close with the **honest terminal token**: an `N units: X shipped, Y PR'd, Z blocked` reconciliation and then `ok` **only** when nothing claimable remains undone, else `pending` (the `/goal /implement ok` caller contract, I4). **Neither command ever overrides a gate**: a `secret` finding hard-stops, and a `size`/`leak` block or a missing deployment-confirmation method **demotes the unit to the PR path** — `auto` means "no *approval* needed", never "no *gate* applies". The per-ticket approval prompt is **retired, not loosened**: approval moved to artifact creation (`merge_policy`, on a mission and a ticket alike) and to **merging the artifact's pull request** (decision K1, 2026-07-31 — the mission `approve.sh` flip that briefly held that role is gone), and the non-delegable looking-through relocated to the PR.

The executors that preceded it are gone, and their ideas are recorded where they landed (decisions I1/I3/I5, 2026-07-28). `/monitor`'s parallel per-worktree execution, honest terminal token, PR auto-creation, agent-hour accounting, and front-loading doctrine are all in the Unified Run above; its interactive pre-flight is superseded by creation-time `merge_policy` plus the claim protocol. `/trip`'s design discussion is the feedback stream, its decomposition is `/mission` and `/propose`, its execution is `/drive`. `/carry`'s hand-off is unnecessary because in-flight state lives on the pushed claim branch, which the next run re-claims and resumes (`claim.sh resume`, gated on same-identity plus a lapsed heartbeat). `.workaholic/trips/` stays on disk and in the layout allowlist as **legacy, read-only history** with no writer — knowledge is never deleted — so `trips/` still holds the *why* behind the tickets those sessions produced.

### No Per-Workflow Agent Files

Workflow orchestration does **not** get dedicated agent `.md` files. A command spawns `subagent_type: "general-purpose"` subagents whose prompts instruct them to preload the relevant `workaholic` skill and run a named section. The knowledge lives in the skill; the subagent is a throwaway leaf. There are **no** agent `.md` files in the repo at all — the three that existed served only the retired `/trip`.

When adding a new fan-out step, do not create an agent file — write the knowledge as a section in a `workaholic` skill and have the command spawn a `general-purpose` subagent that preloads it.

### One-Level Fan-Out

A subagent **cannot** nest `Task` calls and **cannot** call `AskUserQuestion`. Therefore:

- All fan-out happens at the command/main-agent level. No subagent-that-spawns-subagents.
- All user interaction (`AskUserQuestion`) happens at the command/main-agent level.
- Leaf `general-purpose` subagents do non-interactive work only and return JSON for the command to act on.

When a workflow needs both parallel work and a user decision, the command spawns the leaves, collects their JSON, then issues the `AskUserQuestion` itself.

### Plugin Dependencies

There is **one** plugin (`workaholic`) with `dependencies: []` — everything that was previously split across `core`, `standards`, and `work` now lives in it. All skill references are same-plugin: `${CLAUDE_PLUGIN_ROOT}/skills/<name>/...` (no cross-plugin `../<name>/` paths) and `workaholic:<skill>` namespaces. The `workflows` plugin in the marketplace is the **generated** `outputs/workflows` bundle, not an authored plugin.

### Cross-Agent Skill Exposure

The `workaholic` plugin's skills are installable by non-Claude agents (Cursor, OpenCode, Codex, Pi, 50+) via `npx skills add qmu/workaholic`. The `skills` CLI (`vercel-labs/skills`) reads `.claude-plugin/marketplace.json`: the `skills` array on the `workaholic` plugin entry labels its cross-agent discovery group (the `design`/`implementation`/`operation` policy skills). Three rules keep the exposed set honest:

- **Script-bearing skills carry `metadata.internal: true`** in their SKILL.md frontmatter. Claude Code ignores this field (the skills still load normally); the `skills` CLI hides them from cross-agent discovery. This is required because the CLI always scans the plugin's `skills/` dir — `metadata.internal` is the only per-skill exclusion. Any new skill that invokes a bundled script MUST include it.
- **The pure-prose skills are intentionally exposed** (no `metadata.internal`): the three policy skills (`design`/`implementation`/`operation`) plus `write-release-note` and `review-sections`. They have no bundled script, no `${CLAUDE_PLUGIN_ROOT}`, no namespaced preload — so they resolve on every agent via the `skills` CLI. The script-bearing workflow skills also reach non-Claude agents, but only through the generated `outputs/workflows` bundle (see below), never the source.
- **Why script-bearing skills stay internal** (verified 2026-05-26): every agent (Claude Code, OpenCode, Codex, Pi) runs a skill's shell in the *project* CWD and only injects the skill's base directory as text for the *model* to prepend — none `cd`s into the skill dir. Claude Code additionally expands `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_SKILL_DIR}` deterministically at load time. Rewriting a script call to the spec-relative `scripts/X.sh` form would drop that determinism and rely on non-deterministic model-prepend — unacceptable for skills workaholic runs in its own Claude-Code `/drive`/`/ship` critical path. So script-bearing skills keep the token and stay internal.

The plugin's `commands/`, `agents/`, `hooks/`, and `rules/` are Claude-Code-only: the `skills` CLI reads only the `skills/` dir, so those directories are not exposed through that path. Codex still installs the raw plugin directory from `.agents/plugins/marketplace.json` and parses `hooks/hooks.json`; that file must keep `hooks` as its only top-level key because Codex rejects Claude-only sibling keys such as `description`. Hook documentation lives here and in `.claude-plugin/plugin.json`, not in `hooks/hooks.json`.

To preview what the CLI would install, run `npx skills add . --list` (add `INSTALL_INTERNAL_SKILLS=1` to include internal skills).

#### Cross-agent distribution (workflow skills, built)

The workflow skills (ticket/drive/report/ship/catch/mission) depend on shared scripts elsewhere in the plugin (via `${CLAUDE_PLUGIN_ROOT}`) and so are **not** self-contained in source. They ship to non-Claude agents as a **generated, self-contained, committed plugin** at `outputs/workflows/`, produced by `scripts/build-plugins` from the DRY `plugins/workaholic` source. **One neutral generated dir serves every non-Claude agent** through two manifests that point at it:

- **Codex** reads `.agents/plugins/marketplace.json` (repo root); its `workflows` plugin `source.path` is `./outputs/workflows`, and Codex consumes the co-located `outputs/workflows/.codex-plugin/plugin.json`.
- **OpenCode, Cursor, Pi, 40+** get it via the `skills` CLI, which reads the `workflows` plugin entry in `.claude-plugin/marketplace.json` (`source: ./outputs/workflows`) and its `skills/` (the `design`/`implementation`/`operation` policy skills are exposed the same way). The `skills` CLI ignores the co-located `.codex-plugin/` dir, so the same folder serves both systems. `write-release-note` and `review-sections` ship inside this plugin too.

Source-vs-artifact rule: the `plugins/workaholic` workflow skills keep `metadata.internal: true` and `${CLAUDE_PLUGIN_ROOT}` (so the `skills` CLI never offers the broken source); the **committed `outputs/workflows/` artifacts** are the public versions — self-contained, `${CLAUDE_PLUGIN_ROOT}` rewritten to relative paths, and `metadata.internal` / `user-invocable` / the `skills:` preload block stripped with namespace prefixes flattened by `publicizeSkillMd`. **Regenerate with the argument-less `node scripts/build-plugins/build.mjs` whenever a `workaholic` workflow skill or its script closure changes** (a *targeted* build only writes a throwaway scratch dir and does not touch `outputs/`). `outputs/` is **committed, not gitignored** — Codex and the `skills` CLI install by reading repo paths, so the artifacts must be present. The `Outputs Freshness` CI workflow (`.github/workflows/outputs-freshness.yml`) rebuilds and fails on any `outputs/` diff, keeping artifact and source in lockstep.

**A skill may relocate detail into a companion `reference/` directory**, and the build carries it. When a `SKILL.md` grows past the ~50-150 line guideline, the reference-only material — per-script contracts, per-field schema detail — moves to `skills/<name>/reference/<topic>.md` and the `SKILL.md` keeps an index that links to it. `build.mjs` copies that directory into the bundle, rewrites its script references with an extra `../` (a reference file sits one level below the skill root the `SKILL.md` form is relative to), and runs `publicizeSkillMd` over it, because a reference file carries the same namespace prefixes and ships to the same non-Claude agents. `computeClosure` reads it too, so a cross-skill reference appearing only in a reference file still pulls that skill's `scripts/` into the bundle. `verify.mjs` asserts every intra-bundle markdown link resolves — deliberately narrowed to links into or out of a `reference/` dir, because a skill's prose is full of relative-looking paths that are *not* bundle files (runtime artifact paths in the consuming project, `<placeholder>` templates), and a containment check that cries wolf gets deleted. `skills/mission/` is the first user of this (562 → 380 lines).

Claude Code reads `plugins/` directly and consumes nothing from `outputs/`. The `workflows` entry in `.claude-plugin/marketplace.json` is read by Claude Code too, so it is **opt-in** — its description points Claude users to the `workaholic` plugin to avoid installing duplicate workflow skills.

That entry carries **`"strict": false`**, and must keep it. Claude Code's marketplace sync otherwise requires the plugin's `source` directory to hold a `.claude-plugin/plugin.json` (or a top-level `SKILL.md`) and **skips the plugin with an error** when it finds neither — which is exactly the generated bundle's shape, since `outputs/workflows/` deliberately carries only `.codex-plugin/plugin.json` for Codex. `strict: false` tells the sync to use the **inline** manifest already present in the marketplace entry (name, version, author, `skills`), so the bundle stays Codex/`skills`-CLI-shaped and Claude Code stops erroring on it. Do **not** "fix" this by generating a `.claude-plugin/plugin.json` into `outputs/workflows/`: that would add a fifth version file to the lockstep set and make the duplicate-skills install the description warns against easier, not harder.

#### OKF knowledge bundle (generated)

`outputs/okf/` is an [Open Knowledge Format](https://github.com/GoogleCloudPlatform/knowledge-catalog/tree/main/okf) (OKF v0.1) bundle of the four pillars' policy hard copies, generated by `scripts/build-plugins/okf.mjs` (wired into the same argument-less `build.mjs` full build): one concept document per `plugins/workaholic/skills/<pillar>/policies/*.md` with OKF frontmatter (`type`/`title`/`description`/`resource`/`tags`; no `timestamp` — commit-date-derived values would break the rebuild-and-diff freshness guard), bundle-absolute intra-bundle links, and a bundle-root `index.md` declaring `okf_version`. Any OKF consumer reads it straight from the repo path — no marketplace manifest involved, so neither Claude Code nor the `skills` CLI/Codex ever installs it as a plugin. `verify.mjs` asserts freshness plus OKF conformance (parseable frontmatter, non-empty `type`, reserved filenames, in-bundle link resolution); the `Outputs Freshness` CI diff over `outputs/` covers it like every other generated artifact. The adoption record (reason/assessment/monitoring/exit) is `docs/dependencies/okf.md`; OKF vocabulary stays inside `okf.mjs` — the translation boundary — and never shapes the source conventions.

#### `.workaholic/` as an OKF bundle (runtime)

Separately from the generated policy bundle, the `.workaholic/` tree of any project using the plugin is itself kept OKF-compatible **as the workflows generate documents**: every written artifact carries frontmatter with a non-empty `type` (tickets `enhancement|bugfix|refactoring|housekeeping`, stories `Story`, missions `Mission`, feedbacks `Feedback`, release notes `Release Note`, release records `Release`, and — in the legacy `trips/` tree, which has no writer since 2026-07-28 — trip artifacts `Direction|Model|Design|Review|Rollback|Trip Plan|Event Log`), and the internal `okf` skill's `refresh-index.sh` deterministically regenerates the bundle hierarchy — `.workaholic/index.md` (bundle root, declares `okf_version`) plus per-area `index.md` files — before each knowledge commit (called by `drive`'s `archive.sh`, `ship`'s `commit-release-note.sh`/`extract-deferred-concerns.sh`, and the `report` story flow). `stories/index.md` stays report-maintained; `tickets/` internals are never index-managed (the queue scripts and structure guards own that tree). `README.md` and `index.md` are the two files allowed at the `.workaholic/` root. Those same commit seams also **roll any related mission** (`missions/active/<slug>/mission.md`; ended missions live under `missions/archive/<slug>/`, moved there by the mission skill's `close.sh` — the only sanctioned way to end a mission — with a living migration relocating any legacy flat `missions/<slug>/` dir by its `status` on the next mission-script touch): when an archived ticket, shipped story, or extracted/resolved concern carries a `mission:` relation, the seam appends a `## Changelog` line and reconciles the mission's `## Acceptance` checklist through the `mission` skill's shared, idempotent mutators (`append-changelog.sh` / `tick-acceptance.sh`), so a mission's progress (`checked ÷ total`, computed — never stored) and history stay current without hand-editing. **What makes an item tickable at all is its `(#<filename>)` link, and the link is stamped by the seam that *emits* the ticket set** — the Creation Interrogation, a replan, or `/propose` — through `link-acceptance.sh`, the only writer of a link and one that takes the pairing from its caller rather than inferring it (2026-08-03; `skills/mission/reference/schema.md`, *The link contract*). An acceptance item is legitimately written **before** its ticket exists, so nothing demands the link at authoring time; what is demanded is that the emitting seam close the loop. When it does not, the board is *unaddressable* rather than merely unfinished, and both the ticker (`unlinked_items`, distinct from `no_unchecked_match`) and `progress.sh` (an `unlinked` count beside `checked`/`total`) say so rather than letting `0/N` read as stalled work — the state six missions were in when this was measured, 37 unlinked items to zero linked. The relation is **many-valued** — an artifact records every mission it advances (`mission: [alpha, beta]`; a bare `mission: alpha` still reads as one) and each seam rolls all of them, reading the field through the mission skill's single reader (`read-relation.sh`) rather than parsing frontmatter itself. Data is plural, **placement stays singular**: `.worktrees/<slug>` remains keyed 1:1 to a mission, and a ticket is still driven in exactly one worktree. `/drive` holds a ticket to the quality gate of **every** mission it names — naming a mission is a commitment, not a label. **The strategy layer is retired** (2026-07-28 — `docs/loop-engineering-workflow.md` B3): long-lived direction accretes in the **feedbacks stream** instead of a second direction artifact, a mission is an **optional, epic-equivalent grouping** — never a required parent of any ticket (B5) — and a lingering `.workaholic/strategies/` tree is folded by the living migration (`mission/scripts/migrate-strategies.sh`, run from every mission script's `lib/resolve.sh` seam: each strategy document survives verbatim as a feedback record, its `assignees` fold down into linked active missions, then the directory is removed; legacy `strategy:` keys on missions are tolerated and ignored). **Ownership is a field on every artifact, and one reader answers it** (B4 for a mission, 2026-07-28; **P2 for a ticket, 2026-08-06**). A ticket's owner used to be its directory — `.workaholic/tickets/todo/<user-slug>/` — which made an unreadable queue and an empty one the same observation (a runner with no `git config user.email` found no directory and reported a healthy idle tick over a full backlog), made reassignment a file move, and left the queue with no unowned state while a mission had one. The queue is now **flat** and a ticket carries the mission's model verbatim: plural `assignees`, **empty means team-owned/claimable**, resolved through the one oracle `gather/scripts/owners.sh` (with `gather/scripts/owns.sh` returning the `mine`/`unowned`/`other`/`unresolved` verdict — `unresolved` deliberately distinct from `other`, since "somebody else's" and "I cannot tell whose" must never render identically, and compared by **slug** so a migration-stamped `a-qmu-jp` still matches `a@qmu.jp`). `plan-units.sh` therefore reports `backlog_size` and `owner_unresolved` where it used to report `identity_unresolved`: it reads the queue whatever its identity, offers the unowned half, and **forbids `ok`** rather than terminating. The **claim protocol's identity use is unchanged** — claim authorship and resumption still key on `git config user.email`, because that asks "is this my own run" and fails loudly. Every reader tolerates the legacy `todo/<user>/` shape and `gather/scripts/migrate-todo-owners.sh` converges it at the **write** seams (`/ticket`'s publish step, `promote-icebox.sh`, `archive.sh`) and never from the side-effect-free survey, so the migration is convergent rather than a gate; `create-ticket/scripts/sweep-todo.sh`, which routed strays the other way, is retired. **Mission ownership is carried on the mission itself** (B4): a mission's own plural `assignees` (creator-seeded by `create.sh` — the creator is the default owner; empty = team-owned/claimable) is the primary source, resolved through `gather/scripts/owners.sh` with a **legacy fallback** to the mission's singular `assignee`, so nothing predating the field is orphaned. Every ownership consumer — the mission lens, `/drive`'s survey, `summary.sh`, `list.sh`'s `relation`, `/ticket`'s summary, `ship`'s queue check and its concern-lane owner — reads through `gather/scripts/owners.sh`; `validate-mission.sh` stopped being one when ownership ceased to be a floor (K2). **The mission lifecycle is one axis with one in-flight state** (I2, 2026-07-28; K1, 2026-07-31): `status: active | achieved | abandoned | carried`. `drive_authorized` was retired into `status: approved` by I2, and **`draft` and `approved` were then retired into `active` by K1** — since J4 every mission arrives behind a pull request, so **merging that pull request is the approval**, and a status-word gate only re-asked the question the PR had already answered (the observable cost was six active missions on `main`, none claimable, `/drive` reporting `pending` every tick). `active` lives in `active/`, the three end states in `archive/`; **`close.sh` is the only status flip that remains**, and `mission/scripts/approve.sh` plus the `/mission approve` subcommand are **gone**. **Drivability is no longer a status word**: a mission is claimable when it is in the active area, has a plan (`no_plan`) and has a queued ticket naming it (`no_tickets` when it never had one, **`queue_drained` when every ticket it had was driven and archived** — two opposite states one reason word used to hide, which left four finished missions reading as never-planned for four days) — `plan-units.sh` stops reading `status` for the offer, and `not_approved` left its reason vocabulary. Retired `status: draft`/`status: approved` files fold to `active` on the next mission-script touch (`lib/resolve.sh`, dropping the long-retired `drive_authorized` key line), and every reader keeps a legacy-tolerance branch for the transition window. **Merge policy is a separate, orthogonal axis** (G5, amended by K2): `merge_policy: auto | review` is recorded **at creation** on a mission and a ticket alike — **absent means `review`**, the conservative default, validated by `validate-ticket.sh` only when present. **Deferred concerns live in the feedback stream** (`docs/loop-engineering-workflow.md` H2/H3, 2026-07-28): a branch story records every concern in its **Concerns** section (the immutable record — the story *file* carries every severity; the PR *body* is a rendering of it with the `low` blocks dropped for the reviewer, `report/scripts/filter-low-concerns.sh`), and `ship`'s `extract-deferred-concerns.sh` persists each one as a `kind: concern` feedback record — every severity, append-only, keyed on `concern_id` so a known id is never re-emitted. The **open set is computed, never stored**: a concern is open until a later record names it in `supersedes` — which is exactly what `/report`'s judge seam writes (`apply-deferred-concern-verdicts.sh`) for each concern a branch resolved. The former `concerns/` corpus and its lifecycle machinery (promotion floor, triage, merge/re-grade/demote) are retired — curation is the reader's judgment over the stream — and a lingering `concerns/` tree is folded by the living migration (`feedback/scripts/migrate-concerns.sh`, run from the extraction and report seams; archived files become `closed:`-stamped records so history never resurfaces as open).

### Claim protocol (the repository is the coordination medium)

**Implemented** (`docs/loop-engineering-workflow.md` G3/I6; the model is stated once in `plugins/workaholic/skills/drive/SKILL.md`'s **Claims** section, which the scripts implement and never extend silently). Before driving a unit a runner *claims* it on a **pushed branch**, and every runner reads the claims in flight from the **unmerged remote branches** — so a 5-minute tick, or a runner on another machine, never double-picks work. Run-locks are gone entirely: there is no lock file and no server, so nothing leaks when a runner dies mid-run.

- **PR-unit** — one approved **mission** (unit id = the slug) or one **batch** of backlog tickets (`batch-<YYYYMMDDHHMMSS>`, minted at claim time). One unit ↔ one branch ↔ one worktree ↔ one PR.
- **Claim** — `drive/scripts/claim.sh mission <slug>` / `claim.sh batch <ticket-file>...`: a `Claim <unit-id>` commit on a fresh `work-*` branch cut from `origin/main` by the standard creator, stamping `claim: <branch>` into the claimed artifacts' frontmatter **in the worktree checkout**, pushed immediately. The stamp is written **only in the worktree**, so the runner's main checkout stays clean between ticks (which the `/propose` batch depends on). **A stamp that reaches the base is history, not a claim** (decision M1, 2026-08-04): a `handoff` or `blocked` unit merges its PR with tickets still in `todo/`, so a base-side stamp on a live queue item is expected — the unmerged-branch scan (`list-claims.sh`) is the only claim oracle, and it already reports a merged branch's claim as released. Never prompts.
- **Reader** — `drive/scripts/list-claims.sh`: pure read, emits `{fetched, shallow, stale_hours, heartbeat_stale_minutes, base, claims: [{unit, branch, artifacts, last_commit_at, stale, author, resumable, resume_reason}]}`. Claims come from git refs, but a survey's **artifacts** come from the working tree — so `/drive` fast-forwards before surveying and `plan-units.sh` reports `current` either way (J3). A driven ticket is *renamed* by `archive.sh`, so each stamp is read at the file's **current** path via a tree-to-tree rename map — **and, when git reports no rename, by an exact lookup on the ticket's filename**, which is unique in the tree by construction. That second path is load-bearing rather than belt-and-braces: `--find-renames` pairs a delete with an add only above 50% similarity, and `archive.sh` also appends the Final Report, so a short ticket with a long report is reported as add + delete. The resulting **empty artifact list is two visible failures at once** (measured 2026-08-04): `plan-units.sh` subtracts a claim *by its artifacts*, so it offered an already-driven ticket as fresh backlog with no `excluded[]` row; and `claims_has_work`'s conservative "no artifacts means unknown" branch called a drained unit `resumable`, inviting a takeover of a branch under review. The fallback is scoped to `.workaholic/tickets/` and to an unambiguous single match — every mission's artifact is named `mission.md`, so an unscoped lookup could cross one mission's claim onto another's file. `claim.sh` no longer tolerates a mission absent from the claiming checkout: it refuses `mission_missing`, because after J1 absence means a wrong slug or a stale checkout, not an artifact hiding on an unmerged branch. **The whole model assumes complete history, and now says so** (2026-08-04): "unmerged" is `git rev-list --count <base>..<ref>`, which cannot be reduced across a shallow graft, so in a **shallow clone** a fully merged branch still counts as ahead and the scan reports a PR-unit that shipped days ago. Measured on the hourly cloud runner, whose container always clones shallow: `origin/claude/sharp-rubin-xiorxm` (merged as PR #109) counted **154** ahead while shallow and **0** after `--unshallow`, and was offered as `resumable` past *both* safety gates — the identity matched because this runner had claimed it, and the heartbeat had lapsed five days earlier precisely because the work finished. ~180 merged-but-undeleted branches here are candidates, and which surface depends on the container's clone depth, so the symptom is intermittent. The answer is **repair first, degrade loudly second**: `claims_fetch` deepens a shallow clone (`--unshallow`) before anything reads ancestry — a plain `git fetch --prune` never deepens — and when origin is unreachable so it cannot, `claims_shallow` reports `shallow: true` out to both consumers and any branch whose merge base is unresolvable is reported `resumable: false` with reason **`shallow_history`**, because an unanswerable question must not render as `heartbeat_lapsed`. `shallow: true` **forbids `ok`** exactly as `current: false` does, and on a different axis: `current` asks "did I see the base's latest commit", `shallow` asks "can I tell which branches reached the base at all". The claim is still *listed* when history is truncated — over-reporting a claim makes a runner wait, under-reporting it double-picks work — so it is the **verdict** that is suppressed, never the row.
- **Release** — a **merge** releases by definition (the commits reach the base, so the claim leaves the unmerged set with no bookkeeping); **discarding** an unfinished unit is the explicit `drive/scripts/release-claim.sh <unit-id>`, which tears the worktree down *before* deleting the remote branch so a refused teardown never publishes "this unit is free" over unpushed work. It deletes the branch, so it is **not** the recovery path for an interrupted unit — resumption is. **Its two steps can disagree, and the outcome says so** (2026-08-05): it reports `state: released | half_released | untouched` beside `released`, because the teardown-then-delete order means a refused *delete* lands after a successful teardown. Measured on the hourly runner, whose container may **push but not delete** a branch (403 twice on a remote that had just accepted several pushes; the GitHub MCP server offers `create_branch` and no delete), so the second step could never succeed and a bare `released: false` was indistinguishable from "nothing happened". `half_released` means the worktree is gone and **the claim branch is still live** — a human must delete it, nothing in the loop can, and until then the unit is re-offered as `resumable` once its heartbeat lapses. Two alternatives are recorded as rejected in the script's header rather than left to be re-proposed: a **tombstone** commit the scan honours (it would add a second, weaker release channel to a protocol resting on exactly one invariant — an unmerged branch carrying a `Claim` commit *is* a live claim — and a runner that failed to write the tombstone lands in the same state), and **inverting the order** so a refused delete leaves the worktree intact (it would publish "this unit is free" over possibly-unpushed work; the teardown is also the cheap half to lose, since `claim.sh resume` rebuilds the worktree at the branch tip).
- **Landing** — `drive/scripts/land-unit.sh <unit-id> --developer-present [--override-scan]` is the **third route** out of §6 (2026-08-05), and the only one a *developer* chooses rather than the run. `review` publishes a unit but does not make it **claimable**: a ticket minted mid-run sits on the claim branch, invisible to every other session until a human merges the PR out of band (J1), so "wrap this up so a fresh session can resume immediately" was served by neither route and got hand-rolled instead — a local `git merge`, a manual recovery when `origin/main` had moved, a manual worktree cleanup, a manual branch delete, every step already a sanctioned script with nothing composing them. This is that composition: catch up in the unit's own worktree (`catchup-main.sh`, so the append-only `.workaholic/` resolution and the mechanical/content classification are inherited), run the gates, push the branch tip **onto the base ref** (`git push origin <branch>:main` — merging into a local `main` would leave the caller's checkout ahead of origin on a rejected push, and the only cheap repair for that is the forbidden `git reset --hard`), tear the claim's housing down, and fast-forward the landing checkout so the very next survey offers the leftover tickets. **The developer is the review, which is why it refuses headless**: `CLAUDE_CODE_REMOTE=true`/`CI`/`WORKAHOLIC_HEADLESS=1` refuse as `headless_context` **first and unoverridably**, and a missing `--developer-present` refuses as `no_developer_instruction` — the flag is the *instruction*, not a proof of presence (no script an agent runs can demonstrate a human was there, and it is not sold as doing so); what it buys is that the route is never taken by omission. `/drive` never calls it. Gates are unchanged — a `secret` finding is non-overridable, `size`/`leak` need an explicit `--override-scan` that is reported as `scan_verdict: "overridden"`. Its order is the **inverse** of `release-claim.sh`'s: the work lands **first**, because a failed teardown after a successful land loses nothing, while tearing down first would destroy the branch still to be pushed.
- **Resumption** — a claim whose run stopped is **taken over**, not stranded (2026-08-01, closing decision I5, which the code had contradicted: every claimed unit was dropped as `claimed` and refused as `already_claimed`, so no survey ever offered it again — fatal for a cloud runner whose worktree dies with its sandbox, leaving the pushed branch as the sole surviving copy with nothing routing anyone to it). The verdict is computed in the one shared scan and carried on every row (`resumable`, `resume_reason`), because a writer free to decide it independently could take over a unit the reader still calls active. Two conditions, identity first: the claim commit's **author must be this runner's `git config user.email`** (a colleague's claim is `foreign_identity`, untouchable at any age — *a pushed claim is the loop's work*, but only your own loop's), and its **heartbeat must have lapsed** (`WORKAHOLIC_CLAIM_HEARTBEAT_STALE_MINUTES`, default 30). `claim.sh resume <unit-id>` re-creates the worktree **at the pushed branch tip** (`create-mission-worktree.sh --branch`) so archived tickets are not re-driven, then publishes an empty `Resume <unit-id>` commit whose push **is** the race arbiter — two runners racing resolve non-fast-forward exactly as `branch_collision` resolves two fresh claims, never by comparing clocks.
- **The heartbeat is the branch tip** — `drive/scripts/heartbeat.sh <unit-id>` pushes an empty commit, and every ordinary work commit refreshes the same signal. No lock file, no heartbeat file, no server: all three leak when a runner dies, which is the failure the claim protocol exists to avoid. An empty commit changes no file, so it never reaches the PR diff, and the merge or release that cleans up the claim cleans it up too. `commit.sh --allow-empty` is the one seam these coordination markers use, so they still get the subject gate and the trailers — and there **`--allow-empty` means empty**, not "empty is tolerated": git's own flag merely permits a changeless commit and still commits whatever the index holds, so a beat fired mid-ticket over a staged `git rm` produced a `Refresh heartbeat` commit carrying three real deletions (measured 2026-08-04), a commit whose content was right and whose message was wrong. The commit is now built against a scratch index seeded from `HEAD`, so its tree equals `HEAD`'s by construction and the caller's index is left byte-identical. Refusing to beat over a dirty index was rejected: mid-ticket is exactly when the index is dirty and exactly when a missed beat makes a working unit look abandoned.
- **Staleness is reported, never auto-broken, and is a different axis from resumability** — past `WORKAHOLIC_CLAIM_STALE_HOURS` (default 24) a claim is marked `stale: true` and **nothing acts on it**, because reclaiming a colleague's work on a local verdict can silently duplicate it. Resumption acts, and is narrower by exactly that margin: your own claim, heartbeat lapsed, measured in minutes — a routine that recovers its own dropped unit only after a day is not a recovery path.
- **Under `/implement`, a unit announces its start and its finish into its feedback item's thread** (2026-08-05; scoped to the unattended run 2026-08-07 — the threaded posts exist so an *absent* operator can tell a working fleet from a dead one, so an attended `/drive` session posts nothing to Slack) — **per unit, never per run**: "a run started" names no item, so it had no thread to land in and was posted as a top-level line, which is the scattering *One thread per feedback item* exists to end. `drive/scripts/unit-feedback-stems.sh` resolves the unit's artifacts (a mission's `mission.md`, or a batch's ticket files) to the deduped feedback stems through the one existing reader, and the session posts the 🟠 start at claim time and **one** finish at the route step into each of those threads — the finish's shape following the outcome (🟢 merge requested, 🚀/🟣 merge, 🟡 handoff, 🔴 blocked), a handoff being the finish rather than a third post. Several stems post into each thread, because an item's thread is meant to carry that item's whole life; **no** stem keys on `unit:<unit-id>` — never keyless, and the unit id rather than the PR number because the start is posted before any pull request exists. The merge line cannot duplicate `[Consent]`'s: that routine already refuses to re-announce a pull request whose merge is already in the channel.
- **Claiming announces on the bot surface, which is a different thing from the threaded start** — `claim.sh` posts one Slack line (unit, branch, member count) after its push succeeds, reporting `announced`/`announce_reason` in its JSON. It goes through the bot token with no threading and is deliberately not grown into the threaded post; the threaded posts are the session's, through the Slack connector that can search `fb:<stem>` and reply. It is the operator's first signal that a run started; before it, the first human-visible artifact was the PR opened after every ticket was driven, and a working fleet was indistinguishable from a dead one for tens of minutes. **Never load-bearing**: a missing token, an unreachable Slack, or a broken notifier leaves the claim intact, and a refused claim announces nothing.
- **Worktree lifecycle (I6)** — worktrees are **claim-born and ship-torn**. `claim.sh` creates `.worktrees/<unit-id>/`; ship or `release-claim.sh` removes it. **Every one of those teardowns needs a run to reach its end**, so a sweep backs them up: `branching/scripts/survey-worktrees.sh` reports each worktree's size, `ahead`, `dirty` and `reclaimable` state, `reap-worktrees.sh` (dry run by default, `--apply` to act) removes only those both **merged and clean** and names every skip with its reason, and `prune-worktree-artifacts.sh` reclaims build output from a worktree worth keeping (git-ignored **and** a named build directory — ignored-ness alone would take `.env`). Creation now reports `size_bytes`. **`.worktrees/` sits inside the repository root**, so it enters any build context, archive, or index rooted there — add it and `.publish/` to `.dockerignore` and to any archiver's ignore list; `.git/info/exclude` covers git and nothing else. **`/mission-close` keeps only the archive move** — a lingering worktree is an in-flight or stale *claim*, which the reader surfaces and a human decides about, so a bookkeeping action no longer doubles as a destructive one. `cleanup-mission-worktree.sh` is unchanged and still the sanctioned cleaner.
- **The claim is the only creator of a *worktree*, and the only creator of a branch a runner may drive** (decision J1, 2026-07-30, amended by J4, 2026-08-01). J1's rule was "artifact creation publishes to `main` and cuts no ref", motivated by immediate visibility to every runner and fresh clone. **J4 keeps the isolation and changes the destination**: an artifact writer still writes into a **publish tree** — never the caller's checkout — but pushes onto a `work-*` branch and opens a pull request, because the project standard is that every artifact reaches `main` through a *merged* PR, and the merge is the event that can be announced. The cost is accepted knowingly: an artifact is **not** visible to a runner until its PR merges, so "published" and "queued" are now different states and every source reports which one it reached. A publication branch carries no `Claim <unit-id>` commit, and the claim scan keys on that subject rather than on the branch name, so it is never mistaken for a claim. `create.sh` and `create-mission-worktree.sh` remain claim-side creators only.

**The publish tree** (`plugins/workaholic/skills/branching/SKILL.md`, decision J2) is a checkout of `origin/main` at the fixed, git-ignored `.publish/` on the fixed local branch `publish-main`, opened → written → published → closed by `open-publish-tree.sh` / `publish-tree-pr.sh` (the default — a `work-*` branch plus a pull request) or `publish-tree-commit.sh` (direct to the base, reserved for seams already downstream of a merge, such as ship-time concern extraction) / `close-publish-tree.sh`. It exists because publication must not depend on, or disturb, the caller's checkout: `/propose` can guard on "on `main`, clean tree, else abort" because it is a headless batch, but a developer types `/ticket` and `/mission` mid-work on a dirty branch. The caller's branch, staged set, untracked set, and file contents are left byte-identical; the commit goes through `commit.sh` (so it inherits the subject gate and the trailers, which is why the branch is named rather than detached). The **local** branch is always `publish-main`; only the remote ref differs — `publish-main:main` for a direct publish, `publish-main:refs/heads/work-YYYYMMDD-HHMMSS` for the PR path. The `work-*` name on the remote is safe because `claims_scan` keys on a `Claim <unit-id>` **commit subject**, not on the branch name, and a publication branch carries none. **A publish tree is not a claim worktree**: it holds no unit, is never pushed as a branch, and is disposable at any moment; and neither `open` nor `close` will destroy recoverable state (both refuse a dirty publish tree, and `close` also refuses a clean one whose `publish-main` still carries unpushed commits — the state a `diverged` publish leaves behind). Publishing to `main` is necessary but not sufficient: a runner whose local `main` trails `origin/main` would still survey a stale queue, so `/drive` fast-forwards first through `sync-main.sh` (decision J3).

Two design points worth not re-litigating. **The reader degrades offline, the writer fails loudly**: `list-claims.sh` reports `fetched: false` and answers from the last-known remote-tracking refs, while `claim.sh` refuses outright — a stale reader over-reports claims (a runner waits), but a claim nobody else can see is not a claim, and false "unclaimed" is the error that double-picks work. And **one scan serves both**: `drive/scripts/lib/claims.sh` holds the scan, `list-claims.sh` merely renders it as JSON and `claim.sh` verifies through it, because a writer carrying its own scan would be free to disagree with the reader. The optional `claim:` key on a ticket is **tolerated and never validated** by `hooks/validate-ticket.sh` — its truth lives in git (does an unmerged remote branch of that name still carry the artifact?), which a hook reading one file cannot answer.

### The release tier (`release/*`)

**One extra branch form, and nothing else** (decisions L1-L3, 2026-08-03). No `develop`, no `hotfix/*`; `main` stays the default and production branch, and **the per-unit claim/branch/worktree/PR mechanics above are untouched** — there is still exactly one merge target for a unit, which is why "what does a claim mean against a second base" and "what does `merge_policy` mean under two merge targets" do not arise. Full Git Flow was rejected for creating that second base; a `develop`-only tier was rejected because a continuous integration branch has no bounded identity to attach a durable record to, and the record was half the ask.

- **A `release/*` branch is the QA window between *merged onto `main`* and *released to production*.** It is cut from `main` by `branching/scripts/cut-release-branch.sh`, which mints `release/YYYYMMDD-HHMMSS` (the second literal pattern `guard-git-branch.sh` permits), pushes it, and **never checks it out**. Cutting one is **batch-level and explicitly invoked** — `/drive` neither cuts nor confirms one, and a unit landing on `main` is unchanged by the tier. If it were a step of the per-unit ship, every `auto` unit would open a release window.
- **The branch carries no commits of its own.** It is exactly a pointer at a `main` commit, which is what keeps "which commits did this release carry" answerable by `git log <previous>..<tip>` forever. It is invisible to the claim protocol for the same reason the publication branches are: `claims_scan` keys on a `Claim a PR-unit` subject / `Unit:` trailer, and a release branch carries no commit at all.
- **The durable ship record is `.workaholic/releases/<release-branch>.md`**, written by `ship/scripts/record-release-cut.sh` at the cut and `confirm-release.sh` at each confirmation attempt, both on `main` and both derived from git — never hand-authored. It is **additive**: `.workaholic/release-notes/<branch>.md` and the story's `## Deployment Evidence` block keep exactly their shape. `since_reason` records how the carried range was chosen (`given` / `prior_release` / `latest_tag:<tag>` / `full_history`), because a range is only trustworthy if the reader can see how it was picked.
- **A promotion adds a second confirmation; it never weakens the first.** `/ship` §5's per-unit confirmation proves one branch before it lands; §6's proves a batch already on `main`. Inverting the evidence-before-merge rule is the one thing a promotion must not do. **A failed confirmation deletes nothing**: the release branch is the rollback boundary, `main` is unaffected because its units are already merged there, the failure is recorded rather than erased, and the next attempt cuts a *fresh* release branch — re-pointing or force-pushing one destroys the only account of what was tried.

### Design Principle

**Thin commands, comprehensive skills.**

- **Commands**: Thin skill-aliases (a few lines each): name the skill and section to run, state what is entry-point-specific (argument routing, the attended/unattended contract), and nothing else. All user interaction (`AskUserQuestion`) still happens at the command/main-agent level; the flows themselves live in the skills.
- **`general-purpose` subagent prompts**: Orchestration only. Name the `workaholic` skill to preload, the section to run, the inputs, and the return schema — no per-workflow `.md` file.
- **Skills**: Comprehensive knowledge (~50-150 lines). Contain templates, guidelines, rules, and bash scripts.

Skills are the knowledge layer. Commands (and the leaf subagents they spawn) are the orchestration layer.

### Common Operations

Commands, skills, and subagent prompts must use skills for common operations instead of inline shell commands:

| Operation | Skill | Usage |
| --------- | ----- | ----- |
| Git context (branch, base, URL) | gather | `bash ${CLAUDE_PLUGIN_ROOT}/skills/gather/scripts/git-context.sh` |
| Ticket metadata (date, author) | gather | `bash ${CLAUDE_PLUGIN_ROOT}/skills/gather/scripts/ticket-metadata.sh` |

Never write inline git commands like `git branch --show-current` or `git remote show origin` in command or agent markdown files. Preload the skill and gather context through it.

### Shell Script Principle

> **CRITICAL: Never use complex inline shell commands in subagent or command markdown files.**

This includes:
- **Conditionals**: `if`, `case`, `test`, `[ ]`, `[[ ]]`
- **Pipes and chains**: `|`, `&&`, `||`
- **Text processing**: `sed`, `awk`, `grep`, `cut`
- **Loops**: `for`, `while`
- **Variable expansion with logic**: `${var:-default}`, `${var:+alt}`

Extract ALL multi-step or conditional shell operations to bundled scripts in skills (`skills/<name>/scripts/<script>.sh`). This ensures consistency, testability, and permission-free execution.

**Wrong** (inline conditional):
```bash
current=$(git branch --show-current)
if [ "$current" = "main" ]; then echo "on_main"; fi
```

**Correct** (skill script):
```bash
bash ${CLAUDE_PLUGIN_ROOT}/skills/branching/scripts/check.sh
```

### Skill Script Path Rule

> **CRITICAL: All skill shell script references must use `${CLAUDE_PLUGIN_ROOT}`.**

Claude Code expands `${CLAUDE_PLUGIN_ROOT}` inline in all plugin content (skills, agents, commands) at load time. This resolves to the plugin's installed directory. Relative paths like `.claude/skills/` do NOT resolve at runtime and cause exit code 127 failures.

**Wrong** (relative path):
```bash
bash .claude/skills/gather/scripts/ticket-metadata.sh
```

**Correct** (same-plugin reference):
```bash
bash ${CLAUDE_PLUGIN_ROOT}/skills/gather/scripts/ticket-metadata.sh
```

**Correct** (cross-plugin reference — declared dependency):
```bash
bash ${CLAUDE_PLUGIN_ROOT}/../core/skills/branching/scripts/check-worktrees.sh
```

### Plugin Boundary Rule

> **CRITICAL: Reach skills only through `${CLAUDE_PLUGIN_ROOT}` and their loaded namespace. Never spelunk for skill content on disk.**

A command's skills are already loaded via its `skills:` frontmatter and resolved through `${CLAUDE_PLUGIN_ROOT}` at load time. Invoke them by their loaded namespace (`workaholic:`) and run their bundled scripts via `${CLAUDE_PLUGIN_ROOT}`. Do **not**:

- read or run anything under `~/.claude/plugins/marketplaces/` or any other global/marketplace install — those may be stale copies from older versions and will silently run obsolete logic;
- guess a plugin or skill namespace. `drivin`, `trippin`, `core`, `standards`, and `work` are **obsolete** plugin names — all merged into the single `workaholic` plugin, which is the only live plugin.

If a skill you expect is not in context, ask the user which plugins are loaded — do not search the filesystem for it. Every `workaholic` command body ends with a one-line echo of this rule.

### Closed `.workaholic/` layout (lockstep registration)

The `.workaholic/` tree is a **closed structure**: the set of permitted top-level artifact directories is fixed and defined in **two sources of truth that MUST stay in lockstep** — `hooks/workaholic-layout-allowlist.txt` (one directory per line) and the table in `plugins/workaholic/rules/workaholic.md`. Introducing a new top-level artifact directory (as `strategies/` was) is a **deliberate amendment**, not an ad-hoc `mkdir`, and it is subject to a verifiable rule an auditor can check:

- **Register in the same change.** A new artifact directory MUST be added to **both** `hooks/workaholic-layout-allowlist.txt` **and** the `rules/workaholic.md` table — plus any skill/docs that name the artifact — **in the same commit** that first writes to it. A new artifact type is not shipped until its directory is registered. (Concretely: `git show <commit>` for any commit that introduces `.workaholic/<newdir>/…` must also touch both sources of truth.)
- **The guards are load-bearing, not advisory.** The working-directory and `.workaholic/` layout guards **block by construction** whenever the plugin is installed — enforcement is built into the plugin code with **no env-var toggle** (see `hooks/guard-working-directory.sh` and `hooks/validate-ticket.sh`). So the layout source of truth is enforced, not decorative: a **stale allowlist becomes a correctness bug** — it hard-blocks a legitimate write into the unregistered directory — which is exactly why registration must ride in the same change.
- **Named anti-drift audit.** `hooks/layout-doctor.sh [path]` reports undesignated directories against the allowlist without mutating the tree (read-only). Run it when amending the structure; it is wired into the **`## Local Verification`** commands and the **`Validate Plugins`** CI workflow, which **fails the merge** on `conforming: false` — so a drifted allowlist is caught before merge rather than surfacing as a guard block later.

This policy exists because `strategies/` shipped live (its skill and `create.sh` writing `.workaholic/strategies/active/`) while both sources of truth stayed stale — the class of drift the lockstep rule and its gate now prevent.

## Commands

| Command                          | Description                                      |
| -------------------------------- | ------------------------------------------------ |
| `/ticket <description>`          | Write implementation spec for a feature **in this repository**, recording its `merge_policy` (`auto` \| `review`; absent reads as `review`) at creation, and **publish it onto a `work-*` branch behind a pull request** through a publish tree — from any branch, with the caller's checkout and uncommitted work untouched. `/drive` can claim it once that pull request merges. It creates **no branch** and asks nothing about one (the worktree guard is retired with the branch, decision J1). Bare `/ticket` reports the queue instead of creating one — the tickets this developer owns plus the unowned, claimable ones. The `summary` literal is retired (P5, 2026-08-06): a behaviour selected by the first word of an argument is a second command wearing one name |
| `/drive [<unit>]`                | **The executor, attended.** Fast-forward the checkout to the base first (`sync-main.sh`; a stale survey can never report `ok`, and `not_on_main`/`dirty_workspace`/`diverged` terminate `pending` rather than surveying the wrong queue), then survey what is claimable (the active missions on `main` that this runner owns or that are unowned — a colleague's is dropped as `owned_by_other` — plus the unclaimed backlog, minus the claims in flight, plus any of its own claims left resumable by a lapsed heartbeat), partition it into **PR-units** ("what deserves one merge" — a mission is one unit; related backlog tickets batch into one, conservatively), claim each on a pushed branch, implement it in the claim's own worktree, report it as a PR, and route it by the unit's **effective merge policy** (`auto` → the full evidence-gated `/ship` doctrine, then claim teardown; `review` → stop at the PR, reporting its URL in the session — the Slack post is `/implement`'s; **absent = review**). **It asks exactly one thing**: when more than one unit is claimable or resumable it asks once (`multiSelect`, over the reported partition) which to take, drives them in the chosen order, and reports the rest as `deferred_by_operator` (still claimable, so still forbidding `ok`). It never confirms the partition, never asks per ticket, and never overrides a gate just because a developer is present. `<unit>` narrows the run to one mission slug or ticket path — a **scope, not a mode**, and a run already pointed at one unit has nothing left to ask. The unattended twin is `/implement`; the run below §2 is identical between them |
| `/implement [<unit>]`            | **The executor, unattended** (decision P1, 2026-08-06, superseding O1's second invocation form of `/drive` — a behaviour selected by a first word is a second command wearing one name, and a contract a loop rests on must not be an argument a caller can forget). Same survey → partition → claim → drive → report → route → account as `/drive`, and **no `AskUserQuestion` anywhere, at any step**: a decision the run cannot make is deferred and recorded in the final report, never asked. It **never overrides a gate**: a secret hard-stops the unit as `blocked`, a size/leak block or a missing confirmation method demotes it to the PR path, and a demotion is reported as a demotion with the gate that caused it. An unqueued problem met mid-run becomes a ticket rather than a stop, and a unit it half-drove ends in **`handoff`** — its state and next step written into the PR body's non-droppable `## Handoff` section, which is what a later run resumes from. Ends with an `N units: X shipped, Y PR'd, Z blocked` reconciliation and an honest terminal token (`ok` only when nothing claimable remains undone — no `handoff`, no untaken resumable unit — the survey was current with the base, **and** the backlog was actually readable), the `/goal /implement ok` contract. This is what the routine template and every caller-side loop invoke by name |
| `/commit`                        | Commit working changes with a policy-conformant message (small non-ticketed changes; prefer `/drive` for ticketed work) |
| `/propose`                       | **Proposing at the capture seam** — the judgment happens in the session that receives the ask (developer's ruling, 2026-08-04, superseding the batch seat shipped the same day; `docs/proposal-loop-runbook.md`). Its input is what is **in hand**: the reported ask and the feedback record the session writes from it — with nothing in hand it reports `nothing_in_hand` rather than sweeping. It writes the record into a publish tree, reads the repository's own state from the base as a **constraint** (`survey-state.sh` — the missions, the todo queue, and a bounded slice of recent commits, with `since_reason` naming how that range was chosen), dedups against what existing artifacts already answer (`list-proposed-refs.sh`), judges against the propose skill's conservative bar — where feedback is the only input that can *originate* a proposal and the rest are *constraints* that can only shrink or veto one — and emits, in **one** publish-tree pull request, the record together with what **the form the work's shape selects** — a **mission with its ordered ticket set** when the direction decomposes into two or more units, **one loose backlog ticket** when it is atomic, or **the record alone** when it is neither. Record-only is now a *judgment* a reader can disagree with rather than the mechanical silence of a proposer that could not see the record; the record is written whatever the judgment concludes, and one merge approves both halves. `/propose` **acts only on an ask that is its own** (P8, 2026-08-06): when the ask came from a GitHub issue carrying an assignee, it compares that assignee against the session's own GitHub identity (`gh api user`) and reports `not_mine` otherwise. The routines UI offers no assignee filter, so every developer's `[Propose]` fires on every assigned issue, and without this each would open a pull request for the same one — the dedup only sees proposals that already reached a branch. The check lives in the **command**, never in the routine prompt: `/implement` filters at its survey (`owned_by_other`) because it claims artifacts that already carry `assignees`, while `/propose` filters at its input because it *creates* the artifact that will carry them, and stating the rule in two prompts is stating it twice. A proposal **carries the triggering issue's assignee** as its `assignees` (P6, 2026-08-06 — `--assignee` on both scaffolds; the owner enters once at the trigger and rides every artifact the run emits, and falling back to the *running* identity is forbidden, since that stamps whichever container executed the batch). An unassigned issue still yields a team-owned artifact. This is the half P2 left silent, and its absence was measured: an unowned proposal is claimable by **everyone**, so on a repository with several developers every runner raced for the same work and whose push landed first decided whose job it was. `merge_policy` stays empty so it reads as `review`, and the proposal is `feedback:`-linked — a mission's tickets carry the `mission:` relation, so `plan-units.sh`'s `mission_member` exclusion keeps them out of the loose backlog and they are driven only as part of the mission's unit, while a loose ticket carries no mission relation and is offered as ordinary backlog. A loose ticket's `feedback:` refs are **mandatory** (`no_feedback`): with no mission to hold the relation they are the only record of what it answers, and the dedup set — the union of `feedback:` across missions *and* tickets, queued and archived, **plus the same artifacts on unmerged remote branches** — is what it keys on. That last term was added 2026-08-05: the seam reads the publish tree, a checkout of `origin/main`, so a proposal still open as a pull request was invisible and an ask proposed ten minutes earlier was proposed again. It uses the claim protocol's own oracle rather than `gh`, and it errs toward *including* a ref — a duplicate proposal is the loud failure, a suppressed one is quiet — so a shallow clone over-reads and reports that on stderr, and a pull request closed without merging keeps its ask suppressed until its **branch is deleted**, the same act that releases a claim. Everything lands on a `work-*` branch behind the one pull request whose **merge is the approval** — its **title** carries the `[Proposal]` prefix through `WORKAHOLIC_PR_TITLE` (kept separate from the commit subject, which `check-subject.sh` forbids a `[bracket]` prefix on; the prefix is what the `[Implement]` routine's trigger filters on), and its **body** carries the chain's notification target as one `Notify-Thread: <url>` line through `WORKAHOLIC_NOTIFY_TARGET`, read back by `branching/scripts/read-notify-target.sh` so the next routine replies where the ask arrived instead of re-deriving the thread by search — announced to Slack (the `[Propose]` routine posts the item's thread root through the account's connector; the bot notifier is the CLI-side path and is a graceful no-op without a token). Never prompts |
| `/fb [<content>]`                | Register one **immutable** feedback record into `.workaholic/feedbacks/` — a design conclusion, an instruction, a development-born concern, or customer material (`kind`: insight/instruction/concern/material/answer; `source`: meeting/slack/discussion) — via the feedback skill's `create.sh` (the only sanctioned writer; resolution is a new record naming the old one via `supersedes`, never an edit). **Abbreviated deliberately**: Claude Code ships a built-in `/feedback` that sends feedback to Anthropic, so the bare name would silently do the wrong thing. Only the trigger is short — the artifact, the `workaholic:feedback` skill, and the *feedback stream* keep the full word. **Given a target repository it crosses the boundary instead** (`/fb <the ask> to <owner/name>`, a slug, a GitHub URL, or "ask <repo> to…"): it composes the ask in the **target's** vocabulary, applies the masking judgment, shows the **one** non-skippable verbatim confirmation (destination, `visibility`, exact title and body), runs the content scan and the identifier backstop, and opens the ask as a **GitHub issue on the target** (`gh issue create -R`) — writing into no checkout of it, and reporting the issue URL. The target's `[Propose]` routine ingests that issue like any other inbound report, so the recording and the proposal judgment happen in the target's own loop. This is the **only** sanctioned crossing; `/request`, which wrote a ticket file into the target's tree, was retired 2026-08-05. Bare `/fb` is unchanged |
| `/report`                        | Context-aware: generate story or journey report and create PR (warns on the branch-safety scan) |
| `/ship`                          | Context-aware: merge PR, deploy, and verify (blocks pre-merge on the branch-safety scan; secrets non-overridable) |
| `/mission ["<title>" \| "<instruction about an existing mission>"]` | Create a mission (an **optional, epic-equivalent grouping** — a bounded, information-rich batch of **two or more** tickets; never a required parent of any ticket, and long-lived direction lives in the feedbacks stream — creating one asks the one human `merge_policy` ruling, interrogates the developer to a drive-ready state, then **publishes onto a `work-*` branch behind a pull request** — in one commit — the mission statement and the **whole** ordered ticket set it emitted; **under two tickets nothing is published at all** and the refusal names what to write instead (a feedback record for a bare direction, a plain ticket for a single unit of work), it creates no worktree, and an abandoned interrogation publishes nothing. **Merging that pull request is the approval** — there is no `approve` subcommand (retired 2026-07-31, decision K1), and a mission on `main` is claimable as soon as it has a plan and a queue), **replan** an existing in-flight one (a free-form instruction referencing it — no subcommand — re-enters the interrogation scoped to what changed, applies the delta, and emits delta tickets; also how a `carried` successor or a thin hand-authored mission gets fleshed out), show the **developer-centric roadmap** (bare `/mission` — full treatment with next step and changelog movement for **your** and unclaimed active missions from `list.sh`'s computed `relation` partition, one-liners for colleagues' and archived ones), **No word of `$ARGUMENT` is a subcommand** (P5, 2026-08-06): it names the mission you mean and nothing else, and the three literals that used to be intercepted are gone — `summary` and `approve` were already-retired modes whose deprecation stubs *were* the fork, and ending a mission moved to its own command. When a mission's **direction changes** mid-flight, **reorganize-and-carry is the encouraged answer** — replan to drop the now-moot criteria, then `carried` into an existing successor (`--successor <slug>`) — rather than grinding to `achieved` or `abandoned`. **A carry has exactly one route** (2026-08-04): `--successor <slug>` **appends** the predecessor's unmet items — markers intact, extracted by `mission/scripts/lib/acceptance.sh` — to an existing mission whose own Goal, gates and items are untouched, idempotent by item text, with `carried_from` becoming a list under a second carry. **`--successor-title` is refused** by the ticket floor: a minted successor arrives with zero tickets, so that route violated the floor on every use, and it was deleted rather than gated once the append route could carry the remainder. Create the successor through the ordinary mission path first — that interrogation is what emits its ticket set. Both defects behind this are recorded: the append route inherited *nothing* until 2026-08-04 (it resolved a path and fell through while two documents promised it carried the remainder — data loss reported as success), and the mint route put a ticketless mission on `main` the same day |
| `/catch [window]`                | Read-only by-developer catch-up report over a recent window (commits, tickets, stories) plus a Missions view (each active mission's derived progress, merged activity, and unmerged in-flight work) and an **Orchestration Throughput** block (`gather/scripts/commit-kpi.sh`: agent-commit count/share, median/p90 changed lines, oversize count over the window — how well the agent fleet is orchestrated, not human output), then follow-up Q&A |
| `/explain <question> [dir]`      | Answer a repo question and export a printer-ready PDF report (HTML printed by a real browser); exports to `dir`, else Desktop→Home (Home write asks permission) |
| `/workaholify`                   | Wire the current repo to the standards: refer to the `workaholify` gateway skill (reaches the `policies/`), audit `CLAUDE.md` against the documentation standard, **survey and provision this repository's scheduled routines**, and confirm the working-directory advisory hook is active — rules stay in the skill, not copied into `CLAUDE.md`. It also checks the **web bootstrap** first (`check-bootstrap.sh`): Claude Code on the web starts each session in a fresh container where `enabledPlugins` installs nothing, so without `.claude/hooks/session-start.sh` (canonical copy: `skills/workaholify/bootstrap/`) plus its `SessionStart` entry, **every cloud routine for the repository stops at its own "the workaholic plugin must be loaded" precondition** — firing on time and doing nothing, while reading as healthy from the routines list. Each problem is named separately (`hook_missing` / `hook_stale` / `not_registered` / `matcher` / `timeout` / `enabled_plugin` / `marketplace`) because each needs a different fix; the hook is POSIX `sh`, deliberately has **no `set -e`** (it must never block session start), and is idempotent (qmu/workaholic#126); its already-installed fast path is **version-gated against the checkout's `.claude-plugin/marketplace.json`** — a container image's baked-in stale install is refreshed with `plugin update`, never skipped on mere presence, which is the defect that dirtied the tree and aborted every hourly drive tick on 2026-08-02〜04. Two limits of that gate are **decided, not pending** (2026-08-04): the hook runs at SessionStart and **never refreshes a running session** (swapping the plugin under already-loaded skills and always-on hooks mid-turn is worse than being one version behind, and there is no signal to hang a refresh on that is not polling), and `WANTED` is read from a checkout that **can itself be behind the base**, so a stale clone and a stale baked-in install agree and the fast path skips exactly when it should not. Both are answered by making the drift **legible** rather than adding a gate: `check-deps/scripts/check.sh` reports `checkout_version` and `version_drift` beside the loaded `version`, and `/drive` prints them in its run report. The contract is *the bootstrap makes the install correct at session start; check-deps makes it honest afterwards*. **That contract is narrowed by a second drift axis** (2026-08-05): "legible, not gated" holds for a checkout that moved, and it had a blind spot in exactly the case it was designed for. `plugin update` unpacks the new version **beside** the old one and deletes nothing, so a session can bind a *superseded* cache directory — and because a SessionStart hook may not refresh a running session, nothing repairs it. Measured 2026-08-04T22:58Z: the registry recorded 1.0.129 (updated ~85 s before the session's first commit) while the session ran 1.0.112, whose `claims.sh` predates the rename-following resolution and `queue_drained`; the survey called five already-driven tickets fresh backlog and the tick **claimed one** — a double-pick reaching a pushed ref. So `check.sh` now also reports `registry_version` / `registry_unreadable` / `loaded_version_behind_registry` against `~/.claude/plugins/installed_plugins.json`, and **`/drive` terminates `pending` on it before surveying** rather than printing it (its §1). The two axes stay separate because they have different fixes: a checkout that moved means the run may be missing a fix, while a superseded binding silently changes the *survey's answer*. Two properties make the check trustworthy at any plugin age — **neither operand is plugin content** (the harness's binding versus the harness's registry, which is also why `version` now prefers `${CLAUDE_PLUGIN_ROOT}` over the manifest beside the script: the script's location answers "where is this file", not "what is running"), and **the absence of the field counts as the condition**, since a build too old to emit it is by construction the stale build it exists to catch. **The repair half was investigated and there is none inside the plugin** (2026-08-05, recorded with its evidence in `bootstrap/session-start.sh`'s header): the cache holds every version side by side — 25 directories measured on one machine — while the registry carries exactly **one** entry, so a session never picks the wrong directory; it binds whatever the registry named **at startup**, strictly before the SessionStart hook can run. Sweeping superseded directories therefore repairs nothing and is unsafe besides, since a running session is bound to a directory that becomes superseded the instant an update lands. And because every tick is a fresh container off the same image, a stale image reproduces the condition hourly rather than self-healing — four consecutive ticks stopped at the gate with a claimable queue on 2026-08-05. The binding, the cache layout and the registry are all the harness's, so what would fix it is a fresher image or a harness that rebinds after SessionStart: an **ask**, not a workaround, and the gate stays exactly as it is because detecting the condition is not the bug. Until then the interim safety is that a persisting failure stops reading as a healthy idle tick (the red-alert dedup rule's threaded reply, same date). Routines are **Claude Code Web routines** (cloud sessions reached through `RemoteTrigger`), never cron: the plugin holds **one set of two templates, each four lines long** (`skills/workaholify/routines/` — `fb` = `[Propose]`, `implement` = `[Implement]`) and applies them to whichever repository the command runs in, so **no per-repository routine file exists and `.workaholic/` gains nothing**. **Two, because a developer configures these by hand and every field multiplies by the number of projects** (P3, 2026-08-06): `[Propose Batch]` went on 2026-08-04, and `[Consent]` — the merge announcement — on 2026-08-06, at a **stated** cost rather than a discovered one: a **human-merged pull request is now announced by nobody**, since `[Implement]` posts only for the units it ran. That is the accepted price of one fewer standing process per project; do not reintroduce a third routine to recover it. A prompt carries only what the plugin cannot know — the environment, the payload, the one command, and **the channel and post shape**, which is the one thing a routine cannot defer because it *is* its output contract. **A routine cannot subscribe to a repository event, and the templates now say so** (measured 2026-08-05 over the live account): a routine record's whole trigger surface is `cron_expression`, `run_once_at`, and an API token letting an external caller POST `/v1/code/triggers/<id>/run` — there is no field naming a pull request, a merge or a webhook, so `trigger: event` became `trigger: invoked`, which is what the API supports. The measurement also answered the standing ask to start `[Drive]` on a merged proposal: `[Consent]` keeps ownership of the merged-pull-request event (one event, one owner, and it is the template whose subject already is that event), `[Drive]` keeps its clock — which is not a floor beneath an event path but the **only** path, since **retracted 2026-08-06** — that read an absent `last_fired_at` as proof of never having fired, and a web session did the `[Propose]` routine's exact job on 2026-08-05 with the key still absent, so how an unscheduled routine is invoked is an open question rather than a settled absence — and the clock survives an invoker anyway, because handoff resumption, a lapsed claim, and any ticket `/ticket` wrote have no merge event to key on. Standing an invoker up is a human act under *What may be applied unattended*. **A template is a thin pointer, not a procedure** (2026-08-05): it states the routine's own environment and which policy points apply, and defers everything else to the skill that owns it — the run to `workaholic:drive`, the notification rules (post shapes, red-alert dedup, mention resolution, thread routing) to the `workaholify` SKILL, the standing prohibitions to the always-loaded `rules/`. `[Drive]` was the exception that proved it: 141 lines of restated run procedure against the other two at ~55, and the restatement had drifted into instructing a hand-filter for mission ownership `plan-units.sh` had performed for weeks. A prompt that duplicates a procedure is a second source of truth, and the drift is one-directional — the routine is what runs, and nothing rebuilds it from the skill. **The account-management surface is retired** (2026-08-06): `plan-routine-change.sh` / `authorize-routine-change.sh` (the digest gate) and `compare-routines.sh` / `list-routines.sh` (drift and fleet reporting) are deleted with their tests, because they managed the half of a routine the API exposes while blind to the half that decides whether it runs — measured cost the same day, a paginated `list` surveyed as the whole account and six duplicate records refreshed through the gate while the real wired routine ran a stale prompt beyond page one, a "drift-free fleet" reported both times. What remains is `render-setup-sheet.sh`: per template, the name, model, repository, the prompt verbatim, and the **UI steps derived from the template's `trigger_kind`/`trigger_event`/`trigger_filters` declaration**, plus the `dev-<repo>` channel probe (`check-slack-channel.sh`, whose **`checked: false` rather than `exists: false`** rule survives — a locked store returns the same error as a missing channel). The digest gate's reasoning is preserved in the feedback record rather than in code: it closed substitution and batching, and the bar is not weakened — the acts it gated no longer exist. Do not reintroduce a reader "just to report what exists": the trigger wiring is invisible to the API, so such a report is authoritative about what does not matter and silent about what does |
| `/mission-close <slug> [achieved\|abandoned\|carried]` | **End a mission** and move it into `missions/archive/`, published to `main` like any other mission write (the archive move only — worktrees are claim-born and ship-torn, never torn down by close). It states the Mission Position Report before asking anything, asks the three-way outcome when the argument does not state it, and runs `mission/scripts/close.sh`, which stays the **only sanctioned writer of an end state**. Its own command since P5 (2026-08-06) rather than `/mission close <slug>`: a behaviour selected by the first word of another command's argument is a second command wearing one name. Unlike the `summary` and `approve` modes retired before it, it could not simply be dropped — the single-writer property is what keeps the archive move from growing a second path — so the behaviour moved instead |
| `/setup-routines [repository name]` | Render **copy-paste setup sheets** for this repository's routines: per template, the name, model, repository, the prompt verbatim in one block, the **UI steps derived from the template's structured trigger declaration** (`trigger_kind`/`trigger_event`/`trigger_filters`), the connectors to keep and the `dev-<repo>` channel to have ready — plus the two preconditions (Slack channel, web bootstrap) and a plain statement of what cannot be verified from here. **It manages nothing** (developer's ruling, 2026-08-06): a routine's GitHub trigger is configurable in the web UI only and the API record carries no event field, so the wiring can be neither read, written nor drift-checked from a session — a tool that managed the readable half while blind to the half that decides whether the routine runs misled more than it helped (measured the same day: a paginated `list` surveyed as the whole account, six duplicate records refreshed through the digest gate while the real wired routine ran a stale prompt beyond page one). The command therefore makes **no `RemoteTrigger` call** and issues no confirmation — the developer acts in their own browser |

## Development Workflow

1. **Create specs**: Use `/ticket` to write implementation specs — published onto a `work-*` branch behind a pull request, from whatever branch you are on; merging the PR is what queues them
2. **Implement specs**: Use `/drive` (attended) or `/implement` (unattended) to implement and commit each spec
3. **Create PR**: Use `/report` to generate story and create PR
4. **Ship**: Use `/ship` to merge PR, deploy, and verify

## Type Checking

No build step required - this is a configuration/documentation project.

## Local Verification

Before pushing changes to workflow scripts or plugin manifests:

```bash
node scripts/build-plugins/build.mjs              # regenerate outputs/ AND hooks/policy-index.md if you touched a core skill, a pillar SKILL.md, or build.mjs
node scripts/build-plugins/verify.mjs             # assert generated skills are self-contained AND the policy index is in sync
node scripts/build-plugins/validate-metadata.mjs  # assert Codex manifests are well-formed and version-aligned
node scripts/test-workflow-scripts.mjs            # hermetic smoke tests for branching + drive scripts
bash plugins/workaholic/hooks/layout-doctor.sh .  # audit this repo's .workaholic/ for an unregistered artifact directory (see the closed-layout policy)
```

The smoke tests create throwaway repositories under the OS temp dir, exercise the scripts there, assert on JSON output and filesystem state, and clean up. They never touch the working tree or call `gh`/network — safe to run anywhere.

`layout-doctor.sh .` audits this repo's own `.workaholic/` tree (read-only) against the canonical allowlist; `conforming: false` means a live directory is unregistered — the drift the closed-layout policy exists to catch. The `Validate Plugins` CI workflow runs the same audit and **fails the merge** on `conforming: false`, so a stale allowlist is caught before it surfaces as a guard block.

### Commit-subject and branch-name enforcement

The commit-subject rule (present-tense, ≤50 chars, no `feat:`/`[bracket]` prefix — see `skills/commit/SKILL.md`) and the branch-name rule (`work-YYYYMMDD-HHMMSS` named only by `create.sh`, plus the release tier's `release/YYYYMMDD-HHMMSS` named only by `cut-release-branch.sh` — two literal patterns, nothing else) are enforced as gates, not just prose, in **two layers**:

- **Agent/harness surface (always on, zero opt-in):** `hooks/guard-git-commit.sh` and `hooks/guard-git-branch.sh` are blocking `PreToolUse(Bash)` hooks (shipped active in `hooks.json`). The commit gate blocks a direct `git commit` only when its `-m` subject is off-policy (it does **not** block `Co-Authored-By` trailers, conformant subjects, or script-wrapped commits like `commit.sh`/`archive.sh`, which never expose a top-level `git commit`). The branch gate blocks off-pattern/variable branch creation. Both route the caller to the sanctioned script.
- **Human-terminal surface (opt-in):** `hooks/git/commit-msg` is a git-native hook that enforces the **same subject rule** on every commit (including a developer's own terminal `git commit`), since `PreToolUse(Bash)` cannot see those. It is subject-only (never rewrites the message). Install it deliberately per-repo:

  ```bash
  sh ${CLAUDE_PLUGIN_ROOT}/hooks/install-git-hooks.sh   # sets core.hooksPath; refuses to clobber an existing one / classic .git/hooks without --force
  ```

  Bypass a single commit with `git commit --no-verify`; undo with `git config --unset core.hooksPath`. All layers share one rule source — the canonical validator is `skills/commit/scripts/check-subject.sh` (it pins `LC_ALL` to a UTF-8 locale so a multibyte subject measures identically on every host), which `commit.sh` runs itself before staging anything (closing the script-wrapped path the `PreToolUse` gate deliberately does not inspect) and which both hooks reach through the stable `hooks/lib/check-subject.sh` delegator — so the layers cannot drift. Because the validator lives in the commit skill's `scripts/`, the generated `outputs/workflows` bundle carries the gate too (rebuild on change); the hooks themselves remain Claude-Code-only / git-native with no `outputs/` footprint. `commit-msg` is exempt from `posix-lint` only because git requires that exact extensionless name; it is POSIX `#!/bin/sh -eu` by construction.

### One behaviour per command (P5, 2026-08-06)

**No command's behaviour depends on the first word of its argument.** A fork on a literal word is a second command wearing one name: invisible in the command list, undocumentable in one table row, and it reserves a word the argument can then never legitimately be. Every command was inventoried and each fork decided separately:

| Fork | Decision |
| ---- | -------- |
| `/mission close <slug>` | **Moved** to `/mission-close <slug>`. It could not be dropped — `mission/scripts/close.sh` is the only sanctioned writer of an end state, and that single-writer property is what keeps the archive move from growing a second path. |
| `/mission summary`, `/mission approve` | **Dropped.** Both modes were already retired (2026-07-22, 2026-07-31); what survived was a deprecation *stub* intercepting the literal, which was the fork itself — a courtesy message bought at the price of two words no mission may be titled. |
| `/ticket summary` | **Dropped.** Bare `/ticket` already reports the queue, and `summary` reserved a word no ticket could be described with. |
| `/drive auto`, `/drive night` | **Moved** to `/implement` (P1). |

**Bare-versus-argument is a scope, not a mode, and is kept**: `/mission` with no mission named opens the planning session over all of yours, `/ticket` with nothing described reports instead of writing, and `/drive [<unit>]`, `/implement [<unit>]`, `/catch [window]`, `/setup-routines [repo]` and `/explain <q> [dir]` all narrow a single behaviour. **`/fb`'s cross-repository mode is kept** and is the one judgement call: it routes on *destination* (an `owner/name`, a URL, an explicit "to \<repo\>"), never on a first word, and the substance of that flow is its non-skippable masking confirmation — splitting it would relocate a human gate, which is a different decision from this one.

### AskUserQuestion project-label enforcement

The `[<project label>]` prompt-prefix convention (each `AskUserQuestion` question body opens with the owning repo's label, from `skills/gather/scripts/project-label.sh`, so a developer with many parallel sessions sees which repo is asking) is enforced as a gate, not just prose. `hooks/guard-askuserquestion-label.sh` is a blocking `PreToolUse(AskUserQuestion)` hook (shipped active in `hooks.json`): it reads `tool_input.questions[].question` and exits 2 (re-issue) when any body lacks a leading `[…]` label. It fires on every `AskUserQuestion` by design — labeling any prompt with its repo is a net good, so the over-fire onto ad-hoc questions is intentional. Claude-Code-only, POSIX `#!/bin/sh -eu`, **no `outputs/` footprint**. The rule itself stays where it was — the per-skill "User interaction" prose — this hook only makes it machine-checked.

**Whether a prompt should fire at all** — a separate question from its label — is governed by `rules/interaction.md` (always-loaded): ask only for genuine decisions, else **act and report** and let the developer correct. Its sharpened form is the **Recommended-label test** — if you could honestly mark an option "(Recommended)", do not ask; decide it, record the decision and its reason where the work is written, and let the developer veto. This stays deliberately **not** hooked, and the sharpening does not change that: necessity — and *a fortiori* whether an option was recommendable — is a judgement a `PreToolUse(AskUserQuestion)` hook cannot read (it sees only the prompt text), the same reason the cross-repository masking step is a judgement, not a matcher. The label hook checks a prompt's shape; the rule governs its necessity. The interrogation surfaces (`/ticket` §4b, `/mission` Creation Interrogation and replan) all reference the one canonical statement in `rules/interaction.md` rather than restating it.

A companion `hooks/guard-working-directory.sh` (`PreToolUse(Bash)`, shipped active) **denies** a top-level cwd-moving `cd` via `permissionDecision: "deny"` naming the offending command and the sanctioned alternatives — **unconditionally, with no env-var toggle**: enforcement is built into the plugin code, so "plugin installed = guard active", identical on every machine and fresh clone (an injectable opt-in switch fails open exactly when it is not set, which is when the guard is needed, and an advisory reminder is text an agent ignores). The match set is unchanged (a `( cd … )` subshell, an absolute path, and a `--prefix`-style command always pass silently). It is the machine-enforced half of the `workaholify` working-directory ground rules (`skills/workaholify/`); like the other guards it is Claude-Code-only, POSIX `#!/bin/sh -eu`, with no `outputs/` footprint.

### Repository confinement

Every write lands inside the current repository or one of its own worktrees. Crossing to another repository is done **only** through `/fb`'s cross-repository mode, which masks this project's customer context and then opens the ask as a **GitHub issue on the target** — writing into no checkout of it at all. Two layers, and the order matters:

- **The rule is primary.** It lives in `rules/general.md` ("Never modify another repository", plus "Never carry one repository's context into another's artifacts"), which is `paths: '**/*'` — always loaded, and shipped to every repo that installs the plugin. This is deliberately where the weight sits: the rule is what an agent reads before it acts.
- **The hook is a syntactic backstop.** `hooks/guard-repo-confinement.sh` is a blocking `PreToolUse(Write|Edit)` hook (shipped active) that resolves `tool_input.file_path` against `git rev-parse --show-toplevel` plus every entry of `git worktree list`, and exits 2 when the target is outside all of them. It runs PreToolUse because `validate-ticket.sh` is PostToolUse — refusing *after* the file exists in the foreign repo is not confinement. It accepts worktrees in both directions, because missions run in `.worktrees/<slug>/` and a toplevel-only test would break the mission model. It also exempts the agent's per-project memory store (`~/.claude/projects/<slug>/memory/`): that is the harness's own store — not another repository — the harness itself directs the agent to write there, and a stale memory's only correction path is a write (blocking it once left a memory contradicting the current design firing every session with no way to repair it). It fails open outside a git repo. Claude-Code-only, POSIX `#!/bin/sh -eu`, no `outputs/` footprint.

**Do not grow the hook toward content matching.** A path is syntax and a matcher handles it well; a customer's vocabulary is semantic and a matcher cannot recognise it — the terms that leak are usually not enumerable until the moment they leak. That judgement belongs to the crossing flow, where a person confirms it.

**`/fb`'s cross-repository mode** (`commands/fb.md` + `skills/feedback/`'s *Crossing a repository boundary* section) is the outlet the confinement needs, and **the carrier is an issue, not a file** (FB `20260805101319`, 2026-08-05): the crossing arrives where the target's owners already read, their `[Propose]` routine ingests it like any other inbound report, and nothing appears in a checkout they did not expect to change. `resolve-target.sh` resolves the target — a local path, a bare `owner/name`, or a GitHub URL, since an issue needs no clone — and reports its **visibility** (public vs private is a different decision, and the developer must see which they are making); `open-issue.sh` wraps `gh issue create -R <owner/name> --title --body-file` and reports the issue URL, passing a `gh` refusal through verbatim rather than routing around the target's own decision. Between them sits the step that actually does the work — **the developer confirms the destination and the exact body, verbatim, in one confirmation that cannot be skipped** (the destination is folded in, but the verbatim body is the surviving gate). Two mechanical layers sit *beneath* that judgement and are not assurance: `scan-outbound-body.sh` applies the release scan's own `secret` (hard, never overridden) and `leak` (confirm) rules to the composed body — deliberately not `scan-branch-safety.sh`, which answers "what is this **branch** about to publish" and would report a verdict about the caller's unrelated working branch; and `check-outbound-body.sh` refuses a body still naming this repo. **This whole outlet was `/request` until 2026-08-05** — a command whose `submit-request.sh` copied a conforming ticket file into the target's `.workaholic/tickets/todo/`. That command and that script are gone; everything below is the measured history of the surviving checks, which moved into the feedback skill unchanged in substance. **"Clone URL" means every form of it**: git rewrites remotes through `url.<replacement>.insteadOf <original>` — possibly injected via the `GIT_CONFIG_COUNT`/`KEY`/`VALUE` environment triple, which `git config --global --list` does not show — so `git remote get-url` and `git config --get remote.origin.url` can name one repository differently. Both are read through `feedback/scripts/lib/remote-url.sh` and the body is matched against all of them, **clone-URL rule across every form before the `owner/name` rule**, because every clone URL contains its own slug and the rule that fires is what the developer is told to mask. Matching a single form was measured on 2026-08-04 (the cloud runner's container injects `url.https://github.com/.insteadOf git@github.com:`) to let a body carrying this repository's literal clone URL through the clone-URL rule entirely. `resolve-target.sh` takes the other branch of the same fact and reports the **configured** URL — the destination a human confirms and a colleague could clone, never one a local rewrite invented. The bare-name check matches an **identifier, not a substring** — it was a plain case-insensitive substring until 2026-08-02, which made a repository whose basename is an ordinary English word unable to raise any ask whose body listed paths embedding that word (including directories belonging to the *target* repo), refusing it **after** the developer had confirmed the body verbatim and instructing them to "mask" something that could not be removed. This backstop is the one place a legitimate ask is refused past the human gate, so its false-positive rate is a usability property, not only a safety one; every refusal now cites the matched text and its line. The test suite asserts this directly: the four real leaked sentences from the incident pass through the script *without complaint*, because none of them names this repo. That is not a bug to fix — it is the measured reason the human gate exists, and the feedback skill's crossing §1 explains why no matcher can replace it. One more envelope defect is fixed and pinned: `gh api` prints its error body to **stdout** with a non-zero status, so the idiomatic `2>/dev/null || echo unknown` fallback concatenated a JSON blob with the fallback word and made `visibility` — the one field the confirmation shows — unparseable; `resolve-target.sh` now whitelists `public`/`private`/`internal` and answers `unknown` for everything else.

The guard watches the Write/Edit tools; it sees neither a shell redirect from Bash nor an API call `gh` makes on the crossing flow's behalf, and neither is a write performed by an MCP server (which is how `/explain`'s browser prints its PDF to the developer's Desktop). The threat model is an agent doing the natural thing, not one evading a gate.

**It cannot exempt a caller, and that shapes `/explain`.** A `PreToolUse` hook receives only `tool_input.file_path` — never which skill is asking — so an `.html` bound for Home is indistinguishable from any other write there and there is no narrow carve-out to write. `/explain` therefore stages its HTML **in-repo** at a git-ignored `.explain/<slug>.html` and lets the **browser** write the PDF to `chosen_dir` over MCP. The export is unchanged; only the staging file moved. `skills/explain/SKILL.md` §2-4 carries the rationale, and `test-workflow-scripts.mjs` pins the constraint ("confine blocks a non-repo path outside the repo (export dir)") so the dead `<chosen_dir>/<slug>.html` path cannot be reintroduced silently.

**The rule only reaches a repo that installs the plugin.** This is the failure that produced the incident: rules, hooks, and the scan are all inert in a repo where `workaholic@workaholic` is not in `enabledPlugins` — no matter what is written here. Enable it at the user level (`~/.claude/settings.json`) so every repo is covered, not per-project.

### Always-on policy lens

`hooks/policy-lens.sh` is a non-blocking `UserPromptSubmit` hook that injects the engineering-policy lens for the workflow commands that carry the `workaholic:policy-lens` sentinel marker (`/ticket`, `/report`, `/ship`, `/drive`, `/catch`, `/explain`). The sentinel rides in the expanded command body, so the lens fires once per workflow invocation and stays in accumulated context. Two layers, by design:

- **Refer for bodies**: the hook points at the four pillar policy skills (`workaholic:planning`/`design`/`implementation`/`operation`); the full Goal/Responsibility/Practices live in `skills/<pillar>/policies/<slug>.md` and are read **on demand** when a change touches that policy.
- **Embed the index only**: the hook appends `hooks/policy-index.md` — a **generated** table of contents (per-policy heading + one-line summary across all four pillars), produced by `scripts/build-plugins/policy-index.mjs` from the four `SKILL.md` `## Policies` sections. This is the single bounded exception to "refer, never embed": headings are an index, not policy rules. The `## Policies` sections stay the single source of the heading list (kept fresh from qmu.co.jp by the `workaholic-standards-sync` controller). **Regenerate with `build.mjs` after editing any pillar's `## Policies` list.** Freshness is enforced in CI the same way `outputs/` is: the `Outputs Freshness` workflow rebuilds and fails on any diff to the committed `hooks/policy-index.md` (not by `verify.mjs`, which only flags a stale *working-tree* index when run before a build — after a build it is vacuously fresh).

### Always-on mission lens

`hooks/mission-lens.sh` keeps the agent — and the developer — oriented to the roadmap. It is registered on two events and is **non-forcing** on both (it never blocks a stop): on every `UserPromptSubmit` it injects a **model-visible** `additionalContext` line, and on every `Stop` a **user-visible** `systemMessage`. Each names the **active** missions (`.workaholic/missions/active/<slug>/mission.md`) that pass all **three** gates, with derived `checked/total` and the next unchecked acceptance item: **ownership** (the mission is *not somebody else's* — the current `git config user.email` is among its **derived** owners via `gather/scripts/owners.sh`, or it is **unowned**, in which case it is surfaced to everyone as claimable, after the developer's own; only a mission owned solely by others stays silent), **location** (the worktree-focus rule below), and **signal** (at least one acceptance criterion). The signal gate exists because the line is printed unasked, directly above the agent's answer: a mission whose `## Acceptance` is empty renders as `0/0` with no next step — a technical condition with nothing to act on — so it says nothing instead. It is a silent no-op when nothing passes all three, so it usually costs only a few greps per turn (with many missions the per-mission `progress.sh` subshells are not free; if that ever bites, the fix is a batch reader in `skills/mission/scripts/`, not parsing inlined into the hook).

**Summarize on change.** The roster rarely changes within a session, yet a long `/goal` Stop condition re-fires this hook on essentially every turn — and re-injecting the whole block each time buries the developer's own message under redundant context (worse the larger the roster). So the **full** block is emitted only when the roster **changed** since the last turn of this session (or on the first turn, or when `session_id` is absent — a case that cannot be deduped and stays full, keeping the bare-harness/test path backward-compatible). An **unchanged** turn collapses to a compact one-liner — the count plus the single next action plus a bare `/mission` pointer — so the active goal and next step stay visible without the volume. The change-detector is keyed per session **and** per event (`UserPromptSubmit` and `Stop` dedupe independently), cksum-compared under `${TMPDIR:-/tmp}/workaholic-mission-lens/`, and fails open to the full roster; it never blocks a stop, so `/goal`'s gating is untouched. (`/goal` itself is a Claude Code harness feature, not a workaholic command — the only lever this repo owns over that per-turn volume is this hook's roster injection.)

The bare `/mission` view's **full tier** deliberately keeps the **lower** bar — ownership alone (`list.sh`'s `relation`: `mine`/`unassigned`) — so an unfilled `0/0` mission is still visible on demand. An always-on nudge and a view you asked for should not share a threshold; the divergence is intentional, not drift. (The old `/mission summary` mode carried this bar before it was folded into the bare view, 2026-07-22; `summary.sh` remains as the gate's canonical statement.) The **ownership** gate itself is shared through `gather/scripts/owners.sh`: both read "not somebody else's", so an unowned mission surfaces in each (the lens and the bare view mark it unclaimed). Ownership is carried on the mission's own plural `assignees` (2026-07-28), with a legacy fallback to the singular `assignee`; "mine" means the caller is among the owners, since a mission can be co-owned.

The **worktree-focus** rule (missions run in their own `.worktrees/<slug>` worktree) has three cases, each decided rather than left to fall through: inside a mission's worktree it surfaces **only that mission** (you are already focused on it, so other worktrees' missions are noise); inside a worktree that owns **no** mission — a `/drive` worktree such as `.worktrees/work-20260714-005155` — it surfaces **nothing at all** (that session is focused on one ticket; the roadmap is not its business); and in the main tree it surfaces only missions that do **not** own a dedicated worktree (a worktree-owned mission stays silent everywhere but its own worktree). Basename-matching is sound here because `slug.sh` is the single source deriving both the mission directory name and the worktree name. This keeps each worktree's nudge scoped to its own work instead of listing every mission you own.

- **Why two events:** a `Stop` hook cannot inject model-visible context without `decision: block` (which would *force* the agent to keep working) — its only non-forcing channel is the user-facing `systemMessage`. So the model-facing half deliberately rides `UserPromptSubmit` (the same mechanism `policy-lens.sh` uses), which fires exactly when the agent decides "what to do next"; the `Stop` half is the developer-facing nudge. Together they honor "every time the agent stops or asks what next" without ever hijacking the turn.
- **Ownership is the gate:** a mission's owners are its own plural `assignees` (`gather/scripts/owners.sh` — `assignees` first, legacy `assignee` fallback; see the mission skill's *Ownership* section). Because each git worktree checks out its own `.workaholic/`, the lens that fires in a worktree reflects the missions owned by whoever is working that tree.
- **Reuse, not duplication:** the hook computes progress via `mission/scripts/progress.sh` and the next step via `mission/scripts/next-acceptance.sh`, so the Acceptance-scoping convention lives in one place. Claude-Code-only, POSIX `#!/bin/sh -eu`, **no `outputs/` footprint** (lives in `hooks/`, not built). The mission skill itself *is* built into `outputs/workflows`, so the `assignee`/`next-acceptance.sh` changes there require a `build.mjs` rebuild; the hook does not.

### Release-safety scan

The `release-scan` skill (`skills/release-scan/scan-branch-safety.sh`) is a **deterministic, script-only** branch-safety gate that `/report` (warn) and `/ship` (block, pre-merge) run over `git diff <base>..HEAD` to catch, before content reaches a public remote: **credentials** (`secret`, hard block — `release-scan/scripts/lib/secret-patterns.sh`, whose generic-assignment rule matches on the **value**, not the key name: a quoted-alphanumeric or line-ending bare run is a literal, and everything else — identifier, call, env read, template, annotation — is a reference that needs no rule of its own; **generated paths are exempt from this rule** since 2026-08-05, via the same `is_generated_path` predicate the size rule uses — a diff reports a file arriving at a *new* path as wholly added, so a build that grew a bundle's closure re-flagged `record-evidence.sh:19`, a comment *documenting the secret guard* that had been on `main` for weeks, as a hard block on the one tier with no override; the exemption drops only the duplicate of a source finding the scan still makes, and its measured cost — a credential hand-written into a generated path with no source counterpart goes unreported here — is carried by `Outputs Freshness` CI, which rebuilds `outputs/` and fails on any diff), **oversized/too-many files plus per-commit changed-lines** (`size`, overridable — `MAX_FILES`/`MAX_FILE_ADDED_LINES`/`MAX_FILE_BYTES` per file/branch, and `MAX_COMMIT_CHANGED_LINES` per commit via a `too-large-commit` rule that sums a commit's **added lines of non-generated implementation**, exempting binary rows, lockfiles, minified artifacts, `linguist-generated` paths — workaholic marks `outputs/**` so — any file already over the per-file ceiling, **`.workaholic/` artifact prose** (specs and records are not implementation throughput), and **merge commits entirely** (a merge authors nothing, and `/ship`'s own catch-up merge otherwise makes a ten-line branch inherit a block); it counts additions only, so a relocation is not charged twice for moving content. **What it counts is a recorded decision** — `release-scan/scripts/lib/commit-size.sh` carries the rationale, the five measured instances, and the rejected alternatives; read it before changing the rule, which had been overridden four times instead of decided once), and **re-introduction of an already-known client/project term** (`leak`, confirm — the git-ignored, developer-maintained `.workaholic/leak-denylist`). It emits `{verdict, findings[]}` with each finding citing `file:line` + the matched rule (objective, so it can gate a merge); the consumers enforce the severity tiers. It is a script, not a subagent, on purpose — a merge gate must be reproducible, not a model judgment.

**Read the scope of the `leak` rule literally.** It matches listed terms in the branch diff, nothing more. It is blind to content already on `main`, and because the denylist is git-ignored it is a silent no-op in any repo where nobody created the file. It does **not** enforce "keep motivation generic, never name other repos/clients" — that rule is enforced by confining every workflow's writes to the current repo and by the crossing flow's masking confirmation, which is a judgement a person makes when content crosses a repository boundary. A `pass` verdict means "no listed term was re-introduced", never "no client context here". An earlier version of this paragraph claimed the scan machine-enforced the convention; a structured internal-hostname pattern that backed that claim was removed after it was measured to detect none of five real leaks while misfiring on `metadata.internal`.

## Version Management

Version bumps are **manual** — there is no `/release` command. After bumping the files below and pushing to `main`, CI (`.github/workflows/release.yml`) publishes the GitHub Release automatically (it reads the version and the latest `.workaholic/release-notes/` note).

Version files (all must stay at the same semver):
- `.claude-plugin/marketplace.json` - root `version` AND every `plugins[].version` entry (workaholic, workflows)
- `plugins/workaholic/.claude-plugin/plugin.json`
- `plugins/workaholic/.codex-plugin/plugin.json` - hand-maintained Codex-facing manifest

Generated (do NOT hand-edit; rebuild with `node scripts/build-plugins/build.mjs`):
- `outputs/workflows/.codex-plugin/plugin.json` - version is read from `.claude-plugin/marketplace.json`'s `workflows` plugin entry at build time

`.claude-plugin/marketplace.json` is the single source of truth. When bumping version:
1. Read current version from `.claude-plugin/marketplace.json`
2. Increment PATCH by default (e.g., 1.0.0 → 1.0.1)
3. Update the root `version` and every `plugins[].version` in `.claude-plugin/marketplace.json`
4. Update `plugins/workaholic/.claude-plugin/plugin.json` and `plugins/workaholic/.codex-plugin/plugin.json` to match
5. Regenerate `outputs/workflows/` so its Codex manifest picks up the new version
6. Stage and commit: `Bump version to v{new_version}`
