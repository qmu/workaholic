---
paths:
  - '**/*'
---

# General Rules

- **Never commit without explicit user request** - Only create git commits when executing a command that has commit steps (`/drive`, `/pull-request`)
- **Never use `git -C`** - Run git commands from the working directory, not with `-C` flag
