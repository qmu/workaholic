# Canonical .workaholic/ top-level subdirectories — the single source of truth.
#
# Read by hooks/validate-ticket.sh (the layout gate) and by the layout-doctor
# audit. One directory name per line; blank lines and lines starting with # are
# ignored. Keep this list in lockstep with the table in
# plugins/workaholic/rules/workaholic.md (README.md, index.md, scan-allow, and
# leak-denylist are allowed at the root).
#
# EVERY entry is grounded in code: created or read by a live plugin/script
# (grep -rhoE '\.workaholic/<dir>' plugins/ scripts/). That is now the rule, not a
# summary — guides/, policies/ and specs/ were the three exceptions (conventional,
# model-written documentation areas with no writer in the loop) and they were
# retired on 2026-08-13 (issue #436). An area with no writer goes stale and then
# lies: all 17 substantive files in them still described the retired three-plugin
# architecture, untouched since 2026-05-14. Documentation lives in the repository's
# own docs/ tree, outside .workaholic/, where its maintainer is a human who reads it.
#
# The allowlist is permissive (it permits, it does not require), so a repo without a
# listed directory is unaffected — but a repo WITH a de-listed one has every later
# write into it hard-blocked, which is what /workaholify's layout convergence
# reports. Adding a directory here is a deliberate amendment to the closed
# `.workaholic/` structure, not an ad-hoc mkdir: register it here AND in the
# rules/workaholic.md table in the same change (see CLAUDE.md's closed-layout
# policy). The same lockstep applies to removing one.
deployments
feedbacks
housekeeping
missions
release-notes
releases
stories
strategies
terms
tickets
trips
